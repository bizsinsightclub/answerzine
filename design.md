# design.md — ANSWER ZINE 디자인 시스템

**주간 반복 생산을 전제로 한 고정 템플릿 명세다.**
매주 바뀌는 것은 §4의 슬롯 콘텐츠뿐이고, 나머지는 전부 잠겨 있다.

값은 전부 `build/lib/*.mjs`에 실제로 구현된 것이고, 대비 수치는 WCAG 2.1로 계산한 실측값이다.
**단일 소스는 코드다.** 이 문서의 표는 사람이 읽는 사본이므로, 값을 바꿀 때는 코드를 먼저 고친다.
`test/theme.test.mjs`·`test/css.test.mjs`가 아래 규칙 상당수를 자동으로 강제한다.

| 구분 | 원칙 |
|---|---|
| §2 불변식 | 에이전트가 **절대 바꾸지 않는다.** 변경은 사용자 승인 사항. |
| §4 슬롯 | 매주 바뀐다. **분량 예산을 지키면 A4 1페이지가 보장된다.** |
| §6 도메인 색 | 도메인 추가 시에만 건드린다. 선정 규칙이 정해져 있다. |

---

## 1. 테마 — 단일 고정, 흰 바탕

> **이 절은 2026-08-10에 실제 코드에 맞춰 다시 썼다.** 이전 버전은 Night(어두운 기본)·
> Paper(밝은 대체, `[data-theme="paper"]` 토글)·Zine(인쇄) 세 테마 체계를 설명했지만,
> `build/lib/theme.mjs`·`css.mjs`에는 애초에 그 토글 자체가 구현된 적이 없었다 —
> `test/theme.test.mjs`의 "테마 토글이 없다" 검사가 그걸 계속 강제해 왔다. 문서가 코드를
> 못 따라간 상태로 남아 있던 것을 이번에 바로잡는다.

테마는 **하나뿐이다.** 토글이 없다. 사이트는 항상 이 흰 바탕 하나로 읽힌다 — 진입 화면도
예외가 아니다(2026-08-10 전에는 `.intro`만 항상 어두운 별도 팔레트를 썼다).

| | 사이트 (`THEME`) | 인쇄 진 (`ZINE_THEME`) |
|---|---|---|
| 적용 범위 | `:root` | `.zine-page` |
| 배경 | `#FFFFFF` | `#FFFFFF` |

```css
:root {
  --ink:       #17150F;   /* 본문       대비 18.7:1 */
  --secondary: #5C574A;   /* 보조·티저   대비  7.4:1 */
  --tertiary:  #726C5E;   /* 날짜·캡션   대비  5.4:1 */
  --rule:      #17150F;
  --bg:        #FFFFFF;
  --surface:   #FFFFFF;
  --divider:   rgba(23,21,15,.24);
}
```

**세 역할 모두 흰 배경에서 AA(4.5:1)를 넘는다** — 흰색이 예전 크림(`#F4F1E7`)보다 밝아서
대비가 오히려 늘었다. `test/theme.test.mjs`가 매번 실측해 검증한다.

`--surface`가 `--bg`와 같은 흰색이라, 카드 같은 표면 요소는 **배경색이 아니라 1px
테두리(`--divider`)로만** 페이지와 구분된다 — §2 불변식 3번(도메인 색 사용처)과 같은 원칙이다.
**단 하나, 홈의 카테고리 카드(§7)는 예외다** — 도메인색을 풀블리드로 채운다. 그 외에는
색 채움으로 위계를 만들지 않는다.

`@media print`는 화면과 무관하게 항상 `.zine-page`(위 표 오른쪽 칸)를 쓴다 — 지금은 두
값이 사실상 같지만, 인쇄와 화면은 역할이 다르므로 변수 자체는 분리해 둔다.

---

## 2. 불변식 — 매주 바뀌지 않는 것

에이전트는 아래를 변경하지 않는다. 콘텐츠가 안 맞으면 **템플릿이 아니라 콘텐츠를 고친다.**

1. **스토리 구조는 5블록 고정** — `text, stat, text, quote, text`. 추가·삭제·순서 변경 없음.
   (`tools/qa.mjs` 232행이 이 순서를 강제한다.)
2. **`stat` 1개, `insight` 있는 `quote` 1개** — 스토리당 정확히 하나씩. **데이터 계약 얘기다.**
   > **2026-08-10 되돌림.** 2026-08-08부터 `insight.note`(설명)는 화면에 안 보였다(사용자
   > 요청). 사용자가 첨부한 참고 목업(movie.html/print.html)의 `.insight`/`.note2` 콜아웃이
   > 다시 요구해 되돌렸다 — 웹은 `.insight-note`, 진은 `.zine-insight-note`로 `note`를
   > 낸다. `insight.concept`(개념명)은 여전히 라벨로 노출하지 않는다 — 고정 문구
   > "인사이트"를 쓴다. S5 집필 규율(인사이트 3문 테스트)이 기대는 내부 품질 장치라는
   > 성격은 바뀌지 않았다 — 다만 이제 그 결과물(`note`)이 독자에게도 보인다.
3. **도메인 색이 쓰이는 자리는 5곳** — 도메인 태그, 킥커, 풀쿼트 좌측 보더,
   인사이트 콜아웃 좌측 보더, **그리고 홈 카테고리 카드 배경(2026-08-10 추가, 풀블리드
   예외).** 이 다섯 번째 자리를 뺀 나머지 넷은 여전히 텍스트/1px 보더에만 쓴다 —
   버튼 채색은 이 시스템에 없다. 카테고리 카드가 배경 채색 예외로 추가된 경위는 §7
   "카테고리 카드" 참고 — 사용자가 첨부한 참고 이미지(도메인별 색 타일 그리드)가
   명시적으로 요구했다.
4. **진 페이지는 A4 1장.** 2페이지짜리 진을 만들지 않는다.
   > **2026-08-10 되돌림.** "리드 1 + 미니 3" 위계는 없앴다 — 참고 목업(print.html)의
   > `.sheet-story`처럼 그 주 통과분 전원을 동등한 행으로 낸다. A4 1장 제약 자체는
   > 그대로다(§4.2).
5. ~~회전각은 −9°~+7° 범위. 사진 −1.1°, 스티커 −3°, 테이프 −9°/+7°.~~
   **2026-08-10 삭제.** 인쇄 진에서 사진 콜라주·테이프·스티커 시스템 자체를 없앴다 —
   회전각을 적용할 대상이 없다.
6. ~~`clip-path` 다각형 값 — 가위로 오린 가장자리 표현.~~
   **2026-08-10 삭제.** 위 5번과 같은 이유 — 사진 플레이스홀더 자체가 없어졌다.
7. **`@page { size: A4; margin: 0 }`** — 안전 여백은 `.zine-page` 패딩이 전담한다.
8. **웹 본문에 CSS 다단(`column-count`)을 쓰지 않는다.** §5.3 참조.
9. **색 보더는 1px을 넘지 않는다.** 풀쿼트 포함.

---

## 3. 타이포그래피

### 워드마크 — 브래킷 마크 + "the answer company"

> **2026-08-10 축소.** 예전엔 `the answer company` 브래킷 마크 아래에 큰 `ANSWER` 슬랩 +
> `Zine` 스크립트 로고타입까지 한 이미지에 붙어 있었다. 사용자가 첨부한 참고 이미지가
> 브래킷 마크 + `the answer company`만 있는 축소판이라, 그 아래 로고타입 부분은 잘라냈다 —
> 재제작이 아니라 기존 파일의 2차 크롭이다.

| 파일 | 쓰임 | 아트 | 치수(2026-08-11 세 번째 크롭 후) |
|---|---|---|---|
| `assets/img/logo-light.png` | (현재 미사용 — 아래 참고) | 흰색, 배경 투명 | 462×236 |
| `assets/img/logo-dark.png` | 마스트헤드 · 인쇄 진 · OG 카드 | 검정, 배경 투명 | 518×265 |

사이트가 흰 배경 하나뿐이라 밝은 쪽(`logo-light.png`)이 지금은 쓰일 자리가 없다 —
어두운 배경이 다시 생기면 켤 수 있도록 파일과 CSS 스위치(`.logo-light`/`.logo-dark`)는
그대로 남겨 뒀다.

2026-08-10의 크롭은 원본 PNG의 알파 채널에서 두 로고 파트 사이의 빈 행(원본 기준 약
254~299px 지점)을 찾아, 위쪽(마크+워드마크)만 남기고 그 아래(예전 ANSWER/Zine
로고타입)를 잘라낸 것이었다. 원본 자체가 이미 2000×2000 정사각에서
`tools/crop-png.mjs`로 한 번 여백을 잘라낸 파일이라, 그게 **두 번째 크롭**이었다.

> **2026-08-11 세 번째 크롭 — "로고 옆 공백" 신고의 진짜 원인.** 여러 라운드에 걸쳐
> "로고 옆 여백을 없애 달라"는 요청이 반복됐는데, CSS 여백을 아무리 줄여도 해결되지
> 않았다 — 원인이 CSS가 아니라 **파일 자체**였다. 2026-08-10 크롭 후 캔버스가
> 1971×270이었는데, 실제 잉크는 x 748~1245(가로 497px)에만 있었다 — 캔버스 너비의
> 약 38%씩이 좌우로 투명 여백이었다. `tools/crop-png.mjs`(알파 채널 경계 자동 탐지)로
> 내용 경계에 10px 패딩만 남기고 다시 잘랐다:
> ```bash
> node tools/crop-png.mjs assets/img/logo-dark.png assets/img/logo-dark.png 10
> node tools/crop-png.mjs assets/img/logo-light.png assets/img/logo-light.png 10
> ```
> 결과: 1971×270 → 518×265 (비율 7.3:1 → 약 1.96:1로 완전히 바뀌었다 — 넓고 얇은
> 배너에서 정사각에 가까운 마크가 됐다).

```bash
node tools/crop-png.mjs --matte <입력.png> <출력.png> [여백px]   # 여백 자르기(1차, 기존 도구)
```

비율이 1.96:1(거의 정사각)로 바뀌면서 **너비 기준이 아니라 높이 기준으로 잰다** —
7.3:1 시절 쓰던 너비 기준 값을 그대로 두면 세로가 비정상적으로 길어진다(예: 화면
539px 너비 기준이면 세로 275px, 진 62mm 너비 기준이면 세로 32mm — 둘 다 지면을
침범한다). 화면 높이는 `clamp(56px, 6vw, 96px)`, 진에서는 `12mm`다.

배경이 불투명하면 흰 지면에 사각형이 얹힌다. **반드시 투명화한 파일을 쓴다.**

### 서체 — 헬베티카 하나, 웨이트로 위계를 만든다

> **2026-08-10 전환.** 예전엔 헤드라인/UI에 Paperlogy(산세리프), 본문/쿼트에 나눔명조(세리프)
> 를 자체 호스팅해서 역할을 나눴다. 사용자 요청으로 자체 폰트를 걷어내고 시스템 헬베티카
> 스택 하나로 통일했다 — `--sans`/`--serif` 두 변수 이름은 남아 있지만 값은 같다.

```css
--sans: -apple-system, "Helvetica Neue", Helvetica, Arial,
        "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
--serif: 위와 동일;
```

라틴 글자(영문·숫자·기호)는 Helvetica Neue/Helvetica/Arial에서 그려지고, 한글은 그 폰트들에
글리프가 없어 자동으로 다음 순번(Apple SD Gothic Neo·맑은 고딕 — 둘 다 OS 기본 UI 서체이자
헬베티카와 같은 계열의 중립 그로테스크)으로 넘어간다. 브라우저의 문자 단위 폰트 폴백을
이용한 것이라 별도 처리가 필요 없다.

옛 체계는 "한국어 장문 가독은 명조, 수치 조판은 산세리프"라는 역할 분리로 두 서체를
정당화했다. 지금은 그 역할을 **하나의 그로테스크 안에서 웨이트 대비**로 만든다 —
헤드라인 900, 킥커·라벨·바이라인 700, 본문 400. 형태로 나뉘던 목소리가 굵기로 나뉜다.

**로드하는 자체 폰트는 없다.** `@font-face`가 0개면 통과, 1개 이상이면 `test/css.test.mjs`가
실패한다 — 예전엔 "정확히 다섯 개(Paperlogy 3 + 나눔명조 2)"를 강제했던 자리다. 시스템
폰트라 서브셋·다운로드·두부(tofu) 걱정이 구조적으로 없다 — OS 내장 CJK 폰트는 한글
11,172자를 이미 전량 담고 있다.

### 킥커

킥커는 신문 지면의 문법이므로 **유지한다.** 다만 모노스페이스는 뺐다 —
`monospace`를 "기술적"이라는 인상의 의상으로 쓰는 것은 근거가 없다.
산세리프 700 + 자간 `.14em` + `uppercase`로 대체한다.

### 스케일 — 화면

| 역할 | 크기 | 웨이트 | 행간 | 자간 |
|---|---|---|---|---|
| 스토리 헤드라인 | `clamp(32px, 5.2vw, 52px)` | 900 | 1.06 | −0.035em |
| 통합 인사이트 헤드라인 (`.issue-insight`, 2026-08-10) | `clamp(34px, 6.5vw, 72px)` | 900 | 1.04 | −0.03em |
| 섹션 제목 (h2) | `clamp(24px, 3.4vw, 34px)` | 900 | 1.1 | −0.03em |
| 스탯 값 | 30 | 900 | 1.1 | −0.03em (tabular) |
| 풀쿼트 | 20 | 700 | 1.55 | −0.01em |
| 티저 | 18 | 700 | 1.6 | — |
| 본문 | 17 | 400 | 1.9 | 0 |
| 리스트 헤드라인 | 19 | 900 | 1.2 | −0.025em |
| 메타·날짜 | 13 | 400 | — | tabular |
| 킥커·라벨 | 11 | 700 | — | 0.1~0.14em, uppercase |

**자간 하한은 −0.04em이다.** 실사용은 −0.01 ~ −0.035em. `test/css.test.mjs`가 강제한다.

> 예전엔 여기서 "어두운 배경(Night)의 밝은 텍스트는 번져 보이므로 본문 자간을 양수로
> 보정한다"고 적혀 있었다. 그 별도 보정은 실제 CSS(`body { letter-spacing: 0; }`)에 없던
> 서술이었다 — 사이트가 흰 배경 하나뿐인 지금은 애초에 해당하지 않는다.

### 스케일 — 진

> **2026-08-10 개편.** "리드 1 + 미니 3" 위계가 없어지면서 스토리마다 같은 크기의
> 헤드라인·본문을 쓴다 — 리드/미니로 크기를 나누던 예전 표는 무의미해졌다(§8 참고).

| 역할 | 크기 | 웨이트 | 행간 |
|---|---|---|---|
| 스토리 헤드라인 | 16 | 900 | 1.25 |
| 스토리 본문(teaser) | 10 | 400 | 1.55 |
| 인사이트 콜아웃 본문 | 9 | 400 | 1.5 |
| 인사이트 콜아웃 라벨 | 7.5 | 700 | — (0.08em, uppercase) |
| 출처 링크 | 8 | 400/700(링크) | — |
| 스탯 값 | 15 | 900 | 1.2 (tabular) |
| 스탯 라벨 | 7.5 | 400 | — |
| 킥커 | 9 | 700 | — (0.1em, uppercase) |
| 데이트라인·바이라인 | 10 | 700 | — |
| 푸터 | 8.5 | 400 | — (0.04em) |

진의 킥커·라벨류는 전부 `uppercase` + 넓은 자간이다. 신문 조판 관용이다.

### 숫자

`.stat-value`와 메타 날짜에 `font-variant-numeric: tabular-nums`. 세로로 나열될 때 자릿수가 흔들리지 않는다.

---

## 4. 슬롯과 분량 예산 — 매주 바뀌는 것

### 4.1 예산표

단일 소스는 `domains/registry.json`의 `budget`이다. `tools/qa.mjs`가 이 값으로 검사한다.

| 슬롯 | 권장 (≤) | 절대 상한 |
|---|---|---|
| 스토리 헤드라인 | **24자** | 32자 |
| 스토리 티저 | **78자** | 104자 |
| `text` 블록 (각) | **180자** | 240자 |
| `quote.text` | 40자 | 48자 |
| `insight.concept` | 30자 | 34자 |
| `insight.note` | 125자 | 140자 |
| `stat.label` | 22자 | 26자 |
| `stat.value` | 18자 | 22자 |
| `stat.axisCaption` | 52자 | 60자 |
| `kicker` | 22자 | 26자 |
| `trend` 배열 | 4~5점 | 2~8점 |
| `issue.insightNote` (2026-08-10 도입) | 110자 | 140자 |

### 4.2 A4 1페이지 보장

`build/verify.mjs`가 **매 빌드마다 헤드리스 PDF로 실측한다.**

```js
const buf = await page.pdf({ format: "A4", printBackground: true,
                             margin: { top: "0", bottom: "0", left: "0", right: "0" } });
const pages = (buf.toString("latin1").match(/\/Type\s*\/Page[^s]/g) || []).length;
// pages === 1 이어야 한다
```

이 검사가 실제로 결함을 잡았다. 진을 감싼 래퍼의 `margin-top: 32px`와 `.zine-page`의
`min-height: 297mm`가 맞물려 인쇄가 2페이지가 됐다. `@media print`에서 래퍼 여백을 지우고
`min-height`를 해제해 해결했다. **이 규칙을 되돌리면 검증이 다시 실패한다.**

### 4.3 예산 초과 시

문장을 줄인다. **폰트 크기·행간·여백을 줄여서 맞추지 않는다.** 그건 불변식 위반이다.
줄일 수 없다면 소재가 한 호에 담기지 않는 것이다 — 쪼개거나 다음 주로 넘긴다.

---

## 5. 레이아웃

### 5.1 그리드

| 컨테이너 | 값 |
|---|---|
| `.shell` (앱 셸) | `max-width: 1240px` |
| 읽기 측정 | `--measure: 68ch` (45~75자 범위 안) |
| `.zine-page` | 210mm × 297mm — 실측 794 × 1123px @96dpi |

### 5.2 브레이크포인트

| 구간 | 구조 | 변하는 것 |
|---|---|---|
| **≥1200px** | 12칼럼 · 본문 `1 / span 7` / 레일 `9 / span 4` | 레일 `sticky`, 세로 괘선 표시 |
| **<1200px** | 1단 | 레일(스탯·출처·인사이트)이 본문보다 먼저, 본문 흐름 위로 쌓인다 |
| ≤780px (진) | 진 3단 → 1단 | 사진 float 해제, 헤드라인 44→32px |

`build/verify.mjs`가 375 / 768 / 1280 / 1920px에서 **가로 넘침이 없는지 실측한다.**

> **2026-08-11 일곱 번째 라운드 — 실제로 붙였다.** 이 표는 이번 세션 초반부터 있었지만
> `render-story.mjs`/`css.mjs`는 2026-08-10 목업 개편 때 이 스펙을 버리고 폭 전체 한 칼럼
> (§측정 68ch)으로 갔었다 — 문서와 코드가 갈라져 있었다. 사용자가 "넓은 화면에서 본문이
> 왼쪽에만 몰려 있다"고 다시 지적해 `.story-grid`/`.story-col`/`.story-rail`로 이 표
> 그대로 구현했다(`build/lib/css.mjs`, `build/lib/render-story.mjs`). 다만 768–1199px 중간
> 단계(8칼럼·레일 하강)는 만들지 않았다 — 1200px 미만은 전부 한 단으로 통일했다. 표에
> 있던 "768–1199px" 행은 그래서 뺐다 — 실제로 없는 중간 단계를 문서에 남겨두는 게 더
> 헷갈린다.
>
> **구현 시 실측으로 발견한 함정 하나** — DOM에서 레일(`.story-rail`, 칼럼 9/span4)을
> 본문(`.story-col`, 칼럼 1/span7)보다 먼저 두면, CSS grid의 auto-placement 커서가
> 레일이 차지한 칼럼 뒤로 넘어가면서 본문을 다음 행으로 밀어낸다 — 본문 칼럼 시작(1)이
> 커서보다 앞이라 같은 행에 못 들어간다. 두 요소 모두 `grid-row: 1`을 명시해야 나란히
> 앉는다. `grid-template-areas` 없이 명시적 칼럼 배치만 쓸 때 반복될 수 있는 함정이라
> 적어 둔다.

### 5.3 본문에 CSS 다단을 쓰지 않는다

신문이 다단인 것은 **지면이 고정돼 있기 때문**이다. 웹에서 본문을 다단으로 짜면
아래로 읽다가 다시 위로 올라와야 한다.

대신 **비대칭 그리드**로 신문의 인상을 만든다 — 본문은 45~75자 한 칼럼, 숫자와 인사이트는
오른쪽 레일에 세로 괘선으로 붙는다. 다단은 **인쇄 진에서만** 쓴다. 거기서는 지면이 고정이라 맞는 도구다.

레일에 스탯이 상주하므로 **"근거는 늘 옆에 있다"는 태도가 레이아웃으로 표현된다.**

### 5.4 여백

화면은 **px**, 진은 **mm**. 종이 산출물의 치수를 물리 단위로 유지하려는 의도적 분리다.

- 화면: `4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96` px (`--s1`~`--s9`)
- 진: `1 · 2 · 3 · 4 · 5 · 6 · 8 · 10` mm / `.zine-page` 패딩 `8mm 10mm` → ≤780px에서 `6mm 5mm`

**이 패딩을 줄이면 프린터 물리 여백에 잘린다.**

---

## 6. 도메인 컬러 — 확장 규칙

### 6.1 현재 활성 도메인 — 8개

단일 소스는 `domains/registry.json`이다. CSS는 `--dc-<key>` 변수만 참조한다.

| 도메인 | Hue | Night `color` | 대비 | Paper `colorPaper` | 대비 |
|---|---|---|---|---|---|
| 음악 | 35° | `#FF9500` | 8.32:1 | `#A15E00` | 4.51:1 |
| 여행 | 85° | `#59900C` | 4.72:1 | `#4A780A` | 4.67:1 |
| 도서 | 135° | `#34C759` | 8.24:1 | `#217E38` | 4.53:1 |
| 공연 | 172° | `#0C907E` | 4.62:1 | `#0A7B6C` | 4.57:1 |
| 영화 | 210° | `#0A84FF` | 5.01:1 | `#006BD7` | 4.55:1 |
| 뉴스 | 256° | `#8B66F3` | 4.61:1 | `#7142F0` | 4.97:1 |
| OTT | 303° | `#E212D8` | 4.63:1 | `#C110B9` | 4.60:1 |
| 유튜브 | 349° | `#FF2D55` | 5.01:1 | `#DD002A` | 4.52:1 |

**열여섯 값 전부 AA를 넘는다.** Paper 보정색은 **hue를 한 도도 바꾸지 않고 명도만 내린** 값이다.
`test/theme.test.mjs`가 매번 대비와 hue 일치를 계산해 검증한다.

**도메인을 8개로 늘린 이유는 지면을 채우기 위해서가 아니라 결번이 나도 4편을 확보하기 위해서다.**
> **2026-08-10 되돌림.** "리드 1 + 미니 3, 상위 4편만" 위계는 없앴다 — 지면은 이제 그 주
> 통과분 전원을 동등한 행(`.zine-story`)으로 낸다. A4 1장 제약은 그대로다(§4.2) — 통과
> 편수가 늘면 행마다 여백·글자 크기가 아니라 §4.3 규칙대로 **문장을 줄여서** 맞춘다.

> 배경이 `#222222`에서 `#16150F`로 바뀌면서, 영화·유튜브가 4.36:1로 AA 미달이던 오래된 문제가
> 5.01:1로 **저절로 해소됐다.** 색을 바꾸지 않고 배경만 바꿔서 풀렸다.

진 페이지에는 도메인 색을 쓰지 않는다 (진 킥커는 `--secondary`).

### 6.2 신규 도메인 색 선정 기준

네 조건을 **전부** 만족해야 한다.

1. ~~Night 배경 `#16150F` 대비 ≥ 4.5:1~~ — 2026-08-10부터 Night 테마 자체가 없다(§1).
   `registry.json`의 `color`(Night 변형) 필드는 실제로 어디서도 렌더되지 않는 값이다
   (`domainCSS()`는 항상 `colorPaper`만 낸다) — 새 도메인을 추가할 때 이 계산에 시간을
   쓰지 않는다.
2. 사이트 배경 `#FFFFFF` 대비 **≥ 4.5:1** (`colorPaper`) — 예전엔 `#F4F1E7` 기준이었다.
   흰색이 더 밝아 대비는 늘기만 하므로, 기존 도메인 값은 전부 그대로 통과한다.
3. hue는 **자기 자신과만** 일치하면 된다 — `color`/`colorPaper` 두 값을 굳이 나눌 이유가
   없어졌으니, 새 도메인은 `colorPaper` 하나만 정하면 된다.
4. 기존 모든 도메인과 **색상환 거리 ≥ 35°**

### 6.3 색상환은 다 찼다

현재 hue는 35 / 85 / 135 / 172 / 210 / 256 / 303 / 349°다. 간격은 이렇다.

```
35 →85  50°     172→210  38°
85 →135 50°     210→256  46°
135→172 37°     256→303  47°
                303→349  46°     349→35  46°
```

**최소 간격이 37°이고, 이게 8개를 배치할 때의 실질 한계다.**
색상환 360°를 8등분하면 45°지만 기존 4개(35·135·210·349°)가 불균등해 그만큼 못 벌린다.
그래서 `minHueDistance`를 40°에서 **35°로 낮췄다.**

> **9번째 도메인은 이 규칙으로 불가능하다.** 색만으로 구분하는 방식의 상한에 도달했다.
> 더 늘리려면 색 이외의 구분 수단(패턴·아이콘·타이포 변주)이 필요하고, 그건 불변식 변경이라 사용자 승인 사항이다.

### 6.4 도메인 증가 시 레이아웃 영향

- **홈 카테고리 그리드** — `grid-template-columns: repeat(2, 1fr)`(모바일 1열)라 도메인이
  늘면 행이 자동으로 늘어난다. 2026-08-10에 도메인 필터(`.seg`)+평면 리스트(`.list`)를
  이 그리드가 완전히 대체했다 — "줄바꿈되는 필터 탭"이라는 옛 관심사는 없어졌다.
- **진 `.zine-story`** — 2026-08-10부터 스토리 수만큼 행이 늘어나는 구조다(고정 3칸이
  아니다). 활성 도메인이 늘면 그만큼 행도 늘 수 있다는 뜻 — **A4 1장을 넘기면 레이아웃
  결정이 필요하다.** `build/verify.mjs`가 매 빌드마다 실측한다(§4.2).
- **A4 예산** — 지금은 4편 기준으로 실측해 여유가 있다. 통과 편수가 늘면
  `build/verify.mjs`로 재측정하고, 넘치면 §4.3대로 문장을 줄인다.

---

## 7. 컴포넌트 스펙

### 마스트헤드

```
site-header: position: sticky; top: 0 · 항상 화면 상단에 붙는다 · 뷰포트 전체 폭(--edge만 패딩)
워드마크:    로고 이미지(§3 워드마크 참조) · width clamp(234px, 31vw, 359px) · height auto
카테고리 내비: About + 영문 라벨(nameEn) 링크 · 로고와 같은 줄, 우측 정렬 · 좁은 화면에서 가로 스크롤
```

> **2026-08-10 두 번째 라운드.** 예전엔 여기서 워드마크를 "Paperlogy 900 · uppercase" 텍스트로
> 적어 뒀지만, 실제 마스트헤드(`.wordmark`/`.logo`)는 처음부터 텍스트가 아니라 이미지 로고였다
> — §3과 안 맞던 서술을 그때 실제 CSS에 맞춰 고쳤다. 이번 라운드에서 셋을 더 바꿨다:
> 1. **고정(sticky)** — 로고·카테고리 내비가 스크롤 내내 상단에 붙는다(사용자 요청).
> 2. **로고 20% 확대** — `clamp(150px, 20vw, 230px)` → `clamp(180px, 24vw, 276px)`.
> 3. **카테고리 내비 추가, 밑줄(`.ruleline`) 제거** — 로고 옆에 도메인별 영문 라벨
>    (`registry.json`의 `nameEn`)이 링크로 붙는다. 홈 카테고리 카드와 같은 목록·같은
>    "최신 스토리" 링크를 쓴다(`build.mjs`가 한 번 계산해 모든 페이지에 물려준다). 굵은 선 +
>    가는 선의 옛 룰라인은 뺐다 — 사용자 요청.
>
> **2026-08-11 세 번째 라운드.** 넓은 화면에서 로고가 `.shell`(max-width 1240px)에 갇혀
> 좌우로 빈 공간이 남는다는 지적 — 셋을 바꿨다:
> 1. **엣지-투-엣지** — `.site-header`가 `.shell`을 벗고 뷰포트 전체 폭을 쓴다. 로고·내비는
>    새 토큰 `--edge`(`clamp(10px, 2vw, 24px)`)만큼만 여백을 둔다 — `.shell`의 큰 거터
>    대신 이 값 하나로 "코너에 붙었다"는 인상을 준다.
> 2. **로고 30% 추가 확대** — `clamp(180px, 24vw, 276px)` → `clamp(234px, 31vw, 359px)`
>    (2026-08-10의 +20%에 이은 두 번째 확대, 사용자 요청).
> 3. **"About" 링크 추가** — 내비 맨 앞, 신규 `/about/` 라우트로 이어진다.
> 홈의 데이트라인·통합 인사이트·카테고리 그리드·페이저도 `.shell` 대신 같은 `--edge`
> 토큰을 쓰는 `.shell-edge`로 옮겨, 마스트헤드부터 페이지 끝까지 여백이 일관된다.

### 통합 인사이트 — 헤드라인 + 부연설명 (2026-08-10 두 번째 라운드 확장)

```
헤드라인(issue.insight):     h1 · 산세리프 900 · clamp(34px, 6.5vw, 72px) · lh 1.04 · max-width 20ch
부연설명(issue.insightNote):  명조 400 · 18px · lh 1.6 · color --secondary · 웹 전용
```

그 호 4편을 관통하는 한 줄(§CLAUDE.md S5.5)이 이제 홈의 진짜 헤드라인이다 — 이탤릭을 뺐고
(사용자 요청, 이 사이트 전체가 이탤릭을 안 쓴다), PC 기준 72px까지 키워 페이지 최상단의
시각적 무게중심으로 삼았다. `insightNote`는 그 헤드라인이 압축한 것을 한 문단으로 풀어준다
— 필수는 아니다(없으면 헤드라인만 나온다). 인쇄 진은 A4 여유가 없어 `insightPrint`
한 줄만 그대로 쓴다 — `insightNote`는 인쇄에 안 나온다.

### 페이저 · 전체 아카이브 (2026-08-10 두 번째 라운드 도입)

카테고리 카드 그리드 밑, 홈의 진짜 마지막 요소다.

```
.pager: grid-template-columns: 1fr auto 1fr · 이전(← 이전 주 인사이트) · 가운데(전체 아카이브 보기)
        · 다음(다음 주 인사이트 →, 홈에서는 항상 비활성)
```

"다음 주"는 홈이 정의상 항상 최신 회차를 보여주므로 구조적으로 늘 비활성(`.is-off`)이다 —
더 최신인 회차가 있으면 그게 이미 홈이다. "이전 주 인사이트"는 그 회차의 인쇄 진(`/print/`)
으로 보낸다 — 회차별 개별 페이지가 2026-08-08에 없어졌지만, 인쇄 진에 그 주 헤드라인 +
스토리 전원이 그대로 있어서 유일하게 남은 "그 주로 돌아가는" 라우트다.

가운데 "전체 아카이브 보기"는 `/archive/`로 간다 — 홈이 카드 그리드(도메인당 최신 1편)로
바뀌면서 없어졌던 평면 목록을 별도 라우트로 되살린 것이다(`build/lib/render-archive.mjs`).
홈의 카드 그리드 자체는 안 바뀐다 — `/archive/`는 "더 보고 싶을 때"만 가는 보조 경로다.

### 카테고리 카드 (2026-08-10 도입, 2026-08-11 위계 반전 — 홈 전용)

```
그리드: repeat(2, 1fr) · gap 1px · 배경 --ink(칸 사이 괘선) · 640px 이하 1열 · 뷰포트 가장자리까지
카드:   padding 24px/32px · min-height 280px
        기본: 배경 cardColor(밝은 파스텔) · 글자 --ink(검정)
        폴백(cardColor 없음): 배경 --dc-<key>(colorPaper, 어두운 톤) · 글자 흰색(.is-dark)
태그:   우측 상단 절대 위치, 12px · 도메인명(nameEn 아님 — 한국어 그대로)
헤드라인: 카드에서 가장 큰 글자, clamp(26px, 4.2vw, 46px) 900
```

사용자가 첨부한 참고 이미지(도메인별 색 타일 그리드)를 따른다 — 폰트·정확한 배치는
그대로 옮기지 않고 이 사이트의 헬베티카 톤 위에서 재해석했다. **도메인 색(또는
전용 카드색)을 배경으로 풀블리드하는 이 사이트 유일의 자리다**(§2 불변식 3번 다섯
번째 예외).

> **2026-08-11 개편.** 두 가지가 바뀌었다:
> 1. **위계 반전** — 예전엔 도메인명이 카드에서 가장 큰 글자였다. 이제 **스토리
>    헤드라인이 메인**이고, 도메인명은 우측 상단의 작은 라벨이다(사용자 요청).
> 2. **전용 카드색 도입(`cardColor`)** — `colorPaper`는 흰 배경 위 텍스트/보더용으로
>    어둡게 골랐다(흰 글자를 못 올린다). 참고 이미지의 카드는 밝은 파스텔이라 검은
>    글자가 필요했다 — `domains/registry.json`에 도메인별 `cardColor`(밝은 톤)를
>    새로 추가하고, 카드 배경은 `cardColor` + 검은 글자(`--ink`)를 기본으로 쓴다.
>    `cardColor`가 없는 도메인은 예전처럼 `colorPaper` + 흰 글자(`.is-dark`)로
>    되돌아간다. 두 조합 다 `test/theme.test.mjs`가 AA(4.5:1)를 검증한다 — 밝은
>    카드는 흰 글자가, 어두운 카드는 검은 글자가 각각 AA 미달인 것도 함께 검증한다
>    (잘못된 조합을 실수로 못 쓰게).

카드 하나 = 도메인 하나. 그 도메인의 **가장 최신 스토리**로 바로 이어진다(편집 순서가
아니라 `range` 기준 실제 최신 — `build/lib/data.mjs`의 `latestByDomain()`). 아직 통과분이
없는 도메인은 색을 채우지 않는다 — 근거 없이 색만 칠하면 "발행됐다"는 인상을 준다.

두 장마다 한 번, 참고 이미지의 가로 구분 밴드를 낸다(`.category-divider`) — **정적이다,
움직이지 않는다.** §9 "모션은 반응에만 쓴다" 원칙상 마퀴 스크롤 같은 장식용 애니메이션은
가져오지 않았다 — 포맷(밴드가 행을 가른다)만 가져오고 그 안의 모션은 가져오지 않았다.

### 스탯 카드 (근거 레일)

```
border-top 3px --rule · border-bottom 1px --rule · padding 12px 0 16px
≥1200px에서 position: sticky, top 24px
```

**출처 링크는 항상 보인다.** 프로토타입은 `opacity: 0`에 hover로만 노출해 터치 기기에서 접근이 불가능했다.
근거는 숨기는 게 아니라 옆에 두는 것이다.

### 스파크라인 (렌더 중단, 2026-08-08)

사용자 요청으로 스탯 카드에서 차트를 뺐다 — 지금은 라벨 + 수치 + 출처 링크만 보인다.
`sparklineSVG()` 함수(`build/lib/sparkline.mjs`)와 아래 스펙은 데이터·테스트 자산으로 남아 있지만
`build/lib/render-story.mjs`가 더는 호출하지 않는다. 되살릴 일이 생기면 이 스펙을 그대로 쓴다.

```
크기:   160 × 56
정규화: 첫 값을 100으로 두는 기준선 정규화. 기준선이 범위 끝이면 반대쪽에 15% 여백
기준선: 1px 점선(2 3), opacity .25 — "전주 대비"라는 축 의미를 형태로 보여준다
선:     stroke-width 2.5 · round cap/join
끝점:   circle r 3.5, x = w
대체:   <title>에 추세 문장. role="img"
```

min–max 정규화가 아니다. 프로토타입은 `[98,101,100,426]`에서 앞 세 점이 바닥에 뭉개져 형태 정보가 0이었다.
기준선 정규화 + 여백으로 y가 44.26 / 43.91 / 44.03이 되어 바닥(50)에서 떨어진다.

### 스탯 패널

```
2026-08-10 개편: 헤드라인 → byline 바로 다음, flex row · 라벨(왼쪽, ≤60%) + stat.value(오른쪽, 크게)
bg --surface · border 1px --divider · border-radius 16px · padding 24px
```

참고 목업(movie.html)의 `.stat-panel` 구조를 따른다. 출처 링크는 여기 없다 — 아래
`source-box`로 분리했다.

### 인사이트 콜아웃

```
bg --surface · border 1px --divider · border-left 1px 도메인 컬러 · padding 16px 24px
라벨: 산세리프 700 11px · 고정 문구 "인사이트" (quote.insight.concept은 안 쓴다)
본문: quote.insight.note · 14px · lh 1.75
```

> **2026-08-10 되돌림.** 2026-08-08엔 `insight`(개념·설명)를 화면에서 뺐다(사용자 요청).
> 참고 목업의 `.insight`/`.note2` 콜아웃이 다시 요구해 되돌렸다 — `note`만 보여주고
> `concept`은 여전히 라벨로 쓰지 않는다. `tools/qa.mjs`의 데이터 검증(§2 불변식 #2)은
> 그대로다.

### 풀쿼트 & 출처 박스

```
쿼트: 명조 700 · 20px · lh 1.55 · border-left 1px 도메인 컬러 · padding-left 16px
     (본문 세 문단 뒤로 이동 — 2026-08-10)
출처: border-top/bottom 1px --divider · padding 16px 0 · flex space-between
     라벨 "원문 출처" + stat.sourceUrl 링크
```

### 브라우저 표면

`::selection`, `:focus-visible`, `caret-color`, 스크롤바를 전부 팔레트에서 지정한다.
그리지 않은 부분도 디자인을 짊어진다.

---

## 8. 진(zine) 전용 장치 — "한 장 요약"(`/YYYY-wNN/print/`)

> **이건 §11의 "DIY 진"과 다른 산출물이다.** 여기는 A4 세로 1장짜리 요약지고, §11은
> A4 가로 2장을 접어 만드는 8쪽 미니 소책자다. 둘은 서로 다른 라우트·다른 CSS
> 네임스페이스(`.zine-*` vs `.zb-*`)를 쓰고, 한쪽을 고쳐도 다른 쪽은 영향받지 않는다.

> **2026-08-10 전면 개편.** 사용자가 첨부한 참고 목업(print.html)의 포맷을 따라, 아래
> 있던 사진 콜라주·테이프·스티커·QR·3단 세로 괘선 장치를 전부 없앴다(옛 §2 불변식
> 5·6번이 그 대상이라 함께 삭제됐다). "손으로 만든 인쇄물" 인상 대신, **읽는 순서가
> 명확한 표 형태의 요약지**로 바뀌었다 — 스토리 한 편 = 지면 한 행이다.

### 스토리 행 (`.zine-story`)

```
grid-template-columns: 1fr 32mm · gap 6mm · border-bottom 1px --ink
왼쪽: 킥커(도메인·kicker) · h3 헤드라인 · teaser 한 문단 ·
      인사이트 콜아웃(quote.insight.note, 도메인색 좌측 보더) · 출처 링크(stat.sourceUrl)
오른쪽(statcol): stat.value(크게, 900) · stat.label
```

스토리 수만큼 행이 늘어난다 — 리드/미니 구분이 없다(§2 불변식 4번, §6.1). `stat.value`는
숫자+단위로 쪼개지 않는다 — 스토리마다 형태가 다른 자유 문자열이라 정규식 분리가 깨지기
쉽다. 전체를 한 덩어리로 크게 낸다.

### 푸터

```
border-top 1px --ink · padding-top 3mm · text-align center · 8.5px
```
`SITE.name · 회차 · 전체 아카이브 <도메인> · 각 기사의 인사이트는 편집 의견입니다` 한 줄이다.
2026-08-08까지는 QR(`assets/img/qr-<회차>.svg`)이 이 자리에 있었다 — 참고 목업처럼 사람이
읽는 URL 텍스트로 바꿨다. 가리키는 곳은 그대로 홈 아카이브(`answerzine.kr`)다.

---

## 9. 모션

모든 애니메이션은 `@media (prefers-reduced-motion: reduce)`에서 정지하되 콘텐츠는 그대로 보인다.

예외 하나 — 홈의 인트로(§8이 아니라 `build/lib/layout.mjs`의 `intro()`)는 스크롤에 반응하는
발산 트랜스폼이다. 2026-08-10부터 "이거 왜 잘나가?" 스테이트먼트 단계가 없어지고, 인트로
섹션 하나만 남았다 — "THE" / "ANSWER" / "ZINE" 세 줄이 중앙에 겹쳐 있다가 스크롤에 맞춰
THE·ZINE은 오른쪽으로, ANSWER는 왼쪽으로 갈라지며 그 뒤의 아카이브를 드러낸다. `.intro`는
뷰포트보다 60vh 큰 상자이고 안쪽 콘텐츠가 `position: sticky`로 그 여유분만큼 고정된 채
갈라진다 — 그래야 스크롤이 그냥 지나가 버리지 않고 눈에 보이는 전환이 된다
(`assets/app.js`의 `bootIntroMotion()`).
`prefers-reduced-motion: reduce`에서는 이 계산 자체가 꺼지고 세 줄이 중앙에 겹친 채 그냥
스크롤되어 지나간다 — 콘텐츠(텍스트)는 어느 쪽이든 그대로 읽힌다.

> **2026-08-11 여덟 번째 라운드.** 원래 "THE ANSWER"가 한 덩어리(intro-word-1)였는데, 넓은
> 화면에서 그 안에서 줄이 갈라져(THE / ANSWER) 어색하게 읽힌다는 지적으로 THE·ANSWER·ZINE
> 세 단어를 처음부터 각자 한 줄(각자 `white-space: nowrap`)로 뗐다 — 의도치 않은 중간
> 줄바꿈이 구조적으로 안 생긴다. 글자 크기도 키워(`clamp(72px, 21vw, 380px)`) 가장 긴 단어
> (ANSWER)가 어느 뷰포트에서든 폭의 약 85~90%를 채운다 — 화면 가장자리에 살짝 닿는 인상을
> 의도적으로 노린다. `.intro`에 `overflow: hidden`을 붙여 자간 반올림으로 생기는 1px 안팎도
> 가로 스크롤바를 만들지 않게 막는다.

그 외 시스템에는 연출용 애니메이션이 없다. 상태 전환(hover, 필터, `<details>`)만 있고
전부 브라우저 기본 동작이거나 0.15s 이내다. **모션은 반응에만 쓴다.**

---

## 10. 매주 확인할 시각 QA

대부분 `build/verify.mjs`가 자동으로 검사한다. `[자동]` 표시가 그것이다.

1. 진 페이지가 **A4 정확히 1페이지**인가. `[자동]`
2. 콘솔 에러 0건인가. `[자동]`
3. 375 / 768 / 1280 / 1920px에서 가로 넘침이 없는가. `[자동]`
4. 진 헤드라인이 웹 정본과 문자열이 같은가. `[자동]` (같은 값을 참조하므로 구조적으로 보장)
5. ~~서브셋 폰트가 본문의 모든 글자를 담는가~~ — 2026-08-10 자체 폰트 제거로 해당 없음.
   시스템 헬베티카 스택은 서브셋되지 않는다.
6. 내부 링크가 전부 실제 파일로 해석되는가. `[자동]`
7. 새 도메인 색이 §6.2 네 조건을 전부 통과하는가. `[자동]` (`test/theme.test.mjs`)
8. `@font-face`를 쓰지 않는가(자체 폰트 재도입 방지). `[자동]` (`test/css.test.mjs`)
9. 스파크라인이 뭉개지지 않았는가 — 눈으로. `trend` 4~5점 권장.
10. 도메인 색이 실제 흰 배경에서 읽히는가 — 눈으로. (Night/Paper 토글은 없다, §1)

---

## 11. DIY 진 — 하단 CTA · 3D 플립북 · 인쇄 소책자

2026-08-11 아홉 번째 라운드 신규. `build/lib/render-zinebook.mjs`(콘텐츠)·`build/lib/css.mjs`의
`ZINEBOOK` 블록(스타일)·`assets/zinebook.js`(동작) 세 파일이 이 기능 전부다. §8의 "한 장
요약"과는 완전히 다른 산출물이다(§8 상단 안내 참고) — 흑백만 쓴다(도메인 컬러를 참조하지
않는다), showChrome 페이지(홈·스토리·about·아카이브)에만 붙는다.

### 11.1 콘텐츠 — 8개 논리 페이지

```
앞표지 → About → 기사1 → 기사2 → 기사3 → 기사4 → Notes → 뒤표지
```

그 주(최신 회차) 편집 순서(점수순) 상위 4편이 기사 1~4다. 4편에 못 미치면(결번) 그 자리에
확인 안 된 헤드라인을 채우지 않는다 — "이 자리는 통과분이 4편에 못 미쳐 비웠다"만 낸다
(§7.3과 같은 원칙). 8개 페이지는 각각 **한 번만** 문자열로 만들어지고, 플립북 미리보기와
인쇄 시트 양쪽에 그대로 재사용된다 — 정본이 둘로 갈라지지 않는다(§9.3과 같은 원칙).

표지(앞·뒤 둘 다)는 검정 배경·흰 글자, 속지(About·Notes·기사)는 흰 배경·검정 글자다 —
실제 인쇄물의 "겉은 검정, 속은 흰색" 표지/속지 관례를 그대로 재현한다. 앞표지의 "THE ANSWER
ZINE" 3줄 워드마크는 그 반쪽의 왼쪽 경계(접는 선)에서 살짝 잘린다(`left:-14px`) — 단어
자체는 여전히 읽히는 선에서 멈춘다. 더 자르면 인트로 워드마크 사고(§ CLAUDE.md 헤드라인
규칙의 "부자연스러운 줄바꿈 금지"와 같은 원칙)를 반복하게 된다.

### 11.2 물리 인쇄 배치 — 사용자 지정, 바꾸지 않는다

```
Sheet 1(겉장) 앞면 = [뒤표지 | 앞표지]      Sheet 1 뒷면 = [About | Notes]
Sheet 2(속지) 앞면 = [기사4 | 기사1]        Sheet 2 뒷면 = [기사2 | 기사3]
```

A4 랜드스케이프(297mm×210mm) 2장, 양면. `render-zinebook.mjs`의 `SHEETS` 상수가 이 배치의
단일 소스다 — `test/zinebook.test.mjs`가 순서를 고정한다. 두 반쪽 사이에 옅은 점선
(`.zb-fold`)으로 접는 선을 표시한다.

**이름 있는 `@page`를 쓴다.** 이 시트는 §8의 "한 장 요약"(A4 세로, 이름 없는 기본 `@page`)과
같은 스타일시트를 공유하므로, 이름 없는 `@page`를 다시 쓰면 나중에 선언된 쪽이 사이트
전체의 기본 인쇄 크기를 덮어써 버린다(실제로 Chromium에서 `preferCSSPageSize`로 실측
확인했다). 그래서 `.zb-sheet { page: zb-landscape; }` + `@page zb-landscape { size: 297mm
210mm; margin: 0; }`로 이름을 따로 준다 — 기본 `@page`(A4 세로)는 그대로 §8 라우트만
가리킨다.

인쇄 트리거는 오버레이 안의 "Print Zine" 버튼(`window.print()`)뿐이다. 그 페이지의 평소
콘텐츠(`#main-content`, `.site-header`)와 CTA 바는 인쇄에서 강제로 숨긴다 — 안 그러면 진
시트 앞뒤로 그 페이지의 화면용 콘텐츠가 별도 페이지로 끼어든다(실측으로 발견 — 처음엔
정확히 4장이 아니라 7장, 그다음 5장이 나왔다). `body.has-zb`의 하단 여백(고정 CTA 바
자리)도 인쇄에서 0으로 되돌린다 — 안 그러면 마지막 시트 뒤에 그 여백만을 위한 빈 페이지가
하나 더 생긴다.

### 11.3 3D 플립북 미리보기

물리 페이지 곡률을 시뮬레이션하지 않는다 — `perspective` + `rotateY(-180deg)` 회전으로 각
"leaf"를 왼쪽 경계 축으로 넘기는 CSS 3D 카드 뒤집기 기법이다. 넘어가는 면의 뒷면
(`.zb-leaf-back`)은 실제 다음 장 내용이 아니라 옅은 회색 바탕에 마크 하나만 보여주는
단순화된 "종이 뒷면"이다 — 물리책처럼 한 장의 앞뒤에 서로 다른 콘텐츠가 있는 모델이 아니라
"콘텐츠 페이지 한 장 = leaf 하나"인 단순 모델이라 이렇게 충분하다.

**미리보기가 인쇄 결과와 같은 레이아웃으로 보이는 이유(WYSIWYG) —** 콘텐츠 패널(`.zb-panel`)
은 인쇄 반쪽과 같은 비율의 고정 캔버스(561×794px, 96dpi에서 148.5mm×210mm에 해당)로 그려진
뒤, 실제 화면에 렌더된 leaf 크기에 맞춰 `transform: scale()`로 축소된다. 배율은
`assets/zinebook.js`가 `stage.getBoundingClientRect().width / 561`로 실측해 `--zb-scale`
커스텀 프로퍼티에 써 넣는다(오버레이를 열 때 + 리사이즈마다).

### 11.4 발견한 함정 (실측으로 찾은 것들 — 다시 반복하지 않도록)

- **SVG에 `calc()` 퍼센트 크기를 주면 0×0으로 계산될 수 있다.** Notes 배경의 줄무늬 SVG가
  처음엔 안 보였다 — `width: calc(100% - 52px)`가 SVG 루트에서 크기를 못 잡았다.
  `inset:0; width/height:100%`로 단순화하고, 태그에도 `width="100%" height="100%"` 속성을
  같이 줬다.
- **한 문서에 같은 SVG `id`가 두 번 있으면 `url(#id)` 참조가 깨진다.** 8개 논리 페이지를
  한 번만 만들어 플립북·인쇄 두 군데에 재사용하는 구조라(§11.1) `<pattern id="...">`를 쓰면
  문서에 같은 id가 필연적으로 두 번 생긴다. id 없이 `<line>`을 반복해 찍는 방식으로 바꿔
  이 문제 자체를 없앴다.
- **`grid-column`만 다르고 `grid-row`를 안 정하면 auto-placement 커서가 순서를 밀어낸다** —
  이건 §5.3의 본문·레일 그리드에서 겪은 함정이지만 여기도 같은 클래스의 실수를 조심해야
  한다(다단 배치가 있는 곳 어디서든).
- **`[hidden]` 속성이 남아 있으면 명시도가 인쇄 규칙보다 높아진다.** `.zb-overlay[hidden] {
  display:none !important }`(화면용 안전장치)와 인쇄 블록의 `.zb-overlay { display:block
  !important }`가 둘 다 `!important`일 때, 앞쪽이 명시도(0,2,0 > 0,1,0)로 이긴다 — 오버레이를
  한 번도 연 적 없는 상태에서 인쇄를 시도하면(버튼을 안 거치는 경로) 시트가 안 나왔다.
  인쇄 규칙 선택자에도 `[hidden]`을 같이 걸어 해결했다.
