# design.md — ANSWER ZINE 디자인 시스템

**주간 반복 생산을 전제로 한 고정 템플릿 명세다.**
매주 바뀌는 것은 §4의 슬롯 콘텐츠뿐이고, 나머지는 전부 잠겨 있다.

값은 전부 `build/lib/*.mjs`에 실제로 구현된 것이고, 대비 수치는 WCAG 2.1로 계산한 실측값이다.
**단일 소스는 코드다.** 이 문서의 표는 사람이 읽는 사본이므로, 값을 바꿀 때는 코드를 먼저 고친다.
`test/theme.test.mjs`·`test/css.test.mjs`가 아래 규칙 상당수를 자동으로 강제한다.

| 구분 | 원칙 |
|---|---|
| §2 불변식 | 에이전트가 **절대 바꾸지 않는다.** 변경은 사용자 승인 사항. |
| §4 슬롯 | 매주 바뀐다. **분량 예산을 지키면 A4 1페이지가 보장된다.** |
| §6 도메인 색 | 도메인 추가 시에만 건드린다. 선정 규칙이 정해져 있다. |

---

## 1. 세 개의 테마

테마가 셋이고, **CSS 변수 재정의 한 번으로 전환된다.** 역할 이름은 셋 다 같고 값만 다르다.

| | Night (기본) | Paper (대체) | Zine (인쇄) |
|---|---|---|---|
| 적용 범위 | `:root` | `[data-theme="paper"]` | `.zine-page` · `@media print` |
| 성격 | 밤에 읽는 아카이브 | 낮의 지면 | 종이에 인쇄해 카페에 두는 진 |
| 배경 | `#16150F` | `#F4F1E7` | `#F4F1E7` |

```css
:root {                    /* Night — 기본 */
  --ink:       #F2EFE4;   /* 본문       대비 15.89:1 */
  --secondary: #A8A192;   /* 보조·티저   대비  7.12:1 */
  --tertiary:  #8A8375;   /* 날짜·캡션   대비  4.86:1 */
  --rule:      #F2EFE4;   /* 괘선 */
  --bg:        #16150F;
  --surface:   #1E1D16;
  --divider:   rgba(242,239,228,.22);
}

[data-theme="paper"] {     /* Paper — 같은 이름, 낮의 값 */
  --ink:       #17150F;   /* 대비 16.15:1 */
  --secondary: #5C574A;   /* 대비  6.37:1 */
  --tertiary:  #726C5E;   /* 대비  4.62:1 */
  --rule:      #17150F;
  --bg:        #F4F1E7;
}
```

**세 테마의 세 역할 아홉 값이 전부 AA(4.5:1)를 넘는다.** `test/theme.test.mjs`가 매번 계산해 검증한다.

Night는 중성 회색이 아니라 **따뜻한 먹빛**이다. 같은 크림 지면을 밤에 뒤집어 본 인상이라 Paper와 한 세계로 묶인다.
`--secondary`·`--tertiary`도 중성 회색이 아니라 배경 색상에서 뽑은 갈회색이다.

### 테마 전환

- 기본값은 **Night**다. `prefers-color-scheme`로 자동 전환하지 **않는다.**
- 마스트헤드 토글로 Paper로 바꾸고, 선택은 `localStorage`(`az-theme`)에 남는다.
- 첫 페인트 전에 인라인 스크립트가 적용해 깜빡임이 없다.
- `@media print`는 활성 테마와 무관하게 **항상 Zine**을 쓴다.

> OS 라이트 선호를 무시하는 건 브랜드 기본값을 우선한 결정이다.
> 존중하도록 바꾸려면 `build/lib/layout.mjs`의 초기화 스크립트 한 줄만 고치면 된다.

---

## 2. 불변식 — 매주 바뀌지 않는 것

에이전트는 아래를 변경하지 않는다. 콘텐츠가 안 맞으면 **템플릿이 아니라 콘텐츠를 고친다.**

1. **스토리 구조는 5블록 고정** — `text, stat, text, quote, text`. 추가·삭제·순서 변경 없음.
   (`tools/qa.mjs` 232행이 이 순서를 강제한다.)
2. **`stat` 1개, `insight` 있는 `quote` 1개** — 스토리당 정확히 하나씩. **데이터 계약 얘기다.**
   2026-08-08부터 `insight`(개념·설명)는 화면에는 안 보인다(사용자 요청) — 그래도 데이터에는
   계속 있어야 하고 `tools/qa.mjs`가 그대로 검증한다. S5 집필 규율(인사이트 3문 테스트)이
   기대는 내부 품질 장치이지, 독자에게 보여주는 라벨이 아니다.
3. **도메인 색이 쓰이는 자리는 3곳뿐** — 도메인 태그, 킥커, 풀쿼트 좌측 보더.
   배경 채색·버튼 채색은 이 시스템에 없다. (2026-08-08까지는 스파크라인도 포함한 4곳이었다 —
   사용자 요청으로 스탯 카드의 차트 렌더링을 없애면서 빠졌다. `sparklineSVG()` 함수 자체는
   `build/lib/sparkline.mjs`에 남아 있다 — 호출부만 없다.)
4. **진 페이지는 리드 1 + 미니 3, A4 1장.** 2페이지짜리 진을 만들지 않는다.
5. **회전각은 −9°~+7° 범위.** 사진 −1.1°, 스티커 −3°, 테이프 −9°/+7°.
6. **`clip-path` 다각형 값** — 가위로 오린 가장자리 표현. 조정하지 않는다.
7. **`@page { size: A4; margin: 0 }`** — 안전 여백은 `.zine-page` 패딩이 전담한다.
8. **웹 본문에 CSS 다단(`column-count`)을 쓰지 않는다.** §5.3 참조.
9. **색 보더는 1px을 넘지 않는다.** 풀쿼트 포함.

---

## 3. 타이포그래피

### 워드마크 — 원본 로고를 쓴다

프로토타입의 로고를 그대로 승계한다. `the answer company` 브래킷 마크 + `ANSWER` 슬랩 + `Zine` 스크립트다.

| 파일 | 쓰임 | 아트 |
|---|---|---|
| `assets/img/logo-light.png` | Night 테마 | 흰색, 배경 투명 |
| `assets/img/logo-dark.png` | Paper 테마 · 인쇄 진 | 검정, 배경 투명 |

원본은 2000×2000 정사각이고 아트가 세로 41%만 차지해, 지정 높이의 절반도 안 나왔다.
(프로토타입의 34×34 마스트헤드 로고가 사실상 보이지 않던 이유다.)
`tools/crop-png.mjs`로 여백을 잘라내고 배경을 투명화했다 — 의존성 없이 동작하며 재실행 가능하다.

```bash
node tools/crop-png.mjs --matte <입력.png> <출력.png> [여백px]
```

배경이 불투명하면 크림 지면에 흰 사각형이, 먹빛 배경에 회색 사각형이 얹힌다. **반드시 투명화한 파일을 쓴다.**
화면 높이는 `clamp(30px, 4.4vw, 46px)`, 진에서는 `15mm`다.

### 서체 — 둘, 역할이 갈린다

| 역할 | 서체 | 웨이트 |
|---|---|---|
| 헤드라인 · 킥커 · 라벨 · 수치 · UI | **Paperlogy** (산세리프) | 400 / 700 / 900 |
| 본문 · 티저 · 쿼트 | **나눔명조** (세리프) | 400 / 700 |

두 번째 가족을 들인 근거는 명확하다. **한국어 장문 가독은 명조만 하는 일이고, 수치 조판은 산세리프만 하는 일이다.**
데이터와 산문이 서로 다른 목소리를 갖는다.

**로드하는 웨이트는 위 다섯뿐이다.** CSS가 그 밖의 웨이트를 참조하면 `test/css.test.mjs`가 실패한다.
프로토타입은 300/400/700/900만 로드하면서 CSS가 500·590·600·800도 참조해 의도한 위계가 나오지 않았다.

두 서체 모두 **SIL OFL**이라 자체 호스팅·재배포가 허용된다. 한글 11,172자를 전량 커버한다.
`Fonts/`의 `NotoSerif-*.ttf` 72종은 **라틴 전용이라 한글이 0자다. 쓰지 않는다.**

### 킥커

킥커는 신문 지면의 문법이므로 **유지한다.** 다만 모노스페이스는 뺐다 —
`monospace`를 "기술적"이라는 인상의 의상으로 쓰는 것은 근거가 없다.
산세리프 700 + 자간 `.14em` + `uppercase`로 대체한다.

### 스케일 — 화면

| 역할 | 크기 | 웨이트 | 행간 | 자간 |
|---|---|---|---|---|
| 스토리 헤드라인 | `clamp(32px, 5.2vw, 52px)` | 900 | 1.06 | −0.035em |
| 인덱스 리드 헤드라인 | `clamp(30px, 4.6vw, 46px)` | 900 | 1.1 | −0.03em |
| 섹션 제목 (h2) | `clamp(24px, 3.4vw, 34px)` | 900 | 1.1 | −0.03em |
| 스탯 값 | 30 | 900 | 1.1 | −0.03em (tabular) |
| 풀쿼트 | 20 | 700 (명조) | 1.55 | −0.01em |
| 티저 | 18 | 700 (명조) | 1.6 | — |
| 본문 | 17 | 400 (명조) | 1.9 | Night +0.005em |
| 리스트 헤드라인 | 19 | 900 | 1.2 | −0.025em |
| 메타·날짜 | 13 | 400 | — | tabular |
| 킥커·라벨 | 11 | 700 | — | 0.1~0.14em, uppercase |

**자간 하한은 −0.04em이다.** 실사용은 −0.01 ~ −0.035em. `test/css.test.mjs`가 강제한다.

Night에서 본문 자간이 **양수**인 게 핵심이다. 어두운 배경의 밝은 텍스트는 번져 보이므로
행간·자간·웨이트 세 축에서 보정해야 한다. Paper에서는 0으로 되돌린다.

### 스케일 — 진

| 역할 | 크기 | 웨이트 | 행간 |
|---|---|---|---|
| 워드마크 | 26 | 900 | — (자간 .22em) |
| 리드 헤드라인 | 44 | 900 | 1.05 |
| 미니 헤드라인 | 19 | 900 | 1.15 |
| 티저 | 14 | 700 | 1.45 |
| 본문 | 11.5 | 400 | 1.65 |
| 미니 티저 | 11 | 400 | 1.55 |
| 킥커 | 10.5 | 700 | — (0.1em, uppercase) |
| 데이트라인·바이라인 | 10 | 700 | — |
| QR 캡션 | 9.5 | 400 | 1.5 |

진의 킥커·라벨류는 전부 `uppercase` + 넓은 자간이다. 신문 조판 관용이다.

### 숫자

`.stat-value`와 메타 날짜에 `font-variant-numeric: tabular-nums`. 세로로 나열될 때 자릿수가 흔들리지 않는다.

---

## 4. 슬롯과 분량 예산 — 매주 바뀌는 것

### 4.1 예산표

단일 소스는 `domains/registry.json`의 `budget`이다. `tools/qa.mjs`가 이 값으로 검사한다.

| 슬롯 | 권장 (≤) | 절대 상한 |
|---|---|---|
| 스토리 헤드라인 | **24자** | 32자 |
| 스토리 티저 | **78자** | 104자 |
| `text` 블록 (각) | **180자** | 240자 |
| `quote.text` | 40자 | 48자 |
| `insight.concept` | 30자 | 34자 |
| `insight.note` | 125자 | 140자 |
| `stat.label` | 22자 | 26자 |
| `stat.value` | 18자 | 22자 |
| `stat.axisCaption` | 52자 | 60자 |
| `kicker` | 22자 | 26자 |
| `trend` 배열 | 4~5점 | 2~8점 |

### 4.2 A4 1페이지 보장

`build/verify.mjs`가 **매 빌드마다 헤드리스 PDF로 실측한다.**

```js
const buf = await page.pdf({ format: "A4", printBackground: true,
                             margin: { top: "0", bottom: "0", left: "0", right: "0" } });
const pages = (buf.toString("latin1").match(/\/Type\s*\/Page[^s]/g) || []).length;
// pages === 1 이어야 한다
```

이 검사가 실제로 결함을 잡았다. 진을 감싼 래퍼의 `margin-top: 32px`와 `.zine-page`의
`min-height: 297mm`가 맞물려 인쇄가 2페이지가 됐다. `@media print`에서 래퍼 여백을 지우고
`min-height`를 해제해 해결했다. **이 규칙을 되돌리면 검증이 다시 실패한다.**

### 4.3 예산 초과 시

문장을 줄인다. **폰트 크기·행간·여백을 줄여서 맞추지 않는다.** 그건 불변식 위반이다.
줄일 수 없다면 소재가 한 호에 담기지 않는 것이다 — 쪼개거나 다음 주로 넘긴다.

---

## 5. 레이아웃

### 5.1 그리드

| 컨테이너 | 값 |
|---|---|
| `.shell` (앱 셸) | `max-width: 1240px` |
| 읽기 측정 | `--measure: 68ch` (45~75자 범위 안) |
| `.zine-page` | 210mm × 297mm — 실측 794 × 1123px @96dpi |

### 5.2 브레이크포인트

| 구간 | 구조 | 변하는 것 |
|---|---|---|
| **≥1200px** | 12칼럼 · 본문 `1 / span 7` / 레일 `9 / span 4` | 레일 `sticky`, 세로 괘선 표시 |
| **768–1199px** | 8칼럼 · 본문 전폭 | 레일이 본문 흐름 안으로 내려옴, 괘선 제거 |
| **<768px** | 1단 | 스탯이 본문 사이에 인라인 |
| ≤780px (진) | 진 3단 → 1단 | 사진 float 해제, 헤드라인 44→32px |

`build/verify.mjs`가 375 / 768 / 1280 / 1920px에서 **가로 넘침이 없는지 실측한다.**

### 5.3 본문에 CSS 다단을 쓰지 않는다

신문이 다단인 것은 **지면이 고정돼 있기 때문**이다. 웹에서 본문을 다단으로 짜면
아래로 읽다가 다시 위로 올라와야 한다.

대신 **비대칭 그리드**로 신문의 인상을 만든다 — 본문은 45~75자 한 칼럼, 숫자와 인사이트는
오른쪽 레일에 세로 괘선으로 붙는다. 다단은 **인쇄 진에서만** 쓴다. 거기서는 지면이 고정이라 맞는 도구다.

레일에 스탯이 상주하므로 **"근거는 늘 옆에 있다"는 태도가 레이아웃으로 표현된다.**

### 5.4 여백

화면은 **px**, 진은 **mm**. 종이 산출물의 치수를 물리 단위로 유지하려는 의도적 분리다.

- 화면: `4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96` px (`--s1`~`--s9`)
- 진: `1 · 2 · 3 · 4 · 5 · 6 · 8 · 10` mm / `.zine-page` 패딩 `8mm 10mm` → ≤780px에서 `6mm 5mm`

**이 패딩을 줄이면 프린터 물리 여백에 잘린다.**

---

## 6. 도메인 컬러 — 확장 규칙

### 6.1 현재 활성 도메인 — 8개

단일 소스는 `domains/registry.json`이다. CSS는 `--dc-<key>` 변수만 참조한다.

| 도메인 | Hue | Night `color` | 대비 | Paper `colorPaper` | 대비 |
|---|---|---|---|---|---|
| 음악 | 35° | `#FF9500` | 8.32:1 | `#A15E00` | 4.51:1 |
| 여행 | 85° | `#59900C` | 4.72:1 | `#4A780A` | 4.67:1 |
| 도서 | 135° | `#34C759` | 8.24:1 | `#217E38` | 4.53:1 |
| 공연 | 172° | `#0C907E` | 4.62:1 | `#0A7B6C` | 4.57:1 |
| 영화 | 210° | `#0A84FF` | 5.01:1 | `#006BD7` | 4.55:1 |
| 뉴스 | 256° | `#8B66F3` | 4.61:1 | `#7142F0` | 4.97:1 |
| OTT | 303° | `#E212D8` | 4.63:1 | `#C110B9` | 4.60:1 |
| 유튜브 | 349° | `#FF2D55` | 5.01:1 | `#DD002A` | 4.52:1 |

**열여섯 값 전부 AA를 넘는다.** Paper 보정색은 **hue를 한 도도 바꾸지 않고 명도만 내린** 값이다.
`test/theme.test.mjs`가 매번 대비와 hue 일치를 계산해 검증한다.

**도메인을 8개로 늘린 이유는 지면을 채우기 위해서가 아니라 결번이 나도 4편을 확보하기 위해서다.**
지면은 여전히 리드 1 + 미니 3이고, 그 주 통과분 중 점수 상위 4편만 싣는다 (`registry.zineLayout.selection: "score"`).

> 배경이 `#222222`에서 `#16150F`로 바뀌면서, 영화·유튜브가 4.36:1로 AA 미달이던 오래된 문제가
> 5.01:1로 **저절로 해소됐다.** 색을 바꾸지 않고 배경만 바꿔서 풀렸다.

진 페이지에는 도메인 색을 쓰지 않는다 (진 킥커는 `--secondary`).

### 6.2 신규 도메인 색 선정 기준

네 조건을 **전부** 만족해야 한다.

1. Night 배경 `#16150F` 대비 **≥ 4.5:1**
2. Paper 배경 `#F4F1E7` 대비 **≥ 4.5:1** — 보정색을 따로 뽑는다
3. 두 값의 **hue가 같아야 한다** (`test/theme.test.mjs`가 ±1° 이내로 강제)
4. 기존 모든 도메인과 **색상환 거리 ≥ 35°**

### 6.3 색상환은 다 찼다

현재 hue는 35 / 85 / 135 / 172 / 210 / 256 / 303 / 349°다. 간격은 이렇다.

```
35 →85  50°     172→210  38°
85 →135 50°     210→256  46°
135→172 37°     256→303  47°
                303→349  46°     349→35  46°
```

**최소 간격이 37°이고, 이게 8개를 배치할 때의 실질 한계다.**
색상환 360°를 8등분하면 45°지만 기존 4개(35·135·210·349°)가 불균등해 그만큼 못 벌린다.
그래서 `minHueDistance`를 40°에서 **35°로 낮췄다.**

> **9번째 도메인은 이 규칙으로 불가능하다.** 색만으로 구분하는 방식의 상한에 도달했다.
> 더 늘리려면 색 이외의 구분 수단(패턴·아이콘·타이포 변주)이 필요하고, 그건 불변식 변경이라 사용자 승인 사항이다.

### 6.4 도메인 증가 시 레이아웃 영향

- **세그먼티드 필터** — `flex-wrap`이라 자동으로 줄바꿈된다.
- **진 `.zine-secondary-row`** — `1fr 1fr 1fr` 고정이다. **미니가 4개 이상이면 레이아웃 결정이 필요하다.**
  `build/lib/render-zine.mjs`가 초과분을 싣지 않고 경고를 낸다. `tools/qa.mjs` 50행도 경고한다.
- **A4 예산** — 미니 3개 기준이다. 미니가 늘면 `build/verify.mjs`로 재측정한다.

---

## 7. 컴포넌트 스펙

### 마스트헤드

```
워드마크: Paperlogy 900 · clamp(20px, 3vw, 30px) · 자간 .18em · uppercase
룰라인:   border-top 3px + border-bottom 1px · height 5px
```
굵은 선 + 가는 선의 신문 마스트헤드 관용구다.

### 스탯 카드 (근거 레일)

```
border-top 3px --rule · border-bottom 1px --rule · padding 12px 0 16px
≥1200px에서 position: sticky, top 24px
```

**출처 링크는 항상 보인다.** 프로토타입은 `opacity: 0`에 hover로만 노출해 터치 기기에서 접근이 불가능했다.
근거는 숨기는 게 아니라 옆에 두는 것이다.

### 스파크라인 (렌더 중단, 2026-08-08)

사용자 요청으로 스탯 카드에서 차트를 뺐다 — 지금은 라벨 + 수치 + 출처 링크만 보인다.
`sparklineSVG()` 함수(`build/lib/sparkline.mjs`)와 아래 스펙은 데이터·테스트 자산으로 남아 있지만
`build/lib/render-story.mjs`가 더는 호출하지 않는다. 되살릴 일이 생기면 이 스펙을 그대로 쓴다.

```
크기:   160 × 56
정규화: 첫 값을 100으로 두는 기준선 정규화. 기준선이 범위 끝이면 반대쪽에 15% 여백
기준선: 1px 점선(2 3), opacity .25 — "전주 대비"라는 축 의미를 형태로 보여준다
선:     stroke-width 2.5 · round cap/join
끝점:   circle r 3.5, x = w
대체:   <title>에 추세 문장. role="img"
```

min–max 정규화가 아니다. 프로토타입은 `[98,101,100,426]`에서 앞 세 점이 바닥에 뭉개져 형태 정보가 0이었다.
기준선 정규화 + 여백으로 y가 44.26 / 43.91 / 44.03이 되어 바닥(50)에서 떨어진다.

### 풀쿼트 & 인사이트

```
쿼트: 명조 700 · 20px · lh 1.55 · border-left 1px 도메인 컬러 · padding-left 16px
```

**인사이트(`<details>`/`<summary>`, 개념·설명)는 2026-08-08부터 화면에 안 보인다** — 사용자 요청.
`quote.insight`는 데이터에는 계속 있고 `tools/qa.mjs`가 검증한다(§2 불변식 #2 참고) —
S5 집필 규율(인사이트 3문 테스트)이 기대는 내부 품질 장치이지 독자용 라벨이 아니게 됐다.

### 브라우저 표면

`::selection`, `:focus-visible`, `caret-color`, 스크롤바를 전부 팔레트에서 지정한다.
그리지 않은 부분도 디자인을 짊어진다.

---

## 8. 진(zine) 전용 장치

진 페이지는 화면과 다른 어휘를 쓴다. **손으로 만든 인쇄물**이라는 인상이 목적이다.

### 3단 세로 괘선

`.zine-secondary-row`는 `1fr 1fr 1fr` + `gap 6mm`이고, 세로 구분선을 의사 요소 두 개
(`::before` 33.333%, `::after` 66.666%, 1px, `opacity .25`)로 그린다. 780px 이하에서 1단으로 접히고 괘선은 사라진다.

### 사진 콜라주

```
플레이스홀더: aspect-ratio 4/3 · border 2px · transform rotate(-1.1deg)
             clip-path polygon(0% 2%, 3% 0%, 97% 1%, 100% 4%, 99% 97%, 96% 100%, 2% 99%, 0% 96%)
             fallback: 45° 사선 스트라이프 (#DBD7C9 / #EDE9DC, 2px/5px)
테이프:      60 × 20 · rgba(255,255,255,.6) · 1px dashed rgba(0,0,0,.3)
             좌 rotate(-9deg), 우 rotate(7deg)
```

`clip-path`의 불규칙 다각형이 가위로 오린 가장자리를 만든다. **불변식 §2.6.**
사진은 `story.photo.src`로 주입한다. 없으면 스트라이프가 보인다.

### 스티커

```
padding 5px 11px · border 2px · bg --surface · 12px/900 · rotate(-3deg)
```
**리드 `stat.label`과 `stat.value`를 그대로 쓴다.** 따로 입력하지 않으므로 어긋날 수 없다.

### 푸터 바

```
bg --ink · color --bg · padding 4mm 5mm · flex center
QR: 60 × 60
```
페이지 유일의 반전 영역. 인쇄면을 검은 띠로 닫으며 웹으로 가는 다리(QR) 역할을 한다.
QR은 `assets/img/qr-<회차>.svg`이고 `answerzine.kr`(홈 아카이브)을 가리킨다 — **그 주소는 실제로 존재한다.**
(2026-08-08까지는 회차별 URL을 가리켰다 — 그 목록 페이지가 없어지면서 홈으로 바뀌었다.)

---

## 9. 모션

모든 애니메이션은 `@media (prefers-reduced-motion: reduce)`에서 정지하되 콘텐츠는 그대로 보인다.

예외 하나 — 홈의 인트로→스테이트먼트 전환(§8이 아니라 `build/lib/layout.mjs`의 `intro()`)은
스크롤에 반응하는 크로스페이드다. `.intro`는 뷰포트보다 60vh 큰 상자이고 안쪽 콘텐츠가
`position: sticky`로 그 여유분만큼 고정된 채 옅어지며 살짝 커진다 — 그래야 스크롤이 그냥
지나가 버리지 않고 눈에 보이는 전환이 된다(`assets/app.js`의 `bootIntroMotion()`).
`prefers-reduced-motion: reduce`에서는 이 계산 자체가 꺼지고 두 섹션이 각자 100vh를 채운 채
그냥 스크롤된다 — 콘텐츠는 그대로 보인다.

그 외 시스템에는 연출용 애니메이션이 없다. 상태 전환(hover, 필터, `<details>`)만 있고
전부 브라우저 기본 동작이거나 0.15s 이내다. **모션은 반응에만 쓴다.**

---

## 10. 매주 확인할 시각 QA

대부분 `build/verify.mjs`가 자동으로 검사한다. `[자동]` 표시가 그것이다.

1. 진 페이지가 **A4 정확히 1페이지**인가. `[자동]`
2. 콘솔 에러 0건인가. `[자동]`
3. 375 / 768 / 1280 / 1920px에서 가로 넘침이 없는가. `[자동]`
4. 진 헤드라인이 웹 정본과 문자열이 같은가. `[자동]` (같은 값을 참조하므로 구조적으로 보장)
5. 서브셋 폰트가 본문의 모든 글자를 담는가 — 두부(tofu) 없음. `[자동]`
6. 내부 링크가 전부 실제 파일로 해석되는가. `[자동]`
7. 새 도메인 색이 §6.2 네 조건을 전부 통과하는가. `[자동]` (`test/theme.test.mjs`)
8. 로드하지 않은 폰트 웨이트를 참조하지 않는가. `[자동]` (`test/css.test.mjs`)
9. 스파크라인이 뭉개지지 않았는가 — 눈으로. `trend` 4~5점 권장.
10. Night / Paper 양쪽에서 읽히는가 — 눈으로.
