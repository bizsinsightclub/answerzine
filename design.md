# design.md — ANSWER ZINE 디자인 시스템

**주간 반복 생산을 전제로 한 고정 템플릿 명세다.**
매주 바뀌는 것은 §4의 슬롯 콘텐츠뿐이고, 나머지는 전부 잠겨 있다.
값은 전부 `Answer_Zine.html`에 실제로 구현된 것을 추출했거나, Chromium 헤드리스로 실측한 것이다.

| 구분 | 원칙 |
|---|---|
| §2 불변식 | 에이전트가 **절대 바꾸지 않는다.** 변경은 사용자 승인 사항. |
| §4 슬롯 | 매주 바뀐다. **분량 예산을 지키면 A4 1페이지가 보장된다.** |
| §6 도메인 색 | 도메인 추가 시에만 건드린다. 선정 규칙이 정해져 있다. |

---

## 1. 두 개의 테마

테마가 두 개이고, **CSS 변수 재정의 한 번으로 전환된다.**

| | Screen (다크) | Zine (인쇄) |
|---|---|---|
| 적용 범위 | `:root` — 문서 전체 | `.zine-page` 하위 |
| 성격 | 밤에 폰으로 읽는 아카이브 | 종이에 인쇄해 카페에 두는 진 |
| 배경 | `#222222` | `#F4F1E7` (크림) |

```css
/* Screen */
:root {
  --ink:       #F5F5F5;   /* 본문 텍스트   대비 14.59:1 */
  --secondary: #A1A1A6;   /* 보조·티저     대비  6.19:1 */
  --tertiary:  #8E8E93;   /* 날짜·캡션     대비  4.88:1 */
  --divider:   #3A3A3C;   /* 경계선 */
  --bg:        #222222;   /* 페이지 배경 */
  --surface:   #2C2C2E;   /* 카드·행 배경 */
  --accent:    #409CFF;   /* 링크·액션     대비  5.62:1 */
  --seg-bg:    #1A1A1A;   /* 세그먼티드 트랙 — 배경보다 어둡다 */
}

/* Zine — 같은 이름, 종이용 값 */
.zine-page {
  --ink:       #1D1D1F;
  --secondary: #6E6E73;
  --tertiary:  #8E8E93;   /* 두 테마 공통인 유일한 값 */
  --divider:   #D2D2D7;
  --accent:    #0071E3;
  background:  #F4F1E7;
}
```

원칙: **역할 이름은 두 테마에서 같고 값만 다르다.** 컴포넌트 CSS는 역할 토큰만 참조하므로, `.zine-page` 안에 넣기만 하면 종이용으로 렌더된다. 라이트 모드를 추가한다면 세 번째 값 세트만 넣으면 된다.

명도 계층은 `seg-bg(#1A1A1A) < bg(#222222) < surface(#2C2C2E)` 순이다. 세그먼티드 트랙이 배경보다 **어둡다**는 게 핵심이다 — 트랙은 파인 홈이고 선택된 버튼이 `--surface`로 떠오른다.

---

## 2. 불변식 — 매주 바뀌지 않는 것

에이전트는 아래를 변경하지 않는다. 콘텐츠가 안 맞으면 **템플릿이 아니라 콘텐츠를 고친다.**

1. **스토리 구조는 5블록 고정** — `text / stat / quote / text`. 블록 추가·삭제·순서 변경 없음.
2. **`stat` 1개, `insight` 있는 `quote` 1개** — 스토리당 정확히 하나씩.
3. **도메인 색이 쓰이는 자리는 4곳뿐** — 도메인 태그, 킥커, 풀쿼트 좌측 보더, 스파크라인. 배경 채색·버튼 채색은 이 시스템에 없다.
4. **노랑 `#FFCC00`은 인사이트 툴팁 전용.** 다른 곳에 쓰지 않는다.
5. **진 페이지는 리드 1 + 미니 3, A4 1장.** 2페이지짜리 진을 만들지 않는다.
6. **회전각은 −9°~+7° 범위.** 사진 −1.1°, 스티커 −3°, 테이프 −9°/+7°.
7. **`clip-path` 다각형 값** — 가위로 오린 가장자리 표현. 조정하지 않는다.
8. **모션 3단 계층** — 0.15s(반응) / 0.5–0.7s(등장) / 1s+(연출). 섞지 않는다.
9. **`@page { size: A4; margin: 0 }`** — 안전 여백은 `.zine-page` 패딩이 전담한다.

---

## 3. 타이포그래피

### 서체

```
"Paperlogy", -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
"Apple SD Gothic Neo", "Helvetica Neue", sans-serif
```

Paperlogy는 base64 TTF로 인라인, `font-display: swap`. **로드된 웨이트는 300 / 400 / 700 / 900 넷뿐이다.**

> ⚠️ CSS는 500 · 590 · 600 · 800도 참조한다. 로드되지 않았으므로 브라우저가 대체한다:
> 500→400, 590→400, 600→**700**, 800→**900**.
> 즉 `.row-headline`(600)과 `.pullquote`(600)는 700으로, `.story-headline`(800)은 900으로,
> `.domain-tag`(590)는 400으로 렌더된다. **의도한 위계가 실제로는 나오지 않는다.**
> 네 웨이트만으로 스케일을 재정의하거나 필요한 웨이트를 추가 로드해야 한다 (`CLAUDE.md` §9.8).

킥커류에는 시스템 `monospace`를 쓴다. 저널리즘 헤더의 텔레타이프 인상이다.

### 스케일 — Screen

| 역할 | 크기 (모바일 → ≥640px) | 웨이트 | 행간 | 자간 |
|---|---|---|---|---|
| 스테이트먼트 | 24 → 32 | 700 | 1.5 | −0.01em |
| 스토리 헤드라인 | 28 → 36 | 800 | 1.2 | −0.015em |
| 스탯 값 | 24 | 700 | — | −0.01em |
| 풀쿼트 | 18 → 20 | 600 | 1.5 | −0.01em |
| 리스트 헤드라인 | 17 | 600 | 1.4 | −0.005em |
| 본문 문단 · 스토리 티저 | 16 → 17 | 400 | 1.75 / 1.6 | — |
| 내비 헤드라인 | 14.5 | 600 | — | — |
| 백링크 · 빈 상태 | 14 | 500 / 400 | — | — |
| 세그먼티드 · 스토리 메타 · 툴팁 본문 | 13 | 300(활성 500) / 400 | — | — |
| 스토리 킥커 (mono) | 12 | 700 | — | 0.1em |
| 도메인 태그 | 12 (스토리 13) | 590 | — | — |
| 날짜 · 태그라인 · 스탯 라벨 · CTA | 12 | 500 | — | — |
| 내비 라벨 · 스탯 출처 · 축 캡션 · 툴팁 라벨 | 11 | 500 / 700 | — | 0.03em |
| 리스트 킥커 (mono) | 10.5 | 700 | — | 0.08em |

**패턴**: 헤드라인이 클수록 자간이 좁아지고(−0.005 → −0.015em) 행간도 좁아진다(1.4 → 1.2). 본문은 행간 1.75로 넉넉하다 — 한국어 장문 가독성을 위한 선택이다.

### 스케일 — Zine

| 역할 | 크기 | 웨이트 | 행간 |
|---|---|---|---|
| 커버 헤드라인 | 44 | 900 | 1.1 |
| 기사 헤드라인 | 30 | 900 | 1.1 |
| 미니 헤드라인 | 19 | 900 | 1.15 |
| 티저 | 14 | 700 | 1.45 |
| 푸터 헤드라인 | 13.5 | 900 | 1.3 |
| 스티커 | 12 | 900 | — |
| 본문 | 11.5 | 400 | 1.65 |
| 미니 티저 | 11 | 400 | 1.55 |
| 킥커 | 10.5 | 700 | — (0.1em, uppercase) |
| 데이트라인 · 바이라인 · 리드모어 | 10 | 700 / 900 | — |
| QR 캡션 | 9.5 | 400 | 1.5 |
| 푸터 라벨 | 9 | 700 | — (0.08em, uppercase) |

**진 전용 규칙**: 킥커·라벨류는 전부 `text-transform: uppercase` + 넓은 자간(0.03–0.1em). 신문 조판 관용이다. 화면 킥커는 데이터에 이미 대문자로 들어 있어 변환을 걸지 않는다.

### 숫자

날짜·수치에는 `font-variant-numeric: tabular-nums` (`.row-date`, `.stat-value`, 스토리 메타 기간). 세로로 나열될 때 자릿수가 흔들리지 않게 한다.

---

## 4. 슬롯과 분량 예산 — 매주 바뀌는 것

### 4.1 예산표

Chromium 헤드리스로 슬롯 길이를 늘려가며 A4 페이지 수를 측정해 도출했다.
기준 회차(w31)는 페이지의 **855px / 1123px = 76%** 를 쓴다.

| 슬롯 | 권장 (≤) | 절대 상한 | 기준 회차 실측 |
|---|---|---|---|
| 스토리 헤드라인 | **24자** | 32자 | 7 / 9 / 13 / 16자 |
| 스토리 티저 | **78자** | 104자 | 32 / 41 / 45 / 52자 |
| `text` 블록 (각) | **180자** | 240자 | 37~175자 (평균 107) |
| 진 미니 헤드라인 | **12자** | 16자 | 7~13자 |
| 진 미니 티저 | **60자** | 80자 | 32~45자 |
| `quote.text` | 26~40자 | 48자 | 26 / 27 / 32 / 39자 |
| `insight.concept` | 20~30자 | 34자 | 21 / 22 / 22 / 29자 |
| `insight.note` | 110~125자 | 140자 | 110~124자 |
| `stat.label` | ≤22자 | 26자 | 8 / 9 / 19 / 21자 |
| `stat.value` | ≤18자 | 22자 | 7 / 7 / 13 / 17자 |
| `stat.axisCaption` | ≤52자 | 60자 | 34~51자 |
| `kicker` | ≤22자 | 26자 | 11~22자 |
| `trend` 배열 | 4~5점 | 2~8점 | 4 / 4 / 4 / 5점 |

### 4.2 실측 근거

전 슬롯을 기준 회차 대비 동일 배율로 늘렸을 때의 A4 페이지 수:

| 배율 | 헤드라인/티저/본문×3/미니H/미니T | 사용 높이 | 페이지 |
|---|---|---|---|
| 1.00× | 16 / 52 / 120 / 8 / 40 | 827px | 1 ✅ |
| 1.25× | 20 / 65 / 150 / 10 / 50 | 882px | 1 ✅ |
| **1.50×** | **24 / 78 / 180 / 12 / 60** | **940px (84%)** | **1 ✅ ← 권장 예산** |
| 1.75× | 28 / 91 / 210 / 14 / 70 | 979px | 1 ✅ |
| **2.00×** | **32 / 104 / 240 / 16 / 80** | **1050px (94%)** | **1 ⚠️ ← 절대 상한** |
| 2.25× | 36 / 117 / 270 / 18 / 90 | 1107px | **2 ❌ 초과** |

권장 예산(1.5×)에는 183px의 여유가 남는다. 폰트 로딩 실패·프린터 드라이버 차이·긴 단어의 줄바꿈 같은 변수를 흡수하는 완충이다.
절대 상한(2.0×)을 넘기면 A4 2페이지가 되고, **진은 1장이라는 불변식이 깨진다.**

### 4.3 예산 초과 시

문장을 줄인다. **폰트 크기·행간·여백을 줄여서 맞추지 않는다.** 그건 불변식 위반이다.
줄일 수 없다면 소재가 한 호에 담기지 않는 것이다 — 쪼개거나 다음 주로 넘긴다.

---

## 5. 레이아웃

### 그리드

| 컨테이너 | 최대 폭 |
|---|---|
| `.wrap` (앱 셸) | 680px |
| 읽기 칼럼 (`.story-body`, `.story-teaser`, `.stat-card`, `.nav-btn`) | 560px |
| 스테이트먼트 텍스트 | 480px |
| 툴팁 | `min(320px, 80vw)` |
| 진 미리보기 영역 | 900px |
| `.zine-page` | 210mm (A4) — 실측 794 × 1123px @96dpi |

**680px 셸 안에 560px 읽기 칼럼**이 들어간다. 헤드라인과 리스트는 셸 폭을 다 쓰고 본문은 좁게 유지되어, 스크롤할 때 리듬이 생긴다.

### 브레이크포인트

| 쿼리 | 용도 |
|---|---|
| `min-width: 640px` | 화면 테마의 유일한 확대 지점 — 타입 스케일, 여백, 내비 가로 배치 |
| `max-width: 639px` | 마스트헤드 태그라인 숨김 |
| `max-width: 780px` | 진 모바일 폴백 — 3단→1단, 사진 float 해제, 폭 100% |

### 여백

화면은 **px**, 진은 **mm**. 종이 산출물의 치수를 물리 단위로 유지하려는 의도적 분리다.

- 화면 스케일: `4 · 6 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 64` px / `.wrap` 패딩 `32px 16px 64px` → ≥640px에서 `48px 24px 80px`
- 진 스케일: `1 · 2 · 3 · 4 · 5 · 6 · 8 · 10` mm / `.zine-page` 패딩 `8mm 10mm` → ≤780px에서 `6mm 5mm`

### 모서리 반경

| 값 | 적용 |
|---|---|
| 16px | 리스트 컨테이너, 스탯 카드, 빈 상태 |
| 12px | 세그먼티드 트랙, 툴팁 |
| 10px | 인쇄 버튼 |
| 9px | 세그먼티드 버튼 (트랙보다 4px 안쪽 → 동심) |
| 999px | 도메인 점, 셰브론 원 |
| 2px | 진 QR |

진 페이지에는 큰 반경을 쓰지 않는다. 인쇄물의 각진 인상을 유지한다.

---

## 6. 도메인 컬러 — 확장 규칙

### 6.1 현재 활성 도메인

단일 소스는 JS의 `DOMAIN_META`다. CSS에 도메인 색을 쓰지 않는다.

| 도메인 | HEX | Hue | `#222222` 대비 | 비고 |
|---|---|---|---|---|
| 음악 | `#FF9500` | 35° | 7.24:1 ✅ | |
| 도서 | `#34C759` | 135° | 7.17:1 ✅ | |
| 영화 | `#0A84FF` | 210° | **4.36:1 ⚠️** | AA 4.5:1 미달 |
| 유튜브 | `#FF2D55` | 349° | **4.36:1 ⚠️** | AA 4.5:1 미달 |

영화·유튜브 색은 12px(도메인 태그)·10.5px(킥커) 텍스트에 쓰이는데 WCAG AA 일반 텍스트 기준 4.5:1에 근소하게 못 미친다. 기존 색은 브랜드 자산이라 유지하되, **신규 도메인은 반드시 4.5:1을 넘긴다.**

도메인 색이 쓰이는 자리는 §2.3의 4곳뿐이다. **진 페이지에는 도메인 색을 쓰지 않는다** (진 킥커는 `--secondary`). 따라서 대비 기준은 다크 배경 `#222222`에만 적용된다.

### 6.2 신규 도메인 색 선정 기준

세 조건을 **전부** 만족해야 한다.

1. `#222222` 대비 **≥ 4.5:1** (WCAG AA 일반 텍스트)
2. 기존 모든 도메인과 **색상환 거리 ≥ 40°**
   (현재 최소 간격은 유튜브 349° ↔ 음악 35°의 46°다. 이보다 좁히지 않는다.)
3. Apple 다크 시스템 컬러 계열의 채도·명도감 유지 — 형광이나 파스텔로 튀지 않는다

### 6.3 사용 가능한 색상 구간

현재 사용 중인 hue는 35° / 135° / 210° / 349°다. 40° 간격을 확보하면 **남는 구간은 둘뿐이다.**

| 구간 | 계열 | 상태 |
|---|---|---|
| **75°–95°** | 라임 / 옐로그린 | 열림 |
| **250°–309°** | 퍼플 / 마젠타 | 열림 |
| 175°–170° (135↔210 사이) | 틸 / 민트 | **닫힘** — 40° 확보 불가 |
| 29°–355° (349↔35 사이) | 레드 / 코랄 | **닫힘** — 40° 확보 불가 |

즉 **도메인은 최대 6개까지** 이 규칙 안에서 확장 가능하다. 7번째부터는 §6.2의 40° 기준을 재검토해야 한다.

### 6.4 검증된 후보

Apple 다크 시스템 컬러 전량을 §6.2 기준으로 검사한 결과, **통과한 것은 하나뿐이다.**

| 후보 | HEX | Hue | 최소 거리 | 대비 | 판정 |
|---|---|---|---|---|---|
| systemPurple | `#BF5AF2` | 280° | 69° | 4.52:1 | ✅ **5번째 도메인 권장** |
| systemIndigo | `#5E5CE6` | 241° | 31° | 3.14:1 | ❌ 색상 충돌 + 대비 부족 |
| systemMint | `#66D4CF` | 177° | 33° | 9.00:1 | ❌ 색상 충돌 |
| systemTeal / Cyan | `#64D2FF` | 197° | 13° | 9.25:1 | ❌ 색상 충돌 (영화와 근접) |
| systemYellow | `#FFD60A` | 50° | 15° | 11.27:1 | ❌ 색상 충돌 (음악과 근접) |
| systemPink | `#FF375F` | 348° | 1° | 4.52:1 | ❌ 유튜브와 사실상 동일 |
| systemRed | `#FF453A` | 3° | 14° | 4.67:1 | ❌ 색상 충돌 |
| systemBrown | `#AC8E68` | 34° | 1° | 5.17:1 | ❌ 음악과 사실상 동일 |

라임 구간(75°–95°)에는 Apple 표준색이 없다. 6번째 도메인이 필요하면 **`#8FC91D`** (hue 80°, 대비 7.99:1) 같은 값을 커스텀으로 정의하되, 사용자 승인을 받는다.

### 6.5 도메인 증가 시 레이아웃 영향

- **세그먼티드 컨트롤** — `overflow-x: auto`가 이미 걸려 있어 가로 스크롤로 흡수된다. 모바일 375px에서 6개 + `전체`까지는 확인 필요.
- **진 `.zine-secondary-row`** — `grid-template-columns: 1fr 1fr 1fr` 고정이다. **미니가 4개 이상이 되면 레이아웃 결정이 필요하다** (2×2 그리드 / 미니 3개만 게재 / 진을 도메인 격주 로테이션으로). 사용자 승인 사항.
- **A4 예산** — §4의 예산표는 미니 3개 기준이다. 미니가 늘면 재측정한다.

### 6.6 별도 액센트

| 용도 | HEX | 위치 |
|---|---|---|
| 인사이트 툴팁 라벨 | `#FFCC00` | `.insight-tooltip strong`, 전구 아이콘 (`#000` 대비 13.89:1) |
| 툴팁 배경 | `#000000` | `.insight-tooltip` |

### 6.7 알파 값

| 값 | 용도 |
|---|---|
| `rgba(245,245,245,0.45)` | 세그먼티드 비활성 텍스트 |
| `rgba(255,255,255,0.04)` | 행 hover 배경 |
| `rgba(0,0,0,0.24)` | 선택된 세그먼트 그림자 |
| `rgba(0,0,0,0.18)` | 진 페이지 화면 미리보기 그림자 |
| `rgba(255,255,255,0.6)` | 진 테이프 / 진 푸터 라벨 |
| `rgba(255,255,255,0.25)` | 진 푸터 구분선 |

---

## 7. 컴포넌트 스펙

### 세그먼티드 컨트롤

```
트랙:  inline-flex · padding 4px · gap 2px · bg --seg-bg · radius 12px
       overflow-x auto (도메인 증가 대비) · margin-bottom 24px
버튼:  13px/300 · padding 7px 14px · radius 9px · color rgba(245,245,245,.45)
       white-space nowrap · transition all .15s ease
활성:  color --ink · bg --surface · weight 500 · shadow 0 1px 3px rgba(0,0,0,.24)
```

### 리스트 행

```
컨테이너: radius 16px · border 1px --divider · overflow hidden
행:       flex · gap 16px · padding 16px 18px · bg --surface
          border-top 1px --divider (첫 행 제외 — 컨테이너 보더와 중복 방지)
hover:    bg rgba(255,255,255,.04) · 셰브론 원 translateX(2px)
```

행 내부 순서 고정: **메타(도메인 태그 · 날짜) → 킥커 → 헤드라인 → 셰브론.**
셰브론 원은 32×32, `--bg` 배경 — 카드 표면에 구멍이 뚫린 것처럼 보인다.

### 스탯 카드

```
padding 20px 22px · bg --surface · radius 16px · border 1px --divider
max-width 560px · margin 24px 0 · <a>이지만 밑줄/색 상속 없음
```

구조: 상단 `space-between` 2열(좌 라벨+값 / 우 스파크라인) → 점선 구분선(`1px dashed --divider`, `padding-top 10px`) → 축 캡션 → 출처 링크.

**출처 링크는 기본 `opacity: 0`이고 카드 hover 시에만 나타난다.** 근거는 항상 존재하되 읽기를 방해하지 않는다는 태도의 표현이다. (터치 기기에서 드러나지 않는 문제는 `CLAUDE.md` §9.9.)

### 스파크라인

```
크기:   160 × 56 (viewBox 동일)
정규화: y = h − 6 − ((v − min) / (max − min)) × (h − 12)   → 상하 6px 여백
선:     stroke-width 2.5 · linecap/linejoin round · fill none
끝점:   circle r 3.5, x = w (오른쪽 끝)
색:     도메인 컬러
```

축도 눈금도 격자도 없다. 정확한 값은 `.stat-value`와 `.stat-axis-caption`이 문장으로 전달하고, 스파크라인은 **모양만** 보여준다. 그래서 `axisCaption`이 선택이 아니라 필수다.

주간 생산 시 주의: 정규화가 min–max 기반이라 **점이 2개뿐이면 직선**이 되고, 한 점이 극단적으로 크면 나머지가 바닥에 붙는다. 4~5점을 권장하는 이유다.

### 풀쿼트 & 인사이트 툴팁

```
쿼트:   margin 32px 0 · padding 4px 0 4px 20px · border-left 3px 도메인 컬러
        18→20px / 600 / lh 1.5
인사이트: cursor help · tabindex 0
툴팁:   absolute · left 20px · top calc(100% + 8px) · z-index 5
        width min(320px, 80vw) · padding 14px 16px · bg #000 · radius 12px
        13px/400 · lh 1.6 · pointer-events none
        기본 opacity 0 + visibility hidden + translateY(-4px)
        :hover / :focus / :focus-visible → opacity 1 + visible + translateY(0)
        transition .15s ease
라벨:   block · 11px/700 · ls .03em · uppercase · #FFCC00 · margin-bottom 6px
```

### 아이콘

전부 인라인 SVG, `viewBox="0 0 24 24"`, `fill="none"`, round cap/join.

| 아이콘 | 표시 크기 | stroke-width |
|---|---|---|
| 셰브론 | 12–14 | 2 |
| 외부 링크 화살표 | 10 | 2.2 |
| 전구 (인사이트) | 15 | 1.8 |

크기가 작을수록 stroke가 굵어진다. 시각적 무게를 맞추기 위한 것이다.

> ⚠️ `chevronSVGRotated()`와 `arrowSVG()`가 `#0071E3`(진 테마 액센트)을 하드코딩하는데, 이 아이콘들은 다크 화면(`--accent: #409CFF`)에서 쓰인다. 백링크 텍스트는 `#409CFF`, 바로 옆 셰브론은 `#0071E3`이다. `.intro-scroll`·`.statement-cta`의 SVG도 `#8E8E93`을 하드코딩한다. → SVG 생성 함수가 `currentColor`를 쓰도록 바꿔야 한다.

---

## 8. 진(zine) 전용 장치

진 페이지는 화면과 다른 어휘를 쓴다. **손으로 만든 인쇄물**이라는 인상이 목적이다.

### 룰 라인

```css
.zine-ruleline { border-top: 3px solid var(--ink); border-bottom: 1px solid var(--ink); height: 5px; }
```
굵은 선 + 가는 선의 신문 마스트헤드 관용구.

### 3단 세로 괘선

`.zine-secondary-row`는 `grid-template-columns: 1fr 1fr 1fr` + `gap: 6mm`이고, 세로 구분선을 **의사 요소 두 개**(`::before` 33.333%, `::after` 66.666%, 1px, `opacity .25`)로 그린다. 780px 이하에서 1단으로 접히고 괘선은 사라진다.

### 사진 콜라주

```
플레이스홀더: aspect-ratio 4/3 · border 2px solid --ink · transform rotate(-1.1deg)
             clip-path polygon(0% 2%, 3% 0%, 97% 1%, 100% 4%, 99% 97%, 96% 100%, 2% 99%, 0% 96%)
             fallback: 45° 사선 스트라이프 (#DBD7C9 / #EDE9DC, 2px/5px)
테이프:      60 × 20 · rgba(255,255,255,.6) · 1px dashed rgba(0,0,0,.3)
             좌 rotate(-9deg), 우 rotate(7deg)
```

`clip-path`의 불규칙 다각형이 가위로 오린 가장자리를 만든다. **불변식 §2.7.**
플레이스홀더에는 CSS 스트라이프와 인라인 `background-image`가 겹쳐 있다 — 사진이 있으면 스트라이프는 보이지 않는다. 매주 사진을 교체할 때 인라인 스타일만 바꾼다.

### 스티커

```
padding 5px 11px · border 2px solid --ink · bg #fff · 12px/900 · transform rotate(-3deg)
```
핵심 수치 하나를 담는다. **회차당 1개.** 리드의 `stat.value`를 그대로 쓴다.

### 푸터 바

```
bg --ink · color #fff · padding 4mm 5mm · flex center
QR: 60 × 60 · bg #fff · padding 3px · radius 2px
```
페이지 유일의 반전 영역. 인쇄면을 검은 띠로 닫으며 웹으로 가는 다리(QR) 역할을 한다.

---

## 9. 모션

모든 애니메이션은 `@media (prefers-reduced-motion: reduce)`에서 **정지하되 콘텐츠는 그대로 보인다** (`.revealed` 상태를 기본값으로 강제).

| 이름 | 지속 | 이징 | 내용 |
|---|---|---|---|
| `introLogoIn` | 1s | `cubic-bezier(0.16, 1, 0.3, 1)` | opacity 0→1, scale 0.82→1 (1회) |
| `introBounce` | 1.8s | ease-in-out ∞ | 셰브론 translateY 0→5→0 |
| `ctaBlink` | 1.5s | ease-in-out ∞ | opacity 1→0.3→1 |
| 스테이트먼트 텍스트 | 0.7s | ease | opacity + translateY 16px |
| 스테이트먼트 CTA | 0.5s | ease | opacity + translateY 8px |
| `#main-content` 등장 | 0.6s | ease | opacity + translateY 24px |
| 마이크로 인터랙션 | 0.15s | ease | hover, 필터 전환, 툴팁, 출처 노출 |

**시간 계층**: 0.15s는 반응, 0.5–0.7s는 등장, 1s+는 연출. 불변식 §2.8.
`cubic-bezier(0.16, 1, 0.3, 1)`은 인트로 로고 전용 감속 곡선이다.

### 스크롤 시퀀스

```
.intro           min-height 100vh          로고 + 스크롤 힌트
.statement-wrap  height 220vh              스크롤 트랙
  └ .statement-pin   position sticky, top 0, height 100vh
       진행률 > 0.08 → 문구 노출
       진행률 > 0.55 → CTA 노출 (깜빡임 시작)
#main-content    IntersectionObserver threshold 0.15 → .revealed (1회, 이후 disconnect)
```

진행률 = `(-wrap.top) / (wrap.offsetHeight - viewportHeight)`, 0–1 클램프. `scroll` 리스너는 `{ passive: true }`.

---

## 10. 인쇄

```css
@media print {
  @page { size: A4; margin: 0; }
  body { background: #fff; }
  .intro, #main-content, .zine-preview-wrap h2, .zine-preview-wrap > p, .print-btn {
    display: none !important;
  }
  .zine-preview-wrap { padding: 0; border-top: none; }
  .zine-page { box-shadow: none; margin: 0 auto; }
}
```

의도는 `.zine-page` 하나만 남기는 것이다. `@page margin: 0`이므로 안전 여백은 `.zine-page`의 `8mm 10mm` 패딩이 전담한다. **이 패딩을 줄이면 프린터 물리 여백에 잘린다.**

진 페이지 실측은 A4에 정확히 맞는다 — 96dpi 기준 794 × 1123px, 한 장이다.

> ⚠️ **현재 출력은 4페이지다.** 숨김 목록에 `.statement-wrap`이 빠져 있어, `height: 220vh` 트랙이 인쇄 지면에 2,471px(≈A4 2.2장)의 빈 공간으로 남는다. `.statement-wrap`을 목록에 추가하면 1페이지가 된다. **매주 인쇄본을 뽑는 워크플로에서 가장 먼저 고쳐야 할 항목이다** — `CLAUDE.md` §9.1.

---

## 11. 매주 확인할 시각 QA

`CLAUDE.md` §8의 디자인 관련 항목이다.

1. 진 페이지가 **A4 정확히 1페이지**인가 (헤드리스 PDF 렌더로 자동 검사 가능).
2. 사용 높이가 **1050px를 넘지 않는가** — 넘으면 §4.3에 따라 문장을 줄인다.
3. 리드 스티커 수치가 리드 `stat.value`와 일치하는가.
4. 진 헤드라인·티저가 웹 `WEEKS`와 **문자열이 완전히 일치**하는가.
5. 새 도메인 색이 추가됐다면 §6.2 세 조건을 전부 통과하는가.
6. 세그먼티드 컨트롤이 375px에서 가로 스크롤로 전 도메인에 접근 가능한가.
7. 스파크라인이 직선이나 바닥붙음으로 뭉개지지 않았는가 (`trend` 4~5점).
8. `prefers-reduced-motion: reduce`에서 애니메이션이 멈추고 콘텐츠는 보이는가.
