# design.md — ANSWER ZINE 디자인 시스템

**주간 반복 생산을 전제로 한 고정 템플릿 명세다.**
매주 바뀌는 것은 §4의 슬롯 콘텐츠뿐이고, 나머지는 전부 잠겨 있다.

값은 전부 `build/lib/*.mjs`에 실제로 구현된 것이고, 대비 수치는 WCAG 2.1로 계산한 실측값이다.
**단일 소스는 코드다.** 이 문서의 표는 사람이 읽는 사본이므로, 값을 바꿀 때는 코드를 먼저 고친다.
`test/theme.test.mjs`·`test/css.test.mjs`가 아래 규칙 상당수를 자동으로 강제한다.

| 구분 | 원칙 |
|---|---|
| §2 불변식 | 에이전트가 **절대 바꾸지 않는다.** 변경은 사용자 승인 사항. |
| §4 슬롯 | 매주 바뀐다. **분량 예산을 지키면 A4 1페이지가 보장된다.** |
| §6 도메인 색 | 도메인 추가 시에만 건드린다. 선정 규칙이 정해져 있다. |

---

## 1. 테마 — 단일 고정, 흰 바탕

> **이 절은 2026-08-10에 실제 코드에 맞춰 다시 썼다.** 이전 버전은 Night(어두운 기본)·
> Paper(밝은 대체, `[data-theme="paper"]` 토글)·Zine(인쇄) 세 테마 체계를 설명했지만,
> `build/lib/theme.mjs`·`css.mjs`에는 애초에 그 토글 자체가 구현된 적이 없었다 —
> `test/theme.test.mjs`의 "테마 토글이 없다" 검사가 그걸 계속 강제해 왔다. 문서가 코드를
> 못 따라간 상태로 남아 있던 것을 이번에 바로잡는다.

테마는 **하나뿐이다.** 토글이 없다. 사이트는 항상 이 흰 바탕 하나로 읽힌다 — 진입 화면도
예외가 아니다(2026-08-10 전에는 `.intro`만 항상 어두운 별도 팔레트를 썼다).

| | 사이트 (`THEME`) | 인쇄 진 (`ZINE_THEME`) |
|---|---|---|
| 적용 범위 | `:root` | `.zine-page` |
| 배경 | `#FFFFFF` | `#FFFFFF` |

```css
:root {
  --ink:       #17150F;   /* 본문       대비 18.7:1 */
  --secondary: #5C574A;   /* 보조·티저   대비  7.4:1 */
  --tertiary:  #726C5E;   /* 날짜·캡션   대비  5.4:1 */
  --rule:      #17150F;
  --bg:        #FFFFFF;
  --surface:   #FFFFFF;
  --divider:   rgba(23,21,15,.24);
}
```

**세 역할 모두 흰 배경에서 AA(4.5:1)를 넘는다** — 흰색이 예전 크림(`#F4F1E7`)보다 밝아서
대비가 오히려 늘었다. `test/theme.test.mjs`가 매번 실측해 검증한다.

`--surface`가 `--bg`와 같은 흰색이라, 카드 같은 표면 요소는 **배경색이 아니라 1px
테두리(`--divider`)로만** 페이지와 구분된다 — §2 불변식 3번(도메인 색 사용처)과 같은 원칙이다.
**단 하나, 홈의 카테고리 카드(§7)는 예외다** — 도메인색을 풀블리드로 채운다. 그 외에는
색 채움으로 위계를 만들지 않는다.

`@media print`는 화면과 무관하게 항상 `.zine-page`(위 표 오른쪽 칸)를 쓴다 — 지금은 두
값이 사실상 같지만, 인쇄와 화면은 역할이 다르므로 변수 자체는 분리해 둔다.

---

## 2. 불변식 — 매주 바뀌지 않는 것

에이전트는 아래를 변경하지 않는다. 콘텐츠가 안 맞으면 **템플릿이 아니라 콘텐츠를 고친다.**

1. **스토리 구조는 5블록 고정** — `text, stat, text, quote, text`. 추가·삭제·순서 변경 없음.
   (`tools/qa.mjs` 232행이 이 순서를 강제한다.)
2. **`stat` 1개, `insight` 있는 `quote` 1개** — 스토리당 정확히 하나씩. **데이터 계약 얘기다.**
   > **2026-08-10 되돌림.** 2026-08-08부터 `insight.note`(설명)는 화면에 안 보였다(사용자
   > 요청). 사용자가 첨부한 참고 목업(movie.html/print.html)의 `.insight`/`.note2` 콜아웃이
   > 다시 요구해 되돌렸다 — 웹은 `.insight-note`, 진은 `.zine-insight-note`로 `note`를
   > 낸다. `insight.concept`(개념명)은 여전히 라벨로 노출하지 않는다 — 고정 문구
   > "인사이트"를 쓴다. S5 집필 규율(인사이트 3문 테스트)이 기대는 내부 품질 장치라는
   > 성격은 바뀌지 않았다 — 다만 이제 그 결과물(`note`)이 독자에게도 보인다.
   > **2026-08-11 추가.** 풀쿼트(`quote.text`)에도 "THE ANSWER" 라벨이 붙었다 — §7
   > "풀쿼트 & 출처 박스" 참고. `insight-note`와는 별개 컴포넌트다.
3. **도메인 색이 쓰이는 자리는 5곳** — 도메인 태그, 킥커, 풀쿼트 좌측 보더,
   인사이트 콜아웃 좌측 보더, **그리고 홈 카테고리 카드 배경(2026-08-10 추가, 풀블리드
   예외).** 이 다섯 번째 자리를 뺀 나머지 넷은 여전히 텍스트/1px 보더에만 쓴다 —
   버튼 채색은 이 시스템에 없다. 카테고리 카드가 배경 채색 예외로 추가된 경위는 §7
   "카테고리 카드" 참고 — 사용자가 첨부한 참고 이미지(도메인별 색 타일 그리드)가
   명시적으로 요구했다.
4. **진 페이지는 A4 1장.** 2페이지짜리 진을 만들지 않는다.
   > **2026-08-10 되돌림.** "리드 1 + 미니 3" 위계는 없앴다 — 참고 목업(print.html)의
   > `.sheet-story`처럼 그 주 통과분 전원을 동등한 행으로 낸다. A4 1장 제약 자체는
   > 그대로다(§4.2).
5. ~~회전각은 −9°~+7° 범위. 사진 −1.1°, 스티커 −3°, 테이프 −9°/+7°.~~
   **2026-08-10 삭제.** 인쇄 진에서 사진 콜라주·테이프·스티커 시스템 자체를 없앴다 —
   회전각을 적용할 대상이 없다.
6. ~~`clip-path` 다각형 값 — 가위로 오린 가장자리 표현.~~
   **2026-08-10 삭제.** 위 5번과 같은 이유 — 사진 플레이스홀더 자체가 없어졌다.
7. **`@page { size: A4; margin: 0 }`** — 안전 여백은 `.zine-page` 패딩이 전담한다.
8. **웹 본문에 CSS 다단(`column-count`)을 쓰지 않는다.** §5.3 참조.
9. **색 보더는 1px을 넘지 않는다.** 풀쿼트 포함.

---

## 3. 타이포그래피

### 워드마크 — 브래킷 마크 + "the answer company"

> **2026-08-10 축소.** 예전엔 `the answer company` 브래킷 마크 아래에 큰 `ANSWER` 슬랩 +
> `Zine` 스크립트 로고타입까지 한 이미지에 붙어 있었다. 사용자가 첨부한 참고 이미지가
> 브래킷 마크 + `the answer company`만 있는 축소판이라, 그 아래 로고타입 부분은 잘라냈다 —
> 재제작이 아니라 기존 파일의 2차 크롭이다.

| 파일 | 쓰임 | 아트 | 치수(2026-08-11 세 번째 크롭 후) |
|---|---|---|---|
| `assets/img/logo-light.png` | (현재 미사용 — 아래 참고) | 흰색, 배경 투명 | 462×236 |
| `assets/img/logo-dark.png` | 마스트헤드 · 인쇄 진 · OG 카드 | 검정, 배경 투명 | 518×265 |

사이트가 흰 배경 하나뿐이라 밝은 쪽(`logo-light.png`)이 지금은 쓰일 자리가 없다 —
어두운 배경이 다시 생기면 켤 수 있도록 파일과 CSS 스위치(`.logo-light`/`.logo-dark`)는
그대로 남겨 뒀다.

2026-08-10의 크롭은 원본 PNG의 알파 채널에서 두 로고 파트 사이의 빈 행(원본 기준 약
254~299px 지점)을 찾아, 위쪽(마크+워드마크)만 남기고 그 아래(예전 ANSWER/Zine
로고타입)를 잘라낸 것이었다. 원본 자체가 이미 2000×2000 정사각에서
`tools/crop-png.mjs`로 한 번 여백을 잘라낸 파일이라, 그게 **두 번째 크롭**이었다.

> **2026-08-11 세 번째 크롭 — "로고 옆 공백" 신고의 진짜 원인.** 여러 라운드에 걸쳐
> "로고 옆 여백을 없애 달라"는 요청이 반복됐는데, CSS 여백을 아무리 줄여도 해결되지
> 않았다 — 원인이 CSS가 아니라 **파일 자체**였다. 2026-08-10 크롭 후 캔버스가
> 1971×270이었는데, 실제 잉크는 x 748~1245(가로 497px)에만 있었다 — 캔버스 너비의
> 약 38%씩이 좌우로 투명 여백이었다. `tools/crop-png.mjs`(알파 채널 경계 자동 탐지)로
> 내용 경계에 10px 패딩만 남기고 다시 잘랐다:
> ```bash
> node tools/crop-png.mjs assets/img/logo-dark.png assets/img/logo-dark.png 10
> node tools/crop-png.mjs assets/img/logo-light.png assets/img/logo-light.png 10
> ```
> 결과: 1971×270 → 518×265 (비율 7.3:1 → 약 1.96:1로 완전히 바뀌었다 — 넓고 얇은
> 배너에서 정사각에 가까운 마크가 됐다).

```bash
node tools/crop-png.mjs --matte <입력.png> <출력.png> [여백px]   # 여백 자르기(1차, 기존 도구)
```

비율이 1.96:1(거의 정사각)로 바뀌면서 **너비 기준이 아니라 높이 기준으로 잰다** —
7.3:1 시절 쓰던 너비 기준 값을 그대로 두면 세로가 비정상적으로 길어진다(예: 화면
539px 너비 기준이면 세로 275px, 진 62mm 너비 기준이면 세로 32mm — 둘 다 지면을
침범한다). 화면 높이는 `clamp(56px, 6vw, 96px)`, 진에서는 `12mm`다.

배경이 불투명하면 흰 지면에 사각형이 얹힌다. **반드시 투명화한 파일을 쓴다.**

### 서체 — 헬베티카 + SUIT, 웨이트로 위계를 만든다

> **2026-08-10 전환.** 예전엔 헤드라인/UI에 Paperlogy(산세리프), 본문/쿼트에 나눔명조(세리프)
> 를 자체 호스팅해서 역할을 나눴다. 사용자 요청으로 자체 폰트를 걷어내고 시스템 헬베티카
> 스택 하나로 통일했다 — `--sans`/`--serif` 두 변수 이름은 남아 있지만 값은 같다.
>
> **2026-08-13 스물여덟 번째 라운드 — 한글 폴백만 SUIT으로 재전환.** 사용자가 "한글은
> SUIT으로, 영문은 Helvetica Neue 그대로"라고 명시적으로 요청했다 — 시스템 한글 폰트
> (Apple SD Gothic Neo·맑은 고딕)를 웹폰트(SUIT)로 바꾸는 것이라 위 2026-08-10 전환을
> 완전히 되돌리는 게 아니다: **영문은 여전히 시스템 헬베티카**고, 한글 폴백 자리 하나만
> 바뀐다. 다만 "로드하는 자체 폰트가 없다"는 문장은 더 이상 사실이 아니다 — 아래 참고.

```css
--sans: -apple-system, "Helvetica Neue", Helvetica, Arial,
        "SUIT Variable", SUIT, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
--serif: 위와 동일;
```

라틴 글자(영문·숫자·기호)는 Helvetica Neue/Helvetica/Arial에서 그려지고, 한글은 그 폰트들에
글리프가 없어 자동으로 다음 순번(SUIT Variable)으로 넘어간다 — 브라우저의 문자 단위 폰트
폴백을 이용한 것이라 별도 처리가 필요 없다(2026-08-10 이전부터 쓰던 방식과 메커니즘은
같다, 폴백 순번에 SUIT 한 단계가 늘었을 뿐). SUIT 자체 로드가 실패하면(네트워크 문제 등)
그다음 순번인 Apple SD Gothic Neo·맑은 고딕으로 조용히 떨어진다 — 화면이 깨지지 않는다.

옛 체계는 "한국어 장문 가독은 명조, 수치 조판은 산세리프"라는 역할 분리로 두 서체를
정당화했다. 지금은 그 역할을 **하나의 그로테스크 안에서 웨이트 대비**로 만든다 —
헤드라인 900, 킥커·라벨·바이라인 700, 본문 400. 형태로 나뉘던 목소리가 굵기로 나뉜다.
이 위계 자체(900/700/400)는 2026-08-13에도 안 바뀌었다 — 서체만 SUIT으로 그려질 뿐이다.

**SUIT은 sunn-us/SUIT 공식 저장소가 권장하는 jsdelivr CDN 경유 배리어블 폰트다.**
`build/lib/css.mjs`의 `@font-face`(`font-weight: 100 900`) 하나가 100~900 전 웨이트를
파일 하나로 커버한다 — 이 사이트가 실제 쓰는 400/700/900 각각을 별도 파일로 안 받는다.
`font-display: swap`이라 폰트가 늦게 도착해도 텍스트는 폴백 폰트로 이미 보이고 있다(FOIT
없음). `test/css.test.mjs`가 `@font-face`가 정확히 1개고 SUIT Variable인지 검사한다 —
예전엔 "0개"를 강제했던 자리다.

> ⚠️ **이 CDN URL은 이 세션에서 직접 열어 확인하지 못했다.** 2026-08-13 시점 이 세션의
> 네트워크 egress가 전면 차단돼(`kobis.or.kr`·`google.com`까지 `EGRESS_BLOCKED`,
> CLAUDE.md 상단 참고) `cdn.jsdelivr.net`도 열리지 않았다. URL 자체는 SUIT 공식 README가
> 권장하는 표준 스니펫 그대로지만, 실제로 살아 있는지는 GitHub Actions CI(실 네트워크
> 보유)의 `build/verify.mjs` 브라우저 콘솔 검사가 검증한다 — 로컬(이 세션)에서 같은 검사를
> 돌리면 `requestfailed`로 실패할 수 있는데, 그건 URL이 잘못됐다는 뜻이 아니라 이 세션이
> 못 나간다는 뜻이다. CI가 그린이면 실제로 뜬 것이다.

### 킥커

킥커는 신문 지면의 문법이므로 **유지한다.** 다만 모노스페이스는 뺐다 —
`monospace`를 "기술적"이라는 인상의 의상으로 쓰는 것은 근거가 없다.
산세리프 700 + 자간 `.14em` + `uppercase`로 대체한다.

### 스케일 — 화면

| 역할 | 크기 | 웨이트 | 행간 | 자간 |
|---|---|---|---|---|
| 스토리 헤드라인 | `clamp(32px, 5.2vw, 52px)` | 900 | 1.06 | −0.035em |
| 통합 인사이트 헤드라인 (`.issue-insight`, 2026-08-10) | `clamp(34px, 6.5vw, 72px)` | 900 | 1.04 | −0.03em |
| 섹션 제목 (h2) | `clamp(24px, 3.4vw, 34px)` | 900 | 1.1 | −0.03em |
| 스탯 값 | 30 | 900 | 1.1 | −0.03em (tabular) |
| 풀쿼트 | 20 | 700 | 1.55 | −0.01em |
| 티저 | 18 | 700 | 1.6 | — |
| 본문 | 17 | 400 | 1.9 | 0 |
| 리스트 헤드라인 | 19 | 900 | 1.2 | −0.025em |
| 메타·날짜 | 13 | 400 | — | tabular |
| 킥커·라벨 | 11 | 700 | — | 0.1~0.14em, uppercase |

**자간 하한은 −0.04em이다.** 실사용은 −0.01 ~ −0.035em. `test/css.test.mjs`가 강제한다.

> **2026-08-12 스물한 번째 라운드 — `issue.insightNote`를 리드+바디 두 줄로 시각
> 분리.** CLAUDE.md §5.5가 이미 정의한 3단 구조("인용부호로 감싼 한 문장짜리 명제로
> 연다" → 이번 호에서 어떻게 나타났는지 → 구조적 어휘로 확장)는 지금까지 화면에서
> 한 문단(`.issue-insight-note`, 18px/400/`var(--secondary)`)으로만 보였다 —
> 사용자가 스크린샷으로 그 1단(인용부호 문장)을 굵고 밝게, 나머지를 옅게 나눠 달라고
> 요청했다. `render-index.mjs`의 `splitInsightNote()`가 문자열 선두의 `"…"` 패턴을
> 정규식으로 찾아 리드(`.insight-note-lead`, 20px/700/`var(--ink)`)와 바디
> (`.insight-note-body`, 16px/400/`var(--secondary)`)로 쪼갠다. 데이터 필드
> (`issue.insightNote`)는 안 바뀐다 — 렌더링만 갈린다. 리드 패턴이 없는(인용부호로
> 안 시작하는) 과거 회차 문구는 예전처럼 `.issue-insight-note` 통짜 문단 하나로
> 남는다 — `test/zine.test.mjs`가 두 경로 다 검증한다.

> **2026-08-12 스물두 번째 라운드 — 스토리 페이지 바이라인 축약.** 사용자가 첨부한
> 스크린샷대로 `.byline`을 "BY ANSWER ZINE · {도메인} 데이터 기반"에서
> "- BY ANZINE"으로 줄이고 우측 정렬했다(`text-align: right`). 도메인은 바로 위
> `.meta`(도메인 태그+집계 범위)에 이미 있어 중복이었다 — 정보 손실 없이 줄인다.
> "ANZINE"은 인트로 애니메이션·DIY 진 표지가 이미 쓰는 브랜드 축약형과 같은
> 문법이다(`SITE.name` "ANSWER ZINE" 자체는 안 바뀐다). border-bottom으로 헤드라인/
> 티저와 스탯 패널 사이 경계선 역할을 겸하는 건 그대로다.

> 예전엔 여기서 "어두운 배경(Night)의 밝은 텍스트는 번져 보이므로 본문 자간을 양수로
> 보정한다"고 적혀 있었다. 그 별도 보정은 실제 CSS(`body { letter-spacing: 0; }`)에 없던
> 서술이었다 — 사이트가 흰 배경 하나뿐인 지금은 애초에 해당하지 않는다.

### 스케일 — 진

> **2026-08-10 개편.** "리드 1 + 미니 3" 위계가 없어지면서 스토리마다 같은 크기의
> 헤드라인·본문을 쓴다 — 리드/미니로 크기를 나누던 예전 표는 무의미해졌다(§8 참고).

| 역할 | 크기 | 웨이트 | 행간 |
|---|---|---|---|
| 스토리 헤드라인 | 16 | 900 | 1.25 |
| 스토리 본문(teaser) | 10 | 400 | 1.55 |
| 인사이트 콜아웃 본문 | 9 | 400 | 1.5 |
| 인사이트 콜아웃 라벨 | 7.5 | 700 | — (0.08em, uppercase) |
| 출처 링크 | 8 | 400/700(링크) | — |
| 스탯 값 | 15 | 900 | 1.2 (tabular) |
| 스탯 라벨 | 7.5 | 400 | — |
| 킥커 | 9 | 700 | — (0.1em, uppercase) |
| 데이트라인·바이라인 | 10 | 700 | — |
| 푸터 | 8.5 | 400 | — (0.04em) |

진의 킥커·라벨류는 전부 `uppercase` + 넓은 자간이다. 신문 조판 관용이다.

### 숫자

`.stat-value`와 메타 날짜에 `font-variant-numeric: tabular-nums`. 세로로 나열될 때 자릿수가 흔들리지 않는다.

---

## 4. 슬롯과 분량 예산 — 매주 바뀌는 것

### 4.1 예산표

단일 소스는 `domains/registry.json`의 `budget`이다. `tools/qa.mjs`가 이 값으로 검사한다.

| 슬롯 | 권장 (≤) | 절대 상한 |
|---|---|---|
| 스토리 헤드라인 | **24자** | 32자 |
| 스토리 티저 | **78자** | 104자 |
| `text` 블록 (각) | **180자** | 240자 |
| `quote.text` | 40자 | 48자 |
| `insight.concept` | 30자 | 34자 |
| `insight.note` | 125자 | 140자 |
| `stat.label` | 22자 | 26자 |
| `stat.value` | 18자 | 22자 |
| `stat.axisCaption` | 52자 | 60자 |
| `kicker` | 22자 | 26자 |
| `trend` 배열 | 4~5점 | 2~8점 |
| `issue.insightNote` (2026-08-10 도입, 2026-08-12 예산 확대 — `CLAUDE.md` §5.5 참고) | 150자 | 220자 |

### 4.2 A4 1페이지 보장

`build/verify.mjs`가 **매 빌드마다 헤드리스 PDF로 실측한다.**

```js
const buf = await page.pdf({ format: "A4", printBackground: true,
                             margin: { top: "0", bottom: "0", left: "0", right: "0" } });
const pages = (buf.toString("latin1").match(/\/Type\s*\/Page[^s]/g) || []).length;
// pages === 1 이어야 한다
```

이 검사가 실제로 결함을 잡았다. 진을 감싼 래퍼의 `margin-top: 32px`와 `.zine-page`의
`min-height: 297mm`가 맞물려 인쇄가 2페이지가 됐다. `@media print`에서 래퍼 여백을 지우고
`min-height`를 해제해 해결했다. **이 규칙을 되돌리면 검증이 다시 실패한다.**

### 4.3 예산 초과 시

문장을 줄인다. **폰트 크기·행간·여백을 줄여서 맞추지 않는다.** 그건 불변식 위반이다.
줄일 수 없다면 소재가 한 호에 담기지 않는 것이다 — 쪼개거나 다음 주로 넘긴다.

---

## 5. 레이아웃

### 5.1 그리드

| 컨테이너 | 값 |
|---|---|
| `.shell` (앱 셸) | `max-width: 1240px` |
| 읽기 측정 | `--measure: 68ch` (45~75자 범위 안) |
| `.zine-page` | 210mm × 297mm — 실측 794 × 1123px @96dpi |

### 5.2 브레이크포인트

| 구간 | 구조 | 변하는 것 |
|---|---|---|
| **전 구간(스토리 페이지)** | 1단 세로 흐름 | 모든 요소가 문서 순서 그대로 쌓인다(§5.3) |
| ≤780px (진) | 진 3단 → 1단 | 사진 float 해제, 헤드라인 44→32px |

`build/verify.mjs`가 375 / 768 / 1280 / 1920px에서 **가로 넘침이 없는지 실측한다.**

> **2026-08-11 일곱 번째 라운드 — 12칼럼 비대칭 그리드(본문 `1/span7` + 레일 `9/span4`,
> ≥1200px)를 붙였었다.** 2026-08-13 "ANZINE — FINAL STORY PAGE CLEANUP"에서 이 구조를
> 없앴다 — 아래 §5.3 갱신 참고. 이 표의 옛 내용(구간별 칼럼 배치, sticky 레일, auto-placement
> 함정)은 더 이상 유효하지 않아 지웠다 — 되살릴 일이 생기면 git 히스토리에서 이 절의
> 이전 버전을 참고한다.

### 5.3 본문에 CSS 다단을 쓰지 않는다 — 2026-08-13 한 칼럼 세로 흐름으로 재정리

신문이 다단인 것은 **지면이 고정돼 있기 때문**이다. 웹에서 본문을 다단으로 짜면
아래로 읽다가 다시 위로 올라와야 한다. 다단은 **인쇄 진에서만** 쓴다. 거기서는 지면이
고정이라 맞는 도구다.

> **2026-08-11 일곱 번째 라운드~2026-08-13 이전까지는 "비대칭 그리드"였다.** 본문은
> 45~75자 한 칼럼, 숫자와 인사이트는 오른쪽 레일에 세로 괘선으로 붙어 "근거는 늘 옆에
> 있다"는 태도를 레이아웃으로 표현했다 — ≥1200px 얘기고, 그보다 좁은 화면(이 사이트
> 트래픽의 대다수)에서는 레일이 본문보다 **먼저** 나왔다.
>
> **"ANZINE — FINAL STORY PAGE CLEANUP"(2026-08-13)이 이 구조 자체를 지적했다.** 좁은
> 화면에서 레일이 본문보다 먼저 나오는 건 곧 "무슨 일이 있었는지 읽기도 전에 큰 숫자
> (스탯 카드)부터 보게 된다"는 뜻이었다("sudden number" 문제) — ≥1200px에서도 카드의
> 시각적 무게(배경·큰 숫자)가 본문보다 먼저 눈을 끌기는 마찬가지였다. 요청은 명확한
> **하나의 읽기 순서**였다: 카테고리 → 헤드라인 → 무슨 일이 → 근거/숫자 → 설명 → 답
> (THE ANSWER) → 출처. 레일을 없애고, 모든 요소(본문 세 문단·스탯 카드·출처 박스·
> 인사이트 콜아웃·풀쿼트)를 그 순서 그대로 **문서 흐름 하나**에 쌓는다
> (`render-story.mjs`의 `renderStory()`) — 화면 폭과 무관하게 항상 같은 순서로
> 읽힌다. 각 요소가 이미 자기 `max-width: var(--measure)`와 `margin-bottom`을 갖고
> 있어(`.story-body p`·`.stat-card`·`.source-box`·`.insight-note`·`.pullquote`) 래퍼
> 그리드 없이 쌓기만 해도 폭·간격이 맞는다 — 새 CSS 토큰이나 컴포넌트를 추가하지
> 않았다. 12칼럼 그리드·sticky 레일 코드는 지웠다(`build/lib/css.mjs`).
>
> **5블록 데이터 계약(§2 불변식 1번, CLAUDE.md §3.3)은 안 바뀌었다** — `text/stat/text/
> quote/text` 순서 그대로다. 바뀐 건 그 블록들을 화면에 배치하는 순서와 방식(레일 분리
> → 한 흐름)뿐, 블록 자체의 개수·구조·내용은 그대로다.

### 5.4 여백

화면은 **px**, 진은 **mm**. 종이 산출물의 치수를 물리 단위로 유지하려는 의도적 분리다.

- 화면: `4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96` px (`--s1`~`--s9`)
- 진: `1 · 2 · 3 · 4 · 5 · 6 · 8 · 10` mm / `.zine-page` 패딩 `8mm 10mm` → ≤780px에서 `6mm 5mm`

**이 패딩을 줄이면 프린터 물리 여백에 잘린다.**

---

## 6. 도메인 컬러 — 확장 규칙

### 6.1 현재 활성 도메인 — 8개

단일 소스는 `domains/registry.json`이다. CSS는 `--dc-<key>` 변수만 참조한다.

| 도메인 | Hue | Night `color` | 대비 | Paper `colorPaper` | 대비 |
|---|---|---|---|---|---|
| 음악 | 35° | `#FF9500` | 8.32:1 | `#A15E00` | 4.51:1 |
| 여행 | 85° | `#59900C` | 4.72:1 | `#4A780A` | 4.67:1 |
| 도서 | 135° | `#34C759` | 8.24:1 | `#217E38` | 4.53:1 |
| 공연 | 172° | `#0C907E` | 4.62:1 | `#0A7B6C` | 4.57:1 |
| 영화 | 210° | `#0A84FF` | 5.01:1 | `#006BD7` | 4.55:1 |
| 뉴스 | 256° | `#8B66F3` | 4.61:1 | `#7142F0` | 4.97:1 |
| OTT | 303° | `#E212D8` | 4.63:1 | `#C110B9` | 4.60:1 |
| 유튜브 | 349° | `#FF2D55` | 5.01:1 | `#DD002A` | 4.52:1 |

**열여섯 값 전부 AA를 넘는다.** Paper 보정색은 **hue를 한 도도 바꾸지 않고 명도만 내린** 값이다.
`test/theme.test.mjs`가 매번 대비와 hue 일치를 계산해 검증한다.

**도메인을 8개로 늘린 이유는 지면을 채우기 위해서가 아니라 결번이 나도 4편을 확보하기 위해서다.**
> **2026-08-10 되돌림.** "리드 1 + 미니 3, 상위 4편만" 위계는 없앴다 — 지면은 이제 그 주
> 통과분 전원을 동등한 행(`.zine-story`)으로 낸다. A4 1장 제약은 그대로다(§4.2) — 통과
> 편수가 늘면 행마다 여백·글자 크기가 아니라 §4.3 규칙대로 **문장을 줄여서** 맞춘다.

> 배경이 `#222222`에서 `#16150F`로 바뀌면서, 영화·유튜브가 4.36:1로 AA 미달이던 오래된 문제가
> 5.01:1로 **저절로 해소됐다.** 색을 바꾸지 않고 배경만 바꿔서 풀렸다.

진 페이지에는 도메인 색을 쓰지 않는다 (진 킥커는 `--secondary`).

### 6.2 신규 도메인 색 선정 기준

네 조건을 **전부** 만족해야 한다.

1. ~~Night 배경 `#16150F` 대비 ≥ 4.5:1~~ — 2026-08-10부터 Night 테마 자체가 없다(§1).
   `registry.json`의 `color`(Night 변형) 필드는 실제로 어디서도 렌더되지 않는 값이다
   (`domainCSS()`는 항상 `colorPaper`만 낸다) — 새 도메인을 추가할 때 이 계산에 시간을
   쓰지 않는다.
2. 사이트 배경 `#FFFFFF` 대비 **≥ 4.5:1** (`colorPaper`) — 예전엔 `#F4F1E7` 기준이었다.
   흰색이 더 밝아 대비는 늘기만 하므로, 기존 도메인 값은 전부 그대로 통과한다.
3. hue는 **자기 자신과만** 일치하면 된다 — `color`/`colorPaper` 두 값을 굳이 나눌 이유가
   없어졌으니, 새 도메인은 `colorPaper` 하나만 정하면 된다.
4. 기존 모든 도메인과 **색상환 거리 ≥ 35°**

### 6.3 색상환은 다 찼다

현재 hue는 35 / 85 / 135 / 172 / 210 / 256 / 303 / 349°다. 간격은 이렇다.

```
35 →85  50°     172→210  38°
85 →135 50°     210→256  46°
135→172 37°     256→303  47°
                303→349  46°     349→35  46°
```

**최소 간격이 37°이고, 이게 8개를 배치할 때의 실질 한계다.**
색상환 360°를 8등분하면 45°지만 기존 4개(35·135·210·349°)가 불균등해 그만큼 못 벌린다.
그래서 `minHueDistance`를 40°에서 **35°로 낮췄다.**

> **9번째 도메인은 이 규칙으로 불가능하다.** 색만으로 구분하는 방식의 상한에 도달했다.
> 더 늘리려면 색 이외의 구분 수단(패턴·아이콘·타이포 변주)이 필요하고, 그건 불변식 변경이라 사용자 승인 사항이다.

### 6.4 도메인 증가 시 레이아웃 영향

- **홈 카테고리 그리드** — `grid-template-columns: repeat(2, 1fr)`(모바일 1열)라 도메인이
  늘면 행이 자동으로 늘어난다. 2026-08-10에 도메인 필터(`.seg`)+평면 리스트(`.list`)를
  이 그리드가 완전히 대체했다 — "줄바꿈되는 필터 탭"이라는 옛 관심사는 없어졌다.
- **진 `.zine-story`** — 2026-08-10부터 스토리 수만큼 행이 늘어나는 구조다(고정 3칸이
  아니다). 활성 도메인이 늘면 그만큼 행도 늘 수 있다는 뜻 — **A4 1장을 넘기면 레이아웃
  결정이 필요하다.** `build/verify.mjs`가 매 빌드마다 실측한다(§4.2).
- **A4 예산** — 지금은 4편 기준으로 실측해 여유가 있다. 통과 편수가 늘면
  `build/verify.mjs`로 재측정하고, 넘치면 §4.3대로 문장을 줄인다.

---

## 7. 컴포넌트 스펙

### 마스트헤드

```
site-header: position: sticky; top: 0 · 항상 화면 상단에 붙는다 · 뷰포트 전체 폭(--edge만 패딩)
워드마크:    로고 이미지(§3 워드마크 참조) · width clamp(234px, 31vw, 359px) · height auto
카테고리 내비: About + 영문 라벨(nameEn) 링크 · PC에서는 로고 우측(§아래 참고), 좁은 화면에서 가로 스크롤
```

> **2026-08-12 스물세 번째 라운드 — PC에서 로고를 뷰포트 정중앙으로.** 사용자가
> 스크린샷으로 지정. 로고(auto 너비)와 내비(가변 너비)는 폭이 서로 달라
> `justify-content: space-between`으로는 로고가 진짜 화면 중앙에 오지 않는다(두
> 요소 사이 여백만 균등해질 뿐이다) — `.masthead-top`을 768px 이상에서 3칸 그리드
> (`1fr auto 1fr`)로 바꾸고 로고를 가운데 칸(`grid-column: 2; justify-self: center`),
> 내비를 오른쪽 칸(`grid-column: 3; justify-self: end`)에 놓았다. 왼쪽 칸은 빈 채로
> 둔다 — 스페이서 엘리먼트 없이 grid 배치만으로 그 칸이 오른쪽 내비 칸과 항상 같은
> 폭으로 자라 로고가 정중앙에 고정된다. 768px 미만(모바일)은 기존 `space-between`
> (로고 좌측·내비 우측)을 그대로 쓴다 — 내비 항목이 6개라 좁은 화면에서는 가운데
> 배치보다 좌우 붙이기가 공간 활용에 낫다.

> **2026-08-10 두 번째 라운드.** 예전엔 여기서 워드마크를 "Paperlogy 900 · uppercase" 텍스트로
> 적어 뒀지만, 실제 마스트헤드(`.wordmark`/`.logo`)는 처음부터 텍스트가 아니라 이미지 로고였다
> — §3과 안 맞던 서술을 그때 실제 CSS에 맞춰 고쳤다. 이번 라운드에서 셋을 더 바꿨다:
> 1. **고정(sticky)** — 로고·카테고리 내비가 스크롤 내내 상단에 붙는다(사용자 요청).
> 2. **로고 20% 확대** — `clamp(150px, 20vw, 230px)` → `clamp(180px, 24vw, 276px)`.
> 3. **카테고리 내비 추가, 밑줄(`.ruleline`) 제거** — 로고 옆에 도메인별 영문 라벨
>    (`registry.json`의 `nameEn`)이 링크로 붙는다. 홈 카테고리 카드와 같은 목록·같은
>    "최신 스토리" 링크를 쓴다(`build.mjs`가 한 번 계산해 모든 페이지에 물려준다). 굵은 선 +
>    가는 선의 옛 룰라인은 뺐다 — 사용자 요청.
>
> **2026-08-11 세 번째 라운드.** 넓은 화면에서 로고가 `.shell`(max-width 1240px)에 갇혀
> 좌우로 빈 공간이 남는다는 지적 — 셋을 바꿨다:
> 1. **엣지-투-엣지** — `.site-header`가 `.shell`을 벗고 뷰포트 전체 폭을 쓴다. 로고·내비는
>    새 토큰 `--edge`(`clamp(10px, 2vw, 24px)`)만큼만 여백을 둔다 — `.shell`의 큰 거터
>    대신 이 값 하나로 "코너에 붙었다"는 인상을 준다.
> 2. **로고 30% 추가 확대** — `clamp(180px, 24vw, 276px)` → `clamp(234px, 31vw, 359px)`
>    (2026-08-10의 +20%에 이은 두 번째 확대, 사용자 요청).
> 3. **"About" 링크 추가** — 내비 맨 앞, 신규 `/about/` 라우트로 이어진다.
> 홈의 데이트라인·통합 인사이트·카테고리 그리드·페이저도 `.shell` 대신 같은 `--edge`
> 토큰을 쓰는 `.shell-edge`로 옮겨, 마스트헤드부터 페이지 끝까지 여백이 일관된다.

> **2026-08-13 — `--edge` 상한을 24→80px로 키움.** 사용자가 전/후 스크린샷으로 "사이트
> 전반에 여유 공간이 부족하다"고 지적 — 넓은 화면에서 `--edge`의 상한이 24px뿐이라
> 마스트헤드·헤드라인 텍스트가 뷰포트 가장자리에 너무 바짝 붙어 보였다. `clamp(10px, 2vw,
> 24px)` → `clamp(10px, 6vw, 80px)`로 올렸다 — vw 계수도 같이 키워 일반적인 데스크톱
> 폭(1280px 안팎)에서 이미 상한 가까이(76.8px) 도달한다. `.category-grid`는 이 값을 음수
> 마진으로 그대로 상쇄해 풀블리드를 유지하도록 이미 설계돼 있어서(위 §카테고리 카드),
> 카드 그리드 자체는 이 변경과 무관하게 계속 뷰포트 가장자리까지 꽉 찬다 — 늘어나는 건
> 마스트헤드·본문 텍스트 영역의 좌우 여백뿐이다. `--edge` 하나가 여러 자리(마스트헤드·
> shell-edge·카드 그리드 상쇄)에 쓰이는 단일 토큰이라 이번에도 값 하나만 바꿔서 전체가
> 일관되게 따라왔다.

> **2026-08-13 다섯 번째 라운드 — 홈 히어로 구간(마스트헤드~카드 그리드) 세로 간격 확대.**
> 사용자가 As-was/To-be 스크린샷으로 지적: 지금은 마스트헤드·"WEEKLY ANSWER" 데이트라인·
> 통합 인사이트 헤드라인·부연 문단이 세로로 너무 빽빽하게 붙어 있다, PC에서 훨씬 여유
> 있게 벌려 달라. 데스크톱(768px+)에서만 세 지점의 세로 간격을 토큰 스케일 안에서 한
> 단계씩 키웠다(`body.is-home` 스코프, 홈 전용) — `.shell-edge` 상단 패딩 `--s8`(64px)→
> `--s9`(96px), `.dateline` 하단 마진 `--s6`(32px)→`--s8`(64px), `.insight-lead` 하단
> 마진 `--s6`(32px)→`--s9`(96px). 좌우 여백(`--edge`)과 모바일(768px 미만, 기존 값 그대로)은
> 손대지 않았다 — Playwright로 390px 뷰포트에서 `.shell-edge` 계산된 padding-top이 여전히
> 48px(`--s7`, 미디어 쿼리 밖 값)임을 확인.
>
> **참고 이미지에 대한 해석 한계를 사용자에게 투명하게 알렸다.** 첨부된 "To be" 이미지는
> 세로로 매우 긴 크롭이라, 마스트헤드~그리드 구간의 정적 CSS 여백뿐 아니라 그 위
> 인트로 워드마크("ANZINE")가 스크롤에 맞춰 축소·고정되는 절차(`assets/app.js`의
> pin/converge 로직)까지 훨씬 늘어난 것처럼도 읽힐 수 있다 — 이번 라운드는 후자(스크롤
> 애니메이션 타이밍)는 건드리지 않고 전자(정적 CSS 간격)만 반영했다. 실제 홈페이지를
> 스크린샷으로 찍어 사용자에게 보내 이 해석이 맞는지 확인을 요청했다 — 스크롤 구간까지
> 더 벌리고 싶다면 별도 라운드로 진행한다.

> **같은 날 후속 — "변화가 없다"는 반응, 두 가지 문제를 확인.** 사용자가 위 스크린샷을
> 보고 "캡쳐 이미지는 변화가 없는데? 내가 주문한거랑도 다르고"라고 지적했다. 원인 두
> 가지를 확인했다:
>
> 1. **캡처 자체의 문제.** 확인용으로 보낸 스크린샷이 `#main-content`(마스트헤드)부터
>    시작하도록 스크롤해 찍은 것이라, 참고 이미지에 있던 "ANZINE" 큰 로고 부분이 아예
>    안 보였다 — 같은 화면을 비교할 수 없는 캡처였다.
> 2. **간격 크기 자체의 문제.** 1차로 키운 값(토큰 스케일 최대치 `--s9`=96px)이 참고
>    이미지 속 간격(뷰포트 세로의 10~20% 수준으로 보임)에 한참 못 미쳤다 — 고정 96px는
>    작은 노트북 화면에서도 큰 모니터에서도 똑같아서, 상대적으로 "여유"가 안 느껴졌다.
>
> **AskUserQuestion으로 인트로 처리 방식부터 확인했다** — 참고 이미지의 "ANZINE"이
> 지금처럼 스크롤 애니메이션으로 사라지는 방식을 유지할지, 페이지 맨 위에 고정으로
> 항상 보이는 정적 섹션으로 바꿀지. 답: **"지금의 스크롤 인트로 유지"** — 즉 이 라운드는
> 인트로 자체(`app.js`의 pin/converge)는 그대로 두고, 인트로 *다음에 오는 정적 구간의
> 간격*만 훨씬 더 키우면 된다는 뜻으로 확정했다.
>
> 그에 따라 값을 다시 잡았다 — 고정 토큰 대신 `--edge`와 같은 패턴(뷰포트 비례
> `clamp()`)으로 바꿔, 화면이 커질수록 여백도 비례해서 커지게 하고 최소값도 훨씬
> 올렸다: `.shell-edge` 상단 패딩 `clamp(140px, 16vh, 260px)`(1차 96px 고정 대비 최대
> 2.7배), `.dateline` 하단 마진 `clamp(56px, 9vh, 140px)`, `.insight-lead` 하단 마진
> `clamp(96px, 13vh, 200px)`. 확인 스크린샷은 `#main-content`부터가 아니라 "위에서부터
> 무엇이 바뀌었는지 알 수 있게" 마스트헤드~카드 그리드 시작 구간 전체를 새로 캡처해
> 다시 보냈다.

> **같은 날 여섯 번째 라운드 — 세 지점 간격 재요청 + 중앙 정렬, 미리보기 선행.**
> 사용자가 참고 이미지를 다시 보내며 "이게 내가 원하는 구조긴 해"라고 확인한 뒤,
> 곧이어 더 구체적인 세 가지를 요청했다: (1) ANZINE(인트로) ↔ 마스트헤드(Fixed
> Header) 간격, (2) 마스트헤드 ↔ WEEKLY ANSWER 간격, (3) WEEKLY ANSWER ↔
> UNEXPECTED 간격 — "스크롤 로직이 아니라" 간격 문제라고 명시(=`app.js`는 안 건드림).
> 추가로 WEEKLY ANSWER·UNEXPECTED·인사이트 문단을 참고 이미지처럼 페이지 중앙에
> 세로로 쌓아 달라고 요청했고, "UNEXPECTED와 본문 사이 간격도 넓혔으면" 한다고
> 덧붙였다. **그리고 "이 오더들은 사이트에 바로 적용하지 않고, 먼저 캡쳐 이미지가
> 필요해"라고 명시** — 이번 라운드는 커밋 전에 로컬 빌드+스크린샷으로 먼저
> 확인받는 절차를 밟았다.
>
> 구현:
> 1. **ANZINE ↔ 마스트헤드 간격.** `#main-content`가 `#intro`의 형제 요소라는 점을
>    이용해(`layout.mjs`), `body.is-home #main-content { margin-top: clamp(80px,
>    14vh, 220px); }`로 순수 여백만 추가했다 — 인트로의 pin/converge 스크립트
>    (`assets/app.js`)는 전혀 건드리지 않는다. 인트로가 완전히 스크롤되어 나간
>    뒤, 마스트헤드가 나타나기 전까지 빈 검정 스크롤 구간이 추가로 생긴다
>    (390px 뷰포트에서 재확인 결과 `margin-top`은 미디어 쿼리 밖이라 0px 그대로 —
>    모바일 영향 없음).
> 2. **마스트헤드 ↔ WEEKLY ANSWER.** `.shell-edge` 상단 패딩을 `clamp(140px, 16vh,
>    260px)` → `clamp(200px, 22vh, 340px)`로 한 단계 더 키웠다.
> 3. **WEEKLY ANSWER ↔ UNEXPECTED.** `.dateline` 하단 마진을 `clamp(56px, 9vh,
>    140px)` → `clamp(80px, 12vh, 180px)`로 키웠다.
> 4. **중앙 정렬 + 세로 스택.** `.insight-lead`를 `flex-direction: column;
>    align-items: center`로 바꿔 헤드라인과 문단을 나란히(2단)가 아니라 위아래로
>    쌓는다. `.dateline`·`.issue-insight`·`.issue-insight-note`(및 리드/바디
>    분리 변형)에 `text-align: center`를 추가했고, `.issue-insight`에
>    `margin-bottom: clamp(56px, 9vh, 140px)`을 새로 줘 헤드라인과 문단 사이
>    간격도 벌렸다. 전부 `@media (min-width: 768px)` 안, `body.is-home` 스코프라
>    모바일과 다른 페이지는 전혀 영향받지 않는다(390px 뷰포트에서 `flex-direction:
>    row`·좌측 정렬 그대로임을 재확인).
>
> **기존 명시 지시와의 충돌을 투명하게 알렸다.** `.issue-insight-note`의
> `text-align: left`는 2026-08-12 스물한 번째 라운드에 사용자가 "문단은 left
> aligned로"라고 명시적으로 요청해 못박은 값이다 — 이번 중앙 정렬 요청은 그
> 지시를 정면으로 뒤집는다. 스크린샷을 보낼 때 이 충돌을 먼저 알렸고, 사용자가
> "오케이 적용해봐"로 승인한 뒤에야 커밋했다.
>
> **미리보기 절차.** 로컬에서 `node build/build.mjs`로 빌드한 `dist/`를 Playwright로
> 두 시점 캡처했다 — (a) 인트로 "ANZINE"이 완성된 상태(변경 없음, 참고용, 최종
> 메시지에는 첨부하지 않음) (b) 마스트헤드부터 카드 그리드 시작까지 전체
> (`reducedMotion: 'reduce'`로 `data-reveal` 페이드인 타이밍 이슈를 피하고,
> 뷰포트 높이를 콘텐츠 총 높이(1550px)에 맞춰 한 화면에 전부 담았다 — `clip`
> 옵션은 뷰포트보다 큰 영역을 못 담아 처음엔 위쪽만 잘려 나왔다). (b)만 사용자에게
> 보내 확인받았다.

> **2026-08-13 여섯 번째 라운드 — 768~1050px 로고·내비 겹침 실측 수정.**
> "ANZINE — Editorial Readability Pass" 작업 중 §3("실제 기능 결함이면 고친다")에
> 해당하는 버그를 실측으로 찾았다: Playwright로 `.wordmark`(로고)와 `.category-nav`
> (내비)의 `getBoundingClientRect()`를 768~1440px 사이에서 훑어보니, 768/900/1000px에서
> 로고 우측 끝이 내비 좌측 시작을 넘어 겹쳤다(스크린샷으로도 로고 그래픽이 "ABOUT"
> 글자 위에 그려져 "BOUT"처럼 보이는 것을 확인). 원인은 위 스물세 번째 라운드가 넣은
> 3칸 그리드(`1fr auto 1fr`) 자체다 — `1fr`은 `minmax(auto, 1fr)`과 같아서 그 칸의
> 콘텐츠(내비 6개 링크)가 트랙보다 넓으면 트랙이 안 줄어든다.
>
> 1차로 `minmax(0, 1fr)` + `.category-nav { min-width: 0 }`만 시도했지만 겹침이
> 그대로였다 — 실측해보니 `justify-self: end`인 그리드 아이템은 트랙 크기와 무관하게
> 자기 콘텐츠의 선호 폭만큼 렌더되고, 트랙보다 넓으면 정렬 기준의 반대 방향(가운데
> 로고 쪽)으로 넘친다(900px에서 트랙은 329px인데 nav 박스는 393px로 그려짐). 여기에
> `.category-nav { width: 100% }`를 더해서야 실제로 트랙 폭에 맞춰졌다 — 이미 있던
> `overflow-x: auto`(§위 카테고리 내비)가 그때부터 진짜로 작동해, 넘치는 콘텐츠(예:
> BOOK 링크)를 삭제하는 게 아니라 가로 스크롤로 흡수한다. 이건 새 패턴이 아니라
> 768px 미만(모바일)에서 이미 쓰던 "내비가 안 들어가면 가로 스크롤한다" 동작을
> 768~1050px 구간까지 그대로 확장한 것이다 — 겹쳐서 읽을 수 없던 상태보다, 스크롤하면
> 전부 보이는 상태가 낫다고 판단했다. `build/verify.mjs`에 768~1200px 스윕 회귀 검사를
> 추가해 같은 버그가 좁은 범위로 숨어들어도 다시 잡히게 했다.

### 통합 인사이트 — 헤드라인 + 부연설명 (2026-08-10 두 번째 라운드 확장, 2026-08-13 재구조화)

```
고정 라벨(.dateline):       h1 아님 · 산세리프 700 · 12px · uppercase · color --secondary · "UNEXPECTED"
테제(issue.insight, h1):    산세리프 700 · clamp(30px, 7vw, 42px) · lh 1.32 · max-width 32ch
부연설명(issue.insightNote): 명조 400 · 18px · lh 1.6 · color --secondary · max-width 44ch · 웹 전용
```

> **2026-08-13 일곱 번째 라운드 — "ANZINE — WEEKLY ANSWER → UNEXPECTED HERO
> RESTRUCTURE".** 이 자리의 역할이 둘로 갈렸다:
>
> 1. **"UNEXPECTED"가 고정 라벨이 됐다.** 예전엔 `.dateline`이 "WEEKLY ANSWER"라는
>    고정 문구였고, 그 아래 `.issue-insight`(h1)가 매주 바뀌는 `issue.insight` 값을
>    보여줬는데 마침 그 값이 2026-w32엔 "UNEXPECTED"였다 — 라벨처럼 보이는 자리에
>    데이터값이 우연히 라벨 같은 단어로 들어간 상태였다. 이번 요청이 "UNEXPECTED는
>    고정 편집 장치, 그 아래 문장은 매주 바뀌는 진짜 테제"라고 역할을 못박아,
>    `.dateline`의 고정 문구를 "WEEKLY ANSWER"→"UNEXPECTED"로 바꿨다
>    (`render-index.mjs`, 리터럴 문자열 — 데이터가 아니다).
> 2. **`issue.insight`는 이제 실제 문장을 담는다.** 2026-w32의 값을 "UNEXPECTED"
>    (단어)에서 "소비를 움직인 건, 우리가 보고 있던 곳에 없었다."(문장, `insightLines`로
>    쉼표 자리에서 줄바꿈)로 바꿨다 — 필드의 용도 자체는 그대로다("그 호 4편을
>    관통하는 한 줄", CLAUDE.md §5.5), 이번 주부터 그 값이 우연히 라벨과 겹치지
>    않을 뿐이다.
> 3. **타이포그래피를 문장에 맞게 다시 쟀다.** 72px/900/lh 1.04는 "UNEXPECTED"
>    한 단어가 화면을 채우도록 잡은 값이었다 — 실제 문장에 그대로 물려주면
>    사용자가 명시적으로 경계한 "오버사이즈 광고 헤드라인"이 된다. 사용자 요청
>    ("가장 무거운 웨이트가 필요한 게 아니다, 크기·줄높이·폭·여백으로 무게를
>    만들라")대로 900→700, 최대 72px→42px, lh 1.04→1.32로 낮추고 `max-width: 32ch`
>    로 폭을 제한했다(§8 "constrained text width").
> 4. **가운데 정렬 → 다시 왼쪽 정렬.** 2026-08-13 여섯 번째 라운드가 이 블록을
>    좌우 2단에서 가운데 정렬 세로 스택으로 바꿨는데, 이번 요청이 "가운데 정렬한
>    거대 마케팅 헤드라인을 피하고 에디토리얼 왼쪽 정렬을 검토하라"고 정반대로
>    지정해 다시 왼쪽 정렬로 되돌렸다 — 같은 자리를 두고 두 세션이 반대 방향을
>    요청한 경우다, 가장 최근·가장 구체적인 지시를 따랐다.
> 5. **여백의 무게중심이 이동했다.** 예전엔 라벨(`WEEKLY ANSWER`)과 헤드라인 사이가
>    가장 큰 정적 간격이었다(둘이 별개 섹션처럼 느껴지게). 이제는 라벨이 테제
>    바로 위에 붙는 eyebrow라 그 간격을 좁히고(`--s4`→데스크톱 `--s5`), 대신
>    테제 블록 전체(라벨+테제+부연)와 카드 그리드 사이 여백(`.insight-lead`
>    margin-bottom, 데스크톱 `clamp(80px, 14vh, 180px)`)을 이 페이지에서 가장 큰
>    정적 간격으로 삼았다 — "테제가 넷의 개념적 입구처럼 느껴지게" 하는 지점이다.
>
> **`insightNote`(부연설명)는 그대로 남겼다.** 요청의 구조도(§1·§3)엔 라벨→테제→
> 여백→카드만 있고 부연 문단은 안 보이지만, 그 문단을 지우라는 명시적 지시는
> 없었고 §9("기존 편집 카피는 안 바꾼다")도 있어 — 신문의 "덱"(헤드라인 밑 조용한
> 요약 문단)과 같은, 이미 흔한 에디토리얼 패턴으로 남겨 뒀다. 다음 라운드에서
> 아예 빼는 쪽을 원하면 별도로 요청한다.
>
> **카드 간 리듬(§4 "네 스토리 사이 페이싱을 다르게")은 손대지 않았다.** 카드
> 그리드는 이미 1px 헤어라인으로 이어붙인 하나의 표면이고(§카테고리 카드),
> 그 자체가 "넷이 하나로 연결돼 보인다"는 요청(§3 "do not turn the page into
> four isolated sections")을 이미 만족한다. 카드 사이에 인위적으로 다른
> 간격을 넣으면 오히려 그 연결감을 깨고, "카드는 다시 디자인하지 않는다"(§9)
> 요청과도 부딪힌다 — 검토 후 그대로 뒀다.

### 페이저 · 전체 아카이브 (2026-08-10 두 번째 라운드 도입)

카테고리 카드 그리드 밑, 홈의 진짜 마지막 요소다.

```
.pager: grid-template-columns: 1fr auto 1fr · 이전(← 이전 주 인사이트) · 가운데(전체 아카이브 보기)
        · 다음(다음 주 인사이트 →, 홈에서는 항상 비활성)
```

"다음 주"는 홈이 정의상 항상 최신 회차를 보여주므로 구조적으로 늘 비활성(`.is-off`)이다 —
더 최신인 회차가 있으면 그게 이미 홈이다. "이전 주 인사이트"는 그 회차의 인쇄 진(`/print/`)
으로 보낸다 — 회차별 개별 페이지가 2026-08-08에 없어졌지만, 인쇄 진에 그 주 헤드라인 +
스토리 전원이 그대로 있어서 유일하게 남은 "그 주로 돌아가는" 라우트다.

가운데 "전체 아카이브 보기"는 `/archive/`로 간다 — 홈이 카드 그리드(도메인당 최신 1편)로
바뀌면서 없어졌던 평면 목록을 별도 라우트로 되살린 것이다(`build/lib/render-archive.mjs`).
홈의 카드 그리드 자체는 안 바뀐다 — `/archive/`는 "더 보고 싶을 때"만 가는 보조 경로다.

### 카테고리 카드 (2026-08-10 도입, 2026-08-11 위계 반전 — 홈 전용)

```
그리드: repeat(2, 1fr) · gap 1px · 배경 --ink(칸 사이 괘선) · 640px 이하 1열 · 뷰포트 가장자리까지
카드:   position:relative·z-index:0(스태킹 컨텍스트) · padding var(--s7) 24px var(--s5) 24px
        · min-height 420px (바닥일 뿐 상한 아님)
        가로 정렬 왼쪽(align-items:flex-start · text-align:left) · 세로 중앙(justify-content:center)
        기본: 배경 cardColor(밝은 파스텔) · 글자 --ink(검정)
        폴백(cardColor 없음): 배경 --dc-<key>(colorPaper, 어두운 톤) · 글자 흰색(.is-dark)
세로 라벨(.category-card-tag): 우측 전체 높이, 데스크톱 폭 140px·900·120px·line-height .78,
        모바일(≤640px) 92px·64px·line-height 1.05(자간 더 느슨하게) · 낱글자 <span> 세로 적재
        (flex column + space-between, 2026-08-12 열한 번째 라운드부터 카드 위아래
        가장자리까지 꽉 채운다) · -webkit-text-stroke 2px · 색 = darken(cardColor)(§아래 참고)
        · 2026-08-12 열 번째 라운드부터 z-index:-1 + opacity:.16 + pointer-events:none —
        헤드라인 뒤 배경 워터마크. 그래서 카드 우측 패딩도 96px→24px로 돌아갔다(§아래 참고).
헤드라인: 카드에서 가장 큰 글자, 80px 900 (2026-08-11 열 번째 라운드 — 40px에서 두 배)
        headlineLines가 있으면 <br>로 여러 줄(§데이터 계약 참고), 없으면 한 줄
티저:   헤드라인 아래 한 줄, 28px (같은 라운드에서 14px→두 배)
```

> **2026-08-12 여덟 번째 라운드 — 우측 태그를 세로 통 라벨로, 좌측 여백을 넓혔다.**
> 사용자가 참고 스크린샷(핑크 배경의 영화 카드, 우측에 "MOVIES"가 한 글자씩 세로로
> 크게 적재돼 있고 좌측 여백이 예전보다 넉넉한 예시)을 보내며 "4가지 모두 이 룰을
> apply해줘"라고 요청.
> - **좌측 여백**을 10px→24px로 넓혔다. 우측은 세로 라벨 밴드(76px + 여유 20px = 96px)
>   만큼 패딩을 비워 헤드라인·티저 텍스트가 라벨과 안 겹치게 한다.
> - **우측 태그**를 "12px 작은 가로 라벨"에서 "카드 전체 높이를 채우는 세로 글자
>   더미"로 바꿨다. `render-index.mjs`의 `verticalLabel()`이 `nameCard`("MOVIES" 등)를
>   한 글자씩 `<span>`으로 쪼개고, CSS가 `flex-direction:column; justify-content:
>   space-between`으로 카드 세로 폭 전체에 고르게 편다 — 글자 수가 다른 도메인
>   (MUSIC 5자·MOVIES 6자·BOOKS 5자·YOUTUBE 7자)도 전부 같은 글자 크기(40px)를
>   쓰면서 카드 높이를 꽉 채운다(간격만 늘고 줄어든다). 시각적으로만 배열한 낱글자
>   나열이라 `aria-hidden="true"`를 걸고, 실제 라벨 텍스트는 신설한 `.sr-only`
>   유틸리티 클래스로 정상적인 한 단어("MOVIES")를 스크린 리더에 낸다.
> - **색** — 참고 스크린샷의 세로 글자가 카드 배경(cardColor)과 같은 색상(hue)
>   계열이지만 훨씬 짙고 채도 높은 톤이었다. `domains/registry.json`에 또 하나의
>   수동 색 필드를 얹는 대신(cardColor·colorPaper·color 세 필드가 이미 있다),
>   신설한 `build/lib/color.mjs`의 `darken(cardColor)`로 **계산**한다 — 정본은
>   여전히 cardColor 하나뿐이다. 색상(hue)은 유지한 채 명도만 20%로 낮추고 채도를
>   최소 55%까지 끌어올린다 — 파스텔 원색은 채도가 낮아 그대로 어둡게만 하면
>   탁한 회갈색이 된다. **예외** — 원색이 채도 10% 미만인 무채색(예: music의
>   `#E9E9E9`, 거의 회색)이면 채도를 억지로 끌어올리지 않는다. 무채색에서 hue는
>   계산상 임의값(0=빨강)이라, 강제로 채도를 높이면 원래 색과 무관한 색(회색
>   카드에 짙은 빨강)이 나온다 — 무채색은 무채색으로만 어둡게 한다.
>   대비는 `test/color.test.mjs`가 4개 활성 도메인 전부 WCAG 큰 텍스트 기준(3:1,
>   본문 기준 4.5:1이 아니다 — 40px 900은 "large text"에 해당한다)으로 검증한다.
> - **빈 카드(스토리 없음)** 는 이 처리에서 뺐다 — 세로 라벨 색 계산이 cardColor
>   (실제 배경색)를 전제하는데, 빈 카드는 근거 없이 색을 안 칠하는 표면(§7.2)이라
>   전제 자체가 성립하지 않는다. 예전의 작은 가로 라벨(`.category-card-tag-empty`로
>   이름을 바꿨다)을 그대로 남긴다.
> - **draft 배지**(작업 중)는 우측 상단이 세로 라벨 자리가 되면서 헤드라인 위
>   흐름으로 옮겼다.

> **2026-08-12 — 좌측 정렬로 되돌림 + 헤드라인 줄바꿈 도입.** 두 가지 사용자 요청:
> 1. **가로 정렬을 다시 왼쪽으로.** 2026-08-11 여섯 번째 라운드에서 세로·가로 모두
>    카드 한가운데로 바꿨던 걸(`align-items:center; text-align:center`), 이번에
>    "전체를 좌측 정렬로, 가운데·양쪽 정렬은 쓰지 말라"는 요청으로 가로만 다시
>    왼쪽으로 뒤집었다(`align-items:flex-start; text-align:left`). 세로 중앙
>    정렬(`justify-content:center`)은 요청 범위 밖이라 그대로 뒀다. 우측 상단 태그
>    (`.category-card-tag`)는 absolute 포지션이라 이 변경과 무관하게 그 자리 그대로다.
> 2. **헤드라인 쉼표 → 줄바꿈.** "쉼표가 자연스럽게 오는 자리에 줄바꿈을 넣어라"는
>    요청 — 예시로 준 두 문장(`정가 2만 원 표가 / 10만 원에 팔렸다`,
>    `덜 팔고도 / 1위를 가져갔다`)이 실제로 지금 카드에 뜨는 영화·음악 편 헤드라인과
>    정확히 일치했다. 어디서 끊을지는 한국어 문법(주어/부사절 경계) 판단이 필요해
>    범용 알고리즘으로는 못 미덥다 — 정본 문자열(`headline`)은 그대로 두고, 홈 카드
>    전용 표시값 `headlineLines`(문자열 배열)를 `issues/*.json`에 새로 추가해 사람이
>    직접 끊었다. `render-index.mjs`가 있으면 `<br>`로 이어 보여주고 없으면 원래
>    한 줄로 돌아간다. `tools/qa.mjs`가 `headlineLines.join(" ") === headline`을
>    강제한다 — 표시용 줄바꿈이 정본과 몰래 갈라지는 사고를 막는다. 홈의 큰
>    헤드라인(`.issue-insight`)에도 같은 원리로 `issue.insightLines`를 추가했다 —
>    지금 문구("숫자는 다 보여줬다, 이유만 빼고")에 이미 쉼표가 있어 그 자리에서
>    끊었다.

사용자가 첨부한 참고 이미지(도메인별 색 타일 그리드)를 따른다 — 폰트·정확한 배치는
그대로 옮기지 않고 이 사이트의 헬베티카 톤 위에서 재해석했다. **도메인 색(또는
전용 카드색)을 배경으로 풀블리드하는 이 사이트 유일의 자리다**(§2 불변식 3번 다섯
번째 예외).

> **2026-08-11 개편.** 두 가지가 바뀌었다:
> 1. **위계 반전** — 예전엔 도메인명이 카드에서 가장 큰 글자였다. 이제 **스토리
>    헤드라인이 메인**이고, 도메인명은 우측 상단의 작은 라벨이다(사용자 요청).
> 2. **전용 카드색 도입(`cardColor`)** — `colorPaper`는 흰 배경 위 텍스트/보더용으로
>    어둡게 골랐다(흰 글자를 못 올린다). 참고 이미지의 카드는 밝은 파스텔이라 검은
>    글자가 필요했다 — `domains/registry.json`에 도메인별 `cardColor`(밝은 톤)를
>    새로 추가하고, 카드 배경은 `cardColor` + 검은 글자(`--ink`)를 기본으로 쓴다.
>    `cardColor`가 없는 도메인은 예전처럼 `colorPaper` + 흰 글자(`.is-dark`)로
>    되돌아간다. 두 조합 다 `test/theme.test.mjs`가 AA(4.5:1)를 검증한다 — 밝은
>    카드는 흰 글자가, 어두운 카드는 검은 글자가 각각 AA 미달인 것도 함께 검증한다
>    (잘못된 조합을 실수로 못 쓰게).

카드 하나 = 도메인 하나. 그 도메인의 **가장 최신 스토리**로 바로 이어진다(편집 순서가
아니라 `range` 기준 실제 최신 — `build/lib/data.mjs`의 `latestByDomain()`). 아직 통과분이
없는 도메인은 색을 채우지 않는다 — 근거 없이 색만 칠하면 "발행됐다"는 인상을 준다.

> **2026-08-11 열 번째 라운드 — 태그 영문화 + 글자 두 배.** 우측 상단 태그가 그때까지
> 한글 도메인명(`d.name`)을 냈는데, 사용자 요청으로 영문(Movies·Music·Books·YouTube)으로
> 바꿨다. 마스트헤드 내비가 쓰는 `nameEn`(단수형: Movie·Music·YouTube·Book)과는 다른
> 값이라 `registry.json`에 카드 전용 `nameCard`(복수형) 필드를 새로 추가했다 — 하나를
> 억지로 겸용하면 어느 한쪽 표기가 틀어진다. 헤드라인·티저 글자 크기도 각각 정확히
> 두 배로 키웠다(40→80px, 14→28px) — `min-height`는 바닥값이라 카드가 그만큼 더
> 커지는 것으로 받아내고 레이아웃은 안 깨진다(`build/verify.mjs`의 가로 넘침 검사로
> 375~1920px 실측 확인).

두 장마다 한 번, 참고 이미지의 가로 구분 밴드를 낸다(`.category-divider`). 2026-08-11부터
검정 바탕·흰 글자 "THE ANSWER ZINE" 무한 마퀴이자 DIY 진 오버레이를 여는 버튼이다 — §9
"모션은 반응에만 쓴다" 원칙의 명시적 예외. 자세한 내용은 §9 참고.

> **2026-08-12 아홉 번째 라운드 — 세로 라벨을 더 두껍고 촘촘하게.** 사용자 요청: "장르
> 폰트 더 THICK 하게 만들어줘 약간 글씨가 초과되는 느낌으로. 글자 간격 줄여주고."
> `font-weight: 900`은 이미 상한값이라(`var(--sans)` 시스템 헬베티카 스택엔 900 초과
> 웨이트가 없다) 더 굵어 보이게 하는 실제 레버는 굵기가 아니라 둘이다:
> - **font-size를 40px→74px로 키우고 `-webkit-text-stroke: 1.5px currentColor`를
>   더했다.** 낱글자가 76px 폭 라벨 박스에 거의 맞닿거나 살짝 넘치게 커졌다 —
>   `.category-card`에 `overflow` 제한이 없어서(§2 불변식과 무관하게 이 카드는 원래
>   풀블리드 예외다) 실제로 카드 우측 경계를 살짝 넘어가는 게 "약간 초과되는
>   느낌"을 만든다.
> - **"글자 간격"은 가로 `letter-spacing`이 아니라 세로로 쌓인 글자 사이 간격이다.**
>   여덟 번째 라운드까지는 `justify-content: space-between`으로 카드 높이 전체에
>   낱글자를 고르게 펼쳤다(단어 길이가 달라도 항상 카드를 꽉 채우려고) — 그러면
>   글자 사이가 넓게 벌어져 보인다. `justify-content: center` + `line-height: .74`로
>   바꿔 글자를 가운데로 촘촘히 모으고 줄 높이 자체를 줄였다. 그 결과 단어 길이가
>   다르면(짧은 BOOKS·MUSIC과 긴 YOUTUBE) 뭉치는 세로 폭도 달라지지만, 그게 곧
>   "긴 단어일수록 위아래로도 더 넘칠 수 있다"는 같은 방향의 효과라 의도와 맞다.
>
> 대비(`test/color.test.mjs`)는 굵기·크기와 무관하게 색 계산(§darken)만 검사하므로
> 영향 없이 통과한다. `build/verify.mjs`의 375~1920px 가로 넘침 검사도 그대로 통과 —
> 다만 420px 부근에서 카드 자체가 약 12px 이미 넘치는 기존 버그가 있다(이번 변경
> 전부터 있었다, 이번 라운드 범위 밖이라 손대지 않았다). 홈페이지(`/`)는 이 검사가
> 아직 커버하지 않는다 — 검사는 스토리 페이지(`/2026-w31/book/`) 한 장만 돌린다.

> **2026-08-12 열 번째 라운드 — 라벨을 헤드라인 뒤 배경 워터마크로.** 사용자 요청
> 두 가지: "모바일은 자간 넓혀도 될듯" / "제목은 안가리게 백그라운드 느낌으로
> 넣어줘."
> - **배경 워터마크 전환.** 아홉 번째 라운드까지는 라벨이 헤드라인과 같은 앞면
>   레이어에 있었다 — 그래서 겹치지 않으려면 카드 우측에 96px를 늘 비워둬야 했다
>   (공간을 나눠서 안 가리는 방식). 이번엔 레이어 자체를 분리했다: 라벨에
>   `z-index: -1` + `opacity: .16` + `pointer-events: none`을 줘서 헤드라인 뒤로
>   보내고 배경에 녹아들게 했다. `.category-card`는 flex 컨테이너라 헤드라인·티저
>   같은 일반 자식은 CSS Flexbox 명세(Appendix E 기준 "z-index:0 포지션 요소"와
>   같은 페인트 층)상 이미 음수 z-index보다 위 레이어에서 그려진다 — 그래서
>   헤드라인에 별도 z-index를 안 줘도 항상 라벨 위에 그려진다. `.category-card`
>   자체에 `z-index: 0`을 명시해 스태킹 컨텍스트를 그 카드 안에 가뒀다(안 그러면
>   음수 z-index 라벨이 카드 밖 — 옆 카드나 그리드 배경 — 으로 새어나갈 수 있다).
>   레이어가 분리되니 공간을 비워둘 이유가 없어져 카드 우측 패딩을 96px→24px
>   (좌측과 대칭)로 되돌렸다 — 헤드라인이 이제 카드 폭 전체를 쓴다.
> - **크기.** 배경 요소는 전면 요소보다 커도 괜찮다(오히려 더 자연스럽다) —
>   `font-size`를 74px→120px, 라벨 폭을 76px→140px로 키웠다. 불투명도 16%라 아무리
>   커도 헤드라인 가독성을 해치지 않는다(실측 스크린샷으로 확인, 아래).
> - **모바일 자간.** "자간 넓혀도 될듯"은 가로 `letter-spacing`이 아니라 아홉 번째
>   라운드에서 좁혀둔 세로 낱글자 간격(line-height) 얘기다 — 좁은 카드 폭에서
>   촘촘한 간격이 더 빽빽해 보인다는 지적. `@media (max-width: 640px)`에서
>   `line-height`를 .78→1.05로 풀고, 커진 라벨이 좁은 카드에서 과하지 않도록
>   `font-size`(120→64px)·폭(140→92px)도 함께 낮췄다.
> - Playwright로 데스크톱·모바일 카드를 스크롤-유발 후 실측 스크린샷 — 라벨이
>   카드 배경에 자연스럽게 녹아들고 헤드라인이 어떤 텍스트 위를 지나가도 완전히
>   읽힌다. `.category-card-tag`는 이제 장식이 명확해져 `pointer-events: none`도
>   같이 추가했다(원래도 aria-hidden 처리라 클릭 대상이 아니었지만, 이제 시각적
>   레이어링과도 일치한다).

> **2026-08-12 열한 번째 라운드 — 라벨이 카드 위아래를 꽉 채우게, 스트로크는 더
> 굵게.** 사용자 요청: "문구가 공백없이 꽉차도록 수정 좀 더 볼드하게. 자간을
> 좁히라는 얘기는 아니고." 열 번째 라운드에서 `justify-content: center`로 글자를
> 카드 한가운데 뭉쳤더니, 짧은 단어(BOOKS·MUSIC)는 카드 위아래로 빈 공백이 크게
> 남았다 — "공백없이 꽉차게"는 그 여백을 없애 달라는 뜻이었다. 사용자가 명시적으로
> "자간(글자 사이 간격)을 좁히라는 게 아니다"라고 짚어서, 글자 사이를 더 좁히는
> 방향이 아니라 `justify-content: space-between`으로 되돌려 **카드 세로 폭
> 전체에 걸쳐 첫 글자~마지막 글자가 위아래 가장자리에 닿도록** 폈다(아홉 번째
> 라운드 이전과 같은 배치 방식이지만, 이번엔 배경 워터마크 상태 그대로다 — 열
> 번째 라운드의 z-index/opacity 레이어링은 그대로 유지). "더 볼드하게"는
> `font-weight: 900`이 이미 상한이라 `-webkit-text-stroke`를 1px→2px로 굵혀
> 받았다 — 불투명도(.16)는 그대로 둬서 헤드라인을 덮지 않는다는 열 번째 라운드
> 전제가 안 깨지게 했다. Playwright 스크린샷(데스크톱·모바일)으로 4개 카드 모두
> 라벨이 카드 위아래 가장자리에 닿는 것과 헤드라인 가독성을 확인했다.

> **2026-08-12 열두 번째 라운드 — 모바일 낱글자 간격을 좁힘.** 사용자 요청:
> "모바일 뷰에서는 자간 좀더 좁혀줘 글자를 키우던지 늘리던지 해서." 열 번째
> 라운드("모바일은 자간 넓혀도 될듯")와 방향이 정반대다 — 그때는
> `justify-content: center`로 촘촘히 뭉쳐 있던 게 문제였는데, 열한 번째
> 라운드에서 `space-between`으로 바뀌며 이번엔 반대로 짧은 단어(BOOKS·MUSIC)의
> 낱글자 사이가 넓어 보이게 됐다. 사용자가 스스로 제시한 해법(자간을 직접
> 좁히지 말고 "글자를 키우거나 늘려서" 흡수)대로, 모바일 `font-size`를
> 64px→92px, `line-height`를 1.05→1.4로 키웠다 — `space-between`은 글자가
> 차지하는 공간이 커질수록 사이 간격이 자동으로 좁아지는 구조라 이 레버 하나로
> 해결된다. 라벨 폭도 92px→116px로 맞춰 넓혔다. 실측 결과 4장 중 3장
> (MOVIES·MUSIC·YOUTUBE)은 간격이 0으로 붙었고, 헤드라인이 가장 길어 카드
> 자체가 큰 BOOKS만 약 20px의 간격이 남는다 — 카드 높이가 헤드라인 줄 수를
> 따라가는 구조상(§카테고리 카드 상단 스펙) 완전한 균일함은 애초에 안 된다.

> **2026-08-12 열일곱 번째 라운드 — 카드 밖으로 새는 라벨을 잘라냄.** 사용자
> 신고("youtube 도 4개 박스에서 여전히 잘려서 보임", 스크린샷 첨부 — YOUTUBE
> 라벨의 아래쪽 글자가 카드를 벗어나 홈 하단 페이저 영역까지 삐져나온 게
> 보였다). 실측(Playwright `getBoundingClientRect()`)으로 원인을 확인했다 —
> `.category-card-tag`의 `justify-content: space-between`(열한 번째 라운드)은
> 낱글자들을 카드 높이에 맞춰 "고르게 펼치기"만 할 뿐 압축은 못한다. 글자 수가
> 많은 단어(YOUTUBE 7자·MOVIES 6자)는 큰 글자 크기에서 낱글자 전체 높이가 카드
> 자기 높이보다 커져, 그 차이만큼 카드 경계 밖(대개 바로 아래 카드나, 그 아래
> 카드도 없으면 그리드 밖 페이저까지)으로 삐져나온다 — 실측으로 YOUTUBE 약
> 102px, MOVIES 약 50px 초과를 확인했다. `.category-card`에 `overflow: hidden`을
> 걸어 넘치는 부분을 카드 경계에서 정확히 잘라낸다 — 헤드라인·티저는 카드
> 높이 자체가 그 내용에 맞춰 늘어나므로 잘릴 내용이 없고, 라벨만 카드 안에
> 갇힌다. 아홉 번째 라운드부터 의도해 온 "라벨이 카드 자기 폭 안에서 살짝
> 넘치는 느낌"은 그대로 유지된다 — 이번 수정은 카드 **밖으로** 새는 것만 막는다.

### 카드 호버 — 흑백 사진 리빌 (2026-08-13 도입)

> 사용자가 실제 콘텐츠(영화 스틸·앨범 재킷·유튜브 영상 캡처·책 표지) 4장을
> 첨부하며 "호버하면 커버가 우상단→좌하단으로 벗겨지며 뒤의 사진이 드러나게,
> 사진은 전부 흑백으로"라고 요청. 메커니즘은 구현했다 — **다만 이 세션은 채팅에
> 첨부된 이미지를 파일로 저장·접근할 방법이 없고(네트워크도 전면 차단돼 대체
> 사진을 받아올 수도 없다), 그래서 현재 발행된 스토리 중 어느 것도 아직
> `story.image`가 없다.** 실제 사진 4장이 리포에 들어오기 전까지는 카드가
> 예전과 똑같이 보인다(아래 폴백 참고) — CLAUDE.md 상단의 "출처를 열지 않은
> 것" 교훈과 같은 이유로, 없는 파일을 있는 것처럼 지어내지 않는다.
>
> **같은 날 후속 — 사용자가 사진을 받을 자리를 직접 만들었다.** GitHub 웹에서
> `assets/img/covers`라는 이름으로 파일을 하나 만들었는데(커밋 "Create covers"),
> 내용이 1바이트(개행 하나)뿐인 빈 파일이었다 — GitHub 웹 UI로 새 폴더를 만들
> 때 흔히 나는 실수다(git은 빈 폴더를 못 담아서, 폴더 하나만 지정하면 그 이름의
> **파일**이 생긴다). 그 상태로 두면 나중에 같은 경로에 진짜 사진들을 담은
> **폴더**를 못 만든다(파일과 폴더가 같은 이름을 못 쓴다) — 그 빈 파일을 지우고
> `assets/img/covers/README.md`(한국어로 "Add file → Upload files" 사용법 안내)로
> 바꿔 폴더를 제대로 만들었다. 이 과정에서 `build/lib/assets.mjs`의
> `copyAssets()`가 `assets/img/` 하위를 평면(한 겹) `readdirSync`로만 복사하던
> 걸 발견했다 — 하위 폴더(`covers/`)가 생기자 `copyFileSync`가 폴더를 파일처럼
> 취급하려다 `EISDIR`로 빌드 전체가 죽었다. 재귀 복사로 고쳤다(`test/assets.test.mjs`
> 신규 — 하위 폴더가 있어도 안 죽는지 + 실제로 복사되는지 검증). `assets/img/covers/`
> 폴더 자체는 이제 존재하고 리포에 커밋돼 있다 — 사용자가 그 폴더 안에서
> "Add file → Upload files"로 사진 4장을 올리면(README.md가 그 방법을 안내한다)
> 그다음은 이 문서 위 문단이 설명하는 절차(`story.image` 필드 연결→확인→발행)를
> 그대로 밟는다.

```
데이터:  story.image(선택, 문자열 경로/URL) — 있으면 카드가 .has-photo가 된다
레이어:  .category-card(포지셔닝 셸, 사진+커버를 겹쳐 쌓는다)
         └ .category-card-photo(<img>, position:absolute inset:0 z-index:-1,
             object-fit:cover, filter:grayscale(1) — 흑백은 CSS가 강제해서
             데이터에 흑백 사진을 요구하지 않는다, 컬러 원본을 그대로 써도 된다)
         └ .category-card-cover(예전에 .category-card 자신이 하던 배경색·패딩·
             flex 배치를 전부 물려받았다 — 헤드라인·티저·세로 라벨·날짜 전부
             이 안에 있다)
호버(PC 전용):
         @media (hover: hover) and (pointer: fine) {
           .category-card.has-photo:hover .category-card-cover {
             clip-path: inset(0% 100% 0% 0%);  /* rest: inset(0 0 0 0) */
           }
         }
```

> ⚠️ **2026-08-13 네 번째 후속 — 방향 재지정 + 모바일 제외.** 위 대각선 버전
> (우상단→좌하단, 아래 문단이 그 옛 방향 검증 기록)이 실제 사진으로는 잘
> 작동했지만, 사용자가 다시 피드백: "PC는 좋긴 한데, 오른쪽부터 일자로
> 마치 요거트 뚜껑 따듯한 이펙트로 보였으면 좋겠다"고, 또 "흑백 사진 드러나는
> 거는 모바일 버전은 작동하면 안 될 듯 — 모바일에서는 제외"라고 요청했다.
> 두 가지를 반영했다:
>
> 1. **대각선 → 일자(오른쪽에서 왼쪽).** `inset(top right bottom left)`에서
>    top·bottom을 항상 `0%`로 고정하고 **`right` 인셋 하나만** `0%→100%`로
>    움직인다. top·right를 함께 움직이던 이전 버전과 달리 변화하는 축이
>    하나뿐이라, 안 잘리는(커버가 남는) 영역의 오른쪽 경계가 카드 세로변과
>    평행한 수직 직선 그대로 왼쪽으로 미끄러진다 — 대각선 없이 순수 좌우
>    이동. 45% 지점에서 clip-path를 강제 고정해 실측(실제 오디세이 포스터
>    사진)한 스크린샷으로 확인 — 분홍 커버(왼쪽)와 흑백 사진(오른쪽)의
>    경계가 정확히 수직 일직선이다.
> 2. **모바일 제외.** 호버 규칙 전체를 `@media (hover: hover) and (pointer:
>    fine)`로 감쌌다 — 실제 마우스가 있는 기기에서만 걸린다. 터치 전용
>    기기(`hover: none`)에서는 이 미디어 쿼리 자체가 안 걸려 커버가 항상
>    기본 상태(`inset(0% 0% 0% 0%)`, 사진 안 드러남)로 남는다 — 터치에서
>    호버가 아예 없거나 탭 후 눌린 채로 남는(sticky hover) 신뢰할 수 없는
>    동작을 원천 차단한다. Playwright로 데스크톱 컨텍스트(`hover:hover,
>    pointer:fine`)와 모바일 터치 컨텍스트(`hasTouch:true, isMobile:true`)
>    양쪽에서 `matchMedia()` 결과와 실제 `clip-path` computed style을 재고
>    확인했다 — PC는 `true`/호버 시 벗겨짐, 모바일은 `false`/항상
>    `inset(0%)`.
>
> 아래 "왜 대각선처럼 보이는가" 문단과 그다음 실측 문단은 **이전 버전의
> 기록**으로 남긴다 — 같은 실수(대각선 방향)를 다시 시도하지 않기 위해서다.

**(이전 버전 기록) 왜 `inset(top right bottom left)`로 대각선처럼 보였는가.**
top과 right 두 인셋 값을 함께 0→100%로 키우면, 안 잘리는(커버가 남는) 영역은
항상 "좌하단 모서리에 붙은 직사각형"이다 — 이 직사각형은 진행 초반엔 상단·우측
띠가 먼저 깎여 나가 우상단에서부터 시작하는 것처럼 보이고, 좌하단 모서리 한
점에서 마지막으로 사라진다. 대각선 삼각함수·회전 없이 인셋 두 값만으로
만들었다 — 합성 사진(색 그라디언트 SVG)으로 45%/45% 중간값을 실측해 방향을
확인했다.

**커버를 in-flow로 남겨둔 이유.** 커버를 `position:absolute`로 바꾸면 카드
높이가 `min-height`(420px)에 고정돼 버려 긴 헤드라인(2줄 이상)이 카드 밖으로
잘리는 회귀가 생긴다 — 실측으로 확인하고 피했다. 커버는 여전히 일반 흐름에
있어 카드의 실제 높이를 그대로 정하고, 사진 레이어만 `position:absolute;
inset:0`로 그 결과 높이를 뒤에서 따라간다.

**폴백.** `story.image`가 없는 카드(2026-08-13 현재 전부)는 사진 레이어 자체가
안 나오고, 호버 시 예전 그대로 헤드라인 밑줄만 뜬다(`.category-card:hover
.category-card-headline { text-decoration: underline; }`, 안 건드렸다) — 카드
마크업이 새로 `.category-card-cover`로 한 단계 더 감싸졌을 뿐 화면상 아무것도
안 달라진다(Playwright로 전/후 스크린샷 대조 확인).

**빈 카드(`.is-empty`, 통과분 없는 도메인)** 는 이 레이어 구조를 안 쓴다 —
벗겨낼 사진 자체가 없다. `emptyCard()`가 여전히 옛 평면 구조(`<div
class="category-card is-empty">` 직계 자식)를 그대로 쓰므로, 그 구조가 기대하던
flex 배치·패딩·최소 높이를 `.category-card.is-empty` 규칙에 다시 채워 넣었다.

> **같은 날 세 번째 후속 — 실제 사진 4장이 도착했고, 두 가지 버그를 실측으로
> 잡았다.** 채팅 인라인 첨부는 파일로 안 잡히던 문제가(위 두 번째 후속 참고)
> 이번엔 사용자가 다시 보낸 첨부에서 실제로 파일이 잡혔다 — 영화(〈오디세이〉
> 포스터)·음악(〈YOUNGEST〉 앨범 재킷)·유튜브(리센느 "거제 야호" 영상 캡처)·
> 도서(〈수족관〉 표지) 4장을 `assets/img/covers/{movie,music,youtube,book}.jpg`로
> 저장하고, 각 도메인의 최신 스토리(`2026-w32-movie`·`2026-w31-music`·
> `2026-w31-youtube`·`2026-w32-book`)에 `story.image`를 연결했다. 처음 이 기능을
> 설계할 때는 `story.image`가 있는 카드가 하나도 없어서 실제로 렌더된 적이 없었고
> (design.md 위 첫 번째 문단), 그래서 아래 두 버그가 테스트·빌드를 안 거치고
> 넘어갔다 — 실제 사진을 연결하고서야 드러났다.
>
> 1. **`u()` 누락.** `<img src="${s.image}">`를 사이트의 base-path 헬퍼(`u()`,
>    `site.mjs`)에 안 거치고 그대로 썼다 — 이 사이트의 모든 내부 링크가 `u()`를
>    거쳐야 한다는 규칙(`test/site.test.mjs`가 GitHub Pages 프로젝트 사이트 하위
>    경로 배포를 실제로 빌드해 강제한다)을 놓친 것이다. `src="${u(s.image)}"`로
>    고쳤다.
> 2. **짧은 카드에서 사진이 샘.** `.category-grid`는 `align-items` 기본값
>    (stretch)이라 같은 행의 짧은 카드도 옆 긴 카드만큼 키가 늘어난다(사진을
>    넣기 전부터 있던 의도된 동작 — 배경색이 옆 카드와 나란히 꽉 차 보이는
>    이유다). 커버(`.category-card-cover`)는 자기 "내용"만큼만 커서, 카드가
>    늘어난 만큼 커버 아래 빈 틈이 생기고, 그 틈에서 사진 레이어(부모
>    `.category-card` 전체 — 늘어난 키까지 — 를 채운다)가 커버 없이 그대로
>    드러났다. 사진이 없을 땐 커버 자체가 배경색이라 그 틈도 같은 배경색으로
>    보여서 회귀가 안 보였다 — Playwright로 실제 사진을 렌더해 스크린샷을 찍고서야
>    발견했다. `.category-card-cover { height: 100%; }`로 커버가 부모의 늘어난
>    키까지 정확히 채우게 해서 고쳤다. `test/css.test.mjs`에 회귀 테스트를
>    추가했다.
>
> 대각선 리빌 방향(우상단 먼저, 좌하단 마지막)도 실제 사진으로 재확인했다 — 위
> 문단은 합성 그라디언트로 검증한 결과였는데, 실제 사진(영화 포스터)의 45%/45%
> 중간 상태를 스크린샷으로 보면 인물의 머리 쪽(우상단)이 먼저 드러나고 좌하단
> 모서리에 분홍 커버가 마지막까지 남는다 — 예상과 일치했다.

### 스탯 카드

```
bg --surface · border 1px --divider · border-radius 16px · padding 24px
max-width var(--measure) — 2026-08-13부터 레일이 아니라 본문과 같은 세로 흐름 안에 있다
```

**출처 링크는 항상 보인다.** 프로토타입은 `opacity: 0`에 hover로만 노출해 터치 기기에서 접근이 불가능했다.
근거는 숨기는 게 아니라 눈에 보이는 자리에 두는 것이다. `sticky`·"근거 레일"이라는 표현은
2026-08-13 "ANZINE — FINAL STORY PAGE CLEANUP"으로 없어졌다 — §5.3 참고.

### 스파크라인 (2026-08-12 스물여섯 번째 라운드 — 라인 차트 되살림)

2026-08-08엔 사용자 요청으로 스탯 카드에서 차트를 뺐다(라벨 + 수치 + 출처 링크만).
2026-08-12 스물다섯 번째 라운드에서 "ANZINE — Official Data Sources & Comparative Chart
Integration" 문서가 §4에서 "콘텐츠마다 차트가 유용한지 자동 판단해 렌더하라"고 다시
요청해, 그 라운드엔 범위만 정리하고 구현은 미뤘다. **이번 라운드에서 실제로 되살렸다** —
아래가 그 결과다.

> ⚠️ **"자동 판단" 로직을 새로 만들지 않았다 — 이미 있는 게이트를 재사용했다.**
> §3.4(`stat.trend`는 series가 `archived`거나 `trendSnapshots`가 갖춰진 경우에만 채울 수
> 있다, `tools/qa.mjs`가 강제)가 이미 "이 숫자로 재현 가능한 선을 그릴 수 있는가"를
> 판정하고 있었다. `stat.trend`가 있다 = 차트가 유용하다(그릴 수 있다)로 그대로 등치시켰다
> — 별도의 "유용성 판단" 규칙을 새로 짓지 않았다. `trend`가 없는 회차(2026-08-12 현재
> 발행된 4개 회차 전부 — book/music은 snapshot 출처라 애초에 trend를 못 채운다)는
> 이전과 똑같이 라벨+수치만 보인다. **화면상 변화가 즉시 눈에 띄지는 않는다** — 다음에
> `archived` 시리즈(영화·음악)에서 4주 이상 데이터가 쌓이거나 스냅숏이 4주 누적되면
> 그 회차부터 실제로 선이 보인다.
>
> **bar/scatter/pie는 넣지 않았다 — 여전히 범위 밖이다.** 이유는 스물다섯 번째 라운드와
> 같다: 어느 도메인 데이터에도 "경쟁작·전작 대비" 같은 비교 수치 배열이 없다. 지어내지
> 않고서는(§5 "Do not invent missing values") 그릴 데이터 자체가 없다 — 도메인 문서가
> 그런 지표를 데이터 출처로 등록하는 게 먼저다(`domains/README.md` 절차).
>
> **`stat.basis`도 이번에 처음 화면에 낸다.** CLAUDE.md §3.4가 "화면에는 `비교 기준`으로
> 렌더된다"고 이미 적어 두고 있었는데(2026-08-08 이전 어느 라운드의 의도였던 것으로
> 보인다) 실제 `render-story.mjs`는 그 문장을 낸 적이 없었다 — 문서와 코드가 어긋나
> 있었다. 이번 라운드에서 그 문서 기술을 실제로 구현해 문서-코드 불일치를 없앴다.
> `trend`가 있으면 차트+`axisCaption`을, 없고 `basis`만 있으면 "비교 기준 · {basis}"
> 한 줄을 낸다(§3 "Comparative Context Is Important"). 아래 렌더 명세 참고.

`sparklineSVG()`(`build/lib/sparkline.mjs`)를 그대로 쓴다 — 새로 만들지 않았다.
`build/lib/render-story.mjs`의 `statPanel()`이 호출한다. 렌더 조건:

```
stat.trend가 배열이고 길이 ≥ 2  → 스파크라인 SVG + axisCaption 문단
stat.trend 없음 + stat.basis 있음 → "비교 기준 · {basis}" 문단
둘 다 없음                       → 이전과 동일, 라벨+수치만
```

색은 `var(--dc)`(도메인색 CSS 변수)를 SVG `stroke`/`fill`에 그대로 쓴다 — 인라인 SVG는
조상 요소의 CSS 커스텀 프로퍼티를 상속받아 `var()`가 정상 해석된다. `.stat-chart`는
`flex-basis: 100%`로 `.stat-card`(flex-wrap 행)에 강제 줄바꿈을 만들어 라벨+수치 아래
전체 폭 새 줄에 앉는다.

```
크기:   160 × 56
정규화: 첫 값을 100으로 두는 기준선 정규화. 기준선이 범위 끝이면 반대쪽에 15% 여백
기준선: 1px 점선(2 3), opacity .25 — "전주 대비"라는 축 의미를 형태로 보여준다
선:     stroke-width 2.5 · round cap/join
끝점:   circle r 3.5, x = w
대체:   <title>에 추세 문장. role="img"
```

min–max 정규화가 아니다. 프로토타입은 `[98,101,100,426]`에서 앞 세 점이 바닥에 뭉개져 형태 정보가 0이었다.
기준선 정규화 + 여백으로 y가 44.26 / 43.91 / 44.03이 되어 바닥(50)에서 떨어진다.

### 스탯 패널

```
2026-08-10 개편: 헤드라인 → byline 바로 다음, flex row · 라벨(왼쪽, ≤60%) + stat.value(오른쪽, 크게)
bg --surface · border 1px --divider · border-radius 16px · padding 24px
```

참고 목업(movie.html)의 `.stat-panel` 구조를 따른다. 출처 링크는 여기 없다 — 아래
`source-box`로 분리했다.

> **2026-08-13 여섯 번째 라운드 — 주 숫자와 괄호 부연설명을 분리.** "ANZINE — Editorial
> Readability Pass" §7("주 숫자가 시각적으로 지배해야 하고, 부연 맥락은 보조 텍스트로
> 물러나야 한다")에 따라 `stat.value`("213만 (6일 연속 박스오피스 1위)" 형태 — 현재
> 발행된 4편 stat.value 전부가 이 패턴이다) 끝의 괄호 문구를 렌더 시점에만 갈라
> `.stat-value-context`(15px/700/`--secondary`, 주 숫자 아래 새 줄)로 낮춘다. 데이터
> (`issues/*.json`의 `stat.value` 원문 문자열)는 그대로 두고 렌더러(`render-story.mjs`의
> `splitStatValue()`)만 문자열 끝의 `(...)`를 정규식으로 잡아 나눈다 — 괄호가 없거나
> (`+9.9%`) 중간에 있거나 여러 개면 매치되지 않아 원래 문자열 그대로 한 덩어리로
> 나온다. 괄호 기호 자체는 화면에서 안 보인다(내용은 그대로, 감싸는 기호만 뗀다) —
> 이미 이 코드베이스가 〈〉를 렌더 시점에만 벗기는 것과 같은 종류의 처리다(CLAUDE.md
> §6 참고). `test/stat.test.mjs`가 분리·미분리·모호한 경우 셋을 검사한다.
>
> **스탯 카드의 라벨+값 배치(2단 나란히 vs 위아래 스택)는 이번에 손대지 않았다.**
> 값 문자열 길이에 따라 flex-wrap이 자동으로 갈린다(짧은 값 "1위"는 라벨과 한 줄에
> 나란히, 긴 값은 자기 줄로 내려간다) — 실측으로 두 경우 다 확인했지만 어느 쪽도
> 읽기를 방해하지 않아 "고쳐야 할 결함"으로 보지 않았다(§3 "안 익숙해 보인다고 고치지
> 않는다").

### 인사이트 콜아웃

```
bg --surface · border 1px --divider · border-left 1px 도메인 컬러 · padding 16px 24px
라벨: 산세리프 700 11px · 고정 문구 "인사이트" (quote.insight.concept은 안 쓴다)
본문: quote.insight.note · 14px · lh 1.75
```

> **2026-08-10 되돌림.** 2026-08-08엔 `insight`(개념·설명)를 화면에서 뺐다(사용자 요청).
> 참고 목업의 `.insight`/`.note2` 콜아웃이 다시 요구해 되돌렸다 — `note`만 보여주고
> `concept`은 여전히 라벨로 쓰지 않는다. `tools/qa.mjs`의 데이터 검증(§2 불변식 #2)은
> 그대로다.

### 풀쿼트 & 출처 박스

```
라벨: "THE ANSWER" · 산세리프 700 · 11px · letter-spacing .1em · uppercase · 도메인 컬러
     (.answer-label — 2026-08-11 도입)
쿼트: 명조 700 · 20px · lh 1.55 · border-left 1px 도메인 컬러 · padding-left 16px
     (본문 세 문단 뒤로 이동 — 2026-08-10)
출처: border-top/bottom 1px --divider · padding 16px 0 · flex space-between
     라벨 "원문 출처" + stat.sourceUrl 링크
```

> **2026-08-11 라벨 추가.** 사용자가 준 브랜드 방향 문서가 "기사 하나에서 한 부분만
> 스크린샷된다면 그게 답(the Answer)이어야 한다"고 요구했다. `quote.text`는 이미 그
> 역할("A가 아니라 B다" 대구조, §6)을 하는 문장이었지만 화면에서 다른 본문과 시각적으로
> 구분되지 않았다 — `insight-note`가 이미 쓰는 라벨 패턴(`.insight-label`)과 같은 스펙을
> 재사용해 `.answer-label`("THE ANSWER")을 풀쿼트 위에 붙였다. 별도 클래스로 둔 이유는
> 풀쿼트와 인사이트 콜아웃이 다른 컴포넌트라 나중에 독립적으로 조정할 여지를 남기기
> 위해서다. 인쇄 진(A4)에는 적용하지 않는다 — 인쇄 진은 애초에 `quote.text` 자체를 안
> 낸다(teaser+insight만, 여백 예산이 이미 타이트하다). §2 불변식 3번의 "도메인 색이
> 쓰이는 자리 5곳"은 그대로다 — 이 라벨은 기존 풀쿼트 좌측 보더 자리 안에 얹힌 것이지
> 새 자리를 추가한 게 아니다.

> **2026-08-13 여섯 번째 라운드 — 위아래 여백을 비대칭으로.** "ANZINE — Editorial
> Readability Pass" §6("답의 순간의 제시 방식을 강화하되, 결론 자체나 글자 크기를
> 과장하지 않는다")에 따라 `margin`을 `var(--s6) 0`(위아래 32px 대칭)에서
> `var(--s7) 0 var(--s6)`(위 48px·아래 32px)로 바꿨다 — 본문 세 문단을 읽고 이
> 문장에 도달했을 때 숨을 한 번 고르는 간격을 준다. 글자 크기(22px)·굵기(700)·색·
> 라벨·결론 문구는 전부 그대로다 — "더 크게·화려하게"가 아니라 여백만 늘렸다.

### 브라우저 표면

`::selection`, `:focus-visible`, `caret-color`, 스크롤바를 전부 팔레트에서 지정한다.
그리지 않은 부분도 디자인을 짊어진다.

### About — WHY ANZINE (2026-08-13 도입)

```
eyebrow(.why-eyebrow):  산세리프 700 · 12px · letter-spacing .14em · uppercase · --secondary
리드(.why-lead):        명조 400 · 18px · lh 1.7 · --secondary · max-width 46ch
중심 질문(.why-question, h1): 산세리프 900 · clamp(30px, 5.4vw, 52px) · lh 1.3 · max-width 15ch
설명(.why-body p):      명조 400 · 17px · lh 1.75 · --ink · max-width 58ch
닫는 문장(.why-closing): 산세리프 700 · clamp(21px, 2.6vw, 28px) · lh 1.4
```

"ANZINE — ABOUT / WHY ANZINE Refinement" 요청 — About을 "사이트에서 가장 강한 편집
모먼트 중 하나"로 만들어 달라는 요청에, 홈의 통합 인사이트(`.issue-insight`)·스토리의
답의 순간(`.pullquote`+`.answer-label`)이 이미 쓰는 "질문/답을 시각적으로 지배하게
한다" 문법을 이 페이지에도 가져왔다. 별도 클래스로 둔 이유는 이 코드베이스의 기존
관행과 같다(`.answer-label`이 `.insight-label`과 스펙은 같지만 별도 클래스인 것과
동일 — "다른 컴포넌트라 나중에 독립적으로 조정할 여지를 남긴다").

**대체한 것.** 이전 개정(2026-08-11 두 번째)의 `<h1>${SITE.tagline}</h1>` + 옅은
티저 한 줄 오프닝을 걷어내고 그 자리에 이 블록을 앉혔다 — 새 섹션을 얹은 게 아니라
같은 이야기("결과보다 이유")의 약한 버전을 강한 버전으로 바꾼 것이다. `SITE.tagline`
자체는 `site.mjs`에 그대로 있고 홈 메타 설명·OG 이미지 alt에 계속 쓰인다 — 이
페이지의 화면 표시 자리에서만 뺐다.

**중심 질문이 이 페이지의 진짜 `<h1>`이다** — 시맨틱하게도 가장 강한 헤딩을 실제로
가장 강한 시각 요소에 준다. `clamp()` 하한을 30px로 높게 잡은 이유는 모바일에서도
"축소된 본문"이 아니라 "이 페이지의 무게중심"으로 계속 읽히게 하기 위해서다(요청
§8 "데스크톱 타이포를 비례로 줄이지 말 것"). 두 문장 사이는 `<br>` 하드 브레이크를
쓴다 — 뷰포트마다 자동으로 다르게 접히면 문장의 리듬이 흔들린다. 각 문장 자체는
여전히 뷰포트 폭에 따라 자연히 줄바꿈된다(하드 브레이크는 두 문장 "사이"에만
걸려 있다).

**닫는 문장("결과보다 이유를, 순위보다 왜 팔렸는지를.")은 중심 질문보다 작게 눌러
뒀다** — 스포트라이트는 하나만 준다는 원칙(요청 §3 "절제된 볼드")을 지킨다. 이
문장은 `SITE.tagline`("순위 말고, 팔린 이유.")의 확장판이자, `quote.text`가 쓰는
"A가 아니라 B다" 대구조와 같은 자리(§6 문장 규칙)에서 이 페이지의 "답의 순간"
역할을 한다.

**기존 사실 관계 문단(선정 기준·데이터 출처·발행 주기) — 2026-08-13 네 번째 개정에서
삭제.** 사용자가 그 세 문단("순위는 결과다...", "도메인마다 실 구매...", "한 호가
나오면...")을 직접 지목해 삭제를 요청했다. WHY ANZINE 블록과 그 문단들을 나누던
얇은 괘선(`.about-mechanics`)도 감쌀 내용이 없어져 함께 뺐다 — 지금 About은
WHY ANZINE 섹션 하나 + "전체 아카이브 보기" 링크로만 끝난다.

**About 페이지의 UNEXPECTED — 검토했지만 애초에 없었다.** 2026-08-13 Editorial
Readability Pass에서 텍스트 검색 + 스크린샷으로 이미 확인한 사실이다(§위 마스트헤드
겹침 수정 라운드와 같은 날) — `/about/`에는 "UNEXPECTED" 문구가 렌더된 적이 없다.
그 표현은 홈(`.issue-insight`, 매주 바뀌는 `issue.insight` 값)과 DIY 진 7쪽에만
있고, 둘 다 이번 요청이 명시적으로 보호한 자리다 — 그래서 이번 라운드에서
UNEXPECTED 관련해 뺀 것도 바꾼 것도 없다. `test/about.test.mjs`가 이 사실을
회귀 검사로 고정한다("About 페이지에는 UNEXPECTED가 없다").

---

## 8. 진(zine) 전용 장치 — "한 장 요약"(`/YYYY-wNN/print/`)

> **이건 §11의 "DIY 진"과 다른 산출물이다.** 여기는 A4 세로 1장짜리 요약지고, §11은
> A4 가로 2장을 접어 만드는 8쪽 미니 소책자다. 둘은 서로 다른 라우트·다른 CSS
> 네임스페이스(`.zine-*` vs `.zb-*`)를 쓰고, 한쪽을 고쳐도 다른 쪽은 영향받지 않는다.

> **2026-08-10 전면 개편.** 사용자가 첨부한 참고 목업(print.html)의 포맷을 따라, 아래
> 있던 사진 콜라주·테이프·스티커·QR·3단 세로 괘선 장치를 전부 없앴다(옛 §2 불변식
> 5·6번이 그 대상이라 함께 삭제됐다). "손으로 만든 인쇄물" 인상 대신, **읽는 순서가
> 명확한 표 형태의 요약지**로 바뀌었다 — 스토리 한 편 = 지면 한 행이다.

### 스토리 행 (`.zine-story`)

```
grid-template-columns: 1fr 32mm · gap 6mm · border-bottom 1px --ink
왼쪽: 킥커(도메인·kicker) · h3 헤드라인 · teaser 한 문단 ·
      인사이트 콜아웃(quote.insight.note, 도메인색 좌측 보더) · 출처 링크(stat.sourceUrl)
오른쪽(statcol): stat.value(크게, 900) · stat.label
```

스토리 수만큼 행이 늘어난다 — 리드/미니 구분이 없다(§2 불변식 4번, §6.1). `stat.value`는
숫자+단위로 쪼개지 않는다 — 스토리마다 형태가 다른 자유 문자열이라 정규식 분리가 깨지기
쉽다. 전체를 한 덩어리로 크게 낸다.

### 푸터

```
border-top 1px --ink · padding-top 3mm · text-align center · 8.5px
```
`SITE.name · 회차 · 전체 아카이브 <도메인> · 각 기사의 인사이트는 편집 의견입니다` 한 줄이다.
2026-08-08까지는 QR(`assets/img/qr-<회차>.svg`)이 이 자리에 있었다 — 참고 목업처럼 사람이
읽는 URL 텍스트로 바꿨다. 가리키는 곳은 그대로 홈 아카이브(`answerzine.kr`)다.

---

## 9. 모션

모든 애니메이션은 `@media (prefers-reduced-motion: reduce)`에서 정지하되 콘텐츠는 그대로 보인다.

예외 하나 — 홈의 인트로(§8이 아니라 `build/lib/layout.mjs`의 `intro()`)는 스크롤에 반응하는
발산 트랜스폼이다. 2026-08-10부터 "이거 왜 잘나가?" 스테이트먼트 단계가 없어지고, 인트로
섹션 하나만 남았다 — "THE" / "ANSWER" / "ZINE" 세 줄이 중앙에 겹쳐 있다가 스크롤에 맞춰
THE·ZINE은 오른쪽으로, ANSWER는 왼쪽으로 갈라지며 그 뒤의 아카이브를 드러낸다. `.intro`는
뷰포트보다 60vh 큰 상자이고 안쪽 콘텐츠가 `position: sticky`로 그 여유분만큼 고정된 채
갈라진다 — 그래야 스크롤이 그냥 지나가 버리지 않고 눈에 보이는 전환이 된다
(`assets/app.js`의 `bootIntroMotion()`).
`prefers-reduced-motion: reduce`에서는 이 계산 자체가 꺼지고 세 줄이 중앙에 겹친 채 그냥
스크롤되어 지나간다 — 콘텐츠(텍스트)는 어느 쪽이든 그대로 읽힌다.

> **2026-08-11 여덟 번째 라운드.** 원래 "THE ANSWER"가 한 덩어리(intro-word-1)였는데, 넓은
> 화면에서 그 안에서 줄이 갈라져(THE / ANSWER) 어색하게 읽힌다는 지적으로 THE·ANSWER·ZINE
> 세 단어를 처음부터 각자 한 줄(각자 `white-space: nowrap`)로 뗐다 — 의도치 않은 중간
> 줄바꿈이 구조적으로 안 생긴다. 글자 크기도 키워(`clamp(72px, 21vw, 380px)`) 가장 긴 단어
> (ANSWER)가 어느 뷰포트에서든 폭의 약 85~90%를 채운다 — 화면 가장자리에 살짝 닿는 인상을
> 의도적으로 노린다. `.intro`에 `overflow: hidden`을 붙여 자간 반올림으로 생기는 1px 안팎도
> 가로 스크롤바를 만들지 않게 막는다.

> **2026-08-12 열네 번째 라운드 — "ANZINE" 글자 강조.** 사용자가 처음엔 사이트 전체를
> "ANZINE"으로 리브랜딩해 달라고 요청했다 — 확인 질문(`AskUserQuestion`)으로 범위를
> 좁혔다: **인트로 애니메이션만** 바꾸고 `SITE.name`("ANSWER ZINE")·인쇄 진 표지 등
> 사이트 정체성 자체는 그대로 둔다(이전 라운드에 사용자가 이미 "홈 화면은 그대로
> 둔다"고 확정한 결정을 뒤집지 않는다). 셋째 줄 문구를 "ZINE"에서 "MAGAZINE"으로
> 바꾸고, 스크롤을 시작하면 그 안에 숨은 "ANZINE"(ANSWER의 A·N + MAGAZINE의
> Z·I·N·E)만 검정으로 남고 나머지 글자는 회색(`--tertiary`)으로 옅어지는 효과를
> 얹었다. 처음 요청은 "나머지 글자가 사라진다"(opacity)였는데, 후속 확인에서
> "A,N,Z,I,N,E는 검정, 나머지는 회색"으로 구체화됐다 — 그래서 완전히 숨기지 않고
> 색만 옅어진다. 기존의 좌우로 갈라지는 스크롤 트랜스폼(위 문단)은 그대로 유지된다 —
> 색 전환은 그 위에 얹힌 별도 레이어다. `ANSWER`·`MAGAZINE`은 낱글자 `<span>`으로
> 쪼개(`build/lib/layout.mjs`의 `letterSpans()`) `ANZINE`을 이루는 글자에만
> `intro-letter--keep`을 건다. 색 전환 자체는 매 프레임 낱글자 색을 다시 계산하지
> 않는다 — `app.js`가 스크롤 진행률이 0을 살짝 넘는 순간 `.intro-wordmark`에
> `is-revealing` 클래스 하나만 토글하고, CSS `transition: color`가 나머지를
> 처리한다(`color`만 바뀌므로 레이아웃이 전혀 안 흔들려 CLS가 없다). 스크립트가
> 없거나 `prefers-reduced-motion: reduce`면 `is-revealing`이 아예 안 붙어 "THE
> ANSWER MAGAZINE"이 처음부터 끝까지 검정 그대로 읽힌다 — 콘텐츠 손실이 없다.

> **2026-08-12 열다섯 번째 라운드 — 안무를 다시 짜고, 오래된 sticky 버그를 잡았다.**
> 사용자 요청 3가지: (1) "PC 기준 100% VIEW에 맞춰서" THE ANSWER MAGAZINE을 배치할
> 것(화면 밖으로 안 넘치게), (2) 색 옅어짐 효과는 유지, (3) "3번째 줄의 ZINE이
> 2번째 줄의 AN과 만나서 정가운데 배치되는 구도"로 수정.
> - **글자 크기.** 위 열네 번째 라운드까지 `font-size: clamp(72px, 21vw, 380px)`는
>   가장 긴 줄이 ANSWER(6자)이던 시절 값이다 — MAGAZINE(8자)이 그 자리를 차지하면서
>   같은 배율로는 화면 밖으로 심하게 넘쳤다. `21vw`→`15.5vw`(그 외 clamp 하한·상한도
>   함께 축소)로 실측(Playwright, 375~2560px)해 MAGAZINE 한 줄이 항상 뷰포트 안에
>   들어오게 다시 잡았다 — 대략 뷰포트 폭의 79%를 채운다.
> - **안무 교체.** 위 열네 번째 라운드까지의 "세 줄이 좌우로 갈라지며 사라진다"는
>   좌우 분리 트랜스폼은 이번에 **없앴다** — 요청 (3)이 그 자리를 대체하는
>   새 안무다. `ANSWER`→"AN"(`intro-keep`)+"SWER"(`intro-fade`),
>   `MAGAZINE`→"MAGA"(`intro-fade`)+"ZINE"(`intro-keep`)로 다시 쪼갰다
>   (`build/lib/layout.mjs`의 `splitWord()`가 `letterSpans()`를 대체했다 — 이제
>   낱글자가 아니라 "그대로 남는 덩어리"·"사라지는 덩어리" 둘로만 쪼갠다). 스크롤
>   후반부(진행률 20%~75% 구간)에 THE 전체·intro-fade 조각은 투명해지고, "ZINE"
>   조각만 물리적으로 이동해 "AN" 조각의 바로 오른쪽 끝에 붙는다 — "AN"은 전혀
>   움직이지 않는다(이미 가운데 줄에 있어 그 자체가 합쳐질 기준점이다). 이동 거리는
>   `app.js`가 `getBoundingClientRect()`로 두 조각의 실제 위치를 재서(`measure()`)
>   계산한다 — 폰트가 반응형(`vw` 기반)이라 뷰포트가 바뀔 때마다(`resize`) 다시 잰다.
>   진행률 75%~100% 구간은 완성된 "ANZINE"이 그대로 붙박여 있는 홀드 구간이다 —
>   처음엔 수렴이 100%까지 이어지도록 짰다가, 완성되자마자 곧바로 고정이 풀려
>   화면 밖으로 밀려나 버리는(실측 스크린샷으로 발견) 문제가 있어 75%에서 미리
>   끝내고 나머지 25%를 홀드 구간으로 남겼다.
> - **오래된 sticky 버그.** 이 라운드에서 실측하다가, `.intro-inner`(`position:
>   sticky`)가 스크롤 중에도 전혀 고정되지 않고 그냥 페이지와 함께 흘러가 버리는
>   걸 발견했다 — 여덟 번째 라운드부터 있었던 버그다(`.intro`에 걸린
>   `overflow: hidden`이 sticky 자식의 "고정 축" 계산을 가로챈다, 꽤 알려진 CSS
>   함정). 이전 안무(좌우로 갈라지며 사라짐)는 고정이 안 걸려도 "스크롤하며
>   사라진다"는 결과가 크게 다르지 않아 티가 안 났을 뿐이다 — 이번 안무는 "한
>   지점에 붙박인 채 합쳐진다"가 핵심이라 고정이 실제로 걸려야만 성립해서 드러났다.
>   `overflow: hidden`을 `.intro`에서 `.intro-inner`(sticky 요소 자기 자신 — 자기
>   자신에게 건 overflow는 자기 고정 축 계산에 영향이 없다)로 옮겨 고쳤다.
> - CLS는 여전히 없다 — `transform`·`opacity`만 바뀌고 어떤 요소도 문서 흐름상의
>   자기 자리(레이아웃 크기)를 벗어나지 않는다(사라지는 조각도 `opacity: 0`일 뿐
>   `display: none`이 아니라 자리를 계속 차지한다 — 그래야 AN의 위치가 스크롤
>   내내 고정된 기준점으로 쓰인다).

> **2026-08-12 열여섯 번째 라운드 — AN도 움직인다.** 사용자 요청: "AN도 움직여서
> 정중앙에서 만나도록 수정." 바로 위 열다섯 번째 라운드는 AN을 고정점으로 두고
> ZINE만 그 오른쪽으로 옮겼다 — 그런데 AN의 원래 자리는 "ANSWER"라는 단어의
> 왼쪽 절반이라, 그 단어 자체가 화면 중앙에 있었어도 AN만 놓고 보면 화면 중앙보다
> 왼쪽이다. 그 결과 합쳐진 "ANZINE"도 화면 정중앙이 아니라 살짝 왼쪽에 자리
> 잡았다(위 문단이 설명한 "AN은 전혀 움직이지 않는다"는 전제가 이번에 바뀌었다).
> 이제 둘 다 움직인다 — `app.js`의 `measure()`가 AN·ZINE 각각의 실측 너비를 더한
> 폭이 `.intro-inner`(뷰포트를 꽉 채우는 sticky 상자) 정중앙에 오도록 목표 지점을
> 계산하고, AN은 그 목표의 왼쪽 절반으로(오른쪽으로 이동), ZINE은 오른쪽
> 절반으로(왼쪽으로 이동) 서로를 향해 움직인다. 세로도 둘 다 `.intro-inner`의
> 세로 중심으로 모인다. 실측(Playwright)으로 합쳐진 "ANZINE"의 가로·세로 중심이
> 뷰포트 중심과 정확히 일치하는 것을 확인했다.

> **2026-08-12 열여덟 번째 라운드 — 인트로를 검정 배경·흰 글자로.** 사용자 요청:
> "배경 검정에 텍스트 흰색 한번 해보려고." 2026-08-10 리디자인 이후 `.intro`는
> 사이트 기본 흰 테마를 그대로 썼는데(그 전엔 `theme.mjs`의 별도 다크 팔레트
> `INTRO_THEME`를 썼다가 없앴다), 이번엔 그 다크 팔레트를 되살리는 대신 `.intro`
> 안에서만 `background:#000; color:#fff;`로 직접 뒤집었다 — 워드마크 글자색이
> 대부분 `currentColor`로 상속되게 짜여 있어(`.intro-word`의
> `-webkit-text-stroke`도 포함) 이 두 줄만 바꾸면 나머지가 자동으로 따라온다.
> 손으로 더 챙긴 곳이 둘 있다 — `.intro-fade`/`.intro-word-the`가 `is-revealing` 상태에서 옅어지는
> 색(`var(--tertiary)`, 흰 배경용 웜그레이)과 `.intro-scroll` 화살표 색은 검정
> 위에서 대비가 너무 낮아 거의 안 보였다 — 흰 글자 기준 반투명값
> (`rgba(255,255,255,.4)`·`.55`)으로 바꿔 검정 배경에서도 또렷하게 보이게 했다.
> Playwright로 정지 상태("THE ANSWER MAGAZINE" 흰 글자)·전환 중(회색 옅어짐)·
> 완성 상태("ANZINE" 흰 글자, 정중앙)를 모두 스크린샷으로 확인했다.

> **2026-08-12 열아홉 번째 라운드 — 완성 후 홀드를 늘리고, 홈 전체를 다크모드로.**
> 사용자 요청 세 가지를 한 라운드에 반영했다.
> 1. **"ANZINE 문구가 완성되면 바로 메인 콘텐츠로 떨어지지 말고 스크롤 한두번
>    하면 떨어지게."** `.intro`의 높이를 160svh→240svh로 늘리고, `app.js`의
>    `CONVERGE_END`를 .75→.45로 낮췄다 — 합쳐지는 애니메이션 자체가 차지하는
>    절대 스크롤 거리는 이전과 비슷하게 유지하면서, 완성 이후 홀드 구간(여유분의
>    55%)이 뷰포트 높이의 약 77vh로 늘어났다. 마우스 휠 한 칸의 델타는 브라우저·
>    기기마다 달라 "정확히 1~2회"를 픽셀로 못 박을 수는 없다 — 그만큼의 스크롤을
>    확실히 요구하는 물리적 거리로 근사했다.
> 2. **"서울, 대한민국 — 2026년 8월 1주차" 데이트라인을 "WEEKLY ANSWER"로.**
>    `render-index.mjs`의 홈 전용 라벨이라 `dateline()`(layout.mjs) 자체는 안
>    건드렸다 — 인쇄 진(`render-zine.mjs`)은 여전히 그 회차의 실제 날짜를 쓴다.
> 3. **"인트로~스크롤 끝까지 도달하는 전체 페이지는 다크모드로. 로고~Book까지
>    카테고리도 Intro와 같은 배경/폰트 색상으로."** 홈 문서 하나(`body.is-home`,
>    `showIntro`가 true인 페이지에만 붙는다)에서만 전역 테마 토큰(`--bg`/`--ink`/
>    `--secondary`/`--tertiary`/`--divider`/`--rule`/`--surface`)을 검정·흰색
>    계열로 재정의했다 — 이미 그 토큰을 참조하는 컴포넌트(데이트라인·통합
>    인사이트·페이저)는 자동으로 뒤집힌다. 마스트헤드(`.site-header`)는 원래도
>    전역에서 항상 어두운 바(#16150F)였는데, 홈에서만 인트로와 똑같은 #000으로
>    맞췄다. **예외 하나** — 카테고리 카드(`.category-card`)는 밝은 파스텔
>    (`cardColor`) 배경이 그대로고 그 배경에 맞춰 이미 AA 대비를 맞춘 어두운
>    글자색이라(`domains/registry.json` `contrastPass`), `--ink`를 흰색으로
>    물려받지 않도록 원래 색(#17150F)을 리터럴로 고정했다 — 안 그러면 밝은 배경
>    위에 흰 글자가 얹혀 대비가 무너진다. 다른 페이지(스토리·about·아카이브·
>    인쇄 진)는 이 클래스가 안 붙어 기존 흰 테마 그대로다.

예외 둘 — 홈 카드 그리드의 가로 구분 밴드(`.category-divider`)는 2026-08-11부터 검정
바탕에 흰 마퀴다. 원래는 정적 텍스트("전체 아카이브 · 카테고리별 최신 스토리")였는데,
사용자가 순수 장식용 애니메이션을 명시적으로 요청해 "모션은 반응에만 쓴다" 원칙의
예외로 추가했다. 처음엔 "THE ANSWER ZINE"(사이트 이름 반복)이었다가, 2026-08-12 열아홉
번째 라운드에서 "DESIGN YOUR OWN ZINE"으로 바뀌었다 — 이 밴드가 순수 장식이 아니라
DIY 진 오버레이를 여는 버튼(`data-zb-open`)이라는 걸 문구가 직접 말하게 했다. 콘텐츠를
두 번 이어 붙여 `translateX(-50%)`를 18초 주기로 반복하는 순수 CSS 기법이다(외부
라이브러리 없음, §11 DIY 진의 3D 플립과 같은 방식). 하단에 숨겨둔 `.zb-cta`와 같은
오버레이를 연다. `prefers-reduced-motion: reduce`에서는 다른 모든 애니메이션처럼 전역
`MOTION` 규칙(`*` 선택자, css.mjs)이 지속시간을 .001ms로 눌러 사실상 정지시킨다 —
텍스트 자체는 반복 문구뿐이라 콘텐츠 손실도 없다.

그 외 시스템에는 연출용 애니메이션이 없다. 상태 전환(hover, 필터, `<details>`)만 있고
전부 브라우저 기본 동작이거나 0.15s 이내다. **모션은 반응에만 쓴다.**

---

## 10. 매주 확인할 시각 QA

대부분 `build/verify.mjs`가 자동으로 검사한다. `[자동]` 표시가 그것이다.

1. 진 페이지가 **A4 정확히 1페이지**인가. `[자동]`
2. 콘솔 에러 0건인가. `[자동]`
3. 375 / 768 / 1280 / 1920px에서 가로 넘침이 없는가. `[자동]`
4. 진 헤드라인이 웹 정본과 문자열이 같은가. `[자동]` (같은 값을 참조하므로 구조적으로 보장)
5. ~~서브셋 폰트가 본문의 모든 글자를 담는가~~ — 2026-08-10 자체 폰트 제거로 해당 없음.
   시스템 헬베티카 스택은 서브셋되지 않는다.
6. 내부 링크가 전부 실제 파일로 해석되는가. `[자동]`
7. 새 도메인 색이 §6.2 네 조건을 전부 통과하는가. `[자동]` (`test/theme.test.mjs`)
8. `@font-face`를 쓰지 않는가(자체 폰트 재도입 방지). `[자동]` (`test/css.test.mjs`)
9. 스파크라인이 뭉개지지 않았는가 — 눈으로. `trend` 4~5점 권장.
10. 도메인 색이 실제 흰 배경에서 읽히는가 — 눈으로. (Night/Paper 토글은 없다, §1)

---

## 11. DIY 진 — 마퀴 밴드/CTA · 8쪽 전체 그리드 미리보기 · 인쇄 소책자

2026-08-11 아홉 번째 라운드 신규, 같은 날 늦게 "Print Zine Refinement" 요청으로 전면
개편(열두 번째 라운드 안팎). `build/lib/render-zinebook.mjs`(콘텐츠)·`build/lib/css.mjs`의
`ZINEBOOK` 블록(스타일)·`assets/zinebook.js`(동작) 세 파일이 이 기능 전부다. §8의 "한 장
요약"과는 완전히 다른 산출물이다(§8 상단 안내 참고) — 흑백만 쓴다(도메인 컬러를 참조하지
않는다), showChrome 페이지(홈·스토리·about·아카이브)에만 붙는다.

### 11.1 콘텐츠 — 8개 논리 페이지

```
앞표지 → About → 기사1 → 기사2 → 기사3 → 기사4 → 통합 인사이트 → 뒤표지
```

> 이 다이어그램은 2026-08-12 스무 번째 라운드에서 갱신했다 — "About+QR"과 "Notes"는
> 둘 다 몇 라운드 전부터 이미 낡은 이름이었다(QR은 여섯 번째 라운드에 About에서
> Notes로 옮겨갔고, 그 Notes/스캔 페이지 자체가 이번 라운드에서 통합 인사이트로
> 바뀌었다). 논리 키(`FLIP_ORDER`/`SHEETS`의 `"notes"`)는 여전히 그대로다 — §11.6 참고.

**기사 1~4는 "지금 홈 카드 그리드에 뜨는 도메인별 최신 스토리"다** — 예전엔 "최신 회차
파일 안의 스토리"만 썼는데, 활성 도메인이 4개뿐이고 결번이 흔해진 지금은 최신 회차 파일
하나에 1~2편만 있는 주가 많아 진의 절반이 빈 채로 나가는 모순이 있었다(build.mjs의
`visibleDomains`+`latestByDomain` 계산, categoryNav와 동일). 결번 도메인도 지난 회차
스토리가 있으면 그걸 쓴다(홈 카드와 동일) — 한 번도 발행된 적 없는 도메인만 진짜 결번
플레이스홀더로 남는다: "이 자리는 통과분이 4편에 못 미쳐 비웠다"(§7.3과 같은 원칙, 확인 안
된 헤드라인을 채우지 않는다). 8개 페이지는 각각 **한 번만** 문자열로 만들어지고, 그리드
미리보기와 인쇄 시트 양쪽에 그대로 재사용된다 — 정본이 둘로 갈라지지 않는다(§9.3과 같은
원칙).

**표지는 2026-08-12 세 번째 라운드에서 "랩어라운드"로 다시 짰다.** 2026-08-11엔 흰
배경·검정 글자 + 뒤표지는 별도 어두운 톤(`#16150F`)이었다가, 같은 날 두 번째 라운드에서
사용자가 "뒤표지(8쪽) 콘텐츠를 지워라, 접었을 때를 생각하라"고 요청해 뒤표지를 색만 남긴
빈 판으로 바꿨다. **그런데 세 번째 라운드에서 사용자가 실제로 접어 만든 실물 사진을
보내며 "이 사진과 똑같이, 폰트 두께와 비율 전부"라고 요청** — 사진은 "THE ANSWER ZINE"
타이포가 접힌 선(스파인)을 가로질러 뒤표지·앞표지 두 면에 걸쳐 하나로 이어져 있었다. 즉
"뒤표지는 비운다"가 아니라 "표지 타이포 자체가 뒤표지·앞표지 경계를 무시하고 시트 전체를
캔버스로 쓴다"가 최종 방향이다.

구현(`render-zinebook.mjs`의 `wraparoundCanvas()`): 폭 1122px(561×2 — 시트 전체 폭)짜리
캔버스 하나에 "THE / ANSWER / ZINE"을 한 번만 그리고, 뒤·앞 두 패널이 각자 561px짜리
창(`overflow:hidden`)으로 그 캔버스의 왼쪽/오른쪽 절반만 잘라 보여준다
(`.zb-wrap-canvas--back`은 오프셋 0, `--front`는 -561px). 두 패널이 그리드 미리보기에서는
서로 안 붙어 있어(§11.3, 8개 타일이 나란히 안 놓인다) 각자 "반쪽 글자"로 보이지만, 인쇄
시트에서는 같은 폭·같은 폰트 크기·같은 세로 중앙 정렬을 공유하므로 실제로 나란히 인쇄될 때
이음매 없이 맞아떨어진다 — 실측(PDF 래스터화)으로 확인했다. 폰트는 여전히 시스템 헬베티카
스택뿐이다(§3 불변식) — 사진의 정확한 서체 자체는 커스텀 웹폰트 없이 재현 불가능하므로,
`font-weight:900`(최대) + `font-stretch:condensed`(맥OS/iOS의 Helvetica Neue Condensed가
있으면 그걸 쓰고, 없으면 조용히 무시된다) + `line-height:.76`(3줄이 거의 맞닿을 만큼) +
실측으로 잡은 `font-size:262px`("ANSWER"가 1122px 캔버스의 99.6%를 채운다)로 두께·밀도를
최대한 근접시켰다. 뒤표지는 이제 자체 마크·사이트명·태그라인이 없다 — 그런 요소가 있으면
접었을 때 캔버스가 잘려 보인다. 속지(About·기사)는 원래도 흰 배경·검정 글자였다.

> **2026-08-12 여섯 번째 라운드 — eyebrow·caption 텍스트도 뺐다.** 앞표지에 남아 있던
> "ANSWER ZINE · 8P MINI ISSUE"(위)와 "직접 접어 만드는 8쪽 진 · 이번 호 큐레이션"(아래)
> 두 줄을 사용자가 지워 달라고 요청 — 랩어라운드 캔버스만 남는다. `.zb-wrap-viewport`가
> 이미 `position:absolute; inset:0`로 패널 전체를 차지하고 있어 텍스트를 빼도 레이아웃
> 변화는 없다.

> **2026-08-12 열아홉 번째 라운드 — "THE ANSWER ZINE"을 "THE ANSWER MAGAZINE"으로,
> 그 안의 "ANZINE"만 진하게.** 사용자 요청 그대로다. 홈 인트로가 2026-08-12 열네~
> 열다섯 번째 라운드에서 이미 같은 전환(문구 자체·스크롤로 드러나는 "ANZINE" 강조)을
> 마쳤으므로, 표지도 같은 브랜드 마크를 따라간다 — 다만 표지는 스크롤이 없는 정적
> 인쇄물이라 애니메이션 없이 두 색을 처음부터 고정해서 낸다. `wraparoundCanvas()`가
> `splitCoverWord()`(layout.mjs `splitWord()`와 같은 기법)로 "ANSWER"의 A·N과
> "MAGAZINE"의 Z·I·N·E을 `.zb-wrap-keep`(#111, 원래 표지 색 그대로)으로, 나머지를
> `.zb-wrap-fade`(#b3b0a6, 중간 회색)로 감싼다. 표지는 흰 배경(`.zb-panel--cover`)이라
> 인트로(검정 배경, `rgba(255,255,255,.4)`)와는 반대 방향 회색을 쓴다.

> **2026-08-13 일곱 번째 라운드 — MAGAZINE이 잘리는 회귀를 잡았다.** 사용자 신고:
> "zine 표지의 magazine이 잘리는데." 원인은 바로 위 열아홉 번째 라운드 자체에 있었다
> — 문구를 "ZINE"(4자)에서 "MAGAZINE"(8자)으로 바꿨을 때, `font-size:262px`(세
> 번째 라운드에 "ANSWER"가 캔버스의 99.6%를 채우도록 잡은 값)를 다시 검산하지
> 않았다. 실측(Playwright, `display:inline-block`으로 바꿔 block 스트레치를
> 없앤 뒤 고유 폭을 잰다 — `scrollWidth`는 이미 박스 폭에 클램프돼 있어 못 쓴다)
> 결과 MAGAZINE은 1122px 캔버스에서 1334px, 212px 초과였다. `text-align:center`라
> 양쪽 끝에서 106px씩 균등하게 넘쳤고, 두 패널(각 561px, `overflow:hidden`)의
> 바깥 경계 밖으로 넘어간 부분은 어느 패널에도 그려지지 않아 — 이음매에서
> 나뉘어 보이는 게 아니라 글자 일부가 완전히 유실됐다("M"의 왼쪽 일부, 끝
> "E").
>
> **고침** — 같은 계산 원칙(가장 긴 줄이 캔버스의 ~99.6%를 채운다)을 새 최장
> 단어 MAGAZINE 기준으로 재적용했다: `font-size` 262px → 219px, MAGAZINE 고유
> 폭 1115px(캔버스의 99.4%). THE·ANSWER는 전용 스타일이 없어(공유 클래스) 함께
> 작아졌다 — 세 줄이 항상 같은 크기를 유지해야 한다는 기존 설계 원칙과 일치한다.
> 사용자가 "적용전에 캡쳐좀"이라고 명시해 커밋 전에 로컬 빌드+스크린샷 3장
> (기존/앞표지/뒤표지)으로 확인받았고, "오케이 적용해봐"로 승인받은 뒤 커밋했다.
>
> **회귀 방지 검사 추가.** `build/verify.mjs`에 랩어라운드 캔버스 폭 검사를
> 새로 넣었다 — THE·ANSWER·MAGAZINE 각 단어를 `display:inline-block`으로 재서
> 고유 폭이 1122px을 넘으면 발행을 막는다. 다음에 셋째 줄 문구가 더 긴 단어로
> 또 바뀌어도, 이번처럼 조용히 잘려서 발행되는 사고 없이 CI가 먼저 잡는다.

**About 페이지는 표지 바로 뒷장, 페이지 전체를 쓴다**(사용자 요청 — "not a small section of
an A4 page").

> **본문 — 2026-08-12 다섯 번째 라운드, 사용자가 통째로 교체.** 처음엔
> `render-about.mjs`(웹의 실제 About 문구)를 A5 지면에 맞게 줄여 재사용했다 — "두 곳이
> 서로 다른 말을 하면 안 된다"는 원칙이었다. 이번 라운드에서 사용자가 이 페이지 전용
> 카피를 직접 지정해 교체를 요청했다 — 웹 `/about/`과는 다른 문구다(웹 쪽은 안
> 건드렸다). 제목은 "About" 하나, 36px(`.zb-about-title`). 웹 About과 내용이 갈리는 걸
> 명시적으로 받아들인 예외이므로, 나중에 웹 `/about/` 문구를 바꿀 일이 있어도 이 페이지를
> 자동으로 맞출 필요는 없다.

> **2026-08-12 여섯 번째 라운드 — 문장 하나와 QR 띠를 뺐다.** "Answer Zine은 여기서
> 멈추지 않습니다." 한 문장과, 하단에 있던 QR+"전체 아카이브"+URL 띠(`.zb-about-scan`)
> 전체를 사용자 요청으로 없앴다. QR은 장식이 아니라 **7쪽(구 Notes) 전용 콘텐츠로
> 옮겼다** — 아래 참고. About은 이제 순수 텍스트 페이지다(`justify-content:flex-start`로
> 위에서부터 자연스럽게 흐른다 — QR을 바닥에 고정하던 `margin-top:auto` 대상이 없어졌다).
> 본문 크기(사용자 지정 범위 18~24px)는 18px(하한)를 유지한다 — 문단이 줄었어도 여전히
> 시(詩)처럼 짧은 줄이 이어지는 구조라 그대로 뒀다.

**7쪽(구 Notes)은 QR 전용 페이지다.** 2026-08-12 여섯 번째 라운드에서 전면 교체(사용자
요청 — "Remove all text from the previous design... Leave only the QR code perfectly
centered... add 'Scan for Mobile'"). 줄 쳐진 노트 배경·"NOTES" 라벨·메모 안내문을 전부
빼고, About에서 옮겨온 QR(`qrcode` npm 패키지, 빌드 시점에 SVG로 굽는다 — §2.2
devDependency 참고)을 페이지 정중앙에 크게(92px→160px, `.zb-qr--large`) 놓고 그 밑에
"Scan for Mobile" 캡션만 남긴다. URL 텍스트는 안 낸다 — 요청이 "QR + 캡션, 그 외 없음"으로
명확했다. `render-zinebook.mjs`에서 이 페이지를 만드는 함수 이름은 `scanPanel()`로
바꿨지만, `FLIP_ORDER`/`SHEETS`의 논리 키는 여전히 `"notes"`다 — 그건 물리 배치 계약
(§11.2)이라 이름을 안 건드린다.

**기사 페이지는 웹과 같은 5블록 전체를 낸다**(사용자 요청 — "same editorial quality and
substance as the website... not a promotional summary"). `blocksOf(story)`의 texts[0]
(현상)·stat(숫자)·texts[1](맥락)·quote+insight.note(THE ANSWER + 인사이트)·texts[2]
(마무리)를 전부 쓴다 — `teaser`는 뺐다(texts[0]와 중복). 어느 한 편도 다른 편보다 크거나
강조되지 않는다(§2 불변식 4번 "리드/미니 위계 없음"과 같은 원칙, 사용자가 명시적으로
"Do NOT create a hero story" 요청).

> **헤더 — 2026-08-12 여섯 번째 라운드, "인사이트 NN/총 · 도메인"에서 영문 도메인명
> 하나로.** 예전엔 "인사이트 01/04 · 영화"처럼 번호 매기기 + 한글 도메인명이 본문 흐름을
> 차지하는 문단이었다. 사용자 요청으로 번호를 완전히 빼고, `story.domainMeta.nameEn`
> (registry.json 단일 소스, `allStories()`가 주입)을 대문자로("MOVIE") 우측 상단에
> 절대 위치로만 놓는다(`.zb-article-domain`, 표지 eyebrow와 같은 패턴). domainMeta가
> 없으면(테스트 목 데이터 등) 한글 도메인명으로 되돌아간다.

> **줄간격 — 2026-08-12 여섯 번째 라운드, 페이지를 더 넉넉히 쓰도록 다시 키웠다.**
> "콘텐츠 줄간격이 너무 좁다, 페이지를 fully 사용하라"는 지적으로 본문·스탯·쿼트·
> 인사이트 노트 전 요소의 `line-height`·`margin`을 키웠다(예: 본문 line-height 1.6→1.8,
> 문단 margin 10px→16px). 헤더가 번호 매기는 문단에서 절대 위치 라벨로 바뀌면서 그만큼
> 흐름 공간이 남아 여유가 생겼다 — 실측(`build/verify.mjs`의 콘텐츠 잘림 검사)으로 한
> 페이지 안에 들어오는 걸 확인했다.

> **인쇄용 출처 — 2026-08-12 여섯 번째 라운드, 클릭 유도 문구에서 격식 있는 인용으로.**
> "인쇄물은 링크를 못 누른다, 저자·발행일·기사 제목을 명시한 격식 있는 인용으로 바꿔라"는
> 요청. 결과: 웹의 `sourceLabel`("뉴스1 Nbox 확인하기" 같은 클릭 유도 문구)에서 "확인하기"
> 류 동사를 떼고, 매체명 · `story.anchorDate`(이미 검증된 기준일) · 원본 URL 순서로
> 다시 조합한다(`citationLine()`, render-zinebook.mjs).
> **한계 — 저자명·정확한 기사 제목은 못 채웠다.** 이 라운드를 진행한 세션은 네트워크
> egress 프록시가 실제 출처 도메인(news1.kr·edaily.co.kr 등)을 차단해 원문을 열어
> 바이라인·정확한 표제를 확인할 수 없었다 — CLAUDE.md §7.3("확인되지 않은 사실을 싣지
> 않는다")에 따라 저자명·제목을 지어내지 않았다. 매체명·날짜·URL(전부 이미 QA를 통과한
> 검증된 값)만으로 조합한 상태다 — 네트워크가 열리거나 사람이 원문을 열어 확인하면
> 저자·표제를 추가해야 한다.

> **한터차트·예스24는 매체명만 — 2026-08-12 여덟 번째 라운드.** 위 한계 문단에서 이미
> 짚었듯 이 둘은 애초에 기사가 아니라 실시간 차트·베스트셀러 페이지라 저자 바이라인
> 자체가 없다. 처음엔 그래도 다른 출처와 같은 형식(매체명·날짜·URL)으로 냈는데, 사용자가
> "저자 없이 매체명만 남겨줘"라고 요청 — 날짜·URL도 함께 뺐다. 이유: 차트 페이지에
> 붙는 "날짜"는 실제로는 기사 발행일이 아니라 스토리의 편집 기준일(`anchorDate`)이라,
> 다른 뉴스 출처와 같은 "매체명·날짜·URL" 형식으로 내면 마치 그 날짜에 발행된 기사가
> 있는 것처럼 보이는 착시가 생긴다 — 없는 정확성을 있는 척하는 셈이다. `citationLine()`의
> `NAME_ONLY_OUTLETS` 목록(이름 접두어로 매칭)에 이 둘을 등록해 매체명 문자열 하나만
> 낸다. 그 외 실제 기사 출처(뉴스1·이데일리 등)는 형식이 그대로다.

### 11.2 물리 인쇄 배치 — 사용자 지정, 바꾸지 않는다

```
Sheet 1(겉장) 앞면 = [뒤표지 | 앞표지]      Sheet 1 뒷면 = [About | 7쪽·QR]
Sheet 2(속지) 앞면 = [기사4 | 기사1]        Sheet 2 뒷면 = [기사2 | 기사3]
```

A4 랜드스케이프(297mm×210mm) 2장, 양면. `render-zinebook.mjs`의 `SHEETS` 상수가 이 배치의
단일 소스다 — `test/zinebook.test.mjs`가 순서를 고정한다. 두 반쪽 사이에 옅은 점선
(`.zb-fold`)으로 접는 선을 표시한다. **이 배치가 실제로 올바른 새들스티치 임포지션인지
2026-08-11에 실측으로 확인했다** — 논리 페이지 1~8을 8/1, 2/7, 6/3, 4/5로 짝지으면 2장을
겹쳐 반으로 접었을 때 정확히 읽는 순서(앞표지→About→기사1~4→7쪽·QR→뒤표지)가 나온다.
바꿀 필요가 없었다 — 원래도 맞았다. `SHEETS`의 논리 키 이름 자체는 여전히 `"notes"`다
(§11.1 7쪽 문단 참고 — 콘텐츠는 QR로 바뀌었지만 물리 배치 계약이라 키 이름은 안 바꿨다).

> **2026-08-12 열세 번째 라운드 — "양면 인쇄하면 뒷면이 거꾸로 나온다" 신고 조사.**
> 결론부터: **배치·순서·회전 어디에도 코드 버그가 없다.** 확인한 것:
> 1. 페이지 순서(8/1·2/7·6/3·4/5)는 표준 새들스티치 임포지션과 정확히 일치한다
>    (2장 접기 새들스티치 8쪽의 정석 패턴 그 자체다 — 위 문단의 2026-08-11 실측
>    확인이 이 순서를 검증한 것이었다).
> 2. `.zb-sheet`·`.zb-half`·표지 랩어라운드(§11.1) 어디에도 `transform: rotate`나
>    그 비슷한 회전 규칙이 없다 — 앞면·뒷면 4개 시트 전부 같은 좌표계로, 회전 없이
>    "있는 그대로" 렌더된다(`grep -n rotate build/lib/css.mjs`로 재확인).
> 3. 표지(sheet1-front)의 랩어라운드 크롭 오프셋(뒤표지=0, 앞표지=-561px)도 물리
>    좌우 배치(`SHEETS`의 `["cover-back","cover-front"]` 순서)와 정확히 맞는다.
>
> 그럼 왜 뒷면이 거꾸로 나오는가 — **양면 인쇄의 "뒤집는 축"(긴 변 기준/짧은 변
> 기준)은 OS·프린터 드라이버가 정하는 하드웨어 설정이고, 웹페이지는 그 값을 읽거나
> 지정할 방법이 없다.** CSS Paged Media 명세(`@page`)에도, 어떤 브라우저의 인쇄
> API에도 duplex flip 축을 제어하는 프로퍼티가 없다 — `size: landscape`는 페이지
> **치수**만 정할 뿐, 양면 인쇄기의 물리적 뒤집기 동작과는 무관하다. 가로(landscape)
> 문서를 책처럼 좌우로 넘겨 읽으려면 인쇄 대화상자에서 **"짧은 변 기준 뒤집기"
> (Flip on Short Edge / Short-Edge binding)**를 골라야 한다는 것은 이 사이트가
> 정하는 규칙이 아니라 인쇄업계의 표준 관행이다(GNOME 인쇄 도움말, 브라더 프린터
> 지원 문서 등에 동일하게 문서화돼 있다) — "긴 변 기준"(Long-Edge, 세로 문서의
> 기본값인 경우가 많다)이 걸려 있으면 지금 이 배치가 정확히 사용자가 겪은 증상
> (뒷면이 180도 돌아간 것처럼 보임)을 만든다.
> **대안으로 검토했다가 버린 것 — "뒷면 콘텐츠를 소프트웨어에서 미리 180도
> 돌려서 긴 변 기준 기본값에 맞추기".** 이건 실제 수정이 아니라 실패 모드를
> 옮기는 것뿐이다 — 드라이버가 이미 짧은 변 기준으로 정확히 동작하는 사용자
> (표준을 따른 쪽)에게는 이 "보정"이 오히려 없던 결함을 만든다. 어느 쪽이 더
> 흔한 기본값인지는 OS·드라이버마다 달라 안전하게 가정할 수 없다 — 그래서
> 페이지 콘텐츠는 손대지 않는다(사용자 요청 "전체 레이아웃을 그냥 돌리지 말라"와도
> 방향이 같다).
> **실제로 구현한 것** — 코드로 통제할 수 있는 유일한 지점은 "인쇄 전에 올바른
> 대화상자 설정을 사용자가 놓치지 않게 하는 것"이다. 툴바에 `.zb-duplex-hint`를
> 추가했다(`render-zinebook.mjs`) — "A4 · 가로(Landscape) · 양면 인쇄 · 짧은 변
> 기준 뒤집기(Flip on Short Edge)"를 인쇄 버튼과 항상 나란히, 오버레이를 여는
> 순간부터 보여준다. `.zb-toolbar` 전체가 인쇄에서 숨는 기존 규칙을 그대로
> 물려받아 실제 지면에는 안 찍힌다 — 화면에서만, 인쇄 버튼을 보는 시점에 항상
> 보인다.

**이름 있는 `@page`를 쓴다.** 이 시트는 §8의 "한 장 요약"(A4 세로, 이름 없는 기본 `@page`)과
같은 스타일시트를 공유하므로, 이름 없는 `@page`를 다시 쓰면 나중에 선언된 쪽이 사이트
전체의 기본 인쇄 크기를 덮어써 버린다. `.zb-sheet { page: zb-landscape; }` + `@page
zb-landscape { size: A4 landscape; margin: 0; }`로 이름을 따로 준다 — 기본 `@page`(A4 세로)
는 그대로 §8 라우트만 가리킨다.

> **2026-08-11 랜드스케이프 키워드 추가 — 실측으로 찾은 두 번째 함정.** 사용자가 "Print를
> 누르면 세로로 최적화된다"고 보고. `preferCSSPageSize:true`로 헤드리스 실측하면 PDF의
> MediaBox 자체는 이미 297×210mm로 정확했다 — 페이지 박스 크기 문제가 아니었다. 원인은
> `size: 297mm 210mm`처럼 raw mm 치수만 주면 브라우저 인쇄 대화상자의 "레이아웃(세로/가로)"
> 토글이 자동으로 가로를 고르지 않는다는 것 — `size: A4 landscape`처럼 `landscape` 키워드를
> 명시해야 그 토글 자체가 맞춰진다(페이지 박스 치수는 둘이 동일하다). `build/verify.mjs`가
> 이제 `preferCSSPageSize`로 매 빌드마다 실측해 회귀를 잡는다.

> **2026-08-12 네 번째 라운드 — named page만으로는 부족해 문서 기본 @page도 덮어쓴다.**
> 위 키워드 수정 후에도 사용자가 "실제로 인쇄해보니 여전히 세로로 나온다"고 재보고했다.
> named page(`page: zb-landscape` 프로퍼티)는 비교적 최근에 표준화된 CSS Paged Media
> 기능이라 브라우저별 지원이 고르지 않다 — 우리 헤드리스 Chromium(Playwright) 검사는
> 통과했지만, 그게 모든 브라우저·프린터 드라이버 조합에서 통한다는 뜻은 아니었다. 더
> 폭넓게 지원되는 평범한 `@page{size}` 오버라이드로 이중 방어선을 추가했다: DIY 진이
> 있는 문서(`layout.mjs`의 `page()`, `zb`가 참일 때)는 `<head>`에 인라인
> `<style>@media print { @page { size: A4 landscape; margin: 0; } }</style>`를 심어 그
> **문서 자체의 기본(이름 없는) @page**를 랜드스케이프로 덮어쓴다. 이게 안전한 이유:
> DIY 진이 있는 문서(showChrome 페이지 전부)는 인쇄 시 `#main-content`·`.site-header`가
> 항상 강제로 숨겨지고 진 시트만 보인다(오버레이를 연 적이 없어도 마찬가지, §위 인쇄
> 트리거 문단) — 그 문서에서 인쇄되는 건 항상 진뿐이므로 문서 전체의 기본 페이지를
> 랜드스케이프로 바꿔도 다른 용도와 충돌하지 않는다. `/print/`(한 장 요약, 세로)는
> `showChrome:false`라 `zinebook` 자체를 안 받으므로 이 오버라이드가 안 붙는다 — 공유
> 스타일시트의 세로 기본값을 그대로 쓴다. `test/zinebook.test.mjs`가 두 경로를 모두
> 확인한다.

인쇄 트리거는 오버레이 안의 "Print Zine" 버튼(`window.print()`)뿐이다. 그 페이지의 평소
콘텐츠(`#main-content`, `.site-header`)와 CTA 바는 인쇄에서 강제로 숨긴다 — 안 그러면 진
시트 앞뒤로 그 페이지의 화면용 콘텐츠가 별도 페이지로 끼어든다(실측으로 발견 — 처음엔
정확히 4장이 아니라 7장, 그다음 5장이 나왔다). `body.has-zb`의 하단 여백(고정 CTA 바
자리)도 인쇄에서 0으로 되돌린다 — 안 그러면 마지막 시트 뒤에 그 여백만을 위한 빈 페이지가
하나 더 생긴다.

### 11.3 전체 8쪽 그리드 미리보기

**2026-08-11 되돌림 — 3D 플립북에서 정적 그리드로.** 예전엔 `perspective`+`rotateY(-180deg)`
로 한 번에 한 쪽만 넘겨 보여줬다. 사용자가 "인쇄 전에 8쪽 전체를 볼 수 있어야 한다,
cropped·partial한 미리보기는 안 된다"고 요청해 읽는 순서 그대로 정적 그리드에 8쪽을 한
번에 낸다(`.zb-grid`, 데스크톱 4열·태블릿 2열·모바일 1열). 회전·3D 변환 같은 연출은
없앴다 — "불필요한 장식·애니메이션·새 UI 시스템을 더하지 말라"는 요구와도 맞는 방향
(오히려 이전보다 단순해졌다: leaf 상태·페이지 넘김 로직·리사이즈 리스너가 통째로 없어졌다).

**축소 배율은 자바스크립트가 아니라 CSS 컨테이너 쿼리로 계산한다.** 콘텐츠 패널
(`.zb-panel`)은 인쇄 반쪽과 같은 비율의 고정 캔버스(561×794px, 96dpi에서 148.5mm×210mm에
해당)로 그려진다. 각 타일(`.zb-tile-face`)에 `container-type: inline-size`를 주고, 그 안의
`.zb-panel`이 `transform: scale(calc(100cqw / 561px))`로 스스로 축소된다 — 타일 폭이 1cqw =
1%로 등록되므로 리사이즈해도 JS 없이 배율이 따라온다. `assets/zinebook.js`는 이제 열고/
닫고/인쇄하는 것만 한다.

> **실측으로 찾은 함정 — `scale()`은 unitless 숫자만 받는다.** 처음엔 `calc(100cqw / 561)`
> 라고 썼다 — `561`이 unitless라 `100cqw`(길이)를 나눈 결과가 여전히 길이(예: `0.499px`)가
> 되고, `scale()`은 길이를 거부해 transform 자체가 조용히 무효화됐다(패널이 축소 없이 원본
> 크기로 렌더돼 타일 밖으로 넘쳤다). `561px`(길이)로 나누면 길이/길이 = unitless 비율이
> 나와 정상 동작한다.

### 11.4 발견한 함정 (실측으로 찾은 것들 — 다시 반복하지 않도록)

- **`scale()`은 unitless 숫자만 받는다** — §11.3 참고. 컨테이너 쿼리 단위로 배율을 계산할
  때 반드시 길이로 나눠야 한다(`561px`이지 `561`이 아니다).
- **`@page` 랜드스케이프는 raw mm 치수가 아니라 `landscape` 키워드로 선언해야 인쇄
  대화상자의 방향 토글이 맞춰진다** — §11.2 참고.
- **SVG에 `calc()` 퍼센트 크기를 주면 0×0으로 계산될 수 있다.** Notes 배경의 줄무늬 SVG가
  처음엔 안 보였다 — `width: calc(100% - 52px)`가 SVG 루트에서 크기를 못 잡았다.
  `inset:0; width/height:100%`로 단순화하고, 태그에도 `width="100%" height="100%"` 속성을
  같이 줬다.
- **한 문서에 같은 SVG `id`가 두 번 있으면 `url(#id)` 참조가 깨진다.** 8개 논리 페이지를
  한 번만 만들어 그리드·인쇄 두 군데에 재사용하는 구조라(§11.1) `<pattern id="...">`를 쓰면
  문서에 같은 id가 필연적으로 두 번 생긴다. id 없이 `<line>`을 반복해 찍는 방식으로 바꿔
  이 문제 자체를 없앴다.
- **`[hidden]` 속성이 남아 있으면 명시도가 인쇄 규칙보다 높아진다.** `.zb-overlay[hidden] {
  display:none !important }`(화면용 안전장치)와 인쇄 블록의 `.zb-overlay { display:block
  !important }`가 둘 다 `!important`일 때, 앞쪽이 명시도(0,2,0 > 0,1,0)로 이긴다 — 오버레이를
  한 번도 연 적 없는 상태에서 인쇄를 시도하면(버튼을 안 거치는 경로) 시트가 안 나왔다.
  인쇄 규칙 선택자에도 `[hidden]`을 같이 걸어 해결했다.
- **`overflow:hidden`은 콘텐츠가 넘쳐도 조용히 자른다.** 기사 페이지가 웹과 같은 전체
  5블록을 담게 되면서, 다음 주 스토리가 이번 주보다 길면 소리 없이 잘릴 위험이 생겼다.
  `build/verify.mjs`가 매 빌드마다 그리드를 열어 각 패널의 실제 콘텐츠 높이를 실측해
  가용 높이를 넘는지 확인한다(design.md의 A4 지면 실측과 같은 원칙) — 넘으면 빌드가
  실패한다. 이 검사는 `position:absolute`인 자식(예: 표지의 랩어라운드 뷰포트처럼 의도적으로
  패딩 박스 전체에 bleed하는 요소)은 건너뛴다 — 그런 요소는 자기 자신의 `overflow:hidden`으로
  이미 패널 경계에 갇혀 있어, 일반 계산식(자식 높이 합 vs 가용 높이)에 넣으면
  `paddingBottom`만큼 항상 헛경고가 뜬다.
- **브라우저 인쇄 대화상자의 "배경 그래픽" 토글은 기본이 꺼짐이다.** 꺼진 채로 인쇄하면
  `background-color`·`background-image`가 전부 흰 배경으로 날아간다 — 실사용자가 뒤표지
  전체가 빈 흰 페이지로 나왔다고 보고했다(당시엔 뒤표지가 단색 배경이던 시기, 2026-08-12
  세 번째 라운드 직전). `print-color-adjust: exact`(+`-webkit-` 접두사)는 상속 속성이라
  `html` 셀렉터 한 곳에만 걸면 이 스타일시트가 쓰는 모든 배경에 전파된다 — 토글 상태와
  무관하게 항상 인쇄되도록 강제한다. Playwright의 `printBackground:true`는 이 토글이 항상
  켜진 것처럼 동작해 이 클래스의 버그를 절대 못 잡는다 — 반드시 `printBackground:false`로도
  한 번 더 PDF를 뽑아 배경이 유지되는지 확인해야 한다(`test/css.test.mjs`에 CSS 존재만
  확인하는 단위 테스트는 있지만, 실제 PDF 검증은 자동화돼 있지 않다 — §11.2·§11.3처럼
  코드로 강제하는 회귀 테스트가 아니라 사람이 릴리스 전에 확인해야 하는 항목이다).
- **`position:absolute; top:50%; transform:translateY(-50%)`는 화면 스크린샷에서는
  멀쩡한데 실제 인쇄 PDF에서만 깨질 수 있다 — 반드시 진짜 `page.pdf()`로 검증해야 잡히는
  함정.** 랩어라운드 캔버스의 세로 중앙 정렬을 처음엔 이 방식으로 짰다.
  `page.emulateMedia({media:'print'})` + 특정 시트 엘리먼트를 `.screenshot()`으로 찍으면
  텍스트가 패널 한가운데 완벽하게 들어갔다 — 그런데 실제로 `page.pdf({printBackground:true,
  preferCSSPageSize:true})`를 생성해 `pdftoppm`으로 래스터화하면 "ANSWER"·"ZINE"이 페이지
  아래로 밀려 잘려 있었다. 원인으로 가장 유력한 설명: 인쇄용 4장(`.zb-sheet`)이
  `break-after:page`로 이어진 하나의 문서 흐름이라, Chromium의 PDF 페이지네이션 엔진이
  퍼센트 기반 `top:50%`를 화면 렌더링 패스와 다른 기준 높이로 계산한 것으로 보인다 — 화면
  스크린샷은 이 페이지네이션 전용 레이아웃 패스를 거치지 않으므로 문제를 재현하지
  못했다. `top:50%`/`translateY(-50%)`를 flex(`display:flex; align-items:center`)로
  바꾸자 실제 PDF에서도 정상화됐다. **교훈: 인쇄 레이아웃(특히 여러 `@page`가 이어지는
  다중 페이지 흐름) 안의 세로/가로 중앙 정렬은 되도록 flex로 하고, 퍼센트 기반
  `position:absolute` 중앙 정렬을 쓴다면 반드시 실제 `page.pdf()`를 생성해 래스터화
  (`pdftoppm` 등)하거나 최소한 PDF 뷰어로 직접 열어 확인한다 — 화면 스크린샷·
  `emulateMedia`만으로는 이 클래스의 버그를 못 잡는다.**
- **컨테이너 쿼리 컨테이너(`container-type: inline-size`)에 자식을 추가하는 순간, 그
  직후 발생하는 합성 `click` 이벤트의 `e.target`이 실제로 클릭한 요소가 아니라 그 뒤에
  있는 다른 요소로 잡힐 수 있다 — headless Chromium에서 실측으로 재현.** 스티커를
  `.zb-tile-face`(컨테이너 쿼리 컨테이너)에 드롭하면 그 프레임에 새 자식이 생기는데,
  바로 뒤이어 브라우저가 합성하는 `click` 이벤트의 `e.target`이 (레이아웃이 그 프레임에
  아직 안정되지 않아서인 것으로 보인다) 타일이 아니라 전체 화면을 덮은 배경 오버레이로 잡혔다
  — "오버레이 바깥 클릭 시 닫기" 핸들러가 걸려 스티커를 놓자마자 오버레이가 닫혀버렸다.
  `document.elementFromPoint`로 직접 확인해서 재현했다(`e.target === overlay`가 `true`).
  **해결: 근본 원인(레이아웃 타이밍)을 고치는 대신, "방금 드래그가 끝난 직후의 클릭
  한 번"을 플래그로 무시하도록 방어했다**(`assets/zinebook.js`의
  `suppressOverlayCloseOnce()` — 드래그 종료 시 플래그를 세우고 `setTimeout(fn, 0)`으로
  다음 매크로태스크에서 되돌린다 — 합성 click은 그 사이에 동기적으로 먼저 발생한다).
  **교훈: "바깥 클릭 시 닫기" 패턴과 "그 안에서 DOM을 변형하는 드래그앤드롭"을 같이 쓸
  때는, 드래그 직후 발생하는 클릭의 `e.target`을 곧이곧대로 믿지 않는다.** `build/verify.mjs`가
  실제 `page.mouse` 드래그로 이 회귀를 매 빌드마다 재현·검사한다(`page.evaluate`로 이벤트를
  직접 dispatch하면 이 클래스의 버그 자체가 재현되지 않는다 — 반드시 진짜 마우스 좌표
  기반 hit-test가 필요하다).

### 11.5 스티커로 꾸미기 (2026-08-12 일곱 번째 라운드 신규)

사용자가 참고 이미지(플랫 지오메트릭 스티커 그리드 — 원·부채꼴·아치·고리·클로버·팩맨·
점묶음·별·꽃 등)를 보내며 "미리보기 화면 하단에 스티커 팔레트를 두고, 드래그앤드롭으로
진 위에 자유롭게 배치·재배치할 수 있게 하라"고 요청했다. 마우스로 "클릭해서 끌기", 터치로
"터치해서 끌기" 둘 다 요구했다 — HTML5 드래그앤드롭 API는 터치에서 안 통하므로, Pointer
Events(`pointerdown`/`pointermove`/`pointerup`)로 마우스·터치를 하나의 코드 경로로
처리한다.

```
팔레트: 미리보기 화면(.zb-viewer) 바로 밑, 오버레이 하단(.zb-sticker-palette)
        인쇄에는 안 나온다 — 도구지 콘텐츠가 아니다(@media print가 숨긴다)
스티커: 10종, 인라인 SVG(요구사항 #5), 흑백 규칙 예외(장식 레이어)
좌표계: %(픽셀 아님) — 그리드 타일(.zb-tile-face)과 인쇄 시트 반쪽(.zb-half)이
        종횡비(148.5:210)만 같고 실제 크기는 다르므로, %만이 두 컨텍스트에
        그대로 옮겨 쓸 수 있는 단위다
```

**놓기:** 팔레트 칩을 끌어 그리드 타일 위에 놓으면(`document.elementFromPoint`로 드롭
지점의 `.zb-tile-face`를 찾는다) 그 자리에 스티커가 생긴다. 캔버스 밖에서 놓으면 취소 —
아무것도 안 만든다.
**옮기기:** 이미 놓인 스티커를 다시 끌면 같은 방식으로 재배치한다(다른 타일로도 옮길 수
있다).
**지우기:** 두 번 탭(`dblclick`)하면 지운다.

**미러링이 핵심이다.** 화면에서 꾸민 게 실제로 인쇄되지 않으면 이 기능은 장난감일 뿐이다.
8개 논리 페이지는 그리드(`.zb-tile-face`)와 인쇄 시트(`.zb-half`) 양쪽에 같은 문자열이
재사용되는 구조라(§11.1), 이번 라운드에서 두 요소 모두에 `data-zb-page="<논리 키>"`를
심었다 — 스티커를 놓거나 옮길 때마다 같은 `data-zb-page` 값을 가진 `.zb-half`를 찾아
같은 %로 미러 스티커를 만들거나 갱신한다(`data-zb-instance`로 그리드·인쇄 사본을 짝짓는다).
실측(`page.pdf()` 생성 후 `pdftoppm` 래스터화)으로 스티커가 실제 인쇄 페이지에 정확한
위치로 나오는 것을 확인했다.

**발견한 버그는 §11.4 "컨테이너 쿼리 컨테이너에 자식을 추가하는 순간..." 항목 참고** —
이 기능을 만들다가 찾았다.

> **2026-08-12 열아홉 번째 라운드 — 크기 조절 추가(사용자 요청, "스티커는 사이즈도
> Adjustable하게").** 놓은 스티커 우측 하단에 작은 원형 손잡이(`.zb-sticker-resize`)가
> 붙는다. 손잡이를 끌면 커지거나 작아진다 — 이동과 같은 Pointer Events 방식이라 마우스·
> 터치 둘 다 된다. 크기도 좌표와 같은 이유(그리드·인쇄 두 컨텍스트 호환)로 %(캔버스 폭
> 기준, 6~32% 범위)로 저장하고 그대로 미러링한다. 손잡이는 그리드 사본에만 있다 — 인쇄
> 사본은 순수 정적 표시라 상호작용이 필요 없다. 중심점을 고정한 채 자라도록(모서리가
> 아니라 스티커 한가운데를 기준으로 커진다) 크기가 바뀔 때마다 위치도 같이 보정한다.
> 손잡이의 pointerdown은 `stopPropagation()`으로 스티커 본체의 이동 드래그로 안 새게
> 막는다 — 안 그러면 손잡이를 끌 때 스티커 전체가 같이 끌려간다.

### 11.6 편집 디자인 재정비 (2026-08-12 스무 번째 라운드)

사용자가 "ZINE DESIGN EDIT GUIDELINE"이라는 이름으로 4가지를 요청했다. 웹 스토리
페이지가 아니라 DIY 진(`render-zinebook.mjs`)에 한정된 변경이다.

**0. 7쪽(구 Notes/스캔) 전면 교체.** "page 7 삭제하고, UNEXPECTED & 오른쪽에 적혀있는
문단을 page 7 가운데 정렬+중간 맞춤으로 넣어줘." QR 스캔 기능(2026-08-12 여섯 번째
라운드 도입)을 완전히 없애고, 홈의 통합 인사이트(`issue.insight` 큰 단어 +
`issue.insightNote` 분석 문단, `.insight-lead`)를 그대로 옮겨왔다. 웹은 좌우 2단
flex지만 여기는 "가운데 정렬+중간 맞춤" 요구라 한 칼럼으로 세로로 쌓고 페이지
정중앙(가로·세로 둘 다)에 낸다(`.zb-panel--insight`, `render-zinebook.mjs`의
`insightPanel()`). 논리 키(`"notes"`)는 물리 배치 계약(`FLIP_ORDER`/`SHEETS`)이라
그대로 뒀다 — 이 페이지가 내용을 바꾼 게 이걸로 세 번째다(메모란 → QR 스캔 →
통합 인사이트). QR이 완전히 빠지면서 `qrcode` devDependency·`build.mjs`의
`loadQR()`/`qrSvg` 전체를 걷어냈다 — 호출부가 없는 코드를 남겨두지 않는다는 원칙
(CLAUDE.md §2.2)을 그대로 따랐다.

**1~2. 좌우 여백 축소 + 세로 공간 재분배.** 사용자가 준 가이드라인은 Tailwind 스타일
일반 조언(`max-w-xl`→`max-w-6xl` 등)이었지만, 이 코드베이스엔 그런 유틸리티 클래스가
없다 — 실제로 해당하는 지점을 찾아 같은 효과를 냈다. `.zb-panel`(표지·About·통합
인사이트·기사 4종 공용 디자인 캔버스, 561×794px 고정)의 패딩을 34px 30px→28px
18px로 줄였다 — 좌우가 특히 좁아져(30→18) 기사 본문이 쓸 수 있는 폭이 넓어졌다.
표지의 랩어라운드 캔버스(`.zb-wrap-viewport`)는 이 변경과 무관하다 — absolute 자식의
컨테이닝 블록이 조상의 **패딩 박스** 기준이라, 패딩 값이 바뀌어도 561px 폭 계산이
그대로 유지된다(실측으로 확인).

세로는 `.zb-panel--article`에 `justify-content: space-between`을 추가했다 —
`.zb-panel`이 이미 flex column이라 이 한 줄로 충분하다. 예전엔 `.zb-source`
(출처 줄)에만 `margin-top: auto`를 걸어 그 앞의 남는 공간을 전부 출처 줄 바로 위
한 곳에 몰아넣었다 — 사용자가 첨부한 스크린샷이 정확히 이 증상(쿼트/인사이트
블록과 출처 사이의 큰 빈 공간)을 보여줬다. `margin-top: auto`를 빼고
`space-between`으로 바꾸자 남는 공간이 모든 블록 사이에 고르게 나뉜다 — 콘텐츠가
이미 페이지를 꽉 채우는 주(週)에는 효과가 거의 없고(분배할 여유 공간 자체가 없다),
짧은 주에는 블록 사이사이로 공간이 퍼진다. 사용자가 요청한 `break-inside: avoid`도
쿼트·스탯 박스·인사이트 노트에 추가했다(`.zb-stat`/`.zb-quote`/`.zb-insight-note`) —
내부 요소가 인쇄 중 쪼개지는 걸 막는다.

**`@page` 마진은 사용자 예시(`margin: 15mm`)를 그대로 적용하지 않았다 — 의도적
예외다.** DIY 진의 물리 인쇄 시트는 `@page zb-landscape { size: A4 landscape;
margin: 0; }`로 A4 전체를 여백 없이 채워야 두 A5 반쪽(`.zb-half`, 148.5×210mm씩)이
정확히 물리 시트 절반씩을 차지한다 — 이게 새들스티치 접기 임포지션의 전제다(§11.2).
`margin: 15mm`를 적용하면 인쇄 가능 영역이 267×180mm로 줄어 두 반쪽이 실제 시트에
안 맞고 브라우저가 축소·잘라낼 위험이 있다 — 실측 검증된 접기 물리 계약을 깨는
변경이라 적용하지 않았다. 대신 같은 "여백 늘리기"라는 목적을 `.zb-panel`의 패딩
조정(위 문단)으로 대체했다.

**3. 우측 상단 카테고리 라벨 → 배경 세로 워터마크.** 작은 텍스트(`.zb-article-domain`,
11px)를 지우고 홈 카테고리 카드의 `.category-card-tag`(§7)와 똑같은 문법을 적용했다 —
도메인 영문명을 한 글자씩 세로로 쌓아 페이지 우측 가장자리를 가득 채우는 대형
워터마크(`.zb-article-watermark`, 58px, `opacity: .06`)로 바꾸고, `absolute` +
`z-index: -1`로 콘텐츠보다 뒤 레이어에 둬 헤드라인·본문이 그 위를 지나가도 안
가려진다(`.zb-panel--article`에 `z-index: 0`을 명시해 이 스태킹 컨텍스트를 그 패널
안에 가둔다 — 홈 카드와 같은 이유). 흑백 규칙(요구사항 #5, `test/zinebook.test.mjs`의
"흑백만 쓴다" 테스트가 강제)을 지키느라 색은 도메인 컬러가 아니라 항상 검정 리터럴
6% 불투명도다. 실제 라벨 텍스트는 `.sr-only`로 접근성을 지킨다(낱글자 나열은
스크린 리더에 뜻이 없다 — render-index.mjs의 세로 라벨과 같은 이유).

### 11.7 여백·워터마크 후속 조정 (2026-08-12 스물두 번째 라운드)

§11.6 바로 다음 라운드에서 사용자가 두 가지를 더 요청했다.

**여백을 2.5cm씩으로.** §11.6에서 `.zb-panel` 패딩을 `34px 30px`→`28px 18px`로
줄였는데(콘텐츠 폭을 넓히는 방향), 이번엔 반대로 "Zine의 레이아웃 여백 2.5cm씩"을
요청받았다 — 더 최근의 명시적 지시라 그대로 따랐다. `padding: 2.5cm`로 바꿨다 — `cm`는
CSS 절대 단위라 뷰포트 크기와 무관하고, 디자인 캔버스(561×794px)가 애초에 실물 A5
(148.5×210mm)를 96dpi로 그대로 옮긴 크기라(561px≈148.5mm) 화면 미리보기(cqw 축소
표시)와 실제 인쇄(`.zb-half`가 mm 단위로 A5 절반을 정확히 차지) 양쪽에서 물리적으로
같은 2.5cm 여백이 나온다. 가용 콘텐츠 영역이 372×605px로 줄어(원래 525×738px 대비
가로 29%·세로 18% 감소) 기사 3편이 페이지 밖으로 넘쳤다(`build/verify.mjs` 실측,
최대 84px) — 문장을 자르는 대신 기사 페이지 타이포(헤드라인·본문·스탯·쿼트·인사이트
노트·출처)의 크기·행간·블록 여백을 전반적으로 10~15% 낮춰 좁아진 상자에 다시
맞췄다. 5블록 전부를 웹과 같은 문장으로 싣는다는 원칙(§11.1)은 그대로다.

**배경 워터마크 확대 + 간격 좁히기.** "MOVIE~BOOKS 글자 간격 좁히기, 사이즈 50%
이상 키울것." `.zb-article-watermark`의 `justify-content`를 `space-between`(글자를
페이지 세로 전체에 걸쳐 끝까지 펼친다)에서 `center`로 바꾸는 것만으로 "간격이
좁아진다" — 남는 공간을 억지로 나눠 갖는 대신 `line-height`만큼만 붙어서 뭉친다.
`font-size`는 58px→92px(+58.6%, "50% 이상" 조건을 만족한다)로, 컬럼 폭도 커진 글자에
맞춰 74px→118px로 같이 늘렸다 — 안 늘리면 커진 글자가 패널의 `overflow: hidden`에
잘린다.

### 11.8 워터마크 좌상단 배치 (2026-08-12 스물네 번째 라운드)

"좌상단으로 배치해주고 왼쪽 위에서 시작해 아래로 떨어지게 만들어줘. 크기는 30%
정도 더 크게." 세 값을 같이 바꿨다: `right: 0`→`left: 0`(우측 가장자리→좌측
가장자리), `justify-content: center`→`flex-start`(페이지 세로 중앙에 뭉쳐 있던
글자가 맨 위에서 시작해 아래로 흘러내린다 — `top: 0`/`bottom: 0`은 그대로라
컨테이너 자체는 패널 전체 높이를 쓰지만, `flex-start`라 글자는 위쪽에만
뭉친다), `font-size: 92px`→`120px`(+30.4%, "30% 정도" 조건을 만족한다). 컬럼
폭도 같은 비율로 118px→154px로 키웠다. "MOVIE"(5자)는 물론 가장 긴 "YOUTUBE"
(7자)도 커진 글자로 필요한 높이(약 689px)가 패널의 세로 전체 여유(794px, 워터마크의
컨테이닝 블록은 `.zb-panel--article`의 패딩 박스 — 즉 패널 테두리 안쪽 전체다)
안에 들어와 잘리지 않는다 — 실측(`build/verify.mjs`)으로 확인했다.
