# LESSONS.md — 지금까지 배운 것들

**같은 실수를 두 번 하지 않으려고 만든 파일입니다.**

에이전트는 일을 시작하기 전에 자기와 관련된 항목을 읽고,
끝나고 나서 어떤 항목을 참고했는지 작업 기록에 남깁니다.

> **목표는 항목을 모으는 게 아니라 `자동화`로 졸업시키는 것입니다.**
> 적어두기만 한 교훈은 언젠가 잊힙니다. 기계가 막아줘야 진짜로 해결된 것입니다.

각 항목은 세 단계를 거칩니다.

| 상태 | 뜻 | 다음 단계 |
|---|---|---|
| `관찰` | 아직 한 번밖에 안 겪어서 규칙으로 만들기 이르다 | 또 생기면 `수동`으로 |
| `수동` | 규칙은 정했지만 사람이 눈으로 봐야 한다 | 검사로 만들면 `자동화`로 |
| `자동화` | `qa.mjs`나 제작기가 알아서 막는다 | 끝 |

**항목을 직접 고치는 건 Michael님만 하십니다.** `az-retro`는 제안만 합니다.

---

## 적는 형식

`tools/qa.mjs`가 이 파일을 읽어서 검사합니다. **형식이 어긋나면 검사가 실패합니다.**
아래 일곱 항목을 이 순서 그대로 적어야 합니다.

```markdown
### L-00N · 짧은 제목

- **발생**: YYYY-wNN · 도메인 (또는 전역)
- **상태**: 자동화 | 수동 | 관찰
- **분류**: 수집 | 선별 | 검증 | 집필 | 해석 | 빌드 | 운영
- **증상**: 무슨 일이 실제로 일어났는가
- **규칙**: 앞으로 어떻게 하는가 — 한 문장, 행동으로
- **탐지**: 누가 어떻게 잡는가
- **반영**: 어느 문서 어느 절에 반영했는가
```

| 항목 | 무엇을 적나요 |
|---|---|
| `발생` | 언제, 어느 카테고리에서 |
| `상태` | `자동화` / `수동` / `관찰` 중 하나 |
| `분류` | `수집` `선별` `검증` `집필` `해석` `빌드` `운영` 중 하나 — 에이전트가 이걸 보고 자기 것만 골라 읽습니다 |
| `증상` | 실제로 무슨 일이 있었나 |
| `규칙` | 앞으로 어떻게 하나 — **한 문장, 행동으로** |
| `탐지` | 누가 어떻게 잡나 (사람 / `qa.mjs` / 제작기) |
| `반영` | 어느 문서에 반영했나 |

---

## 지금 현황

| 상태 | 개수 |
|---|---|
| 🤖 자동화 | 6 |
| 👁 수동 | 3 |
| 🌱 관찰 | 1 |
| **자동화율** | **6/10 (60%)** |

대시보드(`tools/dashboard.html`)가 이 값을 자동으로 계산해서 보여줍니다.
위 표는 눈으로 보시라고 옮겨 적은 것이라 조금 오래됐을 수 있습니다.

---

## 항목

> 여기부터는 에이전트가 읽는 부분이라 지시문 어조(반말)로 되어 있습니다.

### L-001 · 최초 발표 날짜를 확정일로 오인

- **발생**: 2026-w31 · 영화
- **상태**: 자동화
- **분류**: 검증
- **증상**: 〈오디세이〉 국내 개봉일을 2025년 12월에 발표된 `2026-07-15`로 썼다. 이 날짜는 2026년 4월에 `2026-08-05`로 변경됐고, 북미 개봉일은 `2026-07-17`로 또 달랐다. 집계 기간이 `2026.07.28 – 08.03`이라 **한국에서 개봉도 하지 않은 주간의 KOBIS 데이터를 인용**했다. `4주 차 감소율 -14%`는 계산이 성립하지 않는 수치였다. 회차 1편 폐기.
- **규칙**: 날짜는 **가장 최근 보도** 기준으로 확인한다. 최초 발표는 계획이지 사실이 아니다. 독립된 출처 2곳 이상에서 교차 확인한다.
- **탐지**: `qa.mjs` — 기준일 ≤ 집계 시작일 검사, 주차 검산 (`stat.label`의 "N주 차" vs 기준일 계산값)
- **반영**: `market-scope.md` §2·§5.1, `domains/movie.md` §3.5, `.claude/agents/az-verify.md` §B

### L-002 · 영문 제목에 꺾쇠를 쓰면 화면에서 사라진다

- **발생**: 2026-w31 · 전역
- **상태**: 자동화
- **분류**: 집필
- **증상**: 본문이 `<투명한 나선>` 형태로 작품명을 표기했다. 한글 제목은 HTML 토크나이저가 태그로 인식하지 않아 우연히 정상 출력됐지만(`<` 다음이 ASCII 알파벳일 때만 태그로 파싱), `<Odyssey>` 같은 영문 제목이었다면 미지의 태그로 파싱되어 화면에서 통째로 사라졌을 것이다. 인쇄본은 `&lt;`로 이스케이프돼 있어 두 매체의 출력이 갈릴 뻔했다.
- **규칙**: 작품명에는 `〈 〉`(U+3008/3009)를 쓴다. ASCII 꺾쇠 `<>`를 본문에 쓰지 않는다.
- **탐지**: `qa.mjs` — 본문에 `/<[A-Za-z]/` 패턴 검사. 제작기도 같은 검사
- **반영**: `CLAUDE.md` §6·§9.7, `domains/*.md` 표기 규칙

### L-003 · 인쇄 시 빈 지면 2.2장이 딸려 나온다

- **발생**: 2026-w31 · 전역
- **상태**: 자동화
- **분류**: 빌드
- **증상**: `@media print`의 숨김 목록에 `.statement-wrap`이 빠져 있었다. `height: 220vh` 스크롤 트랙이 인쇄 지면에 2,471px(≈A4 2.2장)의 빈 공간을 만들어 총 4페이지가 출력됐다. 진 페이지 자체는 정확히 A4 1장이었다.
- **규칙**: 웹 전용 UI는 전부 `@media print` 숨김 목록에 넣는다. 스크롤 트랙처럼 화면에서만 의미 있는 요소를 빠뜨리지 않는다.
- **탐지**: `qa.mjs` 미구현 — Chromium 헤드리스 PDF 렌더로 페이지 수 검사 가능. 현재는 `CLAUDE.md` §8-15 수동 확인
- **반영**: `CLAUDE.md` §9.1, `design.md` §10

### L-004 · 조회수를 판매의 증거로 쓸 뻔했다

- **발생**: 2026-w31 · 유튜브
- **상태**: 자동화
- **분류**: 검증
- **증상**: 유튜브 도메인에는 예스24·멜론·KOBIS에 해당하는 1차 출처가 없다. 조회수는 "봤다"의 증거이지 "샀다"의 증거가 아닌데, 이 웹진의 명제는 판매다. 해당 회차는 `sourceUrl`이 신문 기사였고 근거의 상당 부분이 매장 직원 구두 발언이었다.
- **규칙**: 유튜브 도메인은 **계기 측(영상 게시일·조회수)과 결과 측(판매·품절)을 서로 다른 출처로 각각 확인**해야 통과한다. 조회수만으로는 절대 통과시키지 않는다.
- **탐지**: `qa.mjs` — 화이트리스트 `evidence_only` 등급은 `sourceUrl`로 쓸 수 없게 차단. 제작기도 동일
- **반영**: `domains/youtube.md` §2·§8, `sources/README.md` §1

### L-005 · 국내 서사에 해외 지표를 섞을 위험

- **발생**: 2026-w31 · 전역
- **상태**: 자동화
- **분류**: 검증
- **증상**: L-001과 같은 뿌리다. 집계 출처(KOBIS·국내)와 계기 날짜(북미 개봉일)가 서로 다른 시장을 가리켰다. 빌보드·Box Office Mojo 같은 해외 출처를 국내 소비 서사에 쓰면 같은 사고가 난다.
- **규칙**: 집계 지표와 계기 날짜는 **같은 시장**의 것이어야 한다. 해외 소재는 `market-scope.md` §6의 세 조건을 전부 충족할 때만 다룬다.
- **탐지**: `qa.mjs` — 화이트리스트 `market` 필드와 스토리 `market` 필드 대조. `sourceUrl`과 `anchorSourceUrl` 양쪽 검사
- **반영**: `market-scope.md` §1·§3·§6, `sources/whitelist.json`의 `market` 필드

### L-006 · 분량이 넘치면 A4 1장이 깨진다

- **발생**: 2026-w31 · 전역
- **상태**: 자동화
- **분류**: 집필
- **증상**: 인쇄 진은 리드 1 + 미니 3이 A4 한 장에 들어가는 구조인데, 슬롯별 상한이 정의돼 있지 않았다. 헤드리스 실측 결과 기준 회차 대비 2.25배 분량에서 2페이지로 넘어갔다.
- **규칙**: `design.md` §4.1의 예산을 지킨다. 초과하면 **문장을 줄인다.** 폰트·행간·여백을 줄여 맞추는 것은 불변식 위반이다.
- **탐지**: `qa.mjs` — 슬롯별 글자 수 검사 (권장 초과는 경고, 절대 상한 초과는 실패). 제작기는 실시간 카운터
- **반영**: `design.md` §4, `domains/registry.json`의 `budget`

### L-007 · 이례를 큰 숫자로 착각한다

- **발생**: 2026-w31 · 영화
- **상태**: 수동
- **분류**: 선별
- **증상**: 영화 편의 이례성은 `-14%`라는 **감소** 수치였다. 업계 평균 감소율이 -35%인데 그 절반도 안 빠졌다는 게 이례였다. 큰 숫자를 찾는 습관으로는 이런 소재를 놓친다. 반대로 큰 숫자만 보고 통상 범위인 것을 이례로 오인하기도 쉽다.
- **규칙**: 이례는 절대값이 아니라 **그 도메인에서 이 시점에 기대되는 값과의 차이**다. 항상 기준선을 먼저 세우고 비교한다.
- **탐지**: 사람 — `az-scout`이 `scoreReason`에 기준선과 비교 과정을 적었는지 확인한다. 기준선 언급이 없으면 채점이 부실한 것
- **반영**: `CLAUDE.md` §4.2, `domains/*.md` §3 이례 판정

### L-008 · 계기가 광고·할인·팬덤이면 명제에서 벗어난다

- **발생**: 전역
- **상태**: 수동
- **분류**: 선별
- **증상**: 이례성과 인과가 모두 뚜렷해도, 계기가 `광고비를 썼다` / `할인했다` / `팬덤이 밀었다`로 환원되면 "왜 팔렸는가"의 답으로 가치가 없다. 이 웹진은 대중이 왜 지갑을 열었는지를 다룬다.
- **규칙**: 계기가 마케팅 지출·가격 인하·조직적 동원이면 **인과 점수를 0~1점으로 낮춘다.** 협찬 표기가 있는 영상은 소재로 쓰지 않는다.
- **탐지**: 사람 — `az-scout`의 `cause` 필드와 `az-verify`의 §E 이례성 재확인
- **반영**: `domains/youtube.md` §3(협찬), `domains/music.md` §3(팬덤 동원), `domains/book.md` §3(프로모션)

### L-009 · 최초 발표 이후 갱신을 확인하지 않는 습관

- **발생**: 2026-w31 · 영화
- **상태**: 수동
- **분류**: 수집
- **증상**: L-001의 상류 원인이다. 검색 결과 상단에 뜨는 기사가 가장 오래된 발표 기사인 경우가 잦다. 개봉일·발매일·출간일은 편성 사정으로 자주 바뀌는데, 첫 기사만 보고 넘어가면 철회된 날짜를 그대로 쓰게 된다.
- **규칙**: 날짜를 확인할 때는 **검색 결과를 날짜순으로 다시 정렬**해 가장 최근 보도를 찾는다. 최초 발표와 최근 보도의 날짜가 다르면 최근 것을 쓰고 변경 사실을 기록한다.
- **탐지**: 사람 — `az-verify` §B의 3중 확인 중 "최신" 항목. 자동화하려면 발표일 메타데이터가 필요한데 국내 언론 사이트가 일관되게 제공하지 않는다
- **반영**: `market-scope.md` §5.1, `.claude/agents/az-scout.md` 5단계

### L-010 · 스파크라인이 값 분포에 따라 뭉개진다

- **발생**: 전역
- **상태**: 관찰
- **분류**: 빌드
- **증상**: 스파크라인이 min–max 정규화라, 점이 2개면 직선이 되고 한 점이 극단적으로 크면 나머지가 바닥에 붙는다. `[98, 101, 100, 426]` 같은 배열에서 앞 세 점의 차이가 시각적으로 사라진다. 아직 오독을 유발한 사례는 없다.
- **규칙**: `trend`는 4~5점을 쓴다. 극단값이 있는 경우 축 캡션이 기준값을 명시하는지 확인한다.
- **탐지**: `qa.mjs` — 점 개수만 검사한다. 분포 왜곡은 미검출
- **반영**: `design.md` §7 스파크라인

### L-011 · 다시 그릴 수 없는 선을 그린다

- **발생**: 2026-w27 · 음악 / 2026-w31 · 도서
- **상태**: 자동화
- **분류**: 검증
- **증상**: `스트리밍 지수 (채널 개설 시점 대비) +2,019%`에 `[100,140,320,980,2019]` 추세선이 붙어 있었다. 멜론은 그런 지수를 공개하지 않고, 지난 주 차트도 사후에 열리지 않는다. 도서 편의 `[98,101,100,426]`도 예스24에서 재현되지 않는다. 두 선 모두 다음 주에 같은 방법으로 다시 그릴 수 없다 — 근거처럼 생겼지만 근거가 아니고, 근거처럼 생겼다는 점에서 없는 것보다 나쁘다.
- **규칙**: 추세선은 출처의 `series`가 `archived`일 때만 그린다. `snapshot`·`none` 출처에서는 `trend`를 지우고 `basis`(이 값이 무엇과 비교한 값인가) 한 문장으로 대체한다. `trend`를 그리면 `trendSource`로 각 점의 출처를 적는다.
- **탐지**: `qa.mjs` — `stat.sourceUrl`의 호스트를 화이트리스트에서 찾아 `series`를 읽고, `archived`가 아닌데 `trend`가 있으면 실패시킨다. `trend`가 없는데 `basis`도 없으면 실패
- **반영**: `CLAUDE.md` §3.4, `sources/whitelist.json` `seriesKinds`

### L-012 · 등록해 둔 출처 URL이 조용히 죽는다

- **발생**: 2026-w32 · 공연 / 음악
- **상태**: 자동화
- **분류**: 수집
- **증상**: `kopis.or.kr`의 `pblprfrList.do`·`rankBoxOfficeList.do`와 `circlechart.kr`의 `page_chart/onlinechart.circle`이 전부 404였다. 두 사이트가 개편됐는데 화이트리스트는 예전 주소를 그대로 들고 있었다. 이 주소를 `sourceUrl`로 쓴 기사는 독자에게 죽은 링크를 내민다.
- **규칙**: 수집 단계에서 출처를 열어보고 죽었으면 `urlStatus: "broken-YYYY-wNN"`과 `seriesNote`를 화이트리스트에 남긴다. 새 경로를 확인하기 전에는 그 출처로 회차를 만들지 않는다 — 결번이 죽은 링크보다 낫다.
- **탐지**: `qa.mjs` — `urlStatus`가 `broken`으로 시작하는 출처를 `sourceUrl`로 쓰면 실패시킨다
- **반영**: `sources/whitelist.json`, `CLAUDE.md` §7.1

### L-013 · 헤드라인 규칙을 절반만 지켜 대상이 익명이 된다

- **발생**: 2026-w31 · OTT
- **상태**: 자동화
- **분류**: 집필
- **증상**: §6은 `고유명사를 헤드라인에 넣지 않는다. 본문에서 밝힌다`인데, 앞 절만 지키고 뒷 절을 빠뜨렸다. OTT 편이 `한 연애 리얼리티의 후속 편성`으로만 불리고 끝까지 작품명이 나오지 않았다. 사용자 평가는 `정보가 검열된 수준`이었다. 헤드라인에서 감추는 건 결과를 먼저 보여주려는 장치지 대상을 숨기려는 게 아니다. 무엇에 주목해야 하는지도 함께 사라진다.
- **규칙**: 티저 또는 `blocks[0]`에서 대상의 이름을 댄다. 작품명은 `〈 〉`(U+3008/3009)로 표기한다. 본문 중 한 문장은 **이 기사에서 볼 것이 무엇인지** 직접 말한다.
- **탐지**: `qa.mjs` — 작품이 주인공인 도메인(movie·ott·book·music·stage)에서 본문·티저에 `〈 〉`가 하나도 없으면 실패시킨다
- **반영**: `CLAUDE.md` §6 헤드라인
