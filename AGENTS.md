# AGENTS.md — 에이전트 5명이 각각 하는 일

한 호를 만드는 일을 다섯으로 나눠 맡겼습니다. **Michael님은 채워 넣지 않고 확인만 하시면 됩니다.**

```
                  ┌─ az-scout (영화) ─┐
  주간 실행 지시 ──┼─ az-scout (음악) ─┼→ az-verify → az-write → az-editor → 최종 승인
                  ├─ az-scout (유튜브)┤   후보별      카테고리별   회차 조립      ↓
                  └─ az-scout (도서) ─┘                                       발행
                                                                               ↓
                                                        발행 후 ── az-retro ──→ LESSONS.md
```

`tools/dashboard.html`을 열면 이 흐름이 지금 어디까지 왔는지 보입니다.

---

## 1. 누가 무엇을 하나요

| 이름 | 하는 일 | 몇 번 도나요 |
|---|---|---|
| `az-scout` | 데이터를 뒤져 후보를 찾고 점수를 매깁니다 | 카테고리마다 1번 (4번) |
| `az-verify` | **후보를 떨어뜨리려고 시도합니다.** 숫자와 날짜를 다시 확인합니다 | 후보마다 1번 |
| `az-write` | 기사 본문을 씁니다 | 카테고리마다 1번 |
| `az-editor` | 1면 기사를 고르고, 못 실을 카테고리를 정하고, 검사를 돌립니다 | 회차마다 1번 |
| `az-retro` | 이번 주에 뭘 놓쳤는지 보고 규칙 추가를 제안합니다 | 회차마다 1번 |

### 찾는 사람과 검증하는 사람을 왜 나눴나요

`az-scout`은 소재를 찾는 게 일이라 **자기가 찾은 걸 좋아하게 됩니다.**
같은 에이전트에게 검증까지 맡기면 자기 후보를 감싸게 됩니다.

그래서 `az-verify`에게는 정반대 임무를 줬습니다. **"이걸 떨어뜨려 보라."**
떨어뜨리려고 해봤는데 실패했을 때만 통과입니다. 애매하면 탈락시킵니다.
둘을 합치면 처음에 났던 개봉일 사고(`LESSONS.md` L-001)가 다시 납니다.

---

## 2. 각자 읽는 문서가 다릅니다

**에이전트는 자기 목록에 있는 문서만 읽습니다.** 전부 읽으면 읽을거리에 밀려 판단이 흐려집니다.

| 에이전트 | 읽는 문서 | 안 읽는 문서 |
|---|---|---|
| `az-scout` | 담당 카테고리 문서(출처·판정 기준), 출처 목록, 3조건 채점표, 관련 교훈 | 디자인 규칙, 다른 카테고리 문서 |
| `az-verify` | `market-scope.md` **전체**, 출처 목록, 담당 카테고리의 판정 기준과 기준일 정의, 관련 교훈 | 디자인 규칙, 글쓰기 규칙 |
| `az-write` | 기사 형식과 문장 규칙, 담당 카테고리의 글쓰기 주의사항, 글자 수 예산, 최근 3주에 쓴 개념 목록, 관련 교훈 | 출처 목록, 다른 카테고리 문서 |
| `az-editor` | 1면 선정 기준, 중단 규칙, 카테고리 목록, 글자 수 예산, 검사 결과 | 원본 데이터 출처 |
| `az-retro` | 이번 주 작업 기록 전체, `LESSONS.md` 전체 | — |

`LESSONS.md`의 각 항목에는 **분류** 표시가 있습니다 (`수집` `선별` `검증` `집필` `해석` `빌드` `운영`).
에이전트는 자기와 관련된 분류만 골라 읽습니다.

---

## 3. 서로 결과를 어떻게 넘기나요

말로 설명하지 않고 **정해진 형식의 파일**로 주고받습니다.
모든 기록은 `runs/YYYY-wNN.json` 한 파일에 쌓입니다.

```
az-scout   → 후보 목록과 점수
az-verify  → 통과·탈락 판정과 그 이유
az-write   → issues/YYYY-wNN.json 에 기사 초안
az-editor  → 1면 순서 정리, 검사 결과
az-retro   → LESSONS.md 에 추가할 항목 제안
```

### 지켜야 할 세 가지

- **앞 사람이 만든 걸 뒤에서 몰래 고치지 않습니다.** `az-write`가 숫자를 바꾸고 싶으면 `az-verify`에게 돌려보냅니다.
- **어떤 교훈을 참고했는지 꼭 적습니다.** 이걸 안 적으면 `az-retro`가 무엇을 자동화할지 판단할 수 없습니다.
- **떨어뜨린 후보도 기록에 남깁니다.** 왜 버렸는지가 다음 주에 쓸모 있는 자산이 됩니다.

---

## 4. Michael님이 판단하셔야 하는 때

에이전트가 알아서 하되, 아래는 **반드시 물어봅니다.**

| 언제 | 무엇을 정하시나요 |
|---|---|
| 후보를 다 모았을 때 | 부고·사고·질병 같은 민감한 소재를 실을지 |
| 회차를 조립했을 때 | 4개 중 2개 이상이 소재를 못 찾았을 때 이번 주를 어떻게 할지 |
| 회차를 조립했을 때 | **최종 승인.** 에이전트는 스스로 발행하지 않습니다 |
| 회고가 끝났을 때 | 새 교훈을 등록할지, 출처 목록을 바꿀지 |
| 언제든 | 데이터 사이트가 바뀌어서 값을 못 읽을 때 |

### 에이전트가 절대 안 하는 것

- **발행.** 회차 상태를 `ready`로 바꾸는 건 승인 후에만 합니다
- 막아둔 출처를 슬쩍 풀어주기
- 디자인 규칙 바꾸기
- `LESSONS.md` 직접 고치기 — **제안만 합니다**

나머지 중단 규칙은 `CLAUDE.md` §7.1에 있습니다.
헷갈리면 진행하지 말고 물어보라고 지시해뒀습니다.

---

## 5. 부르는 법

Cowork나 Claude Code에서 이름을 부르시면 됩니다.

```
"az-scout으로 이번 주 4개 도메인 후보를 모아줘"      → 4개가 동시에 돕니다
"az-verify로 상위 후보들을 검증해줘"                 → 후보마다 하나씩 돕니다
"az-write로 통과한 도메인 스토리를 써줘"
"az-editor로 회차를 조립하고 qa를 돌려줘"
"az-retro로 이번 호 회고해줘"
```

각 단계가 끝나면 `tools/dashboard.html`에서 확인하세요.
직접 검사해보고 싶으시면 `node tools/qa.mjs`를 돌리시면 됩니다.

지시문 원본은 `.claude/agents/` 폴더에 있습니다. 여긴 에이전트에게 주는 명령서라
반말로 쓰여 있고, 숫자 기준과 필드 이름이 정확해야 해서 손대지 않으시는 편이 안전합니다.

---

## 6. 배운 걸 쌓아가는 방식

이 시스템에서 제일 중요한 건 **같은 실수를 두 번 하지 않는 것**입니다.

```
사고가 남 → az-retro가 규칙을 제안 → Michael님이 승인
   → "관찰" 또는 "수동" 상태로 등록
   → 규칙이 분명해지면 자동 검사로 만듦
   → "자동화"로 졸업              ← 여기가 목표
```

**적어두기만 한 교훈은 언젠가 잊힙니다.**
`LESSONS.md`의 목적은 항목을 모으는 게 아니라 **기계가 막는 검사로 바꾸는 것**입니다.
대시보드가 지금 몇 퍼센트나 자동화됐는지 보여줍니다.

`az-retro`는 매주 두 가지를 확인합니다.

1. 이번 주에 **사람이 손으로 잡아낸 게** 있었나 → 자동화할 거리입니다
2. 기존 교훈 중 이번 주에 **또 걸린 게** 있나 → 먼저 졸업시킬 항목입니다
