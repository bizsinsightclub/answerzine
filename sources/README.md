# sources/ — 근거로 쓸 수 있는 출처 목록

`whitelist.json`이 **어떤 사이트를 근거로 쓸 수 있는지**를 정해둔 원본 파일입니다.
제작기와 자동 검사가 이 파일을 읽어서 판정합니다.

---

## 1. 다섯 등급

| 등급 | 기사 근거로 쓸 수 있나요 | 뜻 |
|---|---|---|
| `primary` | ✅ 됩니다 | 그 기관이 직접 집계해서 공개하는 데이터 |
| `conditional` | ⚠️ 조건부 | 언론 기사. **기사 안에 어느 기관 숫자인지 적혀 있을 때만** |
| `secondary` | ❌ 안 됩니다 | 참고용. 본문에서 언급은 하되 근거 링크로는 못 씁니다 |
| `evidence_only` | ❌ 안 됩니다 | 계기 확인용. 판매의 증거는 될 수 없습니다 |
| `blocked` | ❌ 안 됩니다 | 개인 글, 위키, 검색 결과 |

목록에 없는 사이트는 **막힌 것으로 봅니다.** 새로 쓰시려면 먼저 등록해야 합니다 (§4).

### `conditional`이 따로 있는 이유

언론 기사는 대부분 **다른 기관의 숫자를 옮겨 적은 것**입니다.
그러면 진짜 원본은 그 기관의 페이지죠.

기사를 근거로 쓰는 건 **그 기관이 데이터를 공개하지 않아서 기사밖에 없을 때**로 제한합니다.
그런 경우에도 기사 안에 "어디서 집계한 숫자인지"가 적혀 있어야 합니다.

> 유튜브 카테고리는 이 문제를 안고 있습니다.
> 판매를 집계해서 공개하는 곳이 없어서 신문 기사에 기대고 있습니다.
> 자세한 사정은 `domains/youtube.md` §2에 적어뒀습니다.

### `evidence_only`가 따로 있는 이유

유튜브 조회수는 **"봤다"는 증거이지 "샀다"는 증거가 아닙니다.**

이 웹진이 다루는 건 판매입니다. 그래서 조회수는 계기를 확인하는 데만 쓰고,
실제로 팔렸다는 건 별도 출처로 증명해야 합니다.

---

## 2. 국내와 해외 구분

모든 출처에 `market` 표시가 있습니다.

| 값 | 뜻 |
|---|---|
| `KR` | 국내. **기본값이고 이 웹진이 주로 다루는 범위입니다** |
| `OVERSEAS` | 해외. `market-scope.md` §6의 조건을 모두 채울 때만 씁니다 |

**국내 기사에 해외 출처를 섞으면 제작기와 자동 검사가 막습니다.**
처음에 났던 개봉일 사고를 다시 내지 않기 위한 장치입니다.

---

## 3. 무조건 막히는 주소

등급과 상관없이 아래가 주소에 들어 있으면 차단됩니다.

- `/search` — 검색 결과 페이지는 볼 때마다 내용이 바뀌어서 나중에 확인할 수가 없습니다
- `google.com`, `search.naver.com`, `search.daum.net` — 검색 엔진

---

## 3.5 조건부 출처 — 남의 숫자를 옮긴 곳을 쓸 때

원칙은 하나입니다. **집계한 곳의 페이지를 씁니다.**
그런데 집계한 곳이 지난 주를 다시 안 열어주는 경우가 있습니다. 멜론이 그렇습니다.
그 주에 기록해 두지 않았으면 원본으로는 영영 확인할 수 없습니다.

이때만 `conditional`을 씁니다. 네 조건을 **모두** 충족해야 합니다.

1. 어느 집계의 값인지 화면이나 기사에 **명시**돼 있을 것
2. 그 원 출처가 화이트리스트에 **`primary`로 등록**돼 있을 것
3. 본문에서 원 집계 주체를 **이름으로 밝힐 것** — `멜론 주간 차트에 따르면`
4. 원 기관이 그 시점을 다시 열어줄 수 있으면 **원 기관 쪽을 쓸 것**

언론 보도와 차트 아카이브가 여기 해당합니다. 예: 이데일리(예스24 수치 인용), 가이섬(멜론 주간차트 보관).

> ⚠️ **가이섬을 집계 주체로 쓰지 않습니다.** 기사에는 늘 `멜론 주간 차트`라고 씁니다.
> 2차 가공은 원본과 어긋날 수 있고 사이트가 사라지면 링크도 죽습니다.
> 그래서 `tools/collect.mjs`가 매주 떠서 `runs/raw/`에 남깁니다 — 그쪽은 우리 것입니다.

---

## 4. 새 출처 추가하기

### 먼저 확인할 것

1. **직접 집계하는 곳인가요?** 남의 숫자를 옮긴 곳이면 원래 기관을 찾아야 합니다
2. **매주 같은 방식으로 볼 수 있나요?** 한 번짜리 보도자료는 등록 대상이 아닙니다
3. **로그인이나 결제 없이 열리나요?** 독자가 링크를 눌러 확인할 수 없으면 근거로 못 씁니다

### 등록하기

`whitelist.json`의 `sources` 목록에 한 줄 추가하시면 됩니다.

```json
{
  "host": "example.co.kr",
  "name": "화면에 표시할 이름",
  "tier": "primary",
  "market": "KR",
  "domains": ["book"],
  "reads": "이 사이트에서 무슨 값을 읽는지",
  "url": "https://…",
  "sourceLabel": "… 확인하기",
  "note": "주의할 점"
}
```

그리고 두 가지를 함께 해주세요.

- 파일 위쪽의 `version`과 `updated` 날짜를 바꿉니다
- 해당 `domains/<이름>.md`의 데이터 출처 표에도 추가합니다

### 등급 올리기

`secondary`를 `primary`로 올리는 건 그 사이트가 **숫자를 명시한 공식 발표**를 시작했을 때만 합니다.
예를 들어 올리브영이 주간 판매 순위를 숫자와 함께 공개하기 시작하면 올릴 수 있습니다.

---

## 5. 관리

- **분기에 한 번 점검하세요.** 주소가 바뀌거나 서비스가 없어지거나 유료로 바뀝니다
- 출처를 못 읽게 되면 그 주는 그 카테고리를 쉬고 알려달라고 에이전트에게 지시해뒀습니다
- **막아둔 사이트를 푸는 건 Michael님만 하십니다.** 에이전트는 임의로 풀지 않습니다
- 등급을 낮추는 건(더 엄격하게) 에이전트가 판단해도 되지만, 이유를 `note`에 남기게 했습니다

---

## 5.5 보류 중인 출처 — 검증 전까지 등록하지 않음

"ANZINE — Official Data Sources & Comparative Chart Integration" 문서가 5개 후보
(KOBIC·Circle Chart·Spotify Charts·YouTube Music Charts·YouTube Data API)를
검토하라고 여러 차례 요청했다. 2026-08-13 재확인 결과 **4개는 이미 등록돼 있다**
(아래 표, 등록 경위 포함) — 처음 문의에 "5개 다 미등록"이라고 잘못 답한 적이
있어 바로잡는다. 실제로 미등록·검증 전인 건 1개뿐이다.

| 출처명 | 목적 | 공식 URL | 필요 데이터 | 검증 상태 | 비고 |
|---|---|---|---|---|---|
| KOBIC | 도서 보조 출처 | https://www.kobic.net | 출판 통계·서지 정보 | ✅ 등록됨(`primary` tier), **의도적으로 도메인 1차로는 안 씀** | 2026-08-07 사용자 제공 Whitelist.xlsx로 등록. `book.md` §2.5에서 이유 확정(2026-08-13) — 데이터 범위가 "베스트셀러 순위"가 아니라 "출판 통계"라 이 도메인 용도와 안 맞고, 실제 페이지도 아직 못 열어봤다. `registry.json` `book.primarySources`에 추가하지 않기로 결정 |
| Circle Chart | 음악 1차 출처 | https://circlechart.kr | 국내 통합 음원·음반 집계 | ✅ 등록됨(`primary`), **코드는 완성, 실수집은 0회** | `registry.json` `music.primarySources`에 있고 `tools/collect.mjs`에 Playwright 어댑터도 있다 — 그러나 `runs/raw/`에 `circlechart.json`이 한 번도 생성된 적 없다(egress 차단, KOBIS도 같은 세션에서 동일하게 막힘). `music.md` §2·§2.5에서 사용 규칙 확정(2026-08-13) — 문서(구 "보조")·registry(primary)가 어긋나 있던 걸 문서를 registry에 맞춰 바로잡았다 |
| Spotify Charts | 음악 보조 출처(해외) | https://charts.spotify.com | 글로벌/국가별 스트리밍 차트 | ✅ 등록됨(`primary`, market: OVERSEAS) | 국내 서사에 못 씀(`market-scope.md` §6). `music.md`에 전용 절 신설해 역할 확정(2026-08-13) — 스트림을 매출로 임의 환산 금지 포함. 수집기는 아직 없음 |
| YouTube Data API | 유튜브 계기 측 보조 출처 | https://developers.google.com/youtube/v3 | 조회수·좋아요·구독자 등 공식 API 지표 | ✅ 등록됨(`primary`) | 2026-08-07 사용자 제공 Whitelist.xlsx로 등록. API 키·코드 필요해 자동 어댑터 없음(사람이 콘솔에서 수동 조회). 조회수는 `evidence_only`와 동급 취급 — "샀다"의 증거 아님. `youtube.md`에서 trend·추정 매출 금지 명시(2026-08-13) |
| **YouTube Music Charts** | 유튜브/음악 보조 출처 | https://charts.youtube.com | 인기곡·뮤직비디오 순위, 주간/일간 차트 변동 | ⏳ **PENDING / EGRESS_BLOCKED** | 아래 참고 — 이번 라운드에서도 상태 변화 없음 |

> **2026-08-13 후속 — 소스 역할(role) 정의 확정.** 위 표는 "화이트리스트에 등록됐는가"만
> 판정한다. **"등록됨"이 "그 도메인에서 실제로 쓸 수 있게 준비됐다"는 뜻은 아니다** —
> az-verify 감사에서 Circle Chart·KOBIC이 정확히 이 차이로 문서·registry가 어긋나 있었다.
> 각 소스의 실제 사용 규칙(언제 `sourceUrl`로 쓰는지, 언제 `trend`로 쓰는지, 수집기
> 유무)은 이제 해당 도메인 문서(`domains/music.md` §2·§2.5, `domains/book.md` §2.5,
> `domains/youtube.md` YouTube Data API 절)에 있다 — 이 표를 보충 설명 없이 "primary니까
> 바로 쓸 수 있다"로 읽지 않는다.

### YouTube Music Charts — 미등록 사유와 재개 절차

**검증 전까지 등록하지 않는다.** URL이 존재한다는 것만으로 등재하지 않고,
접근성·데이터 존재를 지어내지 않는다(§4 "먼저 확인할 것" 원칙 그대로).

- **막힌 지점:** 1단계("URL을 직접 연다")부터 막혀 있다. 이 세션(그리고 이전
  여러 세션)에서 egress가 전면 차단돼 있고, 대조군으로 이미 등록된 `kobis.or.kr`
  조차 같은 세션에서는 열리지 않는다 — `charts.youtube.com`만의 문제가 아니라
  이 실행 환경 자체의 네트워크 제한이다. 2026-08-13 `WebFetch`로 재확인:
  `EGRESS_BLOCKED`.
- **등록 전 확인해야 할 것(§4 체크리스트 그대로):**
  1. URL을 직접 열어 접근 가능한지 확인
  2. 실제로 인기곡·뮤직비디오 순위 데이터가 있는지 확인
  3. 매주 같은 방식으로 다시 열 수 있는지(주차 아카이브 여부, `series` 등급 판정)
  4. 로그인·결제 없이 열리는지
  5. 전부 통과하면 `sources/whitelist.json`에 `host: "charts.youtube.com"`으로
     등재하고, 이 표의 해당 행을 지운다
- **재개 방법:** 네트워크가 열리는 세션에서 `curl -sS "$HTTPS_PROXY/__agentproxy/status"`로
  이 세션과 같은 패턴(대조군 `kobis.or.kr`까지 막혀 있는지)부터 확인한 뒤,
  대조군이 뚫려 있으면 위 체크리스트를 그대로 밟는다.

---

## 6. 지금 등록된 것

| 등급 | 개수 | 주요 출처 |
|---|---|---|
| `primary` (국내) | 8 | 예스24, 멜론, 써클차트, KOBIS, 영진위, 출판진흥원, 통계청, 정책브리핑 |
| `primary` (해외) | 4 | Box Office Mojo, The Numbers, Billboard, Spotify Charts |
| `conditional` | 5 | 연합뉴스, 뉴스1, 아시아경제, 매일경제, 한국경제 |
| `secondary` | 10 | 교보문고, 알라딘, 지니, 벅스, CGV·롯데·메가박스, 데이터랩, 올리브영, 다이소 |
| `evidence_only` | 1 | 유튜브 |
| `blocked` | 11 | 나무위키, 위키백과, 블로그 3곳, 커뮤니티 4곳, SNS 2곳 |

### 카테고리별로 믿을 만한 출처가 있나요

| 카테고리 | 1차 출처 | 상태 |
|---|---|---|
| 도서 | 예스24, 출판진흥원 | ✅ 충분합니다 |
| 음악 | 멜론, 써클차트 | ✅ 충분합니다 |
| 영화 | KOBIS, 영진위 | ✅ 네 개 중 가장 든든합니다 |
| 유튜브 | **없습니다** | ❌ 구조적 한계 — `domains/youtube.md` §2 |
