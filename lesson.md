# lesson.md — 교훈·설계결정 로그

이 프로젝트에서 배운 것과 설계 결정의 이유를 남긴다. 최신 항목이 위로 온다.
`CLAUDE.md`가 "지금 무엇을 어떻게" 라면, 이 문서는 "왜 그렇게 됐나"다.

---

## 2026-09-07 · 방향 전면 전환: 발산 엔진 → 트렌드 보드

**무엇이 바뀌었나**
- 이전: 이슈 어휘를 충돌시켜 트렌드 워딩을 대량 발산하는 파이썬 파이프라인(수집→어휘추출→발산×9→정리). 재료 수집에 투자가 크고, 발산 결과에서 사람이 하나 건지는 구조였다.
- 이후: 존재하는 랭킹을 모아 "왜 떴나 / 무엇이 돈이 되나"를 광고대행사 시점으로 읽는 **주간 소비 트렌드 보드**. 생성의 축이 "새 단어를 만든다"에서 "이미 일어난 소비를 해석한다"로 옮겨갔다.
- 옛 코드(`scripts/`·`prompts/`·`web/`·`data/lenses`·`data/lexicon`·`config.yaml` 등)는 삭제했다. git 이력에는 남아 있다.

**그대로 남은 DNA**
- 제일기획 기획자의 냉정한 목소리(옛 `brief_planner.md`의 "그래서 우리는"이 Client Connect로 이어졌다).
- 번역체 제거 감각(옛 `_korean.md`). 문서·프롬프트 모두 어색한 번역투를 피한다.
- "냉소의 과녁은 사람이 아니라 구조" — 옛 CLAUDE.md 9장에서 이어진다.

---

## 2026-09-07 · 수집기(`collect.py`) 구현 · 첫 실행에서 배운 것

- **보드 업로드는 매트릭스 전체를 갈아끼운다.** 그래서 수집기는 자동 열만 담으면 안 되고, 수동 카테고리까지 다 든 xlsx를 내야 한다. 수동 열은 하드코딩해 중복시키지 않고 **현재 보드 HTML 의 `DEMO_MATRIX` 를 런타임에 파싱해** 옮긴다(단일 출처). `const DEMO_MATRIX = {` ~ `\n};` 사이를 잘라 `categories`/`rows` 를 `json.loads` 하면 된다 — JS 배열이 그대로 유효한 JSON이다.
- **Firecrawl v2 JSON 추출로 통일했다.** 멜론·교보·인터파크·넷플릭스마다 HTML 파서를 짜는 대신 `POST /v2/scrape` 에 `formats:[{type:"json", prompt, schema}]` 하나로 순위 목록을 받는다. KOBIS는 공식 JSON API라 여기서 뺐다 — 스크래핑할 이유가 없다.
- **6/7은 깨끗, 멜론은 봇 게이트 → 유튜브 차트로 교체.** KOBIS·인터파크·교보는 정확했다. 멜론은 봇에게 엉뚱한 차트를 줬다(가수 자리에 '새진입·순위상승수N' 변동 표시). 2번 시도 후 멈추고(토끼굴 신호) 출처를 `charts.youtube.com/charts/TopSongs/kr/weekly` 로 바꾸니 한 번에 실제 아이돌 차트가 나왔다 — **막힌 출처를 붙잡고 파느니 출처를 바꾸는 게 싸다.**
- **Firecrawl `location` 로 로케일을 준다.** `{"country":"KR","languages":["ko"]}` 를 주면 유튜브 차트 아티스트명이 한글로 온다. 단, 넷플릭스 Tudum 은 로케일 KR/ko 를 줘도 영어였고 FlixPatrol 도 영어였다 — 한 출처로 한글 넷플릭스 제목을 얻는 길은 없다.
- **넷플릭스 한글은 "검증되면 쓰고 아니면 영어"로만 안전하다.** Tudum 링크의 `{id}` 를 `kr/title/{id}` 로 되짚으면, 영화·리미티드는 맞지만(들쥐·포핸즈) 시리즈는 id 가 회차라 엉뚱한 작품으로 풀린다(I am Solo→마지막 전사). `/watch/{id}` 를 로그아웃 상태로 열면 가입 페이지로 튕겨 홍보작이 잡힌다(Your Honor→우주에서 길을 잃다). 직접 GET 은 og:title 이 "Netflix"뿐(클라이언트 렌더). → **그 페이지의 영어 제목이 Tudum 제목과 맞을 때만 한글을 믿는 검증 게이트**를 뒀다. 절대 틀린 제목을 싣지 않지만, 넷플릭스 렌더가 들쭉날쭉해 회당 2~5개만 한글로 풀린다. 완벽한 한글화는 이 공개 경로의 천장 밖이다 — 나머지는 사람이 보드에서 채운다.
- **윈도우 콘솔은 cp949다.** 한글·em대시 출력이 `UnicodeEncodeError` 를 낸다. `sys.stdout.reconfigure(encoding="utf-8")` 로 열어 둔다.
- **`.env` 키 이름 오탈자(`FIRECRWAL_API`)를 그대로 받았다.** 코드에서 `FIRECRWAL_API` 를 먼저 보고 `FIRECRAWL_API` 를 폴백으로 둔다 — 키 파일을 고치기보다 코드가 관대한 게 안전하다.
- 의존성은 `requests` + `openpyxl` 둘뿐. `python-dotenv` 도 안 쓰고 `.env` 를 직접 몇 줄로 읽는다.

## 2026-09-08 · 수집기 확장 (7→12 자동, 수동은 4개만)

- **규칙이 바뀌었다: 무신사·KREAM·렉스몬드·Jente 만 수동, 나머지 전부 자동.** 지상파·케이블 TV(Nielsen), 유튜브 영상(Playboard), 유튜브 검색(Google Trends RSS), 네이버 웹툰(comic.naver.com)을 붙였다. 다섯 다 첫 실측에서 깨끗했다.
- **스크래핑만 있는 게 아니다.** 유튜브 검색은 Google Trends **RSS(XML)** 라 Firecrawl 이 아니라 `requests`+`xml.etree` 로 받는다 — 공식 피드가 있으면 LLM 추출보다 그게 싸고 정확하다(KOBIS API 와 같은 판단).
- **Trends RSS 는 `<item>/<title>` 만.** 한 `<item>` 에 검색어(title)와 관련 뉴스 제목(`ht:news_item_title`)이 섞여 있어, 전부 긁으면 뉴스 헤드라인이 검색어인 척 들어온다. `findall(".//item")` 후 `item.find("title")` 로 좁힌다.
- **Nielsen 표기 청소.** `채널장르(프로그램명)<본>` → `<...>` 지우고 끝의 `(제목)` 만 남긴다(`_clean_tv`). 채널·장르 접두어는 제목이 아니다.
- **구글 플레이 앱·게임 — 출처 바꿔서 해결.** Play Store `store/apps`(범용 목록)·`thelog.co.kr`(터널 오류)를 버리고 **MobileIndex·게임메카**로 갈아탔다. 게임메카는 wait 를 8초로 늘리니 5개→10개로 다 떴다. 앱은 MobileIndex 주간 사용자 순위 200행을 받아 **상승률 내림차순 상위 10**을 코드에서 골랐다(순위 그대로가 아니라 사용자가 정한 기준) — 결과가 정부24·올리브영·하나은행 등 DEMO 결과 그대로였다. 최종 자동 14 / 수동 4.
- **Excel 락.** `week{n}.xlsx` 를 엑셀에서 열어 두면 저장이 `PermissionError` 로 막힌다 — 수집기 돌리기 전 파일을 닫아야 한다.
- **메일은 funtime 자격증명을 빌린다.** 새 Gmail 키를 만들지 않고 `C:\pjt\funtime\.env` 의 `GMAIL_USER`·`GMAIL_APP_PASSWORD` 를 재사용(`nara/report/notify.py` 와 같은 패턴, `SMTP_SSL smtp.gmail.com:465`, `EmailMessage.add_attachment`). `--email` 플래그로만 보내 수동 실행은 스팸이 안 되고, 스케줄러(`run_collect.bat`)만 `--email` 을 붙인다. 검증은 **로그인까지만**(제3자에게 테스트 메일을 쏘지 않으려고 send 는 생략).
- **출처는 데이터 밑(13행)에.** 보드가 2~11행만 읽으니 12행 아래는 안전하다 — 열별 출처(`SOURCE_LABEL`)와 수집 시각을 거기 적는다.

## 트렌드 보드 HTML에 박혀 있는 설계 결정

`trendboard_week36.html`을 다시 손댈 때 이 이유들을 알고 건드린다.

- **순위 변동은 추정이 아니라 뺄셈이다.** 지난주 스냅샷을 localStorage(`PREV_KEY`)에 남겨 이번 주와 제목 기준으로 대조한다. LLM에게 "올랐을까 내렸을까"를 묻지 않는다 — 근거 없는 상승/하락 서사가 붙는 걸 막기 위해서다.

- **같은 대상 여러 표기는 원본을 유지하고 그룹만 묶는다.** ("민주당 티비" / "민주당 tv" 등) 데이터를 고쳐 하나로 합치면 출처의 원본을 잃는다. 화면에서 variant 그룹으로 묶어 보여줄 뿐이다.

- **카테고리 이유 호출은 병렬이다.** 한동안 "순차 호출이 안전하겠지" 싶어 하나씩 부르게 바꿨는데, 실제 회귀 원인은 따로(있던 `signal` 옵션) 있었고 순차 호출은 전체 대기시간만 최대 5배로 늘렸을 뿐이었다. → 증상이 아니라 원인을 고친다. 지금은 다시 병렬(`Promise.all`).

- **`window.claude` 런타임에 의존한다.** 이유·토플라인 생성(`sample`)과 파일 저장(`downloads`)은 Artifact로 발행해야 산다. 로컬로 열면 미리 채운 기본 차트만 보인다. "왜 분석 버튼이 안 먹지"의 답은 대개 이것이다.

- **기본 차트를 바꾸면 `STORAGE_KEY` 버전을 올린다.** 안 그러면 그 브라우저에 저장돼 있던 낡은 업로드 데이터가 새 기본 차트를 계속 가린다.

- **취향 학습은 서버로 안 간다.** 묶음 카드의 좋아요/싫어요는 localStorage에만 쌓여 다음 프롬프트에 실린다. 모델을 학습시키는 게 아니라 "이런 걸 좋아했다"를 매번 알려 주는 방식이다.

- **어투 규칙(Client Connect `CC_RULES`).** 소비 트렌드 분석서의 평서문("~다"), 구어체·존댓말 금지. 매니페스토는 기·승·전·결 + 실행안 3개. 승 문단만 짧은 의문문 허용. 형용 과장("혁신적인"·"압도적인"·"게임체인저") 금지.

---

## 수집기 만들 때 미리 새길 것 (§CLAUDE.md 6·7)

- 수집기는 순위·항목 텍스트를 **있는 그대로** 옮긴다. 다듬는 순간 보드의 "원본 유지" 원칙이 깨진다.
- 랭킹은 시점에 흔들린다 → **오전 10시 기준**으로 못 박는다.
- KOBIS는 공식 JSON API다. 스크래핑 대상(멜론·교보·인터파크·넷플릭스)과 섞어 firecrawl/exa로 처리하려 하지 말고 API로 바로 받는다.
- 실패한 출처는 건너뛰고, 그 열은 사람이 채운다. 개수 맞추려 억지 재시도하지 않는다.
