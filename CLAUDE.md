# ISSUE EMERGENCE ENGINE — CLAUDE.md

## 0. 이 문서의 역할

이 저장소에서 작업하는 Claude Code가 따르는 유일한 지침이다.
제품 정의, 파이프라인, 코드 규칙, 금지 사항을 담는다.
판단이 갈리면 이 문서가 우선한다.

---

## 1. 제품 정의

**한 줄 정의**
특정 기간의 이슈를 재료로, 그 시기에 활성화된 어휘를 충돌시켜 트렌드 워딩 후보를 대량으로 발산하는 장치.

**이것은 무엇이 아닌가**
- 심사기가 아니다. 점수를 매기지 않는다.
- 정답기가 아니다. 하나를 고르지 않는다.
- 슬로건·카피·헤드라인 생성기가 아니다.
- 연관어 두 개를 붙이는 조합기가 아니다.

**성공 기준**
이슈 하나당 150~200개를 발산했을 때, 사람이 훑어보고 "오." 하는 게 1개 이상 나온다.
고르는 것은 사람이 한다. 시스템은 재료를 잘 모으고 폭넓게 던지는 데 집중한다.

---

## 2. 핵심 원리

### 2.1 창발의 원료는 기발함이 아니라 "지금 뜨거운 단어"다
'애국노'가 강했던 이유는 그 시점에 '매국노'가 논란으로 뜨겁게 활성화돼 있었기 때문이다.
따라서 이 시스템의 투자처는 생성 기교가 아니라 **활성 어휘 수집**이다.
활성 어휘 표가 좋으면 발산은 자동으로 좋아진다.

### 2.2 의미 정합이 아니라 의미 충돌
- 약함: 국도 + 낭만 → 자연스럽게 어울린다 → "그렇지."
- 강함: 익산 + 오디세이 / 조선 + 롤스로이스 → 같은 개념 공간에 있으면 안 되는 조합 → "왜?"
두 단어가 서로 설명해 주면 실패다. 서로 충돌한 뒤 이슈가 그 충돌을 풀어야 한다.

### 2.3 0.5초 원칙
낯설다 → 알아본다 → 만족한다.
첫 반응이 "응?"이어야 하고, 이슈를 알면 "아."로 바뀌어야 한다.
"응?"이 계속되면 난해한 것이고, 처음부터 "그렇지."면 심심한 것이다.

### 2.4 강한 원본 어휘를 빌려온다
후보의 힘은 대부분 빌려온 단어에서 온다.
욕설·찬사·정치어·역사어·명품·신화·종교·범죄어·관계어·군사어·법률어·인터넷 은어는 이미 감정 에너지를 갖고 있다.
중립어(이탈)보다 서사가 실린 단어(외도)가 낫다.

### 2.5 사건이 아니라 현상을 부른다
- 사건: "개그맨이 노래를 했다"
- 현상: "개그맨이 개그가 아닌 곳에서 갑자기 주목받는다"
이름은 두 번째를 겨냥한다. 사건 키워드를 그대로 담은 이름(제주살인, 유튜브퇴직)은 서술이지 발견이 아니다.

### 2.6 기준 사례 (참고용이며 채점용이 아님)
| 사례 | 메커니즘 | 힘의 근원 |
|---|---|---|
| 애국노 | 최소 변형 | 매국노의 한 글자 반전, 당시 논란으로 원본이 활성화 |
| 조선 롤스로이스 | 지위 충돌 | 지역·전통 정체성 × 세계적 위신 상징 |
| 개그외도 | 정체성 충돌 | 개그맨 × 노래 × 외도의 감정 서사 |
| 익산 오디세이 | 의미 충돌 | 평범한 지명 × 서사시의 의미장 |
| 국회차이나 | 이중 독해 | China / "차이가 나" 동시 읽힘 |

이 사례들은 발산 프롬프트에 "이런 결의 것"으로만 쓴다. 후보를 이것과 비교해 점수 매기지 않는다.

---

## 3. 저장소 구조

```
issue-emergence/
├── CLAUDE.md
├── run.bat                     # 두 번 눌러 화면 열기 (Windows)
├── config.yaml                 # 기간, 이슈 수, 발산 수, 모델명
├── .env                        # API 키 (커밋 금지)
├── data/
│   ├── discover/{run_id}.json  # 자동으로 찾아 온 이슈 후보
│   ├── lenses/LENS-0X.md       # magilite에서 가져온 렌즈 사본 (원본은 magilite가 주인)
│   ├── lenses/voice/LENS-0X.md # 그 사람의 말버릇·시그니처 무브 (글쓰기에만 쓴다)
│   ├── raw/{run_id}/           # 수집 원본 (json)
│   ├── vocab/{run_id}/         # 이슈별 활성 어휘 표 (csv)
│   ├── lexicon/
│   │   └── strong_words.yaml   # 강한 원본 어휘 풀 (상시 유지)
│   └── out/{run_id}/           # 발산 결과 (csv, md)
├── prompts/
│   ├── discover_issues.md      # 이슈 후보 찾기
│   ├── select_best.md          # 하나 고르기 (뒤샹 기준)
│   ├── write_column.md         # 기사 쓰기 (렌즈의 목소리)
│   ├── brief_planner.md        # 그래서 우리는 (기획자의 목소리)
│   ├── _korean.md              # 번역체 제거 규칙 (한국어 산문 프롬프트에 항상 붙인다)
│   ├── vocab_extract.md        # 활성 어휘 추출
│   ├── mech_01_minimal_mutation.md
│   ├── mech_02_emotional_reversal.md
│   ├── mech_03_semantic_diagonal.md
│   ├── mech_04_identity_collision.md
│   ├── mech_05_status_collision.md
│   ├── mech_06_double_reading.md
│   ├── mech_07_meme_mining.md
│   ├── mech_08_cultural_translation.md
│   └── mech_09_cross_issue.md
├── scripts/
│   ├── discover.py             # 이슈 후보 찾기 (사람이 고른다)
│   ├── collect_news.py         # 1단계부터 사용
│   ├── collect_youtube.py
│   ├── extract_vocab.py
│   ├── generate.py
│   ├── filter.py
│   ├── pick.py                 # 편집 — 하나 고르고 글쓰기 (파이프라인 밖, 4.8)
│   ├── sync_lenses.py          # magilite에서 렌즈 가져오기
│   ├── llm.py                  # 제공자 계층 (claude_agent / gemini / anthropic)
│   ├── common.py               # 설정·로그·JSON 파싱
│   ├── serve.py                # 로컬 웹 화면 (표준 라이브러리만)
│   ├── webapi.py               # 웹 화면이 부르는 일들
│   └── run.py                  # 전체 실행 (터미널)
├── web/                        # 화면 — index.html / app.js / style.css (빌드 없음)
└── runs/{run_id}.log
```

`run_id`는 `YYYYMMDD-HHMM` 형식. 한 번의 실행이 하나의 폴더로 남는다.

---

## 4. 파이프라인

```
[이슈 지정] → [수집] → [활성 어휘 추출] → [발산 ×9] → [자동 정리] → [표 출력] → 사람
```

### 4.1 이슈 지정
- `discover.py`가 이슈 후보 8~12개를 찾아 늘어놓는다. **고르는 것은 사람이 한다. 자동 선정은 하지 않는다.**
- 사람이 직접 `config.yaml`에 적어도 된다. 화면에서 고른 것도 결국 여기에 저장된다.
- **1단계 이후**: `collect_news.py`가 기간 내 기사량 급증 키워드도 함께 제시한다.
- 이슈는 결과물에 `source: user`로 표시한다. 후보를 제시한 것과 고른 것은 다르며, 자율 발견인 것처럼 꾸미지 않는다.

**`discover.py`가 하는 일**

두 가지 방식이 있다. 기간을 주면 두 번째로 돈다.

| 방식 | 재료 | 쿼터 | 언제 |
|---|---|---|---|
| 지금 뜨는 것 (기본) | 인기 차트 3종 — 뉴스·정치(25), 연예(24), 전체 | 3유닛 | 오늘·어제 것을 볼 때 |
| 기간 정해서 | 씨앗 검색어(`discover.seed_queries`)로 그 기간의 영상을 조회수 순으로 검색 | 검색어 하나당 100유닛 | 지난주·지난달을 되짚을 때 |

- 기간 방식은 `search.list`에 `publishedAfter`/`publishedBefore`를 건다. `q` 없이 카테고리만으로는 결과가 0이므로 씨앗 검색어가 반드시 있어야 한다.
- 영상 제목을 LLM에 넘겨 **반복되는 사안**만 이슈로 묶게 한다. 한 채널만 다룬 단발성 화제는 버린다.
- 이슈마다 `keyword`(유튜브 검색어), `proper_nouns`(자동 정리에 쓸 고유명사), `mentions`(그 사안을 다룬 영상 수)를 함께 낸다.
- `mentions`는 개수이지 점수가 아니다. 후보를 줄 세우지 않는다.

### 4.2 수집

**네이버 뉴스 검색 API** (`collect_news.py`)
- 이슈 키워드로 기간 내 기사 제목·요약 수집. 최대 300건.
- 1단계부터: 최근 N일 기사량을 직전 N일과 비교해 급증률 상위 키워드를 산출한다.

**YouTube Data API v3** (`collect_youtube.py`)
- 이슈 키워드로 검색 → 조회수 상위 영상 10개 → 각 영상 댓글 100개.
- 방송사 뉴스 채널(KBS·MBC·SBS·JTBC·YTN 등) 영상을 우선한다. 이것이 뉴스 댓글의 대체재다.
- 영상 제목도 함께 저장한다. 제목은 제작자가 이미 압축한 언어다.
- 쿼터: 검색 100유닛, 댓글 1유닛. 이슈당 약 1,100유닛. 하루 8개 이슈까지 안전하다.
- 좋아요 수를 함께 저장한다. 어휘 추출 시 가중치로 쓴다.

**수집하지 않는 것**
- 커뮤니티(디시·에펨·더쿠 등) 크롤링. 하지 않는다.
- 네이버 뉴스 댓글. 하지 않는다.
- 검색 트렌드. 공식 경로가 없으므로 기사량 급증으로 대체한다.

### 4.3 활성 어휘 추출 (`extract_vocab.py`, LLM 1회/이슈)
입력: 기사 제목 300개 + 영상 제목 10개 + 댓글 1,000개
출력: 이슈당 20~30행의 표

| 열 | 내용 |
|---|---|
| expression | 단어 또는 짧은 표현 |
| source | news / title / comment |
| frequency | 등장 횟수 (댓글은 좋아요 가중) |
| polarity | 분노 / 조롱 / 감탄 / 연민 / 중립 |
| type | 별명 / 비유 / 욕 / 찬사 / 밈 / 사건어 / 반복구 |
| note | 왜 이 표현이 반복되는지 한 줄 |

`prompts/vocab_extract.md` 지시 요지:
- 사건 자체를 요약하지 말고, 사람들이 **어떤 말로** 반응하는지만 뽑는다.
- 이미 별명·밈이 된 표현은 그대로 적는다. 다듬지 않는다.
- 감정이 실린 표현을 중립 표현보다 우선한다.
- 기사에만 있고 댓글에 없는 단어는 `source: news`로만 표시하고 낮게 둔다.

### 4.4 강한 원본 어휘 풀 (`data/lexicon/strong_words.yaml`)
상시 유지하는 목록. 이슈와 무관하게 감정 에너지를 가진 단어들.

```yaml
욕설_비하: [매국노, 역적, 배신자, 사기꾼, 위선자, 기생충, ...]
찬사_영웅: [영웅, 의인, 구국, 전설, 명장, ...]
정치_역사: [친일, 쿠데타, 탄핵, 계엄, 독립군, 밀정, ...]
명품_위신: [롤스로이스, 에르메스, 샤넬, 페라리, ...]
신화_종교: [오디세이, 부활, 심판, 순교, 창세기, ...]
범죄_법률: [공범, 사주, 은닉, 무기징역, 알리바이, ...]
관계_서사: [외도, 이혼, 재혼, 짝사랑, 파혼, ...]
군사: [전면전, 항복, 탈영, 참전, 휴전, ...]
인터넷_은어: [무새, 사이다, 고구마, 손절, 억까, ...]
```

- 초기 목록은 사람이 카테고리당 10~15개를 손으로 채운다.
- 발산 결과에서 잘 작동한 원본 단어를 발견하면 추가한다.
- 단, 같은 단어가 여러 라운드에 걸쳐 반복 등장하면 프롬프트에서 **제외 목록**으로 넣는다 (2.6 참조, '외도 무새' 방지).

### 4.5 발산 (`generate.py`, LLM 9회/이슈)

**규칙**
- 메커니즘 하나당 호출 하나. 한 호출에 메커니즘을 두 개 이상 넣지 않는다.
- 각 호출은 해당 메커니즘의 프롬프트 파일만 읽는다. 다른 메커니즘 설명을 섞지 않는다.
- 호출당 15~25개 생성. 이슈당 합계 150~200개.
- 온도는 높게 (1.0 이상). 다양성이 우선이다.
- 생성 수가 미달이어도 재호출하지 않는다. 억지로 채우지 않는다.

**공통 입력**
- 이슈 요약 5줄 (사건·구도·쟁점)
- 활성 어휘 표 (4.3)
- 강한 원본 어휘 풀 (4.4)
- 제외 단어 목록 (직전 3라운드에서 3회 이상 쓰인 원본 단어)

**출력 형식 (JSON 강제)**
```json
[
  {
    "candidate": "후보 표현",
    "mechanism": "mech_01",
    "source_word": "빌려온 원본 단어",
    "active_word": "이슈에서 가져온 활성 단어",
    "one_line": "20자 이내, 왜 이 조합인지"
  }
]
```

`one_line`은 설명이 아니라 메모다. 약한 후보를 설명으로 살리려 하지 않는다.
파싱 실패 시 1회만 재시도한다.

### 4.6 자동 정리 (`filter.py`, 코드만 사용, LLM 없음)
삭제 규칙 세 가지뿐이다. 그 외에는 걸러내지 않는다.

1. 완전 중복 (공백·조사 차이 무시)
2. 이슈의 고유명사·사건명을 **그대로** 포함한 것 (예: 인물명, 지명, 기관명이 원형 그대로)
3. 5어절 초과

삭제된 건수는 로그에만 남긴다. 품질 판단은 하지 않는다.

### 4.7 출력
발산 중에는 `data/out/{run_id}/stream.jsonl` 에 후보를 만드는 족족 한 줄씩 덧붙인다.
화면이 이 파일을 읽어 실시간으로 쌓아 보여준다. 끝날 때까지 기다리게 하지 않는다.

최종 결과는 `data/out/{run_id}/candidates.csv` 와 `candidates.md`
- 열: candidate, mechanism, source_word, active_word, one_line, issue
- 정렬: 이슈 → 메커니즘 → 생성 순서. 점수 열은 없다.
- md 파일은 사람이 폰에서 훑기 좋게 메커니즘별 소제목 + 후보만 나열한다.

### 4.8 편집 (`pick.py`) — 발산 밖의 별도 단계

**이 단계는 파이프라인이 아니다.** 사람이 시킬 때만 돈다.

1장은 "정답기가 아니다, 하나를 고르지 않는다"이고 8장은 "점수를 매기지 않는다"이다. 그 원칙은 그대로다.
발산 엔진은 여전히 후보에 숫자를 붙이지 않고 줄 세우지 않는다.
`pick.py`는 그 뒤에 붙는 **편집자의 손**이며, 여기서 고른 결과는 파이프라인으로 되먹여지지 않는다.
제외 단어 목록도 `candidates.csv`만 보지 `pick.json`은 보지 않는다.

**고르는 기준 — 뒤샹의 카테고리 전복**
- 예쁘거나 재치 있는 것(망막적인 것)이 아니라, 무언가의 **존재 지위를 바꾸는** 것을 고른다.
- 심판 축은 `PREDICTABLE` / `FORCED` / `ALIEN` 기각, `STRANGE-BUT-USABLE` 생존. 2.3의 0.5초 원칙과 같은 말이다.
- 이 기준은 magilite의 LOKI 모듈(CARD-2 카테고리 전복 + 거리 심판)에서 가져왔다.

**렌즈 인용**
- `data/lenses/`의 렌즈 카드는 magilite `lenses/`에서 가져온 사본이다. **원본의 주인은 magilite다.** 여기서 고치지 않는다.
- 새로 가져오려면 `python scripts/sync_lenses.py`.
- 인용은 LLM이 **번호로 짚고**(`quote_no`), 실제 문장은 코드가 카드에서 꺼내 붙인다. 베껴 쓰게 하면 틀리거나 지어낸다.
- 번호가 맞지 않으면 인용을 빼고 간다. **지어낸 말은 절대 싣지 않는다.**
- 화면에는 한국어 번역(`quote_ko`)을 크게, 원문을 작게 함께 싣는다. 원문 없이 번역만 싣지 않는다.

**글**
- 2,800~3,500자. 리드 → `## Chapter 1~3` → 마무리. 롱블랙 기사처럼 하나의 글로 읽혀야 한다.
- 2,400자에 못 미치면 한 번만 다시 쓰게 한다. 설명을 붙이는 게 아니라 장면을 더 넣게 시킨다.

**저자는 고른 렌즈의 그 사람이다.** 1인칭으로 쓴다. `data/lenses/voice/`의 말버릇·시그니처 무브를 실제로 쓴다.
- 그 사람이 죽은 해 이후의 것은 아는 척하지 않는다. **전해 받은 기록으로 다룬다.** 그게 오히려 그 사람답다.
- **그 사람의 입에 없는 말을 큰따옴표에 넣지 않는다.** 검증된 인용은 글 맨 위에 한 번 붙고, 본문에서는 다시 인용하지 않는다.
- 글 끝에 "이 글은 어느 렌즈로 썼다"는 한 줄을 남긴다. 실제 발언은 맨 위 인용뿐임을 밝힌다.

**한 이슈만 다룬다. 이게 가장 자주 깨진다.**
- 글쓰기 입력에는 **고른 후보가 나온 이슈 하나**의 요약과 활성 어휘만 넣는다. 여러 이슈 요약을 주면 글이 흩어져 읽는 사람이 길을 잃는다.
- 모델이 이슈 id 대신 검색어를 돌려주는 일이 잦다. `anchor_best()`가 후보 표의 실제 행에 맞춰 바로잡는다.
- 첫 다섯 문단 안에 무슨 일이 있었는지가 분명해져야 한다. 설명 없이 은유부터 던지지 않는다.
- 쉬운 말로 쓴다. 두 번 읽어야 알아듣는 문장은 실패다.

- 톤은 차갑고 정확하다. 냉소의 과녁은 사람이 아니라 구조다 (9장).

**그래서 우리는 (`prompts/brief_planner.md`)**

글 다음에 붙는 별도 블록이다. **목소리가 다르다** — 렌즈가 아니라 제일기획 기획자가 말한다.
렌즈가 현상을 뜯었으면, 기획자는 그것을 실무로 옮긴다.

- **이슈를 캠페인 소재로 쓰자는 제안은 하지 않는다.** 특히 죽음·재난·범죄·정치 갈등이 얽힌 이슈라면 절대로.
  우리가 보는 것은 사건이 아니라 **그 사건이 드러낸 태도의 이동**이다. 그 변화는 사건이 잊힌 뒤에도 남고, 전혀 다른 카테고리의 선택에서 나타난다.
- 짜임: 무엇이 바뀌었나(믿음의 이동 하나) → 어디를 볼까(카테고리 2~3) → 누구에게(나이가 아니라 처지) → 어떻게 말 걸까(슬로건이 아니라 각) → 클라이언트에게 한 문장.
- 없는 시장 수치·점유율을 지어내지 않는다. 브랜드 실명을 근거로 쓰지 않는다. 세대론으로 때우지 않는다.
- 광고 상투어 금지: 진정성, 공감, 소통, 새로운 패러다임, 고객 중심.

**번역체 제거 (`prompts/_korean.md`)**
- 선정과 글쓰기 두 프롬프트 모두에 이 파일이 붙는다.
- 출처는 [epoko77-ai/im-not-ai](https://github.com/epoko77-ai/im-not-ai) 의 humanize-korean 룰북이다. 생성 단계에서 쓸 것만 추렸다.
- 새 한국어 산문 프롬프트를 만들면 여기에도 이 파일을 붙인다.

결과: `data/out/{run_id}/pick.json`, `pick.md`

---

## 5. 메커니즘 정의

각 항목이 `prompts/mech_XX.md`의 원본이다. 프롬프트에는 해당 항목 하나만 들어간다.

### mech_01 최소 변형
이미 강한 표현을 한 글자 또는 한 음절만 바꾼다.
변형이 작을수록 원본이 즉시 떠오르고, 그 뒤에 방향이 뒤집힌다.
원본은 활성 어휘 표에 있거나, 이슈와 강하게 연결된 강한 원본 어휘여야 한다.
예: 매국노 → 애국노

### mech_02 감정 반전
단어의 껍데기는 유지하고 감정 방향만 뒤집는다.
애국↔매국, 찬사↔모욕, 피해자↔가해자, 특권↔불이익, 성공↔실패.
청중이 반전을 **느껴야** 한다. 논리로 이해되는 반전은 약하다.

### mech_03 의미 대각선
멀리 떨어진 두 의미장에서 각각 하나씩 가져와 충돌시킨다.
단, 무작위가 아니다. 이슈를 알고 나면 두 단어의 관계가 명백해져야 한다.
"왜 이 둘이?" → "아, 그래서." 가 성립하지 않으면 버린다.

### mech_04 정체성 충돌
기대되는 정체성 × 예상 밖 행동.
개그맨 × 명창, 배우 × 음악방송, 정치인 × 예상 밖 행동, 명품 × 지극히 평범한 행동.
행동을 서술하지 말고, 그 모순을 담을 다른 의미장의 단어를 찾는다.
예: 개그외도

### mech_05 지위 충돌
가장 위신 높은 상징 × 가장 평범하고 지역적인 상징.
명품·왕실·신화·귀족·군대 ↔ 지명·서민·일상·토착 은어.
대상을 격상시키는 효과와 조롱하는 효과 둘 다 가능하다.
예: 조선 롤스로이스

### mech_06 이중 독해
한 표현이 두 가지로 동시에 읽힌다.
발음·분절·동음이의·외국어 겹침·약어·기존 관용구.
후보를 만든 뒤 반드시 "다르게 읽으면 무엇이 되는가"를 묻는다. 두 번째 읽기가 첫 번째보다 강하면 그것을 취한다.
예: 국회차이나 (China / 차이가 나)

### mech_07 기존 밈 채굴
**생성하지 않는다.** 활성 어휘 표에서 이미 별명·밈·반복구로 자리 잡은 표현을 그대로 옮긴다.
사람들이 이미 붙인 이름이 AI가 만든 것보다 강한 경우가 많다.
이 메커니즘의 출력은 "발견"이며, 손대지 않는다.

### mech_08 문화 번역
다른 문화권·시대·장르의 익숙한 틀을 빌려 현재 이슈를 옮긴다.
사극·무협·성경·그리스 신화·일본 만화·할리우드 장르·조선왕조실록 문체.
틀이 익숙할수록 옮겨진 이슈가 즉시 읽힌다.

### mech_09 이슈 간 교차 (신규)
같은 기간의 서로 다른 두 이슈에서 활성 단어를 하나씩 가져와 충돌시킨다.
단일 이슈 안에서는 절대 나오지 않는 조합이 생긴다.
그 시기를 산 사람만 알아듣는 이름이 되며, "시대에 이름을 붙인다"에 가장 가깝다.
이 호출만 두 이슈의 활성 어휘 표를 동시에 입력받는다.

---

## 6. LLM 호출 규칙

### 6-1. 제공자
셋 중 하나로 돌린다. 고르는 곳은 `config.yaml`의 `llm.provider`이고, 웹 화면에서 고르면 그쪽이 이긴다.

| 제공자 | 무엇 | 키 |
|---|---|---|
| `claude_agent` | 설치된 Claude Code를 그대로 부른다. 구독으로 돈다 | 필요 없음 |
| `gemini` | Gemini API | 사용자가 발급받은 키 |
| `anthropic` | Anthropic API (종량제) | Anthropic 키 |

- 호출은 전부 `scripts/llm.py`를 지난다. 파이프라인 스크립트는 어느 제공자인지 모른다.
- 웹 화면이 고른 값은 환경변수(`IEE_PROVIDER` / `IEE_MODEL` / `IEE_EFFORT`)로 하위 프로세스에 넘긴다.
- 키는 브라우저 localStorage와 하위 프로세스 환경에만 존재한다. 저장소에 쓰지 않는다.
- `claude_agent`는 빈 임시 폴더에서 도구를 모두 끈 채로 부른다. 이 저장소의 CLAUDE.md가 프롬프트에 딸려 들어가면 안 되기 때문이다.
- `--effort`는 속도를 크게 좌우한다. 발산은 깊이보다 양과 다양성이 우선이므로 낮춰도 된다.

### 6-2. 공통
- 모델·온도·최대 토큰은 `config.yaml`에서 읽는다. 코드에 박지 않는다.
- 온도는 제공자가 받을 때만 보낸다. Anthropic 최신 모델은 temperature를 받지 않으므로 effort로 대신한다.
- 한 호출 = 한 메커니즘 = 한 이슈 (mech_09만 두 이슈).
- 시스템 프롬프트에는 해당 메커니즘 파일 + 2장 핵심 원리 요약(10줄 이내)만 넣는다. 이 CLAUDE.md 전체를 넣지 않는다.
- 출력은 JSON 배열만. 앞뒤 설명문 금지. 코드 펜스 제거 후 파싱.
- 실패한 호출은 건너뛰고 로그에 남긴다. 파이프라인을 멈추지 않는다.

---

## 7. 코드 규칙

- Python 3.11 이상. 의존성은 `requests`, `pyyaml`, `python-dotenv`, Anthropic SDK로 제한한다.
  - 웹 화면은 새 의존성을 쓰지 않는다. 서버는 표준 라이브러리(`http.server`), 화면은 순수 HTML/CSS/JS다. 빌드 단계를 만들지 않는다.
  - `anthropic` 패키지는 `provider: anthropic`일 때만 불러온다. 나머지 두 제공자는 없어도 돈다.
- 터미널에서 쓸 API 키는 `.env`에만 둔다.
  `.gitignore`에 `.env`, `runs/`, 그리고 라운드마다 쌓이는 산출물 전부(`data/raw/`, `data/out/`, `data/vocab/`, `data/discover/`)를 넣는다.
  저장소에 남는 데이터는 손으로 관리하는 것 둘뿐이다 — `data/lexicon/`(강한 원본 어휘 풀)과 `data/lenses/`(magilite 사본).
- 웹 화면에서 넣은 키는 브라우저 localStorage에만 저장하고, 실행할 때 하위 프로세스 환경변수로만 넘긴다. 파일에 쓰지 않는다.
- 서버는 127.0.0.1에만 붙인다. 바깥에서 접속되게 하지 않는다.
- 스크립트는 각각 독립 실행 가능해야 한다. 공통 인자: `--run {run_id}`, `--issue {키워드}`.
- `run.py`는 위 스크립트를 순서대로 호출하는 것 이상을 하지 않는다.
- 모든 중간 산출물은 파일로 남긴다. 메모리에서만 넘기지 않는다. 사람이 중간을 열어봐야 한다.
- 주석과 로그 메시지는 한글로 쓴다.
- 함수는 짧게. 하나의 스크립트가 200줄을 넘으면 나눈다.
- 외부 API 호출은 실패 시 3회까지 지수 대기 후 재시도, 그 뒤 건너뛴다.

---

## 8. 하지 말 것

- **점수를 매기지 않는다.** 후보에 숫자를 붙이거나 "강함/약함"을 판정하는 코드·프롬프트를 만들지 않는다.
  (예외는 `pick.py` 하나뿐이다. 그것은 파이프라인 밖의 편집 단계이며 결과가 되먹여지지 않는다 — 4.8 참조. 여기서도 후보에 점수를 붙이지는 않는다.)
- 후보를 설명으로 포장하지 않는다. `one_line`은 20자를 넘기지 않는다.
- 한 프롬프트에 메커니즘 여러 개를 넣지 않는다.
- 직전 라운드에서 잘 나온 원본 단어를 다음 라운드에 다시 쓰지 않는다. 배울 것은 단어가 아니라 구조다.
- 사람이 준 이슈를 자율 발견처럼 표시하지 않는다.
- 커뮤니티를 크롤링하지 않는다.
- 생성 수를 맞추기 위해 재호출하지 않는다.
- 이 CLAUDE.md 전체를 LLM 프롬프트에 넣지 않는다.
- 사람에게 방법론을 길게 보고하지 않는다. 결과 표와 건수만 낸다.

---

## 9. 표현 경계

- 정치·범죄·죽음·사회 갈등·논쟁적 언어를 재료로 다루는 것은 허용된다. 강한 이름은 대개 여기서 나온다.
- 단, 다음은 생성하지 않는다: 특정 개인·집단을 향한 비하·혐오·차별 표현, 성적 표현, 괴롭힘을 부추기는 표현.
- 기준은 메커니즘이 아니라 **대상**이다. '매국노'라는 단어를 분석하는 것과, 특정인을 매국노로 규정하는 이름을 뿌리는 것은 다르다.
- 경계에 걸리는 후보는 조용히 제외한다. 왜 제외했는지 장황하게 쓰지 않는다.

---

## 10. 실행

### 화면으로 (기본)

`run.bat` 을 두 번 누른다. 그게 전부다.
파이썬 확인 → 처음이면 꾸러미 설치 → `.env` 없으면 견본에서 생성 → 서버 시작 → 브라우저 열기까지 한다.

화면은 **한 장씩 넘기는 덱**이다. 한 장에서 한 가지만 정하고 다음 장으로 간다. 아래로 길게 늘어놓지 않는다.

1. **찾기** — 지금 뜨는 것 / 기간 정해서 중에 고르고 이슈 후보를 받아 온다
2. **고르기** — 찾아 온 후보에서 두세 개를 고른다
3. **확인** — 검색어와 이름 목록을 확인하고 던진다
4. **발산** — 후보가 실시간으로 쌓이는 게 보인다
5. **후보** — 다 끝난 목록을 훑고, md·csv 로 받는다
6. **글** — 하나를 골라 렌즈 인용, 기사, 그리고 「그래서 우리는」 제언을 얻는다 (4.8)

제공자·키·기간 설정은 오른쪽 위 [설정] 안에 있다. 화면에서 묻는 말은 시스템 용어가 아니라
처음 보는 사람이 답할 수 있는 말이어야 한다. 왜 그것을 묻는지도 한 줄로 적는다.

글꼴은 Pretendard 하나만 쓴다. 위계는 글꼴이 아니라 **굵기**로 만든다.

터미널에서 직접 띄우려면:
```powershell
python scripts/serve.py          # 포트가 막혀 있으면 다음 번호로 옮겨 붙는다
python scripts/serve.py --port 9000 --no-open
```

`run.bat` 은 UTF-8(BOM 없음)로 저장하고 첫 줄에서 `chcp 65001` 을 부른다. 한글이 깨지지 않게 하기 위한 것이니 인코딩을 바꾸지 말 것.

### 터미널로 (0단계 원래 방식)
```powershell
python scripts/discover.py                        # 지금 뜨는 것에서 이슈 후보를 늘어놓는다
python scripts/discover.py --start 2026-08-18 --end 2026-08-25   # 그 기간에서 찾는다
# 그중 쓸 것을 config.yaml 에 손으로 옮겨 적은 뒤
python scripts/collect_youtube.py --run 20260901-0900
python scripts/extract_vocab.py   --run 20260901-0900
python scripts/generate.py        --run 20260901-0900
python scripts/filter.py          --run 20260901-0900
# → data/out/20260901-0900/candidates.md 를 폰에서 연다

python scripts/pick.py            --run 20260901-0900   # 원할 때만: 하나 고르고 글쓰기
```

**0단계 완료 기준**
- 이슈 3개 × 5라운드를 돌린다.
- 15개 결과 표 중 사람이 "오." 한 후보가 3개 이상이면 1단계로 간다.
- 0개면 수집 인프라를 만들지 않는다. 활성 어휘 표(4.3)의 품질부터 다시 본다. 대부분 원인은 거기 있다.

### 1단계 (반자동)
- `collect_news.py` 추가. 기간 내 급증 키워드 20개 제시 → 사람이 이슈 선택.
- `run.py`로 전체 실행. 라운드 로그 자동 저장.
- 강한 원본 어휘 풀에 발견된 단어 추가, 반복 단어 제외 목록 자동 생성.

### 2단계 (정기 실행)
- 매일 아침 실행 → 이슈 후보 3개 + 각 후보 표를 md 하나로.
- mech_06 이중 독해에 한국어 발음 변환(g2p) + 사전 대조를 코드로 붙여 후보를 기계적으로 보강.

---

## 11. 최종 원칙

찾는다 → 사람들이 이미 어떻게 부르는지 듣는다 → 뜨거운 단어를 모은다 → 멀리서 강한 단어를 빌려온다 → 충돌시킨다 → 많이 던진다 → 사람이 건진다.

기교를 쫓지 않는다.
하나의 정답을 쫓지 않는다.
설명으로 후보를 살리지 않는다.
지난 라운드의 성공 단어에 매달리지 않는다.

시스템은 재료와 충돌을 책임진다. 판단은 사람이 한다.
