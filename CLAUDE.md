# CLAUDE.md — ANSWER ZINE 주간 제작 에이전트

이 문서는 **매주 ANSWER ZINE 1호를 처음부터 끝까지 생산하는 에이전트**의 운영 명세다.
이 저장소에서 작업을 시작할 때 가장 먼저 읽고, 매주 이 절차를 그대로 실행한다.

| 문서 | 역할 |
|---|---|
| `CLAUDE.md` (이 문서) | 무엇을 어떤 순서로 생산하는가 — 파이프라인·판정 기준·중단 규칙 |
| `AGENTS.md` | **누가** 하는가 — 에이전트 역할 분담·컨텍스트 매니페스트·핸드오프 규약 |
| `LESSONS.md` | **무엇을 배웠는가** — 사고 기록과 자동 검사로의 졸업 관리 |
| `design.md` | 어떤 형태로 렌더하는가 — 고정 템플릿, 토큰, 분량 예산 |
| `market-scope.md` | **국내/해외 시장 구분 지침 — S3에서 필수 실행** |
| `sources/whitelist.json` | 사용 가능한 출처 목록 (4등급). 빌더가 자동 판정에 사용 |
| `sources/README.md` | 화이트리스트 운영 규칙과 출처 추가 절차 |
| `domains/README.md` | 도메인 레지스트리와 신규 도메인 추가 절차 |
| `domains/<도메인>.md` | 도메인별 데이터 출처·이례 판정 기준·집필 규칙 |
| `tools/zine-builder.html` | 제작기 프로토타입 — 입력·검증·프리뷰·JSON 출력 |

**도메인별 판단은 반드시 해당 도메인 문서를 열어 확인한다.** 이 문서는 도메인 공통 규칙만 담는다.
**에이전트로 실행할 때는 `AGENTS.md`의 컨텍스트 매니페스트를 따른다** — 자기 표에 있는 것만 읽는다.

> ⚠️ **한 번 사고가 났다.** `2026-w31-movie`는 국내 개봉 전 기간의 데이터를 인용해 게재 불가 상태다.
> 원인과 재발 방지 절차는 `market-scope.md`에 있다. **S3에서 그 문서의 §5 체크리스트를 반드시 실행한다.**

---

## 1. 임무

> 매주, 그 주에 가장 잘 팔린 것 중에서 **"왜 팔렸는지 설명할 수 있는 것"만 골라** 한 호를 만든다.

### 1.1 편집 명제

- 슬로건: **"순위 말고, 팔린 이유."**
- 인트로: **"이거 왜 잘나가? / 순위가 아니라, 팔린 이유를 본다."**

이건 카피가 아니라 **기능 명세다.** 순위표를 나열하는 화면·기사·섹션은 만들지 않는다.
1위라도 이유를 설명할 수 없으면 싣지 않는다. 7위라도 이유가 선명하면 싣는다.

### 1.2 1호의 정의

```
1호 = 도메인당 스토리 1편  +  웹 아카이브 갱신  +  A4 인쇄 진 1장
```

- 스토리 수 = **활성 도메인 수** (현재 4개: 영화·음악·유튜브·도서)
- 인쇄 진 = 리드 기사 1편 + 미니 기사 3편 → **리드로 승격되지 않은 나머지가 미니가 된다**
- 도메인이 5개 이상으로 늘면 인쇄 진 레이아웃 결정이 필요하다 → `domains/README.md` §확장 시 영향

### 1.3 리드 선정

한 호에서 **3조건 점수(§4.2)가 가장 높은 1편**이 리드다. 동점이면 `이례성` 배수가 큰 쪽.
리드는 인쇄 진 1면 전체를 쓰고, 나머지는 미니로 내려간다.

---

## 2. 산출물과 저장소 구조

### 2.1 현재 구조

```
README.md               저장소 안내 + 세팅 절차
CLAUDE.md               이 문서
AGENTS.md               ★ 에이전트 역할·컨텍스트 매니페스트
LESSONS.md              ★ 중앙 학습 기록
design.md
market-scope.md
.claude/agents/
  az-scout.md  az-verify.md  az-write.md  az-editor.md  az-retro.md
runs/
  README.md             실행 로그 형식
  YYYY-wNN.json         에이전트 과정 기록 (lessonsApplied 필수)
  index.json            qa.mjs가 생성
domains/
  registry.json         ★ 도메인·색상·분량 예산 단일 소스
  README.md  _TEMPLATE.md
  movie.md  music.md  youtube.md  book.md
sources/
  whitelist.json        ★ 출처 4등급 단일 소스
  README.md
issues/
  README.md             회차 파일 형식
  2026-w27.json  2026-w31.json
tools/
  dashboard.html        운영 대시보드 — 파이프라인·사람 대기·학습 현황
  zine-builder.html     제작기 (registry/whitelist를 저장소에서 읽는다)
  qa.mjs                자동 검증 — CI와 로컬 공용
.github/workflows/
  qa.yml                푸시·PR 검증 + main이면 Pages 배포
```

**★ 표시가 단일 소스다.** 문서 안의 표는 사람이 읽는 사본이므로, 값을 바꿀 때는 JSON을 먼저 고친다.

### 2.2 아직 남은 것

```
Answer_Zine.html        단일 파일 4.1MB — 데이터·템플릿·에셋 전부 인라인
```

발행 산출물은 여전히 이 파일이고, `WEEKS` 배열과 인쇄 진을 손으로 고친다.
`issues/*.json` → `Answer_Zine.html` 빌드 스크립트(`build/build.mjs`)와 에셋 외부화는 미착수다.
착수 전 §9를 읽고 사용자 승인을 받는다.

그때까지 S6은 **`issues/*.json`을 정본으로 두고 `Answer_Zine.html`에 옮겨 적는** 방식으로 한다.
정본이 둘이 되지 않도록, 수정은 항상 JSON에서 먼저 한다.

---

## 3. 데이터 계약

콘텐츠는 전부 `WEEKS` 배열(마이그레이션 후 `issues/*.json`)에 있다. **마크업에 본문을 직접 쓰지 않는다.**

### 3.1 회차 항목

| 필드 | 타입 | 필수 | 규칙 |
|---|---|---|---|
| `id` | string | ✅ | `YYYY-wNN-<domainKey>`. 예: `2026-w31-book`. 전역 유일. |
| `range` | string | ✅ | `YYYY.MM.DD – MM.DD` (en dash U+2013). 집계 기간. |
| `domain` | string | ✅ | `DOMAIN_META`의 키. `domains/<도메인>.md`가 존재해야 한다. |
| `kicker` | string | — | 영문 대문자. 도메인별 관용구는 각 도메인 문서 참조. |
| `headline` | string | ✅ | 한국어 한 문장, 마침표로 끝냄. **≤24자** (§design.md 예산) |
| `teaser` | string | ✅ | 한 문장. 숫자 포함 권장. **≤78자** |
| `blocks` | Block[] | ✅ | `text` / `stat` / `quote` 3종만. |

### 3.2 블록

```js
{ type: "text",  text: "…" }                       // ≤180자

{ type: "stat",
  label: "주간 판매량 지수",                          // Y축이 무엇인지
  value: "+326.5%",                                 // 크게 보여줄 한 값
  trend: [98, 101, 100, 426],                       // 4~5점 권장, 최소 2
  axisCaption: "X축: 최근 4주 · Y축: 전주 대비 판매 지수 (전주=100)",
  sourceUrl: "https://…",                           // 필수. 검증 가능한 원본.
  sourceLabel: "예스24 베스트셀러 확인하기" }

{ type: "quote",
  text: "이건 마케팅이 만든 베스트셀러가 아니다. 독자들이 스스로 만든 애도다.",
  insight: { concept: "사후 관심 효과 (사망 후 재조명 현상)",
             note: "…왜 일반적 현상인지 + 이번 사례가 그 중 어디에 해당하는지…" } }
```

### 3.3 고정 블록 구성

모든 스토리는 **정확히 이 5블록**이다. 늘리거나 줄이지 않는다.

```
[0] text   현상 제시 — 언제, 무슨 일이 (≤180자)
[1] stat   숫자 한 방 + 출처            (회차당 정확히 1개)
[2] text   누가 샀나 / 왜 이례적인가    (≤180자)
[3] quote  한 줄 해석 + insight        (회차당 정확히 1개)
[4] text   마무리 한 문장              (≤180자)
```

`stat`이 두 개 필요하면 소재를 쪼갠 것이다. 하나를 버리거나 다음 주로 넘긴다.

---

## 4. 주간 파이프라인

한 호를 만드는 8단계다. **앞 단계를 건너뛰지 않는다.** 특히 S3 이전에 문장을 쓰지 않는다.

```
S1 수집 → S2 선별 → S3 검증 → S4 집필 → S5 해석 → S6 빌드 → S7 QA → S8 발행
         ↑                  │
         └──── 탈락 시 ─────┘
```

### S1. 신호 수집

각 도메인 문서의 `데이터 출처` 절에 있는 1차 소스를 연다. 도메인당 **후보 3개 이상**을 확보한다.

기록할 것: 항목명 · 현재 순위 · 직전 4주 수치 · 눈에 띄는 변동.
아직 이야기를 만들지 않는다. **숫자만 모은다.**

### S2. 소재 선별 — 3조건 테스트

각 후보를 세 조건으로 채점한다. **하나라도 0점이면 즉시 탈락**이다.

| 조건 | 질문 | 0점 | 1점 | 2점 |
|---|---|---|---|---|
| **이례성** | 이 숫자가 그 도메인의 평소 범위를 벗어났는가? | 통상 범위 안 | 경계선 | 명백한 이탈 |
| **인과** | 그 변화를 만든 **특정 가능한 계기**가 있는가? | 없음/불명 | 추정 가능 | 시점까지 특정됨 |
| **검증가능성** | 숫자와 계기 **양쪽 다** 공개 출처로 확인되는가? | 불가 | 한쪽만 | 양쪽 다 |

이례성의 기준선은 도메인마다 다르다 — 반드시 `domains/<도메인>.md`의 `이례 판정` 절을 본다.

> **이례 = 큰 숫자가 아니다.** 기존 회차 중 영화 편은 `-14%`라는 **감소** 수치로 실렸다.
> 업계 평균 감소율이 -35%인데 그 절반도 안 빠졌다는 게 이례였다.
> 항상 "그 도메인에서 이 시점에 **기대되는 값**"과 비교한다.

도메인당 최고점 1개를 남긴다. **최고점이 4점 미만이면 그 도메인은 이번 주 결번**이다 (§7.2).

### S3. 검증 — 통과 못하면 버린다

이 단계가 이 웹진의 신뢰를 지탱한다. 타협하지 않는다.
`tools/zine-builder.html`에 입력하면 아래 항목 중 상당수가 자동 검사된다.

**출처 검증**

1. `sourceUrl`의 호스트가 `sources/whitelist.json`에서 **`primary` 또는 `conditional`** 등급인가.
2. `sourceUrl`을 **실제로 연다.** 열리지 않으면 탈락.
3. 그 페이지에서 **`value`의 숫자가 그대로 확인되는가.** 계산이 필요하면 계산 과정을 기록한다.
4. `trend` 배열의 각 점이 어디서 왔는지 말할 수 있는가.

**시장·시간 검증 — `market-scope.md` §5 전 항목**

5. **기준일**(계기가 국내에서 발생한 날)을 특정했는가. 도메인별 정의는 `market-scope.md` §4.
6. 그 날짜가 **최종 확정일**인가. 최초 발표만 보고 갱신을 놓치지 않았는가.
7. 그 날짜가 **국내 기준**인가. 해외 개봉일·발매일을 옮겨 적지 않았는가.
8. **기준일 ≤ 집계 시작일**인가. 기준일이 더 늦으면 그 주에 사건이 아직 없었다는 뜻이다 — 탈락.
9. 주차 표기가 `(집계 시작일 − 기준일) ÷ 7 + 1`과 맞는가.
10. `sourceUrl`의 `market`이 선언한 시장과 같은가. 국내 서사에 해외 지표를 섞지 않았는가.

> 세 개 다 탈락해서 도메인이 비어도 괜찮다. **확인 안 되는 숫자를 싣는 것보다 결번이 낫다.**
>
> 5~9번을 건너뛴 결과가 `2026-w31-movie` 사고다. 개봉일을 최초 발표(7/15)로 쓴 채
> 실제 국내 개봉(8/5) 이전 기간의 KOBIS 데이터를 인용했다. 존재할 수 없는 숫자였다.

### S4. 집필

`§3.3` 5블록 구조로 쓴다. 문장 규칙은 §6.

집필 순서 권장: `stat` → `[0] 현상` → `[2] 맥락` → `quote` → `[4] 마무리` → `headline` → `teaser`.
**숫자를 먼저 확정하고 문장을 붙인다.** 반대로 하면 문장에 맞춰 숫자를 고르게 된다.

### S5. 해석 (insight)

`quote.insight`에 행동경제학·소비심리 개념을 하나 붙인다.

- `concept` — 개념명. 널리 통용되는 이름을 쓴다. 지어내지 않는다.
- `note` — **왜 그 현상이 일반적으로 관찰되는지** + **이번 사례가 그 중 어디에 해당하는지**. 110~125자.

도메인별로 자주 쓰이는 개념 목록이 각 도메인 문서에 있다. 같은 개념을 **4주 연속 쓰지 않는다.**

> `note`는 홍보 문구가 아니다. "그래서 이 제품이 좋다"로 끝나면 다시 쓴다.

### S6. 빌드

1. 새 회차 객체를 `WEEKS` **맨 앞**에 추가한다 (배열 = 최신순).
   같은 주 여러 도메인은 `리드 → 미니1 → 미니2 → 미니3` 순으로 넣는다.
2. 인쇄 진(`.zine-page`) 갱신 — 현재는 수동이다:
   - `.zine-dateline` — `서울, 대한민국 — YYYY년 M월 N주차`
   - `.zine-lead` — 리드의 kicker / headline / teaser / 본문 3문단 / `.zine-sticker` 수치
   - `.zine-secondary-row` — 미니 3편의 kicker / headline / teaser
   - `.zine-lead-photo` 이미지, 푸터 QR과 `answerzine.kr/YYYY-wNN`
3. 인쇄 진 본문은 웹 본문의 **축약**이다. 새로 쓰지 말고 줄인다. **헤드라인과 티저는 웹과 한 글자도 다르면 안 된다.**
4. 분량은 `design.md` §4 예산표를 지킨다.

### S7. QA

§8 체크리스트 전 항목. 하나라도 실패하면 S8로 가지 않는다.

### S8. 발행

빌드 산출물 전달 + 이번 호 요약(도메인별 헤드라인, 사용한 출처, 결번 도메인과 사유) 보고.

---

## 5. 도메인 레지스트리

| 도메인 | key | 컬러 | 문서 | 상태 |
|---|---|---|---|---|
| 영화 | `movie` | `#0A84FF` | `domains/movie.md` | 활성 |
| 음악 | `music` | `#FF9500` | `domains/music.md` | 활성 |
| 유튜브 | `youtube` | `#FF2D55` | `domains/youtube.md` | 활성 |
| 도서 | `book` | `#34C759` | `domains/book.md` | 활성 |

컬러의 단일 소스는 코드의 `DOMAIN_META`다. 이 표는 사본이므로 **양쪽을 함께 고친다.**
신규 도메인 추가는 `domains/README.md`의 절차를 따른다. 색상은 임의로 고르지 않는다 (`design.md` §6).

---

## 6. 문장 규칙

도메인 공통이다. 도메인별 추가 규칙은 각 도메인 문서에 있다.

### 헤드라인

- 한국어 한 문장, **마침표로 끝낸다.** 물음표·느낌표·이모지·말줄임표를 쓰지 않는다.
- 24자 이하. 짧을수록 좋다 (기존: `밈이, 판다.` 7자 / `매진, 또 매진.` 9자).
- **결과를 쓰고 원인을 감춘다.** `부고 다음날, 서점이 붐볐다.`가 `히가시노 게이고 별세로 판매 급증`보다 낫다.
- 고유명사를 헤드라인에 넣지 않는다. 본문에서 밝힌다.

### 본문

- 평서문. 반말 종결(`-다`)로 통일한다.
- 한 문단은 한 가지만 말한다. 180자를 넘으면 나눌 게 아니라 **버릴 게 있는 것이다.**
- 수식어를 쌓지 않는다. `놀랍게도`, `무려`, `충격적인`을 쓰지 않는다. 숫자가 스스로 말하게 둔다.
- 작품명에 꺾쇠(`<>`)를 쓰지 않는다 — 영문 제목일 때 HTML 태그로 먹힌다. `〈 〉`(U+3008/3009)를 쓴다.

### 쿼트

- 26~40자. 한 문장.
- 대비 구조를 쓴다: `A가 아니라 B다` / `A인데 B했다`.
  (기존: `밈은 웃기려고 만들어졌는데, 결국 음원을 팔았다.`)

### 민감 소재

부고·사고·질병이 계기인 경우:

- 숫자와 사실만 전한다. 유족·팬의 감정을 대신 해석하지 않는다.
- `급증`, `특수`, `호재` 같은 시장 용어를 그 사건에 붙이지 않는다.
- 마지막 문단에서 **숫자 뒤에 사람이 있다는 걸 인지하고 있음**을 드러낸다.
  기준선: `숫자 뒤에 슬픔이 있다는 걸 아는 채로, 이번 주는 그 숫자를 그대로 전한다.`
- 판단이 서지 않으면 **싣지 말고 사용자에게 묻는다** (§7.1).

---

## 7. 중단·예외 규칙

### 7.1 멈추고 사용자에게 물어야 하는 경우

- 민감 소재(부고·사고·범죄·질병)의 게재 여부가 애매할 때
- 활성 도메인의 **절반 이상**이 결번일 때 (한 호가 성립하지 않는다)
- 출처가 유료/로그인 장벽 뒤에 있어 검증이 불가능할 때
- 데이터 출처 사이트의 구조가 바뀌어 지난주와 같은 값을 못 읽을 때
- 디자인 템플릿 변경이 필요할 때 (`design.md` 불변식은 에이전트가 임의로 못 고친다)

### 7.2 결번 처리

도메인이 3조건 4점을 못 넘기면 그 주는 결번이다.

- `WEEKS`에 아무것도 추가하지 않는다. 빈 스토리·`준비 중` 플레이스홀더를 만들지 않는다.
- 웹 아카이브는 필터 시 자동으로 `.empty-state`를 띄우므로 별도 처리가 없다.
- 인쇄 진의 미니 슬롯이 3개 미만이 되면 §S6에서 레이아웃 조정이 필요하다 → 사용자에게 알린다.
- 발행 보고에 **어느 도메인이 왜 결번인지** 반드시 적는다.

### 7.3 에이전트가 하지 않는 것

- 확인되지 않은 숫자 게재. 추정치를 실측치처럼 쓰기.
- `sourceUrl`에 검색 결과 페이지·블로그·커뮤니티 글 넣기. **`sources/whitelist.json`의 `primary`/`conditional`만.**
- 국내 서사에 해외 지표·해외 날짜 섞기 (`market-scope.md`).
- 화이트리스트 차단 항목을 임의로 완화하기 (**사용자 승인 사항**).
- 요청 범위 밖의 리팩터링·포맷팅·"개선". §9는 발견 기록이지 실행 허가가 아니다.
- 빌드 도구·프레임워크·npm 의존성 도입 (사용자 승인 사항).
- `design.md` 불변식 변경.

---

## 8. QA 체크리스트

매주 발행 전 전 항목을 확인한다. `build/qa.mjs`로 자동화 가능한 항목은 `[자동]` 표시.

### 데이터

1. 새 회차의 `id`가 기존과 충돌하지 않는가. `[자동]`
2. `range`가 `YYYY.MM.DD – MM.DD` 형식이고 en dash를 쓰는가. `[자동]`
3. `domain`이 `DOMAIN_META`에 있고 대응 도메인 문서가 존재하는가. `[자동]`
4. 블록이 `text / stat / quote / text` 순서 + `stat` 1개 + `quote` 1개인가. `[자동]`
5. 슬롯 길이가 `design.md` §4 예산 안인가. `[자동]`
6. 모든 `stat.sourceUrl`이 200을 반환하는가. `[자동]`
7. 그 페이지에서 `value`가 실제로 확인되는가. **(사람이 눈으로)**
8. `insight.concept`이 최근 3주와 겹치지 않는가. `[자동]`
9. `sourceUrl`이 화이트리스트 `primary`/`conditional` 등급인가. `[자동]`
10. 기준일이 입력됐고 **집계 시작일보다 앞서는가.** `[자동]`
11. 기준일·집계 출처의 `market`이 선언 시장과 일치하는가. `[자동]`
12. 본문의 주차 표기가 기준일로부터 계산한 값과 맞는가. **(사람이 눈으로)**

### 웹

9. 인트로 → 스테이트먼트 → 아카이브 스크롤이 끊기지 않는가.
10. 세그먼티드 전 도메인 + `전체`가 맞게 필터되고, 결번 도메인에서 `.empty-state`가 뜨는가.
11. 모든 회차를 열어 `stat` 카드 출처 링크가 열리는가.
12. 이전/다음 내비게이션이 **시간순**으로 움직이는가 (§9.2 수정 전이면 실패한다).
13. `insight` 툴팁이 hover와 키보드 `Tab` 양쪽에서 뜨는가.
14. 콘솔 에러 0건. `[자동]`

### 인쇄

15. A4 인쇄 미리보기가 **정확히 1페이지**인가. `[자동]`
16. 웹 UI(`.intro`, `.statement-wrap`, `#main-content`, 인쇄 버튼)가 전부 숨겨지는가.
17. 인쇄 진의 헤드라인·티저가 웹 `WEEKS`와 **문자열이 완전히 일치**하는가. `[자동]`
18. QR이 이번 호 URL(`answerzine.kr/YYYY-wNN`)을 가리키는가.

### 반응형·접근성

19. 375 / 768 / 1280px에서 레이아웃이 깨지지 않는가.
20. `prefers-reduced-motion: reduce`에서 애니메이션이 멈추고 콘텐츠는 보이는가.

### 자동 검증 참고

15번은 Chromium 헤드리스로 검사할 수 있다 (본 문서 작성 시 이 방법으로 §9.1을 발견했다).

```js
const buf = await page.pdf({ format: 'A4', printBackground: true,
                             margin: {top:0,bottom:0,left:0,right:0} });
const pages = (buf.toString('latin1').match(/\/Type\s*\/Page[^s]/g) || []).length;
// pages === 1 이어야 한다
```

---

## 9. 현재 코드의 결함 (주간 생산을 막는 것부터)

발견 기록이다. **한 번에 한 항목씩, 사용자가 지정한 것만** 고친다.

### P0 — 매주 발목을 잡는 것

**9.1 인쇄하면 A4 4페이지가 나온다 (검증 완료)**
`@media print` 숨김 목록에 `.statement-wrap`이 빠져 있다. `height: 220vh` 스크롤 트랙이 인쇄 지면에 **2,471px(≈A4 2.2장)의 빈 지면**을 만들고, 그 뒤에 진 1장이 붙어 총 4페이지가 나온다.
Chromium 헤드리스로 실측: 현재 4페이지 → `.statement-wrap` 숨기면 1페이지. 진 페이지 자체는 정확히 A4 1장(794×1123 @96dpi)이다.
→ 한 줄 수정:

```css
.intro, .statement-wrap, #main-content,
.zine-preview-wrap h2, .zine-preview-wrap > p, .print-btn { display: none !important; }
```

**9.2 이전/다음 회차가 시간을 거슬러 간다**
`openStory()`가 `prev = WEEKS[idx+1]`, `next = WEEKS[idx-1]`로 배열 순서 = 최신순을 가정한다. 실제 배열은 `w31 → w27 → w31 → w31` 순이라 어긋난다.
**주간 생산이 시작되면 매주 악화된다.** 같은 주의 4편을 한꺼번에 넣기 때문이다.
→ 렌더 시점에 `range` 기준으로 정렬한 배열을 만들어 그 위에서 인덱싱한다.

**9.3 인쇄본 콘텐츠가 `WEEKS`와 이중으로 존재한다**
`.zine-page` 내용이 HTML에 하드코딩되어 있어 매주 손으로 고쳐야 하고, 웹과 갈라질 수 있다 (지금도 본문 문장이 다르다).
**주간 파이프라인 S6의 최대 비용이자 최대 오류원이다.**
→ `renderZinePage()`로 `WEEKS`에서 리드 1 + 미니 3을 생성. 인쇄용 축약문이 필요하면 스키마에 `zineBody` 선택 필드를 추가한다.

**9.4 라우팅이 없다**
스토리가 URL을 바꾸지 않는다. 공유·뒤로가기·새로고침이 모두 안 된다.
**인쇄 진 QR이 `answerzine.kr/2026-w31`을 가리키는데 그 URL을 처리할 코드가 없다.** 종이를 뿌리는 순간 드러난다.
→ `location.hash` 기반 라우터 + `hashchange` 핸들러. QR URL 규칙을 이때 확정한다.

**9.5 필터와 내비게이션이 어긋난다**
필터를 건 상태에서 스토리를 열면 이전/다음은 전체 `WEEKS`를 따라간다.
→ 정렬 + 필터를 적용한 배열 하나를 단일 소스로 삼는다.

### P1

**9.6 파일이 4.1MB이고 매주 커진다** — 그중 약 4.07MB가 base64 인라인 에셋(Paperlogy TTF 4종, 로고 2, 사진 1, QR 1)이다. 로직·마크업은 36KB뿐. 회차가 쌓이면 여기에 콘텐츠가 계속 붙는다.
→ TTF→WOFF2 변환(통상 60~80% 감소) + 에셋 외부 분리. **폰트 재배포 라이선스를 먼저 확인할 것.**

**9.7 `innerHTML` + 이스케이프 부재** — `WEEKS`의 모든 문자열이 이스케이프 없이 들어가고 `id`는 `onclick` 문자열에 보간된다. 지금은 우리가 콘텐츠를 직접 쓰지만, 외부 입력(JSON fetch, CMS)이 들어오는 순간 XSS다.
작품명 함정도 여기 있다: `〈투명한 나선〉`처럼 한글 제목은 정상 출력되지만(HTML 토크나이저는 `<` 다음이 ASCII 알파벳일 때만 태그로 인식 — 브라우저에서 확인함), **`<Odyssey>`처럼 영문 제목을 꺾쇠로 쓰면 미지의 태그로 파싱되어 사라진다.**
→ `escapeHTML()` + `onclick`을 이벤트 위임으로 교체. 그 전까지는 §6의 `〈 〉` 규칙을 지킨다.

**9.8 폰트 웨이트 불일치** — `@font-face`는 300/400/700/900만 로드하는데 CSS는 500·590·600·800도 쓴다. 브라우저 대체로 `.row-headline`(600)→700, `.story-headline`(800)→900, `.domain-tag`(590)→400이 된다. 의도한 위계가 안 나온다. → `design.md` §3.

**9.9 접근성** — 스파크라인 SVG에 대체 텍스트 없음. 툴팁·출처 링크가 hover 의존이라 터치 기기에서 접근 어려움. 뷰 전환 시 포커스 이동 없음.

### P2

**9.10 공유/SEO 메타 부재** — OG 태그·description·파비콘 없음. 라우팅(9.4) 이후에 회차별 메타가 의미를 갖는다.
**9.11 콘텐츠/코드 분리** — §2.2 목표 구조로의 마이그레이션.
**9.12 QA 자동화** — §8의 `[자동]` 항목을 `build/qa.mjs`로 구현.
**9.13 미사용 CSS** — `.placeholder-body`, `.zine-readmore`, `.zine-footer-col`, `--kicker-color` 폴백. `.zine-footer-col`은 푸터가 원래 다단(다음 호 예고 등)이었음을 시사한다.
