# CLAUDE.md — ANSWER ZINE 주간 제작 에이전트

이 문서는 **매주 ANSWER ZINE 1호를 처음부터 끝까지 생산하는 에이전트**의 운영 명세다.
이 저장소에서 작업을 시작할 때 가장 먼저 읽고, 매주 이 절차를 그대로 실행한다.

| 문서 | 역할 |
|---|---|
| `CLAUDE.md` (이 문서) | 무엇을 어떤 순서로 생산하는가 — 파이프라인·판정 기준·중단 규칙 |
| `AGENTS.md` | **누가** 하는가 — 에이전트 역할 분담·컨텍스트 매니페스트·핸드오프 규약 |
| `LESSONS.md` | **무엇을 배웠는가** — 사고 기록과 자동 검사로의 졸업 관리 |
| `design.md` | 어떤 형태로 렌더하는가 — 고정 템플릿, 토큰, 분량 예산 |
| `voice.md` | **문장이 어떤 소리를 내야 하는가 — 톤 벤치마크. S4·S5에서 필수 참고** |
| `market-scope.md` | **국내/해외 시장 구분 지침 — S3에서 필수 실행** |
| `sources/whitelist.json` | 사용 가능한 출처 목록 (4등급). 빌더가 자동 판정에 사용 |
| `sources/README.md` | 화이트리스트 운영 규칙과 출처 추가 절차 |
| `domains/README.md` | 도메인 레지스트리와 신규 도메인 추가 절차 |
| `domains/<도메인>.md` | 도메인별 데이터 출처·이례 판정 기준·집필 규칙 |
| `tools/zine-builder.html` | 제작기 프로토타입 — 입력·검증·프리뷰·JSON 출력 |

**도메인별 판단은 반드시 해당 도메인 문서를 열어 확인한다.** 이 문서는 도메인 공통 규칙만 담는다.
**에이전트로 실행할 때는 `AGENTS.md`의 컨텍스트 매니페스트를 따른다** — 자기 표에 있는 것만 읽는다.

> ⚠️ **두 번 사고가 났고, 둘 다 같은 뿌리다 — 출처를 열지 않은 것.**
> ① `2026-w31-movie`가 국내 개봉 전 기간의 데이터를 인용했다 (`market-scope.md`).
> ② 같은 회차의 숫자가 KOBIS 실측과 거의 전부 달랐다 (`LESSONS.md` L-015). 둘 다 2026-w32에 바로잡았다.
> **S3에서 `market-scope.md` §5 체크리스트를 실행하고, 본문의 모든 수치를 `runs/raw/`의 수집본과 대조한다.**

---

## 1. 임무

> 매주, 그 주에 가장 잘 팔린 것 중에서 **"왜 팔렸는지 설명할 수 있는 것"만 골라** 한 호를 만든다.

### 1.1 편집 명제

- 슬로건: **"순위 말고, 팔린 이유."**

이건 카피가 아니라 **기능 명세다.** 순위표를 나열하는 화면·기사·섹션은 만들지 않는다.
1위라도 이유를 설명할 수 없으면 싣지 않는다. 7위라도 이유가 선명하면 싣는다.

> **인트로 화면 카피는 2026-08-10 사이트 리디자인으로 뺐다** — 화면에는 이제 "THE ANSWER" /
> "ZINE" 워드마크(사이트명과 맞춘 두 줄, 같은 날 두 번째 라운드에서 "COMPANY"에서 바꿨다.
> 화면을 꽉 채우는 크기다)만 뜨고 곧바로 아카이브로 이어진다. 위 슬로건("순위 말고, 팔린
> 이유") 자체는 화면 카피가 아니라 **소재 선정 기준**이라 그대로 유효하다 — 무엇을 실을지
> 고르는 편집 명제와, 진입 화면에 무슨 문구를 띄우는지는 별개다.

### 1.2 1호의 정의

```
1호 = 도메인당 스토리 1편  +  웹 아카이브 갱신  +  A4 인쇄 진 1장
```

- 스토리 수 = **그 주에 검증을 통과한 편수**. 활성 도메인 수가 상한이다 (현재 7개, `stage`는 휴면)
- 통과분이 4편에 못 미쳐도 채우지 않는다 — 결번이 확인 안 된 숫자보다 낫다 (§7.2)
- 인쇄 진 = 그 주 통과분 **전원**을 A4 1장에 담은 요약지. **2026-08-10부터 "리드 1편 +
  미니 3편" 위계가 없다** — 사용자가 첨부한 참고 목업(print.html) 포맷을 따라, 스토리마다
  같은 크기로 지면 한 행(`.zine-story`)을 쓴다. `design.md` §2·§8 참고.
- 도메인이 늘어 통과분이 A4 1장을 넘기면 레이아웃 결정이 필요하다 → `domains/README.md` §확장 시 영향

### 1.3 리드 선정

한 호에서 **3조건 점수(§4.2)가 가장 높은 1편**이 리드다. 동점이면 `이례성` 배수가 큰 쪽.
`issues/*.json`의 `stories` 배열 순서(점수순, 맨 앞이 리드)는 그대로 유지한다 — 웹 인덱스
정렬·홈 카드 노출 우선순위에 쓰인다. **다만 2026-08-10부터 인쇄 진에서는 리드도 다른
스토리와 같은 크기로 나온다** — "1면 전체를 쓴다"는 옛 문장은 더 이상 맞지 않는다.

### 1.4 소재를 고르는 메커니즘 (2026-08-08 정리)

"쏟아지는 문화 콘텐츠 중 왜 이걸 실어야 하는가"에 대한 답은 매주 새로 지어내지 않는다. 이미 파이프라인에 있다.

```
후보 발견 (S1 az-scout)  →  3조건 점수 (S2)  →  검증 (S3 az-verify)  →  집필 (S4 az-write)  →  인사이트 3문 (S5)
```

- **소재 자체**를 거르는 문턱은 §4.2의 3조건(이례성·인과·검증가능성)이다 — "이 숫자가 실제로 이례적이고, 계기가 특정되고, 양쪽 다 확인되는가."
- **다 쓴 문장**을 거르는 문턱은 §5.1의 인사이트 3문(발견과 놀라움·한 문장 누구나·인간다움)이다 — "이례적인 숫자를 이례적으로 읽히게 썼는가."
- **문장이 어떤 소리를 내는가**는 `voice.md`가 정한다.

이 셋이 곧 "쏟아지는 콘텐츠 중 주목할 만한 것만 고르는" 메커니즘이다. 매주 반복하면 되고, 사람이 매번 새로 판단 기준을 만들 필요가 없다.
실 구매·실 소비 숫자(예스24 판매지수, KOBIS 관객수, 멜론 순위 등)를 우선순위로 두는 이유는 §4.2 이례성 판정이 **의견이 아니라 공개된 숫자**로 걸리기 때문이다 — 화이트리스트 `primary`/`conditional` 등급이 그 전제 조건이다 (`sources/whitelist.json`).

---

## 2. 산출물과 저장소 구조

### 2.1 현재 구조

```
README.md               저장소 안내 + 세팅 절차
CLAUDE.md               이 문서
AGENTS.md               ★ 에이전트 역할·컨텍스트 매니페스트
LESSONS.md              ★ 중앙 학습 기록
design.md
market-scope.md
.claude/agents/
  az-scout.md  az-verify.md  az-write.md  az-editor.md  az-retro.md
runs/
  README.md             실행 로그 형식
  YYYY-wNN.json         에이전트 과정 기록 (lessonsApplied 필수)
  index.json            qa.mjs가 생성
  raw/                  ★ 주간 원본 스냅숏 — collect.mjs가 만든다
domains/
  registry.json         ★ 도메인·색상·분량 예산 단일 소스
  README.md  _TEMPLATE.md
  movie.md  music.md  youtube.md  book.md
sources/
  whitelist.json        ★ 출처 4등급 단일 소스
  README.md
issues/
  README.md             회차 파일 형식
  2026-w31.json
tools/
  collect.mjs           ★ 주간 원본 수집 — 오픈API·fetch·Playwright 어댑터
  make-og.mjs           링크 미리보기 이미지 생성 (결과물은 커밋한다)
  dashboard.html        운영 대시보드 — 파이프라인·사람 대기·학습 현황
  zine-builder.html     제작기 (registry/whitelist를 저장소에서 읽는다)
  qa.mjs                자동 검증 — CI와 로컬 공용
.github/workflows/
  qa.yml                푸시·PR 검증 + main이면 Pages 배포
```

**★ 표시가 단일 소스다.** 문서 안의 표는 사람이 읽는 사본이므로, 값을 바꿀 때는 JSON을 먼저 고친다.

### 2.2 빌드 파이프라인

```
issues/*.json          ─┐
domains/registry.json  ─┼─►  build/build.mjs  ─►  dist/
assets/ (이미지·app.js) ─┘
```

**`issues/*.json`이 유일한 정본이다.** 웹·인쇄 진·OG 태그가 전부 여기서 생성된다.
마크업에 본문을 직접 쓰지 않는다. 손으로 옮겨 적는 단계가 없다.

```
build/
  build.mjs             엔트리. --serve로 로컬 미리보기
  verify.mjs            산출물 검증 — A4 페이지 수·콘솔 에러·링크
  lib/*.mjs             html · data · theme · css · layout · sparkline · render-* · assets
test/*.test.mjs         node --test. 프레임워크 없음
legacy/Answer_Zine.html 프로토타입 보관 — 발행 산출물이 아니다
```

산출물 구조는 정적 디렉터리 라우팅이다. 서버 설정 없이 GitHub Pages에서 그대로 열린다.

```
dist/index.html                홈 — 카테고리 카드 그리드, QR 도착지 (answerzine.kr)
dist/archive/index.html        전체 아카이브 — 모든 스토리 평면 목록 (2026-08-10 두 번째 라운드 도입)
dist/about/index.html          About — 편집 명제 소개 (2026-08-11 도입, 마스트헤드 내비 맨 앞 링크)
dist/2026-w31/book/index.html  개별 스토리
dist/2026-w31/print/index.html A4 인쇄 진
```

> 2026-08-08부터 `dist/2026-w31/index.html`(회차 목록 페이지)은 만들지 않는다 — 그때는 홈
> 아카이브가 모든 스토리를 평면으로 냈으므로 중복이었다. **2026-08-10 두 번째 라운드에서
> 홈 자체가 도메인당 카드 하나(최신 스토리로 링크)인 카테고리 그리드로 바뀌면서, 평면
> 목록은 `/archive/`로 옮겨졌다** — `render-index.mjs`(카드 그리드)와
> `render-archive.mjs`(평면 목록)가 갈렸다. 스토리 페이지의 "← 목록으로"와 QR은 여전히
> 홈(`/`)을 가리킨다. 인쇄 진(`/print/`)은 독립 라우트라 영향받지 않는다.

> **인쇄 진 미리보기 — 2026-08-10 되돌림.** 같은 날 이전 라운드에서 "인쇄하기" 버튼을 빼고
> "← 목록으로" 백링크만 남겼는데, 사용자가 첨부한 참고 목업(print.html)이 인쇄 버튼을 다시
> 요구해 되돌렸다. `/print/` 페이지에는 지금 백링크와 "인쇄 / PDF로 저장" 버튼이 함께 있다
> (`[data-print]` → `window.print()`, `assets/app.js`).

> **DIY 진 — 2026-08-11 아홉 번째 라운드 신규.** `/print/`(A4 세로 1장 요약지)와는 별개로,
> 하단 고정 CTA("Create The Answer Zine")를 누르면 그 주 상위 4편을 접어 만드는 A4 가로
> 2장짜리 8쪽 소책자를 3D 플립북으로 미리 보고 인쇄할 수 있다. 새 라우트가 아니라
> showChrome 페이지(홈·스토리·about·아카이브)에 오버레이로 얹힌다
> (`build/lib/render-zinebook.mjs`, `assets/zinebook.js`). 명세는 `design.md` §11.

**런타임 의존성은 0이다.** `dist/`는 순수 정적 HTML·CSS·JS다 — 폰트는 2026-08-10부터 시스템
헬베티카 스택이라 자체 호스팅하지 않는다(design.md §3). 빌드 단계 devDependency는
`playwright`(A4·콘솔 검증, OG 카드) 하나뿐이다 — 사용자 승인 사항이다 (§7.3). `qrcode`는
2026-08-11 정리 라운드에서 뺐다 — 인쇄 진이 QR 대신 텍스트 URL을 쓰면서(2026-08-10) 호출부
자체가 없어졌다. `npm install` 없이도 빌드는 성공한다 — 브라우저 검사(A4·콘솔·OG 카드)만
건너뛴다.

---

## 3. 데이터 계약

콘텐츠는 전부 `WEEKS` 배열(마이그레이션 후 `issues/*.json`)에 있다. **마크업에 본문을 직접 쓰지 않는다.**

### 3.1 회차 항목

| 필드 | 타입 | 필수 | 규칙 |
|---|---|---|---|
| `id` | string | ✅ | `YYYY-wNN-<domainKey>`. 예: `2026-w31-book`. 전역 유일. |
| `range` | string | ✅ | `YYYY.MM.DD – MM.DD` (en dash U+2013). 집계 기간. |
| `domain` | string | ✅ | `DOMAIN_META`의 키. `domains/<도메인>.md`가 존재해야 한다. |
| `kicker` | string | — | 영문 대문자. 도메인별 관용구는 각 도메인 문서 참조. |
| `headline` | string | ✅ | 한국어 한 문장, 마침표로 끝냄. **≤24자** (§design.md 예산) |
| `teaser` | string | ✅ | 한 문장. 숫자 포함 권장. **≤78자** |
| `blocks` | Block[] | ✅ | `text` / `stat` / `quote` 3종만. |

### 3.2 블록

```js
{ type: "text",  text: "…" }                       // ≤180자

{ type: "stat",
  label: "6주 차 스크린당 관객수",                     // 이 숫자가 무엇인지
  value: "+9.9% (전주 대비)",                        // 크게 보여줄 한 값
  sourceUrl: "https://…",                           // 필수. 검증 가능한 원본.
  sourceLabel: "KOBIS 박스오피스 확인하기",

  // ↓ 아래 둘 중 정확히 하나. 출처의 series가 정한다 (§3.4).
  trend: [408, 287, 165, 182],                      // 4~5점 권장, 최소 2
  axisCaption: "X축: 개봉 3~6주 차 · Y축: 스크린당 주간 관객수(명)",
  trendSource: "각 점을 어디서 어떻게 읽었는지",         // trend가 있으면 필수

  basis: "예스24 종합 주간 1위. 전주 판매지수를 100으로 둔 값." }  // trend가 없으면 필수

{ type: "quote",
  text: "이건 마케팅이 만든 베스트셀러가 아니다. 독자들이 스스로 만든 애도다.",
  insight: { concept: "사후 관심 효과 (사망 후 재조명 현상)",
             note: "…왜 일반적 현상인지 + 이번 사례가 그 중 어디에 해당하는지…" } }
```

> **`stat.value`는 숫자로 시작한다 (2026-08-08 도입).** 순위·비교는 괄호 안 보조 정보로만 쓴다.
> `"1위 (판매량은 3위)"`처럼 순위를 앞세우면 정작 실 구매·소비 숫자가 안 보인다 — `"16만 4,443장
> (지수 1위)"`처럼 숫자를 맨 앞에 놓는다. §4/S2의 "결과 지표 우선순위 — 돈이 이긴다"와 같은
> 원칙을 문구 수준까지 적용한 것이다. `tools/qa.mjs`가 기계로 강제하지는 않는다 — 정규식으로
> 잡으면 "약 16만 장"류 정상 표현까지 오탐한다. §8 QA 체크리스트에서 사람이 매주 확인한다.

### 3.3 고정 블록 구성

모든 스토리는 **정확히 이 5블록**이다. 늘리거나 줄이지 않는다.

```
[0] text   현상 제시 — 언제, 무슨 일이 (≤180자)
[1] stat   숫자 한 방 + 출처            (회차당 정확히 1개)
[2] text   누가 샀나 / 왜 이례적인가    (≤180자)
[3] quote  한 줄 해석 + insight        (회차당 정확히 1개)
[4] text   마무리 한 문장              (≤180자)
```

`stat`이 두 개 필요하면 소재를 쪼갠 것이다. 하나를 버리거나 다음 주로 넘긴다.

### 3.4 추세선은 출처가 정한다 — 그릴 수 없으면 그리지 않는다

`sources/whitelist.json`의 각 출처에는 `series`가 붙어 있다. **"지난 주 값을 지금 다시 읽을 수 있는가"** 하나만 묻는다.

| series | 뜻 | `trend` |
|---|---|---|
| `archived` | 주차를 지정해 과거 수치를 그대로 다시 받는다 (KOBIS·넷플릭스 Tudum·써클차트) | 그려도 된다 |
| `snapshot` | 조회 시점의 순위·지수만 보여준다 (멜론·예스24·교보문고) | **금지** |
| `none` | 연속된 주간 수치를 내지 않는다 (기사·단발 발표) | **금지** |

`snapshot`·`none` 출처에서 뽑은 4주 배열은 **다음 주에 아무도 같은 방법으로 다시 그릴 수 없다.**
그런 선은 근거처럼 생겼지만 근거가 아니고, 근거처럼 생겼다는 점에서 없는 것보다 나쁘다.

그 자리에는 `basis` 한 문장 — **이 값이 무엇과 비교한 값인가** — 를 넣는다. 화면에는 `비교 기준`으로 렌더된다.
`trend`가 있으면 `trendSource`로 **각 점을 어디서 어떻게 읽었는지** 반드시 적는다. 말할 수 없으면 그릴 수 없다.

`tools/qa.mjs`가 셋 다 검사한다. 등록 URL이 죽은 출처(`urlStatus: "broken-*"`)는 `sourceUrl`로도 못 쓴다.

**예외 하나 — 우리가 뜬 스냅숏.** `snapshot` 출처라도 `tools/collect.mjs`가 매주 떠서 커밋한
`runs/raw/`가 4주치 쌓이면 그 선은 재현된다. 각 점이 커밋된 파일을 가리키기 때문이다.

```js
trend: [98, 101, 100, 426],
trendSnapshots: ["runs/raw/2026-w29/yes24.json", "…w30…", "…w31…", "…w32…"],
```

점 수와 파일 수가 같아야 하고, 파일이 실제로 있어야 한다. qa가 확인한다.
**과거로 소급해서는 못 만든다** — 그 주에 뜨지 않았으면 그 점은 영영 없다.

### 3.5 순위를 그릴 때는 축을 뒤집는다

순위는 **작을수록 좋은 값**이다. 그대로 그리면 1위로 올라간 곡의 선이 아래로 흐르고,
독자의 눈은 그것을 하락으로 읽는다. **그림이 문장과 반대로 말하는 건 없는 것보다 나쁘다.**

```js
trend: [5, 3, 1, 1, 1],
trendInvert: true,                                    // 1위 쪽이 위로 간다
axisCaption: "X축: 6월 5주~8월 1주 · Y축: 멜론 주간 순위(위가 상위)",
```

`trendInvert`는 스파크라인의 y축과 SVG 대체 텍스트를 함께 뒤집는다.
`axisCaption`에도 방향을 적는다 — 소리로 듣는 독자에게는 축 캡션이 그림이다.

---

## 4. 주간 파이프라인

한 호를 만드는 8단계다. **앞 단계를 건너뛰지 않는다.** 특히 S3 이전에 문장을 쓰지 않는다.

```
S1 수집 → S2 선별 → S3 검증 → S4 집필 → S5 해석 → S6 빌드 → S7 QA → S8 발행
         ↑                  │
         └──── 탈락 시 ─────┘
```

### S1. 신호 수집

**먼저 수집기를 돌린다. 그 주 원본이 `runs/raw/`에 남는다.**

```bash
npm run collect 2026-w32          # 전 어댑터
npm run collect 2026-w32 -- --only=netflix,yes24
```

| 어댑터 | 방법 | 필요한 것 |
|---|---|---|
| `netflix` | 전 주차 TSV 직접 | — |
| `yes24` | fetch (서버 렌더) | — |
| `kobis` | 공식 오픈API | `KOBIS_API_KEY` (무료) |
| `kopis` | 공식 오픈API | `KOPIS_API_KEY` (무료) |
| `kyobo` · `circlechart` | Playwright | `npm install` |

**수집을 거른 주는 영영 복구되지 않는다.** 스냅숏 출처는 과거를 되돌려주지 않으므로,
수집기는 스냅숏 어댑터에 대해 **현재 주가 아니면 수집을 거부한다.** 화요일 이후에 돈다.

쌓인 스냅숏은 그 자체가 아카이브다 — 4주치가 모이면 스냅숏 출처에서도 추세선을 그릴 수 있다
(`stat.trendSnapshots`, §3.4). `runs/raw/README.md` 참조.

그 다음 각 도메인 문서의 `데이터 출처` 절을 열어 도메인당 **후보 3개 이상**을 확보한다.

기록할 것: 항목명 · 현재 순위 · 직전 4주 수치 · 눈에 띄는 변동.
아직 이야기를 만들지 않는다. **숫자만 모은다.**

### S2. 소재 선별 — 3조건 테스트

각 후보를 세 조건으로 채점한다. **하나라도 0점이면 즉시 탈락**이다.

| 조건 | 질문 | 0점 | 1점 | 2점 |
|---|---|---|---|---|
| **이례성** | 이 숫자가 그 도메인의 평소 범위를 벗어났는가? | 통상 범위 안 | 경계선 | 명백한 이탈 |
| **인과** | 그 변화를 만든 **특정 가능한 계기**가 있는가? | 없음/불명 | 추정 가능 | 시점까지 특정됨 |
| **검증가능성** | 숫자와 계기 **양쪽 다** 공개 출처로 확인되는가? | 불가 | 한쪽만 | 양쪽 다 |

이례성의 기준선은 도메인마다 다르다 — 반드시 `domains/<도메인>.md`의 `이례 판정` 절을 본다.

> **이례 = 큰 숫자가 아니다.** 기존 회차 중 영화 편은 `-14%`라는 **감소** 수치로 실렸다.
> 업계 평균 감소율이 -35%인데 그 절반도 안 빠졌다는 게 이례였다.
> 항상 "그 도메인에서 이 시점에 **기대되는 값**"과 비교한다.

도메인당 최고점 1개를 남긴다. **최고점이 4점 미만이면 그 도메인은 이번 주 결번**이다 (§7.2).

#### 결과 지표 우선순위 — 돈이 이긴다 (2026-08-08 도입)

검증가능성 점수가 같은 후보가 여럿이거나, 한 도메인 안에 후보 지표가 여러 개일 때는
**실 구매·실 소비 숫자**(판매량·판매지수·관객수·음반 실물 판매·재고 소진 등)를
**조회수·검색량·대출량·순위 변동 같은 프록시 지표**보다 항상 우선한다.

- **타이브레이커:** 3조건 합계가 동점이면, 소비/구매 지표를 `stat`으로 쓰는 후보가 프록시 지표
  후보를 이긴다 — §1.3의 "동점이면 이례성 배수" 규칙보다 먼저 적용한다.
- **후보 내 지표 선택:** 같은 후보에 실 구매 지표와 프록시 지표가 둘 다 있으면(예: 멜론 순위 vs
  한터차트 실물 판매량), 검증가능성이 같으면 **항상 실 구매 지표를 `stat`으로 쓴다.** 프록시
  지표는 본문(text 블록)의 계기 설명에만 쓰고 `stat.value`로는 쓰지 않는다.
- **예외:** 유튜브처럼 구조적으로 1차 소비 지표가 없는 도메인은 `domains/youtube.md` §2의
  계기측/결과측 2원 검증 체계를 그대로 따른다 — 이 규칙이 그걸 대체하지 않는다.

### S3. 검증 — 통과 못하면 버린다

이 단계가 이 웹진의 신뢰를 지탱한다. 타협하지 않는다.
`tools/zine-builder.html`에 입력하면 아래 항목 중 상당수가 자동 검사된다.

**출처 검증**

1. `sourceUrl`의 호스트가 `sources/whitelist.json`에서 **`primary` 또는 `conditional`** 등급인가.
2. `sourceUrl`을 **실제로 연다.** 열리지 않으면 탈락.
3. 그 페이지에서 **`value`의 숫자가 그대로 확인되는가.** 계산이 필요하면 계산 과정을 기록한다.
4. `trend` 배열의 각 점이 어디서 왔는지 말할 수 있는가.

**시장·시간 검증 — `market-scope.md` §5 전 항목**

5. **기준일**(계기가 국내에서 발생한 날)을 특정했는가. 도메인별 정의는 `market-scope.md` §4.
6. 그 날짜가 **최종 확정일**인가. 최초 발표만 보고 갱신을 놓치지 않았는가.
7. 그 날짜가 **국내 기준**인가. 해외 개봉일·발매일을 옮겨 적지 않았는가.
8. **기준일 ≤ 집계 종료일**인가. 종료일보다 늦으면 그 주에 사건이 아직 없었다는 뜻이다 — 탈락.
   기준일이 집계 창 **안**에 있으면(개봉·공개 당주) 탈락이 아니라 **선언 대상**이다.
   `anchorInWindow: true`를 적고, 그 숫자가 7일치가 아니라 **며칠치인지 본문에 밝힌다.**
   선언 없이 창 안의 기준일을 쓰면 qa가 막는다 — 며칠치를 7일치로 읽히게 두는 게 사고다.
9. 주차 표기가 `(집계 시작일 − 기준일) ÷ 7 + 1`과 맞는가.
10. `sourceUrl`의 `market`이 선언한 시장과 같은가. 국내 서사에 해외 지표를 섞지 않았는가.

> 세 개 다 탈락해서 도메인이 비어도 괜찮다. **확인 안 되는 숫자를 싣는 것보다 결번이 낫다.**
>
> 5~9번을 건너뛴 결과가 `2026-w31-movie` 사고다. 개봉일을 최초 발표(7/15)로 쓴 채
> 실제 국내 개봉(8/5) 이전 기간의 KOBIS 데이터를 인용했다. 존재할 수 없는 숫자였다.

### S4. 집필

`§3.3` 5블록 구조로 쓴다. 문장 규칙은 §6.

집필 순서 권장: `stat` → `[0] 현상` → `[2] 맥락` → `quote` → `[4] 마무리` → `headline` → `teaser`.
**숫자를 먼저 확정하고 문장을 붙인다.** 반대로 하면 문장에 맞춰 숫자를 고르게 된다.

### S5. 해석 (insight)

`quote.insight`에 행동경제학·소비심리 개념을 하나 붙인다.

- `concept` — 개념명. 널리 통용되는 이름을 쓴다. 지어내지 않는다.
- `note` — **왜 그 현상이 일반적으로 관찰되는지** + **이번 사례가 그 중 어디에 해당하는지**. 110~125자.

도메인별로 자주 쓰이는 개념 목록이 각 도메인 문서에 있다. 같은 개념을 **4주 연속 쓰지 않는다.**

> `note`는 홍보 문구가 아니다. "그래서 이 제품이 좋다"로 끝나면 다시 쓴다.

> **화면 노출 — 2026-08-10부터 다시 보인다.** 2026-08-08엔 `note`를 화면에서 뺐다(웹
> 스토리 페이지·인쇄 진 둘 다 `quote.text`만 노출). 사용자가 첨부한 참고 목업의 인사이트
> 콜아웃 구조가 되돌렸다 — 웹은 `.insight-note`, 인쇄 진은 `.zine-insight-note`가 `note`를
> 그대로 낸다. `concept`은 여전히 화면 라벨로 쓰지 않는다(고정 문구 "인사이트"를 쓴다) —
> 아래 집필 규율은 이 화면 노출 여부와 무관하게 그대로 유효하다.

#### 인사이트 3문 테스트 — `quote` + `insight`는 이 셋을 통과해야 싣는다 (2026-08-08 도입)

3조건 테스트(S2)가 **소재 자체**의 자격을 판정한다면, 이 3문은 그 소재를 **어떻게 썼는지**를 판정한다.
`quote.text` + `insight`를 쓴 뒤 아래 세 질문에 스스로 답한다. 하나라도 "아니오"면 다시 쓴다 — 소재를 버리지 않는다.

1. **발견과 놀라움** — 독자의 내면을 건드리는 새로운 발견이 있는가, 아니면 이미 다 아는 얘기를 숫자만 바꿔 되풀이하는가?
2. **한 문장, 누구나** — `insight.note`를 걷어내고 `quote.text` 한 줄만 읽어도 무슨 일이 일어났는지 이해되는가? 전문 용어로 설명을 대신하지 않았는가?
3. **인간다움** — 숫자 뒤에 사람의 동기·감정·행동 논리가 보이는가, 아니면 판매량 요약에 그쳤는가?

이 3문은 §6 문장 규칙(헤드라인 마침표·물음표 금지 등)을 대체하지 않는다. **문장의 형식은 그대로 두고, 무엇을 말할지를 더 엄격히 고른다.**

#### 인사이트 vs 니즈·파인딩스·상식통념 — 2×2 판정 (2026-08-09 도입)

제일기획 《Champagne of Desire》 그림 10(`voice.md` §1 벤치마크와 같은 리포트)의 틀이다.
`quote.insight`·`issue.insight` **둘 다** 이 판정을 통과해야 싣는다. 두 축은 "본심이 숨겨져
있는가"와 "그게 사람을 실제로 움직이는가"다.

|  | 숨겨진 본심 | 겉으로 드러난 본심 |
|---|---|---|
| **사람을 움직인다** | **인사이트** ← 이것만 싣는다 | 니즈 — 독자가 이미 안다, 새로울 게 없다 |
| **움직이지 않는다** | 파인딩스 — 흥미롭지만 "그래서 뭐?"에서 멈춘다 | 상식·통념 — 확인 사살일 뿐이다 |

써놓은 문장이 왼쪽 위 칸이 아니라면 다시 쓴다: "독자가 이미 알던 걸 숫자로 확인만 시켜주는가
(니즈·상식통념), 아니면 몰랐던 동기를 들춰서 왜 그렇게 움직였는지 설명하는가(인사이트)?" 위
인사이트 3문의 1번("발견과 놀라움")이 정확히 이 구분을 묻는 것이다 — 이 2×2는 그 질문을 판정
가능한 틀로 바꾼 것뿐이다.

### S5.5. 통합 인사이트 (2026-08-08 도입, 2026-08-09 개정 — 콤팩트 제목으로, 2026-08-10 두 번째
라운드 개정 — 부연설명 추가)

그 호 4편이 전부 확정된 **뒤에** 마지막으로 쓴다 — 스토리보다 먼저 쓰면 스토리가 통합 인사이트에
맞춰 억지로 다듬어진다. `issue.insight`를 채운다. 필요하면 인쇄용으로 더 짧게 `issue.insightPrint`도
채우지만, 대개는 `insight` 자체가 이미 짧아서 그대로 써도 된다.

- **이건 요약문이 아니라 제목이다.** 그 주 4편의 사실을 나열하거나 인용하지 않는다 — 홈의 진짜
  헤드라인(`.issue-insight`, PC 기준 72px)이라 그 주 전체를 한 줄로 대표하도록 붙이는 표제다.
  헤드라인(§6)만큼 짧고 압축적으로 쓴다. 길게 설명하고 싶어지면 그건 아직 패턴을 못 찾은 것이다
  — 다시 좁힌다.
- **문자 그대로 설명하지 않는다 — 하상욱 시인류 톤매너로 비튼다(2026-08-11 사용자 피드백).**
  "큰 숫자, 작은 계기에서 왔다."는 사실 관계를 정확히 적었지만 뻔했다("이미 다 아는 얘기를
  숫자만 바꿔 되풀이"하는 인사이트 3문 1번 위반과 같은 종류의 문제) — "먼지 하나, 눈덩이
  됐다."(눈덩이 효과 은유)로 다시 썼다. 현상을 직접 묘사하지 않고 이미지·관용구를 비틀어
  간접적으로 가리킨다. 뜻이 안 통하면 실패다 — 위트가 뜻을 가리면 안 된다.
- **`issue.insightNote`(2026-08-10 두 번째 라운드 도입, 웹 전용)** — 헤드라인 밑에 붙는 부연설명
  한 문단(110~125자 권장). 헤드라인이 압축한 걸 풀어서, "무엇이 왜 그랬는지"를 한 번 더 설명한다.
  혹시 그 주 한 도메인이 유독 두드러지면(빅이슈) 그걸 메인으로 삼아 설명해도 된다 — 넷을 억지로
  균등하게 아우르지 않아도 된다. 인쇄 진에는 안 나온다(A4 여유가 없다, `insightPrint`만 쓴다).
- 바로 위 2×2 판정을 회차 레벨로 적용한다: 4편이 공통으로 배신한 "기대되는 흐름"이 **숨겨져
  있으면서 동시에 그 주 숫자들을 실제로 움직인 것**이어야 한다. 각 스토리의 `quote.insight.concept`을
  훑어 그 밑에 깔린 더 큰 패턴 하나를 찾는다 — "다들 열심히 산다"처럼 아무 데나 붙는 문장(상식·통념
  칸)은 탈락이다.
- `insight` 없이는 `status: ready`로 못 올린다 — `tools/qa.mjs`가 막는다.
- 길이 예산은 `domains/registry.json`의 `issueInsightWeb`/`issueInsightPrint`를 본다 — 헤드라인과
  같은 자릿수 급이다. 넘치면 문장을 압축하지, 미니 teaser를 손대지 않는다(§design.md §4.3 불변식은
  여전히 유효하다).

### S6. 빌드

1. `issues/YYYY-wNN.json`을 만든다. `stories` 배열은 **점수 순**이다 — 맨 앞이 리드다.
2. 빌드하고 검증한다.

```bash
node --test test/*.test.mjs   # 단위 테스트
node tools/qa.mjs             # 입력 검증 (회차 데이터)
node build/build.mjs          # dist/ 생성
node build/verify.mjs         # 산출물 검증 (A4·콘솔·링크·폰트)
node build/build.mjs --serve  # 눈으로 확인
```

`npm run check` 한 줄로 위 넷을 순서대로 돌릴 수 있다.

3. 인쇄 진은 **자동 생성된다.** 손으로 고치지 않는다.
   2026-08-10부터 지면 본문은 `story.teaser`를 그대로 쓴다(스토리마다 한 문단, 리드/미니
   구분 없음) — `zineBodyFor()`/`zineBody` 커스텀 축약은 더는 지면 렌더에 쓰이지 않는다
   (함수 자체는 `build/lib/render-zine.mjs`에 남아 있다).
   **헤드라인·티저·스탯 수치는 웹과 같은 값을 참조하므로 어긋날 수 없다.**
4. 분량은 `design.md` §4 예산표를 지킨다. `tools/qa.mjs`가 검사한다.
5. 빌드가 내는 경고를 읽는다. 미니 슬롯 부족·초과는 여기서 알려준다.

### S7. QA

§8 체크리스트 전 항목. 하나라도 실패하면 S8로 가지 않는다.

### S8. 발행

빌드 산출물 전달 + 이번 호 요약(도메인별 헤드라인, 사용한 출처, 결번 도메인과 사유) 보고.

---

## 5. 도메인 레지스트리

| 도메인 | key | Night 컬러 | 문서 | 1차 출처 | series | 읽히는가 (2026-w32 확인) |
|---|---|---|---|---|---|---|
| OTT | `ott` | `#E212D8` | `domains/ott.md` | 넷플릭스 Tudum | `archived` | ✅ **전 주차 TSV 한 방** |
| 영화 | `movie` | `#0A84FF` | `domains/movie.md` | KOBIS | `archived` | ✅ 날짜 지정 재조회 |
| 도서 | `book` | `#34C759` | `domains/book.md` | 예스24·**교보문고** | `snapshot` | ⚠️ 현재 주만 — 추세선 불가 |
| 음악 | `music` | `#FF9500` | `domains/music.md` | 멜론 (가이섬 아카이브 경유) | `archived` | ✅ 2012년~ 주차별 |
| 뉴스 | `news` | `#8B66F3` | `domains/news.md` | 빅카인즈 | `archived` | ⚠️ 분석 UI 조작 필요 |
| 여행 | `travel` | `#59900C` | `domains/travel.md` | 한국관광 데이터랩 | `archived` | ⚠️ 월 단위 — 주간 판정 어려움 |
| 공연 | `stage` | `#0C907E` | `domains/stage.md` | KOPIS | `archived` | 💤 **휴면** — 서비스키 받으면 복귀 |
| 유튜브 | `youtube` | `#FF2D55` | `domains/youtube.md` | **없음** | `none` | ❌ **구조적으로 없다** |

**도메인이 8개인 이유는 지면을 채우기 위해서가 아니라, 결번이 나도 4편을 확보하기 위해서다.**
지면은 여전히 리드 1 + 미니 3이고 그 주 통과분 중 **점수 상위 4편만** 싣는다. 나머지는 웹에만 실린다.

**결번이 났을 때 먼저 파볼 도메인은 `ott` → `movie` → `music` → `book` 순이다.** 이 넷이 오늘 확실히 읽힌다.

읽을 수 없는 도메인은 `status`를 `dormant`로 내린다. 활성으로 두면 매주 결번만 쌓이고, 필터에는 늘 비어 있는 칸이 남는다. `stage`가 그래서 휴면이다 — `KOPIS_API_KEY`를 발급해 `tools/collect.mjs`의 kopis 어댑터가 실제로 값을 받아오는 것을 확인한 뒤 되돌린다.

`youtube`는 1위를 가져올 곳이 **앞으로도 없다** — 2025년 7월 급상승 페이지가 폐지됐고, 애초에
조회수는 "샀다"의 증거가 아니다. 이 도메인은 **파는 쪽이 집계한 1위**를 가져온다 (`domains/youtube.md` §2).
`stage`·`music`은 사이트 개편으로 막혀 있다 — 되살리려면 KOPIS 서비스키 또는 헤드리스 브라우저 수집이 필요하다.

컬러의 단일 소스는 코드의 `DOMAIN_META`다. 이 표는 사본이므로 **양쪽을 함께 고친다.**
신규 도메인 추가는 `domains/README.md`의 절차를 따른다. 색상은 임의로 고르지 않는다 (`design.md` §6).

---

## 6. 문장 규칙

도메인 공통이다. 도메인별 추가 규칙은 각 도메인 문서에 있다.

> ⚠️ **톤 벤치마크 (2026-08-08 도입, 상시 규칙).** 이 웹진에서 나가는 모든 문장의 톤매너는
> `voice.md`에 정리된 벤치마크(제일기획 《Champagne of Desire》)를 참고해서 쓴다.
> 아래 문장 규칙(헤드라인 마침표 종결, 물음표·느낌표 금지, 24자 상한 등)은 **이 톤 벤치마크보다 우선한다** —
> 벤치마크는 리듬·대비·발견의 태도를 빌려오는 것이지, `tools/qa.mjs`가 강제하는 형식 규칙을 깨는 근거가 아니다.
> 구체적으로 무엇을 가져오고 무엇을 가져오지 않는지는 `voice.md` §3(reconciliation)을 본다.

### 헤드라인

- 한국어 한 문장, **마침표로 끝낸다.** 물음표·느낌표·이모지·말줄임표를 쓰지 않는다.
- 24자 이하. 짧을수록 좋다 (기존: `밈이, 판다.` 7자 / `매진, 또 매진.` 9자).
- **결과를 쓰고 원인을 감춘다.** `부고 다음날, 서점이 붐볐다.`가 `히가시노 게이고 별세로 판매 급증`보다 낫다.
  > **예외(2026-08-11, 사용자 명시적 요청).** 영화 편은 `일주일 만에 극장을 접수했다.`(결과만
  > 보여주는 기본형)가 "기사의 핵심 데이터 인사이트(첫 주말 225만 명이 이후 흥행 속도를
  > 결정한 인과)를 놓친다"는 지적을 받아 `225만이 앞장서자, 400만이 뒤따랐다.`로 바꿨다 —
  > 인과를 헤드라인에 직접 드러낸다. **기본 규칙은 그대로다** — 이건 이 스토리 하나에 대한
  > 명시적 지시였지, 규칙 자체가 바뀐 게 아니다. 다음에 비슷한 요청이 없으면 기본형(결과만)
  > 으로 돌아간다.
- **문장 구조를 매주 반복하지 않는다.** `X, Y다.`(쉼표 도치) 하나만 계속 쓰면 헤드라인이 전부 같은
  리듬으로 읽힌다. 문법상 필요 없는 쉼표를 인위적으로 끼워 넣지 않는다. 발행 전 그 호의 헤드라인들을
  나란히 놓고 **같은 문형이 2개 이상이면 최소 1개를 다시 쓴다.**
- 고유명사를 헤드라인에 넣지 않는다. **대신 본문에서 반드시 밝힌다 — 이 뒷 절이 본체다.**
  헤드라인에서 감추는 건 결과를 먼저 보여주기 위해서지 대상을 숨기기 위해서가 아니다.
  작품·인물·브랜드가 끝까지 익명이면 독자는 무엇에 관한 기사인지 모른 채 읽는다.
  티저나 `blocks[0]`에서 이름을 대는 게 기본이다. `tools/qa.mjs`가 `〈 〉` 표기로 검사한다.

### 본문

- 평서문. 반말 종결(`-다`)로 통일한다.
- 한 문단은 한 가지만 말한다. 180자를 넘으면 나눌 게 아니라 **버릴 게 있는 것이다.**
- 수식어를 쌓지 않는다. `놀랍게도`, `무려`, `충격적인`을 쓰지 않는다. 숫자가 스스로 말하게 둔다.
- 작품명에 꺾쇠(`<>`)를 쓰지 않는다 — 영문 제목일 때 HTML 태그로 먹힌다. `〈 〉`(U+3008/3009)를 쓴다.

### 쿼트

- 26~40자. 한 문장.
- 대비 구조를 쓴다: `A가 아니라 B다` / `A인데 B했다`.
  (기존: `밈은 웃기려고 만들어졌는데, 결국 음원을 팔았다.`)

### 민감 소재

부고·사고·질병이 계기인 경우:

- 숫자와 사실만 전한다. 유족·팬의 감정을 대신 해석하지 않는다.
- `급증`, `특수`, `호재` 같은 시장 용어를 그 사건에 붙이지 않는다.
- 마지막 문단에서 **숫자 뒤에 사람이 있다는 걸 인지하고 있음**을 드러낸다.
  기준선: `숫자 뒤에 슬픔이 있다는 걸 아는 채로, 이번 주는 그 숫자를 그대로 전한다.`
- 판단이 서지 않으면 **싣지 말고 사용자에게 묻는다** (§7.1).
- **위트 있는 톤(`voice.md` §5)도 이 소재를 예외로 두지 않는다** — 다만 위트의 과녁은 항상
  숫자·현상이지 사건·고인이 아니다. 죽음 자체나 유족을 향해 재치를 부리지 않는다. 위 두 항목
  (시장 용어 금지, 감정 대신 해석 금지)이 위트를 이유로 우회되면 안 된다.

---

## 7. 중단·예외 규칙

### 7.1 멈추고 사용자에게 물어야 하는 경우

- 민감 소재(부고·사고·범죄·질병)의 게재 여부가 애매할 때
- 활성 도메인의 **절반 이상**이 결번일 때 (한 호가 성립하지 않는다)
- 출처가 유료/로그인 장벽 뒤에 있어 검증이 불가능할 때
- 데이터 출처 사이트의 구조가 바뀌어 지난주와 같은 값을 못 읽을 때
- 디자인 템플릿 변경이 필요할 때 (`design.md` 불변식은 에이전트가 임의로 못 고친다)

### 7.2 결번 처리

도메인이 3조건 4점을 못 넘기면 그 주는 결번이다.

- `WEEKS`에 아무것도 추가하지 않는다. 빈 스토리·`준비 중` 플레이스홀더를 만들지 않는다.
- 웹 아카이브(홈)는 도메인당 카드 하나다(2026-08-10부터 필터가 아니라 카테고리 카드
  그리드) — 결번 도메인의 카드는 자동으로 색 없는 빈 카드("아직 신호가 없다")로 뜬다.
  별도 처리가 없다.
- 인쇄 진은 통과분 전원을 낸다(2026-08-10부터 미니 슬롯 개념이 없다) — 결번이 나면 그만큼
  지면의 행이 줄어들 뿐, 별도 레이아웃 조정은 필요 없다.
- 발행 보고에 **어느 도메인이 왜 결번인지** 반드시 적는다.

### 7.3 에이전트가 하지 않는 것

- 확인되지 않은 숫자 게재. 추정치를 실측치처럼 쓰기.
- `sourceUrl`에 검색 결과 페이지·블로그·커뮤니티 글 넣기. **`sources/whitelist.json`의 `primary`/`conditional`만.**
- 국내 서사에 해외 지표·해외 날짜 섞기 (`market-scope.md`).
- 화이트리스트 차단 항목을 임의로 완화하기 (**사용자 승인 사항**).
- 요청 범위 밖의 리팩터링·포맷팅·"개선". §9는 발견 기록이지 실행 허가가 아니다.
- 빌드 도구·프레임워크·npm 의존성 도입 (사용자 승인 사항).
- `design.md` 불변식 변경.

---

## 8. QA 체크리스트

매주 발행 전 전 항목을 확인한다. `build/qa.mjs`로 자동화 가능한 항목은 `[자동]` 표시.

### 데이터

1. 새 회차의 `id`가 기존과 충돌하지 않는가. `[자동]`
2. `range`가 `YYYY.MM.DD – MM.DD` 형식이고 en dash를 쓰는가. `[자동]`
3. `domain`이 `DOMAIN_META`에 있고 대응 도메인 문서가 존재하는가. `[자동]`
4. 블록이 `text / stat / quote / text` 순서 + `stat` 1개 + `quote` 1개인가. `[자동]`
5. 슬롯 길이가 `design.md` §4 예산 안인가. `[자동]`
6. 모든 `stat.sourceUrl`이 200을 반환하는가. `[자동]`
7. 그 페이지에서 `value`가 실제로 확인되는가. **(사람이 눈으로)**
8. `insight.concept`이 최근 3주와 겹치지 않는가. `[자동]`
9. `sourceUrl`이 화이트리스트 `primary`/`conditional` 등급인가. `[자동]`
10. 기준일이 입력됐고 **집계 시작일보다 앞서는가.** `[자동]`
11. 기준일·집계 출처의 `market`이 선언 시장과 일치하는가. `[자동]`
12. 본문의 주차 표기가 기준일로부터 계산한 값과 맞는가. **(사람이 눈으로)**
13. 모든 `stat.value`가 숫자로 시작하는가 — 순위·비교는 괄호 안으로 밀렸는가. **(사람이 눈으로)**
14. `issue.insight`가 있고, 그 주 스토리 중 최소 두 편의 구체적 사실을 인용하는가. **(사람이 눈으로)**

### 웹

9. 인트로 → 아카이브 스크롤이 끊기지 않는가 ("이거 왜 잘나가?" 스테이트먼트 단계는
   2026-08-10에 없어졌다 — 인트로 다음이 바로 본문이다).
10. 홈의 카테고리 카드가 도메인당 하나씩 나오고, 각 카드가 그 도메인의 최신 스토리로
    이어지는가. 결번 도메인은 색 없는 빈 카드로 뜨는가 — 2026-08-10부터 필터가 아니라
    카드 그리드다. 카드 안에서 스토리 헤드라인이 가장 큰 글자이고, 도메인명은 우측
    상단 작은 라벨인가(2026-08-11 위계 반전). 밝은 배경(cardColor)엔 검은 글자,
    어두운 배경(colorPaper 폴백)엔 흰 글자인가.
10-1. 마스트헤드(로고 + 카테고리 내비)가 스크롤 내내 상단에 고정되는가. "About" 링크가
      맨 앞에 있고, 나머지 카테고리 내비가 영문 라벨이고 홈 카드 그리드와 같은 "최신
      스토리" 링크로 이어지는가. 로고·내비가 뷰포트 가장자리에 붙어 있는가(2026-08-11
      엣지-투-엣지).
10-2. 홈 하단 페이저(이전 주 인사이트 / 전체 아카이브 보기 / 다음 주 인사이트)가 있고,
      "전체 아카이브 보기"가 `/archive/`(모든 스토리 평면 목록)로 이어지는가.
10-3. `/about/`이 편집 명제(태그라인 + 선정 기준)를 보여주는가 (2026-08-11 도입).
11. 모든 회차를 열어 `stat` 카드 출처 링크가 열리는가.
12. 이전/다음 내비게이션이 **시간순**으로 움직이는가 (§9.2 수정 전이면 실패한다).
13. `insight.note` 콜아웃(`.insight-note`)이 스탯 패널과 본문 사이에 항상 보이는가 —
    2026-08-10부터 hover/`<details>` 없이 정적으로 노출된다.
14. 콘솔 에러 0건. `[자동]`
14-1. 링크 미리보기가 뜨는가 — `og:image`가 **절대 URL**이고 그 파일이 실재하는가. `[자동]`
14-2. 이탤릭 글자가 없는가 — 사이트 전체에서 `font-style: italic`을 쓰지 않는다
      (2026-08-10 두 번째 라운드, `test/css.test.mjs`가 강제).

### 인쇄

15. A4 인쇄 미리보기가 **정확히 1페이지**인가. `[자동]`
16. 웹 UI(`.intro`, `#main-content`, 요약 보기 CTA)가 전부 숨겨지는가.
17. 인쇄 진의 헤드라인·티저가 웹 `WEEKS`와 **문자열이 완전히 일치**하는가. `[자동]`
18. 지면 하단의 URL 텍스트(`.zine-footer`)가 홈 아카이브(`answerzine.kr`)를 가리키는가 —
    2026-08-10부터 QR 대신 텍스트다.

### 반응형·접근성

19. 375 / 768 / 1280px에서 레이아웃이 깨지지 않는가.
20. `prefers-reduced-motion: reduce`에서 애니메이션이 멈추고 콘텐츠는 보이는가.

### 자동 검증 참고

15번은 Chromium 헤드리스로 검사할 수 있다 (본 문서 작성 시 이 방법으로 §9.1을 발견했다).

```js
const buf = await page.pdf({ format: 'A4', printBackground: true,
                             margin: {top:0,bottom:0,left:0,right:0} });
const pages = (buf.toString('latin1').match(/\/Type\s*\/Page[^s]/g) || []).length;
// pages === 1 이어야 한다
```

---

## 9. 결함 기록 — 무엇이 어떻게 해소됐는가

프로토타입(`legacy/Answer_Zine.html`)이 안고 있던 13건이다. 전부 재현 확인했고, 빌드 파이프라인으로 해소됐다.
**남겨두는 이유는 같은 실수를 다시 만들지 않기 위해서다.** 각 항목의 "지금" 열이 재발 방지 장치다.

| # | 무엇이었나 | 지금 |
|---|---|---|
| 9.1 | 인쇄하면 A4 4페이지. `@media print` 숨김 목록에 `.statement-wrap` 누락 | `build/verify.mjs`가 **매 빌드마다 헤드리스 PDF로 페이지 수를 실측**한다. 실제로 이 검사가 새 코드의 2페이지 결함을 잡았다 |
| 9.2 | `openStory()`가 배열 순서를 최신순이라 가정해 이전/다음이 시간을 거슬렀다 | `data.mjs`가 `range` 기준 정렬 배열 하나를 만들고 그 위에서만 인덱싱한다. `test/data.test.mjs`가 강제 |
| 9.3 | 인쇄 지면이 하드코딩이라 **정본이 둘**이었다 | `render-zine.mjs`가 JSON에서 생성한다. **입력에 없으면 지면에도 없다** |
| 9.4 | 라우팅 없음. QR이 가리키는 `answerzine.kr/2026-w31`이 존재하지 않았다 | 정적 디렉터리 라우팅. 그 주소가 실재한다 |
| 9.5 | 필터와 내비게이션이 서로 다른 배열을 봤다 | 정렬+필터를 적용한 배열 하나가 단일 소스다 |
| 9.6 | 4.1MB, 그중 4.07MB가 base64 인라인 에셋 (캐시 안 됨) | 에셋 외부화 + 서브셋. **폰트 전송량 181KB (brotli), 한 번 받아 전 페이지 캐시.** 두 서체 모두 OFL 확인. *(2026-08-10 갱신: 자체 폰트 자체를 걷어내고 시스템 헬베티카로 바꾸면서 폰트 전송량이 0바이트가 됐다 — 서브셋이 더 잘된 게 아니라 다운로드할 폰트가 없다.)* |
| 9.7 | `innerHTML` + 이스케이프 부재, `onclick`에 id 보간 | 모든 문자열이 `escapeHTML()`을 통과한다. 이벤트 위임만 쓴다. `verify.mjs`가 `onclick` 부활을 감시 |
| 9.8 | 4웨이트만 로드하면서 CSS는 8웨이트를 참조 | Paperlogy 400/700/900 + 나눔명조 400/700만. `test/css.test.mjs`가 그 외 참조를 실패 처리. *(2026-08-10부터 이 항목은 더 적용되지 않는다 — 자체 폰트가 아예 없다. `test/css.test.mjs`는 이제 `@font-face`가 0개인지를 강제한다.)* |
| 9.9 | 스파크라인 대체 텍스트 없음, 출처가 hover 의존 | SVG `<title>`에 추세 문장. 출처는 레일에 **상시 노출**. 인사이트는 `<details>` |
| 9.10 | OG·description·파비콘 없음 | 회차·스토리마다 정적 생성. 파비콘 추가 |
| 9.11 | 콘텐츠/코드 미분리 | 이 작업의 본체 |
| 9.12 | QA 자동화 미구현 | `build/verify.mjs` + `test/*.test.mjs` 100건 |
| 9.13 | 미사용 CSS 4건 | `css.mjs` 재작성 시 제거 |

### 되돌리면 안 되는 것

아래는 **자동 검사가 지키고 있다.** 고치면 CI가 막는다.

1. `@media print`에서 `.intro`를 숨기고 `.zine-mount` 여백과 `.zine-page`의 `min-height`를 해제할 것 — 안 그러면 2페이지가 된다. (2026-08-10 이전엔 `.statement-wrap`도 같이 숨겨야 했다 — 그 섹션 자체가 없어지면서 `.intro` 하나만 남았다.)
2. 사용자 문자열은 `escapeHTML()` 또는 `h` 태그드 템플릿을 반드시 경유할 것
3. `onclick` 속성을 쓰지 말 것
4. `@font-face`를 CSS에 다시 넣지 말 것 — 2026-08-10부터 시스템 헬베티카 스택뿐이다(design.md §3). 자체 폰트를 로드하지 않는 한 "로드하지 않은 웨이트 참조" 문제 자체가 생기지 않는다.
5. 웹 본문에 `column-count`를 쓰지 말 것
6. 색 보더를 1px보다 굵게 하지 말 것
7. `font-style: italic`을 쓰지 말 것 — 2026-08-10 두 번째 라운드 사용자 요청, `test/css.test.mjs`가 강제한다.

### 남은 것

- **사진 콜라주 시스템은 인쇄 진에서 없어졌다(2026-08-10).** `story.photo.src`는 이제
  어디서도 렌더되지 않는다 — 데이터 필드 자체는 남아 있지만 읽는 코드가 없다. 사진을
  다시 쓰기로 하면 인쇄 진이 아니라 새 자리(예: 웹 스토리 헤더)를 설계해야 한다.
- **`zineBodyFor()`/`story.zineBody`(인쇄 진 본문 축약)와 `sparklineSVG()`(스탯 카드
  차트)는 렌더러 호출부가 없다.** 둘 다 2026-08-11 정리 라운드에서 검토했지만 지우지
  않았다 — 둘 다 "되살릴 일이 생기면 그대로 쓴다"는 의도로 데이터·테스트 자산으로
  남겨 둔 것이지 방치된 죽은 코드가 아니다(각각 `build/lib/render-zine.mjs`,
  `build/lib/sparkline.mjs`의 주석 참고). 같은 라운드에서 정말 죽어 있던
  것들(QR 생성 코드, `assets/img/qr-<회차>.svg`, `assets/img/logo-light.png`와 그
  전환 스위치, `qrcode`/`subset-font` devDependency, `runs/2026-w32.json` 형식
  예시 시드)은 지웠다.
