node_modules/
dist/
.DS_Store
*.log

# 세션 인프라 — 저장소에 올리지 않는다
.superpowers/
.playwright-mcp/
.claude/*
!.claude/agents/
!.claude/agents/**

# 폰트 원본 55MB — 그중 NotoSerif-*.ttf 72종은 한글 0자라 쓰지 않는다.
# 실제 쓰는 5벌은 assets/fonts/에 커밋한다.
# 맨 앞 슬래시가 없으면 경로 어디서든 매칭돼 assets/fonts/까지 무시된다
# (윈도우는 대소문자를 구분하지 않아 Fonts == fonts).
/Fonts/
.omc/

# 빌드 산출물
dist/
dist-test/
